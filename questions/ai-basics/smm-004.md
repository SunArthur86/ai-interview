---
id: smm-004
difficulty: L2
category: ai-basics
feynman:
  essence: 让模型结合图像和文本进行分步骤逻辑推理。
  analogy: 像老师看图解题，先说观察到什么，再一步步计算。
  first_principle: 如何让模型像人类一样结合视觉信息进行逻辑推理？
  key_points:
  - 扩展文本CoT至图文融合场景
  - 分步生成推理路径(rationale)再输出答案
  - 分两阶段训练或端到端指令微调
  - 提升复杂视觉问答的准确率与可解释性
---

# 什么是多模态思维链(Multimodal Chain-of-Thought)？

🎯 本质：多模态CoT将Chain-of-Thought推理扩展到多模态场景，让模型在面对图片+文字时也能分步推理，通过产生中间推理步骤来提升复杂视觉任务的准确性。

📊 **执行流程对比：**

传统CoT（文本）：
问题：小明有3个苹果... -> 推理：计算逻辑 -> 答案

多模态CoT（图文）：
输入：图片 + "图中牛奶比果汁贵多少？"
1. 感知：定位牛奶价格标签 -> 15元
2. 感知：定位果汁价格标签 -> 8元
3. 推理：计算 15-8 -> 7元
4. 输出：答案

**架构模式（Visual Rationales生成）：**

```text
+----------------------+     +-----------------------+
|   Image Input        |     |   Text Query (Question)|
+----------+-----------+     +-----------+-----------+
           |                               |
           v                               v
+------------------+           +-----------------------+
| Vision Encoder   |           |  Language Model       |
| (e.g., CLIP/ViT) |           |  (Reasoning Engine)   |
+--------+---------+           +-----------+-----------+
         |                               |
         |  Visual Features             |
         +------------->------------------+
                       |
               +-------v-------+
               |   Multimodal |
               |   Fusion Layer|
               +-------+-------+
                       |
         +-------------v-------------+
         |  Rationale Generation     |
         |  (e.g., "Milk is 15...") |
         +-------------+-------------+
                       |
         +-------------v-------------+
         |   Final Answer Infer      |
         +-------------+-------------+
```

**核心训练方法：**

1. **两阶段训练**
   - **Stage 1 (Rationale Generation)**: 冻结LLM，训练适配器或微调模型，使其能生成包含视觉细节的文本描述（如局部化物体、读取文本）。
   - **Stage 2 (Answer Inference)**: 将生成的Rationale作为输入，微调模型使其基于Rationale进行逻辑推理得出答案。
   - *优点*：解耦感知和推理，易于调试。

2. **端到端微调**
   - 构造包含 `<Image> <Question> <Reasoning> <Answer>` 的指令数据。
   - 直接对模型进行全量微调或LoRA微调，强制模型模仿CoT格式。
   - *优点*：感知与推理深度融合，效果上限更高。

**应用场景与价值：**
1. **科学问答**：理解图表数据趋势后再回答（避免只看图猜答案）。
2. **复杂推理**：如"如果左边的箱子移到右边，会不会倒？"（需要物理常识+视觉结构分析）。
3. **减少幻觉**：通过显式的步骤定位（如"Step 1: 找到红色区域"），约束模型关注实际存在的内容。

## 常见考点
- **CoT对幻觉的抑制**：多模态CoT是一定能减少幻觉吗？（不一定，如果Rationale生成阶段就出现了视觉误判，错误的推理步骤反而会强化错误结论，即"错误的一本正经地胡说八道"）。
- **数据构造成本**：多模态CoT数据很难标注，如何解决？（利用GPT-4V等强模型自动生成带有CoT的合成数据来蒸馏小模型）。
- **推理代价**：CoT增加了输出Token长度，如何优化？（可以使用Speculative Decoding speculative decoding 或知识蒸馏，将CoT逻辑内化为模型内部状态，输出时仅保留结果）。
