---
id: "llm-047"
difficulty: "L2"
category: "llm-core"
subcategory: "对齐技术"
tags:
  - "IO"
  - "IOC"
images:
  - "llm100_p111_img1.png"
feynman:
  essence: "🌅在 Direct Preference Optimization (DPO) 中,损失函数 (loss) 的构造基于偏好数据对的对比 学习.DPO 的核心思想"
  analogy: "微调就像给通才毕业生做岗前培训——已有基础能力（预训练），再针对具体岗位做专项训练（指令/偏好对齐）。"
  key_points:
    - "🌅在 Direct Preference Optimization (DPO) 中,损失函数 (loss) 的构造基于偏好数据对的对比 学习.DPO 的核心思想是利用偏好数据,通过优化策略,使得模型在偏好对中选择更优的选 项."
    - "DPO 中假设存在一个偏好模型 ,其参数为 ,该模型能够对给定的两个候选项进行打分,从而表 示出对其中某个选项的偏好.对于给定的一对数据 ,其中 是偏好数据对中更优的选项,"
    - "是较差的选项,DPO 希望通过训练使得模型给出的评分满足以下条件: ,即模 型倾向于选择被偏好的样本. f θ θ (x , x ) + − x"
first_principle:
  problem: "为什么需要 DPO 的 loss？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
---

# DPO 的 loss 是什么

🌅在 Direct Preference Optimization (DPO) 中,损失函数 (loss) 的构造基于偏好数据对的对比 学习.DPO 的核心思想是利用偏好数据,通过优化策略,使得模型在偏好对中选择更优的选 项.
1. 偏好模型的假设
DPO 中假设存在一个偏好模型 ,其参数为 ,该模型能够对给定的两个候选项进行打分,从而表 示出对其中某个选项的偏好.对于给定的一对数据 ,其中 是偏好数据对中更优的选项,

是较差的选项,DPO 希望通过训练使得模型给出的评分满足以下条件: ,即模 型倾向于选择被偏好的样本. f θ θ (x , x ) + − x

+ x

− f (x ) > θ + f (x ) θ −
2. 偏好对的损失函数定义
为了量化模型评分结果的好坏,DPO 通常使用基于对比学习的损失函数,比如二元交叉熵 (binary cross-entropy) 损失或对数损失.

🥛在 DPO 中,常见的损失函数形式可以表示为:

L(θ) = −E log σ(f (x ) −f (x )) (x ,x )∼D + − [ θ + θ − ]
**其中：**
•

是 sigmoid 函数,用于将分数差值映射到 的概率空间. σ(z) =

1 + e−z 1 [0, 1] •

表示偏好对中更优选项和较差选项的评分差. f (x ) − θ + f (x ) θ − •

是偏好数据集的分布. D 这个损失函数的目的是使模型对 和 的评分差尽可能大,从而确保模型更倾向于选择 作为偏 好选项. x

+ x

− x

+
3. 损失函数的解释
DPO 的损失函数本质上是一个对比损失,它通过最大化偏好对中两个选项评分的差异来优化模型. Sigmoid 函数 的输出是模型对 被偏好的概率.如果模型给出更大的正评分差

,那么这个概率会更接近于 1,相应的损失会趋向于 0.反之,如果模型未能正确 区分 和 ,即评分差接近于 0 或为负,则损失值会增大. σ(f (x ) − θ + f (x )) θ − x

+ f (x ) − θ + f (x ) θ − x

+ x

− 通过最小化损失函数,模型会逐渐调整参数 ,以使其输出的评分能够更好地反映训练数据中的偏好 信息. θ
4. 总结
🚅DPO 的损失函数是通过对比偏好数据对中的评分差来进行优化的,其目标是使模型能够更准 确地区分偏好对中的优劣选项.数学上,损失函数可以用如下公式表示:

这个公式通过最大化偏好对之间的评分差, 逐步提升模型的性能. L(θ) = −E log σ(f (x ) −f (x )) (x ,x )∼D + − [ θ + θ − ]
