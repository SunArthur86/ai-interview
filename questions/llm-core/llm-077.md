---
id: llm-077
difficulty: L2
category: llm-core
subcategory: 训练优化
tags:
- IO
feynman:
  essence: 训练初期逐步将学习率从小提升到目标值，防止模型震荡。
  analogy: 冬天发动汽车，先怠速热车一小会儿再高速行驶，避免发动机受损。
  first_principle: 如何避免训练初期因参数随机性导致的优化不稳定和梯度发散问题？
  key_points:
  - warmup_ratio：热身步数占总步数的比例。
  - 典型值：预训练约0.01，微调约0.03-0.1。
  - 作用：防止初期梯度爆炸和Loss剧烈震荡。
  - 配合：大Batch或大LR通常需要更长的Warmup。
follow_up:
- 有什么注意事项或最佳实践？
---

# 什么是warmup_ratio?训练过程中怎么设置

### 1. 什么是 Warmup & warmup_ratio
**Warmup** 是指在训练初期使用较小的学习率，经过一定步数后线性（或非线性）增加到目标 LR 的策略。
**warmup_ratio** 指的是 Warmup 阶段占总训练步数的比例。

### 2. 为什么需要 Warmup？
*   **稳定性：** 训练初期模型参数随机初始化，若使用较大 LR，容易导致梯度爆炸或 Loss 震荡。
*   **适应优化器：** 对于 Adam 等自适应优化器，初期梯度方差大，Warmup 有助于优化器更准确地估计一阶、二阶矩。

### 3. warmup_ratio 设置建议
*   **预训练场景（1 Epoch）：** 建议设为 **0.01**（1%），即前 1% 步数用于升温。
*   **SFT 场景（多 Epoch）：** 可设为 **0.03 - 0.1**。因为 SFT 数据量小，训练步数相对较少，Warmup 占比可适当提高以确保平稳。
*   **高 LR 或大 Batch Size：** LR 越大、Batch 越大，通常需要更长的 Warmup（更大的 ratio）来缓冲。

### 4. 总结
Warmup 是大模型训练的标配，主要作用是防止训练初期的“模型崩溃”，确保优化器进入稳定的收敛轨道。
