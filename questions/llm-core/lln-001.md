---
id: lln-001
difficulty: L4
category: llm-core
subcategory: LLM前沿
tags:
- llm-core
feynman:
  essence: 量化模型训练或推理过程中的浮点运算次数，用于衡量计算成本。
  analogy: 像数搬砖的次数，不管砖多大，搬一次算一次。
  first_principle: 如何量化深度学习模型的计算复杂度和存储需求？
  key_points:
  - 矩阵乘法FLOPs约为 $2 \times M \times N \times L$
  - Transformer主要计算集中在Attention和MLP的矩阵乘法
  - 训练显存开销主要是参数、梯度和优化器状态(约20倍参数量)
  - KV Cache随序列长度和层数线性增长，决定长文本推理上限
---

# FLOPs计算

- **FLOPs定义**: 浮点运算次数，衡量计算量。

- **计算公式**: 输入 $(M, N)$ 和 $(N, L)$ 的矩阵乘法需 $2MNL$ 次运算 (乘法+加法)。

- **Transformer各模块计算 (Batch=b, Seq=s, Hidden=h)**:
  - **Embedding**: 查表操作，不计FLOPs。
  - **Self-Attention**:
    - QKV映射: $3 \times 2bsh^2$
    - Attention Score ($QK^T$): $2bsh^2$
    - Score乘V: $2bsh^2$
    - Output映射: $2bsh^2$
  - **MLP**:
    - 升维 ($h \to 4h$): $8bsh^2$
    - 降维 ($4h \to h$): $8bsh^2$
  - **Output层**: Logits映射 $2bshV$ (V为词表大小)。

- **单层总FLOPs**: $24bsh^2 + 4bsh^2$ (Attention近似)。

- **训练与推理显存 (参数量$\phi$)**:
  - **训练**: 需存FP16参数、梯度、FP32优化器状态、FP32参数副本。约 $20\phi$ 字节 (忽略FP32梯度)。
  - **推理**: 仅存FP16参数及KV Cache。

- **激活值显存 (FP16)**:
  - Attention块: 约 $11bsh + 5bsh^2$ (含Dropout mask等)。
  - MLP块: 约 $19bsh$。
  - LN: 约 $4bsh$。
  - **总激活**: $L \times (34bsh + 5bsh^2)$ (L为层数)。

- **KV Cache**:
  - 存储Prefill阶段计算的K, V用于Decoding。
  - 显存占用: $2 \times b(s+n)h \times L \times 2$ 字节 (FP16, n为生成长度)。
