---
id: "lln-001"
difficulty: "L4"
category: "llm-core"
subcategory: "LLM前沿"
tags:
  - "llm-core"
---

# FLOPs计算

FLOPs表示浮点数运算次数， 对于大小为 
的矩阵 
来说计算矩阵乘法需要进行
次乘法和加法运算，所
以FLOPs为
。输入数据形状 

(M, N), (N, L)
A, B
MNL
2MNL
(batch_size, seq_length) = (b, s)
•
Embedding层
不涉及FLOPs，gather操作，根据ID取token embedding， 

(b, s) →(b, s, h)
•
self-attention层
◦

三个映射矩阵计算为 
，FLOPs 

QKV
(b, s, h) × (h, h)
3 × 2bsh2

◦

计算之前会做split heads操作，所以是

，总的FLOPs为 

QKT
(b, num_heads, s, h/num_heads) × (b, num_heads, h/num_heads, s) →(b, num_heads, s, s)
2bs h
2
◦

计算为 

，所以FLOPs为 

attention_score ⋅V
(b, num_heads, s, s) × (b, num_heads, s, h/num_heads) →(b, num_heads, s, h/num_heads)
2bs h
2
◦
Output线性映射之前会对多头进行融合，所以计算是 
，FLOPs为 

（b, s, h）× (h, h) →(b, s, h)
2bsh2
•
MLP层
◦
MLP 
乘法 
，FLOPs为 

h →4h
(b, s, h) × (h, 4h) →(b, s, 4h)
2 × 4bsh2
◦
MLP 
乘法 
，FLOPs为 

4h →h
(b, s, 4h) × (4h, h) →(b, s, h)
2 × 4bsh2
•
Output层
输出logits从hidden_size转词表大小 
，FLOPS为 

(b, s, h) × (h, V ) →(b, s, V )
2bshV
L层的总共FLOPs为 

L ∗(24bsh +
2
4bs h) +
2
2bshV
10.2.3 训练推理显存计算
•
训练阶段：
主要先分析模型参数、梯度和优化器状态。LLM训练多使用混合精度训练和AdamW优化器，FP16的模型参数量为
，那么
FP16的梯度为
，AdamW优化器状态中有FP32的一阶动量和FP32的二阶动量一共 
，训练的时候优化器更新模型参数的时
候还需要FP32的模型参数
，FP32的梯度
，按照FP16占2字节，FP32占4字节来算，训练阶段的显存占用总共有： 

字节。这里与第六章的 
不太一样，因为额外的FP32的梯度其实不是必须的，不同的实现当中不一
样
ϕ
ϕ
2ϕ
ϕ
ϕ
2 × 2ϕ + 4 × 4ϕ = 20ϕ
16ϕ
•
推理阶段：
推理阶段没有梯度、优化器状态，不用保存中间激活，占用显存大头为模型参数，还有KVCache也会占用显存
10.2.4 激活值计算
假设混合精度训练，中间激活都是FP16的，每个元素占用2个字节，
为batch_size, seq_length, hidden_size, num_head
bsha
•
self-attention块的激活值
◦

五个都需要保存中间激活，占用显存为 

x, Q, K, V , O
5 × 2bsh = 10bsh
◦

需要保存
，占用显存为 

softmax
QKT
2bs a
2
◦

需要保存分数，

attention_score ⋅V
2bs a
2
◦
中间会有两个dropout，dropout操作的mask矩阵每个
元素只占一个字节，一个在softmax之后，一个在输出映
射之后，总共 

bsh + bs a
2
总共占用显存 

11bsh + 5bs a
2
•
MLP块的激活值
◦
第一个线性层保存输入，占用 

2bsh
◦
激活函数保存输入，占用 

8bsh
◦
第二个线性层保存输入，占用 

8bsh
◦
最后dropout，占用 

bsh
总共占用 

19bsh
•
LN的激活值
LN需要保存输入，两个LN总共 

4bsh
对于L层的transformer来说，近似中间激活占用 

L × (34bsh + 5bs a)
2
10.2.5 KV Cache计算
推断（reference/generate）阶段用KVcache进行加速，分为两个阶段：
•
预填充 prefilling：prompt序列，同时生成所有
transformer层的KVcache
•
解码 decoding：利用prefilling的KVcache进行自回归生成
一个一个token生成，同时将KV更新到KVcache中
FP16精度存储，显存占用峰值为 
， 
为输入序列长度和输出序列长度，K，V的形状
就是 

b(s + n)h ∗L ∗2(两个K, V ) ∗2(FP 16)
s, n
(b, s + n, h)
