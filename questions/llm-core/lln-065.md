---
id: lln-065
difficulty: L2
category: llm-core
subcategory: 推理优化
tags:
- IO
feynman:
  essence: 动态管理推理批次，允许请求随时加入离开，消除GPU空闲等待。
  analogy: 像电梯运行，有人到了就下人，外面有人就上人，不用等电梯里所有人一起去同一层再换下一批。
  first_principle: 如何在变长序列推理场景下，最大化消除GPU因等待不同步请求而产生的空闲时间？
  key_points:
  - 将调度粒度从Sequence级降低到Token级
  - 请求完成后立即释放资源给新请求
  - 需配合PagedAttention管理KV Cache内存
follow_up:
- Continuous Batching 如何处理不同长度的输入?
- 它和 In-flight Batching 有什么区别?
---

# Continuous Batching(连续批处理)的原理是什么?为什么它能大幅提升推理吞吐

### Continuous Batching (连续批处理)

**Continuous Batching** 是 vLLM 等推理框架的核心优化技术，也称为 Iterative Batching 或 In-flight Batching。

### 传统 Batching 的问题
- 静态 batch 中不同请求的生成长度不同。
- 短请求完成后必须等待长请求，导致 GPU 空闲（气泡）。

### Continuous Batching 的方案
1. **Token 级别的动态调度**：每生成一个 token 就重新评估 batch 状态。
2. **即时加入/退出**：
   - 新请求随时加入 batch。
   - 完成的请求立即退出，释放 slot。
3. **配合 PagedAttention**：通过分页管理 KV Cache，避免内存碎片，支持动态显存分配。

### 性能提升
- 吞吐量提升 2-22 倍（相比静态 batch）。
- GPU 利用率从约 30% 提升到 80% 以上。

### 关键设计
每个时刻 GPU 都在处理有效的 token 生成，不会因为某个请求未完成而浪费算力。
