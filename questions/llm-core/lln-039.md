---
id: lln-039
difficulty: L1
category: llm-core
subcategory: LoRA与微调
tags:
- IO
feynman:
  essence: 减少参数更新量，降低通信开销，结合低精度技术
  analogy: 就像装修房子时只更换部分家具和灯具，而不是重建整个房子，省时省力。
  first_principle: 通过减少参数量和优化数据传输降低计算开销
  key_points:
  - 只更新部分参数（如Self Attention或特定层）
  - 减少多卡训练时的通信时间
  - 使用低精度技术（FP16、FP8、INT8）加速计算
---

# Lora是否能降低计算量

梯度分析

**其他原因：** 
1. 只更新了部分参数:比如LoRA原论文就选择只更新Self Attention的参数,实际使用时我们还可以选择只更新部分层的参数;
2. 减少了通信时间:由于更新的参数量变少了,所以(尤其是多卡训练时)要传输的数据量也变少了,从而减少了传输时间;
3. 采用了各种低精度加速技术,如FP16、FP8或者INT8量化等.
