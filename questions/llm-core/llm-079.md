---
id: "llm-079"
difficulty: "L2"
category: "llm-core"
subcategory: "注意力机制"
tags:
  - "IO"
  - "Elasticsearch"
feynman:
  essence: "1 概念与公式 🥥 概念与公式 **概念：** 尝试将注意力的计算复杂度从 降低到 或接近线性的方法,通常通过引入 核技巧(kernel trick)或因式分解将 转换为可分离的形式.。"
  analogy: "注意力机制就像在聚会上听人说话——自动聚焦到感兴趣的声音，给不同人不同关注度。"
  key_points:
    - "85. 1 概念与公式 🥥 概念与公式 **概念：** 尝试将注意力的计算复杂度从 降低到 或接近线性的方法,通常通过引入 核技巧(kernel trick)或因式分解将 转换为可分离的形式. O(n"
    - "Choromanski et al. (ICLR 2021) [Rethinking Attention with Performers] 提出 Performer,将 softmax 注意力用随机特"
    - "还有 Linformer (Wang et al. 2020)、Reformer (Kitaev et al. 2020) 等不同思路."
first_principle:
  problem: "为什么需要 Linear Attention(线性注意力)？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
---

# Linear Attention(线性注意力)是什么

85. 1 概念与公式
🥥 概念与公式 **概念：** 尝试将注意力的计算复杂度从 降低到 或接近线性的方法,通常通过引入 核技巧(kernel trick)或因式分解将 转换为可分离的形式. O(n ) 2 O(n) softmax(QK )ᵀ
**公式：**
, 其中 是某种 核映射函数,通过这种“先合并 ,再与 相乘”的方式将原本的 矩阵相乘转化 为两个 的操作(在理想情况下). Attention(Q, K, V ) ≈(φ(Q) [φ(K) V ]) / (φ(Q) [φ(K) 1])ᵀᵀ φ(⋅) V Q O(n ) 2 O(n)
**应用：**
• **仍然是解决长序列问题：** 希望把注意力的时间/空间复杂度降低到可接受范围. • 文本、语音、基因序列、图像 patch 等长序列场景下都可尝试.
85. 2 Linear Attention(线性注意力)
🥥 为什么要做“线性化”?
**标准注意力计算复杂度：**
,当序列长度 很大时(数千、上万 Token),开销(时 间、显存)非常惊人. O(n ) 2 n

**线性注意力目标：** 将标准注意力 通过某种近似或重构,使其计算复杂度 约为 或 ,从而能在长序列场景下更高效地应用. softmax(QK )Vᵀ O(n) O(n log n) **核心思路：** 将 近似为可分解形式 softmax(QK )ᵀ
**标准注意力公式：**

Attention(Q, K, V ) = V ⟹ n×n

softmax( )

d k QKᵀ O(n ) 计算 2 线性注意力则尝试把上式中的 用某种

**函数分解为：**

, 这样,就能把原本的 分解成若 干个向量乘法,再配合一些巧妙操作,使得计算不再需要显式构建 矩阵. softmax( )

d k QKᵀ φ(⋅) softmax(QK ) ≈ᵀ φ(Q) φ(K)ᵀ QK (n ×ᵀ n级别) n × n 典型示例

是一种核映射 / 随机特征近似 φ(⋅) Performer (Choromanski et al. 2021) 中:通过 随机特征映射(Random Feature Method)来近似 ,从而把 变为分解形式. exp(x) softmax(QK )ᵀ **其它方法：** 也可能使用可学习的核函数 ,或者使用 ReLU/ELU 的正变换等,核心都在于 将庞大的矩阵相乘改写为可分解的形式. φ(⋅) 公式展开(以 Performer 为例,示意) 假设 ,其中 是某种 ( 为中间核维度)的映 射,那么 softmax(QK ) ≈ᵀ φ(Q) φ(K)ᵀ φ(⋅) d →r r

Attention(Q, K, V ) = V ≈ n×n

softmax(QK )ᵀ

φ(Q) [φ(K) 1]ᵀ φ(Q) [φ(K) V ]ᵀ •

**的计算：**
φ(K) Vᵀ 先把所有 Key 经过 变成一个 矩阵(若有 个 Token),再与 (

) 相乘,有时可以通过前向累加的方式在 时间内就得到聚合结果. φ(K) n × r n V n × d O(n) •

**的计算：**
φ(Q) [φ(K) V ]ᵀ 每个 Query 向量 只需与 的结果做一次矩阵/向量乘,即不再是与所 有 Key 两两计算. φ(Q ) i φ(K) Vᵀ • 整体上,若设计得当,可以将原本需要显式构造 n×nn\times n 的步骤,替换为两次

的操作(一个是 的预先计算,一个是对每个 Query 做乘法). O(n) φ(K) Vᵀ 这样就达到了近似线性化的目的(虽然要注意,核映射维度 可能也会影响实际速度和内 存,但大体能显著降低针对 的二次方开销). r n 实际操作中的流程
1. 输入序列
,投影得到 . x , x , ... , x

1 2 n Q, K, V

2. 对 K, V 做核映射并合并:

Φ = φ(K) V , z =ᵀ φ(K) 1 (1表示全1向量,用于归一化)ᵀ
3. 对每个 Token i 的 Query
,用上面得到的

**来计算注意力输出：**

φ(Q ) i Φ, z head ≈ i

φ(Q ) z i φ(Q ) Φ i
4. 合并结果:将所有 Token 的注意力输出拼接或在序列维度上堆叠,得到与传统多头注意
力类似的结果. 应用与趋势 • **长序列文本：** 让 Transformer 可以处理上万 Token,无需 的大开销. O(n ) 2 • **多模态长序列：** 图像或视频分块处理、长音频、基因序列等. •
**代表性工作：** Transformers are RNNs (ICLR 2020) Performer (ICLR 2021) Linformer、Reformer 等也在探讨不同线性化/低秩分解思路. Katharopoulos et al. (ICLR 2020) [Transformers are RNNs: Fast Autoregressive Transformers with Linear Attention],引入线性化注意力思想; Choromanski et al. (ICLR 2021) [Rethinking Attention with Performers] 提出 Performer,将 softmax 注意力用随机特征近似进行线性化; 还有 Linformer (Wang et al. 2020)、Reformer (Kitaev et al. 2020) 等不同思路.
