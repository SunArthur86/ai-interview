---
id: "llm-099"
difficulty: "L2"
category: "llm-core"
subcategory: "DeepSeek-R1"
feynman:
  essence: "105. 1 什么是冷启动? 🫐在大型语言模型(LLM)的训练中,冷启动(Cold Start)指在应用强化学习(RL)之前,先 对模型进行一轮监督微调(SFT"
  analogy: "大语言模型就像读过整个互联网的学者——通过预测「下一个词」生成文本，积累了海量语言模式和知识。"
  key_points:
    - "🫐在大型语言模型(LLM)的训练中,冷启动(Cold Start)指在应用强化学习(RL)之前,先"
    - "对模型进行一轮监督微调(SFT).具体而言,这一过程涉及收集一小部分高质量的推理链"
    - "(Chain-of-Thought, CoT)数据,对基础模型进行微调,为后续的强化学习奠定基础."
first_principle:
  problem: "为什么需要 冷启动?为什么要冷启动？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
follow_up:
  - "在实际项目中如何应用？"
---

# 什么是冷启动?为什么要冷启动

105. 1 什么是冷启动?
🫐在大型语言模型(LLM)的训练中,冷启动(Cold Start)指在应用强化学习(RL)之前,先
对模型进行一轮监督微调(SFT).具体而言,这一过程涉及收集一小部分高质量的推理链
(Chain-of-Thought, CoT)数据,对基础模型进行微调,为后续的强化学习奠定基础.

105. 2 为什么要进行冷启动?
**🫐提升输出的可读性和一致性：** 直接通过强化学习训练的模型(如 DeepSeek-R1-Zero)可能
在生成内容时出现可读性差、语言混杂等问题.通过冷启动阶段的监督微调,模型能够学习
到更清晰、结构化的输出格式,增强用户体验.
**稳定强化学习过程：** 在没有初始监督微调的情况下,直接进行强化学习可能导致模型训练过
程不稳定.冷启动通过提供初始的监督信号,使模型在进入强化学习阶段时具备基本的推理
能力,促进训练的稳定性和效率.
**引导模型学习人类先验知识：** 冷启动数据通常包含人类设计的推理模式和总结,这些先验知
识有助于模型更好地理解任务要求,提升推理能力.
105. 3 怎么做冷启动
🫐在实践中,冷启动数据的收集通常包括以下步骤:
•
**生成初始数据：** 使用未经过监督微调的模型(如 DeepSeek-R1-Zero)生成包含详细推理
过程和反思的答案.
•
**人工后处理：** 对生成的内容进行人工审核和修改,确保其可读性和准确性.
•
**设计输出格式：** 采用统一的格式,例如在推理过程后添加总结部分,以提高输出的结构化
和可读性.
通过上述方法,冷启动阶段为模型提供了一个良好的起点,使其在后续的强化学习中能够更
有效地提升推理能力和输出质量.
