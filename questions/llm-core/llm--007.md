---
id: llm--007
difficulty: L2
category: llm-core
subcategory: 长上下文
tags:
- IO
feynman:
  essence: Infini用压缩记忆省显存，Ring用多卡通信换长度。
  analogy: Infini是记笔记只留精华；Ring是多人接力记完整流水账。
  first_principle: 如何突破显存限制实现超长文本处理？
  key_points:
  - Infini-attention：引入压缩记忆，实现单卡无限上下文
  - Ring Attention：利用多卡环形通信，线性扩展上下文长度
  - 前者改模型（显存恒定），后者改系统（算力换长度）
follow_up:
- Infini-attention 的压缩记忆如何避免信息丢失?
- Ring Attention 在多少 GPU 时有收益?
---

# Infini-attention 和 Ring Attention 分别解决什么问题?它们的原理是什么

- **两种扩展注意力窗口到无限长度的方案**

- **Infini-attention (Google, 2024)**
  **核心思想：** **压缩记忆 + 局部注意力**
  
  **原理细节：**
  - 将输入序列分段，每段内部使用标准的**因果自注意力**（捕获局部细粒度依赖）。
  - 引入一个全局的**压缩记忆**（通常存储在线性层参数中，通过 Delta Rule 更新：$M \leftarrow M + \sigma(Q K^T)$），捕获长期历史信息。
  - **输出融合**: $O = \text{softmax}(\frac{QK^T}{\sqrt{d}})V + Q M^T$。即局部注意力结果与记忆检索结果线性相加。
  
  **公式：** $A_{infi} = \sigma(A_{dot}) + (1-\sigma) \cdot A_{mem}$
  (其中 $\sigma$ 是 Sigmoid 门控，平衡局部注意力和长期记忆检索)。

  **优点：** 
  - **显存 O(1)**: 历史信息压缩在固定大小的记忆矩阵中，不随序列长度线性增长。
  - **无限上下文**: 理论上可处理无限长度输入。
  - **训练友好**: 兼容标准 Transformer 训练流程，无需特殊的分段并行策略。

  **架构图**:
```
Segment 1      Segment 2      Segment 3 (Current)
┌───────┐      ┌───────┐      ┌───────┐
│Local  │      │Local  │      │Local  │
│Attn   │      │Attn   │      │Attn   │
└───┬───┘      └───┬───┘      └───┬───┘
    │              │              │
    ▼              ▼              ▼
Update Mem    Update Mem    Retrieve Mem
    │              │              │
    └───────► [Compressed Memory] ◄────┘
                         │
                         ▼
                 Final Output = Local + Mem
```

- **Ring Attention (UC Berkeley, 2023)**
  **核心思想：** **跨 GPU 环形通信 + 块级注意力**
  
  **原理细节：**
  - **序列切分**: 将超长序列按 Block 切分，分布到环形拓扑连接的多个 GPU 上。
  - **流水线通信**: 每个 GPU 计算当前 Block 的 Attention，随后将自己的 KV Block 发送给 Ring 中的下一个 GPU，同时接收上一个 GPU 的 KV Block。
  - **累加计算**: 每收到一个新的 KV Block，就更新一次注意力分数（Softmax 的累加是可以分块进行的）。
  - **Flash Attention 结合**: 在计算局部 Attention 时使用 Flash Attention 优化 IO，通信与计算重叠以掩盖延迟。

  **优点：** 
  - **精确的长上下文**: 没有信息压缩，支持百万级上下文（1M+ tokens），注意力计算结果与非分段模型一致。
  - **模型无关**: 不修改模型结构（如 Attention 公式、FFN），纯系统级优化。
  
  **缺点：** 
  - **资源受限**: 需要的 GPU 数量与序列长度/上下文窗口大小成正比。
  - **通信开销**: 环形通信增加了整体延迟，尽管可以通过计算重叠掩盖，但仍受限于带宽。

  **架构图**:
```
 GPU 0  ──────────────► GPU 1  ──────────────► GPU 2 
  │  (KV0)             │  (KV1)             │  (KV2)
  │     Send KV0 ──────│───── Send KV1 ─────│───── Send KV2
  │◀──── Recv KV2      │◀──── Recv KV0      │◀──── Recv KV1
  │                    │                    │
  ▼                    ▼                    ▼
[Block 0]           [Block 1]           [Block 2]
Compute Attn       Compute Attn       Compute Attn
(KV0+KV1+KV2)      (KV1+KV2+KV0)      (KV2+KV0+KV1)
```

- **对比**: Infini-attention 改模型结构(显存O(1)，引入压缩损失)，Ring Attention 改系统调度(显存O(N)但在多卡分摊，无损注意力)。

- **## 常见考点**
  1. **Infini-attention 的记忆更新机制是什么？**
     通常使用简单的线性累加（类似 Delta Rule）作为联想记忆，或者使用线性层的权重作为记忆槽，这允许在不保留完整历史 Key-Value 对的情况下存储全局信息。
  2. **Ring Attention 如何保证 Softmax 的正确性？**
     利用数学性质：Softmax 可以分段计算分子和分母的最大值。$\text{Softmax}(X) = \frac{e^{x_i - \max(X)}}{\sum e^{x_j - \max(X)}}$。每个 GPU 只需维护当前的分母和，在 Ring 传递中累加即可。
  3. **这两种方法在实际部署中的权衡？**
     Infini-attention 适合单卡或显存受限且能容忍一定精度损失的场景（如超长文档摘要、无限对话）；Ring Attention 适合集群资源丰富且要求高精度的科研级长文本预训练或推理。
  4. **与 Sliding Window 的区别？**
     Sliding Window 严格丢弃窗口外的信息；Infini-attention 压缩了窗口外的信息；Ring Attention 保留了所有信息但分块计算。
