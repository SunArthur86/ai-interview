---
id: "llm-107"
difficulty: "L2"
category: "llm-core"
subcategory: "训练优化"
tags:
  - "IO"
  - "CAP"
feynman:
  essence: "- *Warmup:** 训练初期用极小学习率,逐渐增加到目标值. - *为什么需要 Warmup:** 1. **初始权重随机** - 大学习率会导致梯度爆炸"
  analogy: "CAP 定理就像不可能三角——一致性、可用性、分区容错，网络出问题时只能保两个。"
  key_points:
    - "Warmup: 训练初期用极小学习率,逐渐增加到目标值."
    - "为什么需要 Warmup:"
    - "初始权重随机 - 大学习率会导致梯度爆炸"
first_principle:
  problem: "为什么需要 warmup_ratio?训练时为什么要做 warmup?如何设置？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
follow_up:
  - "Warmup过短会怎样?"
  - "Cosine Decay和Linear Decay哪个好?"
---

# 什么是 warmup_ratio?训练时为什么要做 warmup?如何设置

- **Warmup:** 训练初期用极小学习率,逐渐增加到目标值.

- **为什么需要 Warmup:**
1. **初始权重随机** - 大学习率会导致梯度爆炸
2. **Adam优化器** - 需要时间累积梯度统计量
3. **稳定训练** - 逐步适应损失函数landscape

- **warmup_ratio:** warmup步数占总训练步数的比例.

- **典型设置:**

| 场景 | warmup_ratio | 学习率 |
|------|-------------|--------|
| 预训练 | 0.01-0.05 | 1e-4~3e-4 |
| SFT | 0.03 | 2e-5 |
| LoRA | 0.03 | 1e-4~3e-4 |
| 增量预训练 | 0.05-0.1 | 5e-5 |

- **调度策略:**
- Warmup → Cosine Decay(最常用)
- Warmup → Linear Decay
- Warmup → Constant

- **代码(HuggingFace):**
```python
training_args = TrainingArguments(
warmup_ratio=0.03,
lr_scheduler_type="cosine",
learning_rate=2e-5,
)
```
