---
id: lln-004
difficulty: L5
category: llm-core
subcategory: LLM前沿
tags:
- Transformer
feynman:
  essence: 利用KV缓存存储历史Key和Value，避免推理时重复计算，加速自回归生成。
  analogy: 像考试时记住前几题的解题思路，做下一题时直接拿来用，不用重新读题。
  first_principle: 如何消除LLM推理中重复计算历史Token Attention带来的性能瓶颈？
  key_points:
  - Prefill阶段计算并写入KV Cache，Decode阶段仅更新当前token的KV
  - 缓存形状为 [Batch, MaxSeq, Heads, HeadDim]
  - 解码时Attention计算需拼接历史KV与当前Q
  - 线性回归可由矩阵求逆解析解或SGD迭代求解
---

# 手撕MHA+KV Cache

- **手撕 MHA + KV Cache**:
  - **核心逻辑**:
    1. 初始化全零 `cache_k`, `cache_v` (最大长度)。
    2. Forward中计算当前步的 `xk`, `xv`。
    3. 更新缓存: `cache_k[:, start_pos:...] = xk`。
    4. 拼接历史Key/Value进行Attention计算。
  - **代码**:
```python
class Attention(nn.Module):
    def __init__(self, args):
        super().__init__()
        # 初始化KV Cache
        self.cache_k = torch.zeros((args.max_batch_size, args.max_seq_len, self.n_heads, self.head_dim)).cuda()
        self.cache_v = torch.zeros((args.max_batch_size, args.max_seq_len, self.n_heads, self.head_dim)).cuda()
        # ... 投影层 wq, wk, wv, wo 初始化 ...

    def forward(self, x, start_pos, freqs_cis, mask):
        bsz, seqlen, _ = x.shape
        # 1. 投影并重塑
        xq, xk, xv = self.wq(x), self.wk(x), self.wv(x)
        xq = xq.view(bsz, seqlen, self.n_heads, self.head_dim)
        xk = xk.view(bsz, seqlen, self.n_heads, self.head_dim)
        xv = xv.view(bsz, seqlen, self.n_heads, self.head_dim)

        # 2. 旋转位置编码
        xq, xk = apply_rotary_emb(xq, xk, freqs_cis)

        # 3. 更新 KV Cache
        self.cache_k[:bsz, start_pos : start_pos + seqlen] = xk
        self.cache_v[:bsz, start_pos : start_pos + seqlen] = xv

        # 4. 拼接历史 (Keys, Values)
        keys = self.cache_k[:bsz, :start_pos + seqlen]
        values = self.cache_v[:bsz, :start_pos + seqlen]

        # 5. Attention计算
        xq = xq.transpose(1, 2)
        keys = keys.transpose(1, 2)
        values = values.transpose(1, 2)
        scores = torch.matmul(xq, keys.transpose(-2, -1)) / math.sqrt(self.head_dim)
        if mask is not None:
            scores = scores + mask
        scores = F.softmax(scores.float(), dim=-1).type_as(xq)
        output = torch.matmul(scores, values)
        output = output.transpose(1, 2).contiguous().view(bsz, seqlen, -1)
        return self.wo(output)
```

- **手撕线性回归**:
  - **解析解**: $\theta = (X^T X)^{-1} X^T y$。
  - **SGD**: 迭代更新 $\theta = \theta - \alpha \cdot \nabla J$。
