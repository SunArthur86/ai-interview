---
id: llm-066
difficulty: L2
category: llm-core
subcategory: 推理优化
tags:
- Elasticsearch
images:
- llm100_p181_img1.png
feynman:
  essence: 按算力与访存瓶颈将推理分两段并行优化
  analogy: 像做一道菜：Prefill是备菜阶段，需要集中火力（算力）快速把所有食材切好并准备好（KV Cache）；Decode是炒菜阶段，不需要大火，但需要不断从冰箱（内存）取食材，一步一步炒出每一盘菜。
  first_principle: 如何解决Transformer推理中计算与访存负载不均导致的资源浪费？
  key_points:
  - Prefill阶段：计算密集，并行处理输入Prompt，生成KV Cache
  - Decode阶段：访存密集，串行生成Token，依赖KV Cache避免重复计算
  - 分离目的：针对不同瓶颈（计算量 vs 带宽）独立调度资源，提升吞吐与效率
follow_up:
- 在实际项目中如何应用？
---

# 什么是Prefill-Decode分离?为什么分离

Prefill-Decode分离是将大模型推理分为“预填充”（处理输入Prompt）和“解码”（逐个生成输出Token）两个阶段。Prefill是计算密集型任务，主要负责并行处理输入并生成KV Cache；Decode是访存密集型任务，负责基于KV Cache逐Token生成文本。分离两者是为了针对各自不同的资源瓶颈（算力vs带宽）进行独立的优化和调度，从而提升整体效率和吞吐量。
