---
id: "llm--022"
difficulty: "L2"
category: "llm-core"
subcategory: "推理与部署"
tags:
  - "IOC"
feynman:
  essence: "- *Embedding** 是将文本(或图片)转换为固定维度的向量(如 768 维浮点数组),使语义相近的内容在向量空间中距离也近. - *用途** 1. *"
  analogy: "Embedding 就像把词语放到语义地图上——意思相近的词距离近，无关的词距离远，让机器能计算「相似度」。"
  key_points:
    - "Embedding 是将文本(或图片)转换为固定维度的向量(如 768 维浮点数组),使语义相近的内容在向量空间中距离也近."
    - "语义搜索:'苹果手机' 和 'iPhone' 向量距离近"
    - "RAG 检索:从知识库中找最相关的文档"
first_principle:
  problem: "为什么需要 Embedding(向量化)?它在 AI 中有什么用？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "如何选择 Embedding 模型?"
  - "向量维度越高越好吗?"
---

# 什么是 Embedding(向量化)?它在 AI 中有什么用

- **Embedding** 是将文本(或图片)转换为固定维度的向量(如 768 维浮点数组),使语义相近的内容在向量空间中距离也近.

- **用途**
1. **语义搜索**:'苹果手机' 和 'iPhone' 向量距离近
2. **RAG 检索**:从知识库中找最相关的文档
3. **聚类分类**:相似内容自动分组
4. **推荐系统**:基于向量相似度推荐

- **直觉理解**
- king - man + woman ≈ queen
- 向量空间中'语义关系'可以用方向表示

- **常用模型**:BGE、text-embedding-ada-002、E5、M3E

- **一句话理解**:Embedding = 把文字变成'坐标',让计算机用距离来衡量'语义相似度'.
