---
id: llm-104
difficulty: L2
category: llm-core
subcategory: 模型架构
tags:
- IO
- IOC
feynman:
  essence: 训练时预测多个未来token，提升模型表征能力，推理无副作用。
  analogy: 学英语时同时预判后面三句话（MTP），提升语感，考试时只写一句即可。
  first_principle: 如何通过多步预测任务让模型更深地理解语言上下文依赖？
  key_points:
  - 训练时辅助预测多个token，增强语言理解
  - 推理阶段可丢弃MTP模块，无额外推理开销
  - 不同于Medusa，MTP是联合训练而非微调
  - 提升模型整体性能而非仅加速
follow_up:
- MTP如何改善训练效果?
- MTP模块的深度如何选择?
---

# DeepSeek V3 的 MTP(多令牌预测)是什么?和 Medusa 有什么区别

- **MTP (Multi-Token Prediction):**

传统模型一次预测 1 个 token。MTP 一次预测多个未来 token。

- **DeepSeek V3 的 MTP 实现:**
1. **主模型** 预测第 i 个 token
2. **MTP 模块** 预测第 i+1, i+2... 个 token
3. MTP 模块共享主模型的 embedding 层
4. 训练时用 MTP loss 辅助训练
5. **推理时可丢弃 MTP 模块**(不增加推理成本)

- **vs Medusa:**

| | Medusa | DeepSeek MTP |
|--|--------|-------------|
| 多头位置 | 解码器上 | 独立MTP模块 |
| 训练目标 | 推测解码 | 辅助训练+推测解码 |
| 训练方式 | 后加微调 | 联合从头训练 |
| 推理 | 增加额外计算 | 可选启用 |

- **核心价值:** 训练时提升表征学习能力，推理时可选启用加速。
