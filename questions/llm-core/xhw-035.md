---
id: "xhw-035"
difficulty: "L5"
category: "llm-core"
subcategory: "LLM前沿"
tags:
  - "Transformer"
---

# 手撕自注意力(用Pytorch)

📌Llama 3.1 通过在预训练和后训练阶段的多项创新，实现了性能和能力的全面提升。这些创新
包括：
1. 性能大幅提升：超越同规模模型，部分任务媲美 GPT-4。
2. 三阶段预训练：初始预训练、长文本预训练、退火训练。
3. 行级别去重：提高数据质量，提升模型性能。
4. 拒绝采样、SFT、DPO 训练策略：有效利用合成数据和用户数据，提升模型能力。
5. 负熵原则：引入外部高质量数据，持续优化模型。
6. 数据质量处理：多层次的数据过滤和清理。
7. Scaling Law 应用：合理配置模型规模和数据量。
8. 4D 并行训练：高效利用计算资源，训练超大规模模型。
9. 特定能力优化：针对代码、多语言等领域进行专项优化。
58. 手撕自注意力机制（用Pytorch）（Self Attention）
自注意力（Self-Attention）机制是Transformer的核心机制之一。它允许模型在处理某个序列中的一
个元素时，参考序列中其他元素的表示。这种机制通过计算查询（Query）和键（Key）的相似性，来
加权求和值（Value）。输出就是这些值的加权和，其中的权重是查询和键的相似性分数。

Attention(Q, K, V ) = softmax

V
(

d
k
QKT
)
Query（查询）
代表模型要对输入序列中的哪
些部分进行注意
Key（键）
每个输入元素都有对应的键，
表示它在整个序列中的表示
Value（值）
每个输入元素对应的值，最终
会通过注意力权重加权求
from math import sqrt
import torch
import torch.nn as nn
class SelfAttention(nn.Module):
    def __init__(self, dim_q, dim_k, dim_v):
        super(SelfAttention, self).__init__()
        self.dim_q = dim_q
        self.dim_k = dim_k
        self.dim_v = dim_v
        self.linear_q = nn.Linear(dim_q, dim_k, bias=False)
        self.linear_k = nn.Linear(dim_q, dim_k, bias=False)
        self.linear_v = nn.Linear(dim_q, dim_v, bias=False)
        self._norm_fact = 1 / sqrt(dim_k)
2
4
6
8
10
12
14

    def forward(self, x):
        batch, n, dim_q = x.shape
        assert dim_q == self.dim_q
        q = self.linear_q(x)  # batch, n, dim_k
        k = self.linear_k(x)  # batch, n, dim_k
        v = self.linear_v(x)  # batch, n, dim_v
        dist = torch.bmm(q, k.transpose(1, 2)) * self._norm_fact  # batch, n, n
        dist = torch.softmax(dist, dim=-1)  # batch, n, n
        att = torch.bmm(dist, v)
        return att
16
18
20
22
24
逐行代码解释
class SelfAttention(nn.Module)
#定义了一个继承自PyTorch的nn.Module类的自注意力机制模块。
class SelfAttention(nn.Module):
2
def __init__(self, dim_q, dim_k, dim_v)
    def __init__(self, dim_q, dim_k, dim_v):
        super(SelfAttention, self).__init__()
        self.dim_q = dim_q
        self.dim_k = dim_k
        self.dim_v = dim_v
        self.linear_q = nn.Linear(dim_q, dim_k, bias=False)
        self.linear_k = nn.Linear(dim_q, dim_k, bias=False)
        self.linear_v = nn.Linear(dim_q, dim_v, bias=False)
        self._norm_fact = 1 / sqrt(dim_k)
2
4
6
8
- 这里dim_q 、dim_k 和dim_v 分别代表查询、键和值的向量维度。
- linear_q 、linear_k 和linear_v 是线性变换（全连接层），将输入转化为查询、键和值
的向量表示。
◦
linear_q ：对输入进行线性变换，得到查询向量。
◦
linear_k ：对输入进行线性变换，得到键向量。
◦
linear_v ：对输入进行线性变换，得到值向量。
- _norm_fact = 1 / sqrt(dim_k) ：这是对注意力得分进行归一化的因子，用来避免数值不
稳定性，防止内积结果过大。

def forward(self, x)
    def forward(self, x):
        batch, n, dim_q = x.shape
        assert dim_q == self.dim_q
2
- forward 函数定义了模型的前向传播逻辑。
- 输入x 的形状是(batch_size, sequence_length, dim_q) ，即x 包含了一个批次的序
列数据。
◦
batch ：表示批次大小。
◦
n ：表示序列长度。
◦
dim_q ：表示输入的特征维度。
- assert 语句确保输入的维度与预定义的查询维度一致。
q、k、v计算
        q = self.linear_q(x)  # batch, n, dim_k
        k = self.linear_k(x)  # batch, n, dim_k
        v = self.linear_v(x)  # batch, n, dim_v
2
- q = self.linear_q(x) ：对输入x 进行线性变换，得到查询矩阵q 。
- k = self.linear_k(x) ：对输入进行线性变换，得到键矩阵k 。
- v = self.linear_v(x) ：对输入进行线性变换，得到值矩阵v 。
- 变换后，q 和k 的形状为(batch_size, sequence_length, dim_k) ，而v 的形状为
(batch_size, sequence_length, dim_v) 。
点积相似度计算
        dist = torch.bmm(q, k.transpose(1, 2)) * self._norm_fact  # batch, n, n
- torch.bmm(q, k.transpose(1, 2)) ：使用批次矩阵乘法bmm 计算查询q 与键k 的点
积相似度。k.transpose(1, 2) 将k 的形状从(batch, n, dim_k) 转置为(batch, 
dim_k, n) ，使得矩阵相乘能够计算各个查询与键的相似度。
- 计算结果dist 的形状为(batch, n, n) ，即每个查询与每个键之间的相似度矩阵。然后乘以
归一化因子self._norm_fact 。
