---
id: "slf-003"
difficulty: "L3"
category: "llm-core"
subcategory: "LLM前沿"
images:
  - "svg_transformer.svg"
feynman:
  essence: "多模态LLM让模型理解和生成文本、图像、音频等多种模态： 1. 视觉-语言模型（VLM）： 架构一：投影层（Projector） - Vision Encode"
  analogy: "视觉理解就像教 AI 看图说话——把图片变成数字表示，像处理文字一样理解图片。"
  key_points:
    - "视觉-语言模型（VLM）："
    - "Vision Encoder（CLIP/ViT）提取图像特征"
    - "线性层/MLP将视觉特征投影到文本特征空间"
first_principle:
  problem: "为什么需要 多模态大模型的架构有哪些？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 多模态大模型的架构有哪些？

多模态LLM让模型理解和生成文本、图像、音频等多种模态：

1. 视觉-语言模型（VLM）：

架构一：投影层（Projector）
- Vision Encoder（CLIP/ViT）提取图像特征
- 线性层/MLP将视觉特征投影到文本特征空间
- 与文本token一起输入LLM
- 代表：LLaVA、Qwen-VL
- 简单有效，但投影层信息损失

架构二：交叉注意力（Cross-Attention）
- LLM中插入cross-attention层
- 视觉特征作为K、V
- 代表：Flamingo

架构三：原生多模态
- 图像token和文本token在同一个Transformer中处理
- 代表：GPT-4V、Gemini

2. 音频-语言模型：
- Audio Encoder → LLM
- Whisper encoder + LLM

3. 全模态：
- Gemini Ultra：原生支持文本、图像、音频、视频

趋势：从拼接式（projector）走向原生多模态，统一token空间。
