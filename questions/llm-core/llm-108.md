---
id: llm-108
difficulty: L2
category: llm-core
subcategory: 训练优化
feynman:
  essence: 通过数据、张量、流水线和序列四种维度的切分来扩展训练规模。
  analogy: 像盖摩天大楼，不同人分别负责不同楼层（层）、房间（张量）、图纸（数据）和材料运输（序列）。
  first_principle: 如何突破单卡显存和算力限制，训练超大规模语言模型？
  key_points:
  - DP切分Batch，TP切分层内矩阵，PP切分层，SP切分序列长度
  - TP依赖NVLink高速通信，适合节点内；PP跨节点通信
  - ZeRO优化显存，常与TP/PP混合使用
  - Llama 3.1等超大模型结合了这四种技术
follow_up:
- TP和PP哪个通信量更大?
- ZeRO-3和TP有什么区别?
---

# 什么是 4D 并行训练?数据/张量/流水线/Expert 并行如何组合

- **4D 并行 = Data + Tensor + Pipeline + Expert(序列)并行**

| 维度 | 切分对象 | 通信类型 | 适合 |
|------|---------|---------|------|
| **数据并行(DP)** | Batch | AllReduce梯度 | 通用 |
| **张量并行(TP)** | 权重矩阵 | AllReduce激活 | 大模型 |
| **流水线并行(PP)** | 模型层 | 点对点激活 | 超大模型 |
| **序列并行(SP)** | 序列长度 | AllGather | 长上下文 |

- **组合策略:**
```
GPU总数 = DP × TP × PP × SP

例: 2048 GPU
DP=32, TP=8, PP=8 → 32×8×8=2048
```

- **Llama 3.1 的4D并行:**
- DP: 跨节点数据并行
- TP: 节点内8卡张量并行(NVLink高速互联)
- PP: 跨节点流水线并行
- SP: 序列并行处理长上下文

- **DeepSpeed ZeRO:**
- ZeRO-1: 优化器状态分片
- ZeRO-2: +梯度分片
- ZeRO-3: +模型参数分片(类似DP但更高效)
- 可与TP/PP叠加
