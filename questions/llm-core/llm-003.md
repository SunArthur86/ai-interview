---
id: "llm-003"
difficulty: "L2"
category: "llm-core"
subcategory: "参数高效微调"
feynman:
  essence: "**🎁高效性：** 通过低秩矩阵分解, 大幅减少了需要微调 的参数量.在大规模 预训练模型中,LoRA 仅引入一小部分新的 参数,节省了存储空 间和计算开销."
  analogy: "微调就像给通才毕业生做岗前培训——已有基础能力（预训练），再针对具体岗位做专项训练（指令/偏好对齐）。"
  key_points:
    - "Lora是怎样进行初始化矩阵的"
    - "为什么初始化时需要B初始化为零矩阵"
first_principle:
  problem: "从第一性原理看：LoRA 的优点 的根本优势/劣势来源于什么？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
follow_up:
  - "为什么LoRA用高斯随机初始化A而B用零初始化?"
  - "如果A和B都用零初始化会怎样?"
---

# LoRA 的优点

**🎁高效性：**
通过低秩矩阵分解, 大幅减少了需要微调 的参数量.在大规模 预训练模型中,LoRA 仅引入一小部分新的 参数,节省了存储空 间和计算开销. **📍冻结主干模型：** LoRA 保持原始模型的主干 权重 不变,仅调 整 和 矩阵.这 使得可以在多个任务 之间共享同一个预训 练模型,而不同任务 只需要微调少量的低 秩矩阵. W A B
**🌟通用性强：**
LoRA 可以应用在模型 的多种层次结构(如 自注意力层、前馈网 络层等),广泛适用 于各类 Transformer 模型.

3. Lora是怎样进行初始化矩阵的
在 LoRA 的实现中,矩阵 初始化为零矩阵,矩阵
使用随机高斯分布初始化,这样做的主要目的是
在微调开始时确保权重调整
是零矩阵,从而稳定训练过程并保持模型在初始阶段的表现与预
训练模型一致.下面详细解释这种初始化方式的原因和优点:
B A ΔW = 0
1. 为什么初始化时需要B初始化为零矩阵

LoRA 的目标是在不改变预训练模型原始权重
的情况下,通过微调来对模型进行轻量级的适配.如
果在微调一开始,矩阵 和 的初始值导致较大的权重变化(即 ),模型的表现可能会
发生突变,偏离原本的预训练模型表现,从而导致不稳定的训练过程.
W A B ΔW =0 通过将 初始化为零矩阵, 一开始为零矩阵,这样确保了在微调开始时: B ΔW = A × B

Wnew = W + ΔW = W + 0 = W
即,初始时微调模型的权重与预训练模型的权重保持一致.这确保了模型不会一开始就因为过大的权
重变化而导致性能波动.
2. 随机初始化A的原因
尽管 初始化为零,但
使用随机高斯分布进行初始化.这是为了确保在训练过程中,当
不再 为零矩阵时,
能够提供多样的参数更新方向.具体原因包括:
B A B A 📌 **增加表示能力：** 随机初始化
可以确保它具有丰富的表示能力,使得低秩矩阵
能
够探索广泛的权重调整空间.随机初始化提供了不同的起始点,从而能够在训练过程中捕捉
更多的特征. A A × B **避免对称性问题：** 如果
初始为零或相同的值,可能会限制模型在调整过程中学习到多样
化的特征.随机初始化打破了这种对称性,使得矩阵
能够在训练过程中学到更复杂的特征 组合. A A **提供有效的梯度信息：** 随机初始化
可以防止梯度更新过程中出现梯度消失的问题.初始
化时提供的非零梯度信息可以加速收敛,帮助模型更快地找到适应任务的最优参数.
A
3. 好处与优点
📌
**稳定性：** 在训练开始时,保证权重矩阵的变化是零,能够稳定训练过程,使模型在初始阶段
的表现与预训练模型一致.这样可以避免模型性能在训练初期因为过大的权重调整而大幅波
动. **渐进调整：** 通过将 初始化为零矩阵, 从零开始逐渐增加,权重调整逐渐介入模
型,而不是一次性强烈干扰模型的权重.这样能够让 LoRA 模块在稳定的基础上学习新的特
征. B ΔW
**保护预训练模型的效果：** 因为 LoRA 的初始更新为零矩阵,这样在刚开始的几个训练步骤
中,模型仍然依赖于预训练权重,保护了预训练模型在下游任务中的基础表现能力.
**减少梯度爆炸或消失的风险：** 如果
初始过大,可能导致梯度计算异常,进而引发梯度
爆炸或消失问题.通过保证初始时 为零,能够避免这些梯度异常情况的发生. ΔW ΔW
4. 总结

通过将 初始化为零矩阵以及将
随机初始化,LoRA 保证了模型在微调开始时的稳定性,并能逐
渐引入权重更新.这种初始化方式确保了模型在训练初期不会出现性能波动,同时提供了足够的参数
自由度来进行有效的任务适配,从而使 LoRA 在微调过程中能够实现高效且稳定的性能提升.
B A
