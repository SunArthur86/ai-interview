---
id: llm-060
difficulty: L2
category: llm-core
subcategory: 手撕代码
tags:
- IOC
feynman:
  essence: 两线性层夹非线性激活，逐点处理序列信息。
  analogy: 像消化系统，先分解再吸收，将输入的信息进行“消化”和“重构”。
  first_principle: 如何通过非线性变换增强模型表达能力？
  key_points:
  - 包含两个线性变换
  - 中间插入非线性激活函数(如ReLU)
  - 独立处理每个位置的特征
---

# 手撕Transformer中FFN代码

Transformer中的前馈神经网络(FFN)由两个线性层和一个非线性激活函数组成，用于对每个位置的特征进行非线性变换。公式：FFN(x) = max(0, xW1 + b1)W2 + b2。

Python代码实现：
```python
import torch
import torch.nn as nn

class FeedForwardNetwork(nn.Module):
    def __init__(self, input_dim, hidden_dim, output_dim):
        super(FeedForwardNetwork, self).__init__()
        self.linear1 = nn.Linear(input_dim, hidden_dim)
        self.relu = nn.ReLU()
        self.linear2 = nn.Linear(hidden_dim, output_dim)

    def forward(self, x):
        x = self.linear1(x)
        x = self.relu(x)
        x = self.linear2(x)
        return x
```
