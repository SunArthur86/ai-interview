---
id: xhw-005
difficulty: L2
category: llm-core
subcategory: 训练与微调
tags:
- 预训练
feynman:
  essence: CLM单向预测用于生成，MLM双向预测用于理解。
  analogy: CLM像“成语接龙”只能往后猜；MLM像“完形填空”可以看整句。
  first_principle: 如何让模型学会语言的生成逻辑与语义理解？
  key_points:
  - CLM是自回归的，从左到右预测，适合生成任务
  - MLM是自编码的，利用双向上下文填补空缺，适合理解任务
  - GPT使用CLM，BERT使用MLM
---

# CLM和MLM分别是什么

CLM 和 MLM 是两种不同的预训练语言建模目标：

**1. CLM (Causal Language Modeling)**
- **定义**：因果语言建模，即从左到右的自回归预测下一个词。
- **机制**：每个词只能看到它之前的上下文。
- **代表模型**：GPT 系列。
- **适用任务**：文本生成、创意写作。

**2. MLM (Masked Language Modeling)**
- **定义**：掩码语言建模，随机掩盖句子中的词，让模型根据上下文预测被掩盖的词。
- **机制**：利用双向上下文（前文和后文）。
- **代表模型**：BERT、RoBERTa。
- **适用任务**：文本分类、命名实体识别等自然语言理解任务。
