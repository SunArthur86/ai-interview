---
id: slt2-001
difficulty: L2
category: llm-core
subcategory: Transformer架构
images:
- svg_attention.svg
feynman:
  essence: 缩放点积方差，防止Softmax梯度消失。
  analogy: 数值太大输入softmax会导致“过饱和”，除以根号d像调音量旋钮。
  first_principle: 如何在高维空间维持稳定的梯度流？
  key_points:
  - 高维向量点积方差随维度增大
  - 缩放后方差回归1
  - 防止softmax进入饱和区
  - 保证梯度有效传播
---

# Transformer中Self-Attention为什么要除以根号d？

在计算QKᵀ后除以√d_k（d_k是key的维度），目的是控制内积结果的方差。

当d_k很大时，QKᵀ的值会很大，导致softmax函数进入梯度饱和区（导数趋近于0），训练困难。

数学推导：假设Q和K的元素是均值0、方差1的独立随机变量，则QKᵀ的方差为d_k。除以√d_k后，方差变为1，使softmax的输入保持在合理范围。

不除的后果：softmax输出接近one-hot，梯度几乎为0，训练停滞。
