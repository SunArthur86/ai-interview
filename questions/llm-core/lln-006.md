---
id: "lln-006"
difficulty: "L2"
category: "llm-core"
subcategory: "SFT与RLHF"
tags:
  - "IO"
  - "CAS"
  - "Elasticsearch"
images:
  - "llmnotes_p572_img1.jpg"
feynman:
  essence: "💡奖励模型的训练loss如下, 为 (prompt, response) 分配一个标量奖励值 r φ L = RM −E [log(σ(r (x, y ) −"
  analogy: "RLHF 就像给 AI 请人类老师——人类给回答打分排序，训练奖励模型，再用强化学习让 AI 越来越符合人类偏好。"
  key_points:
    - "💡奖励模型的训练loss如下, 为 (prompt, response) 分配一个标量奖励值 r φ"
    - "L = RM −E [log(σ(r (x, y ) − (x,y ,y )∼D"
    - "w l RM φ w r (x, y )))] φ l • 减少数据长度引起的偏差 偏好数据中存在一些固有的偏差,例如人类倾向于偏好较长、结构化比较好的响应,这样可能会误导大模型,过分强调 数据的次要特征,从而忽视了回复的内容质量 通过对c"
first_principle:
  problem: "从第一性原理看：Reward Model有什么优化的方法吗 的根本优势/劣势来源于什么？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "奖励模型训练中如何处理标注者分歧?"
  - "如何评估奖励模型的质量?"
---

# Reward Model有什么优化的方法吗

💡奖励模型的训练loss如下, 为 (prompt, response) 分配一个标量奖励值 r φ

L = RM −E [log(σ(r (x, y ) − (x,y ,y )∼D

w l RM φ w r (x, y )))] φ l • 减少数据长度引起的偏差 偏好数据中存在一些固有的偏差,例如人类倾向于偏好较长、结构化比较好的响应,这样可能会误导大模型,过分强调 数据的次要特征,从而忽视了回复的内容质量 通过对chosen/rejected偏好对数据的长度差进行分桶,不同的bucket里面放长度差相近的偏好数据,然后平衡每组 数据里面chosen/rejected更长的数量(比如尽量让chosen/rejected长度更长的样本数量一致,如果出现都是chosen 的回复更长或者都是rejected的回复更长,跟容易就学偏了,只学到了长度这种不重要的特征)

d = abs(∣tokenize(y )∣− w ∣tokenize(y )∣) l 此外,分桶的粒度也会影响效果,一般来说更细的桶范围可以更有效地减少长度偏差 •
**提升训练的稳定性：** 奖励模型训练过程中很容易出现奖励值大范围波动导致训练不稳定,所以训练过程中引入类似L2正则化损失,约束 奖励模型分数分布的波动性

L =
**REG**
REG r (x, y ) + φ w 2 r (x, y ) φ l 2 L = total L + RM L

**REG**
REG

•
**利用基于规则的奖励模型：**
对于类似数学、code一类的任务,有明确答案反馈(数学任务可以通过生成特定格式的回答,检测答案,code任务可 以通过编译器运行来检测答案),那么就可以直接应用rule-based奖励函数,这样可以提高奖励模型的可靠性,一定 程度防止reward hacking/exploition,其他任务则继续使用训练出来的RM • 利用CoT降低reward hacking: 构建偏好数据的时候可以提供最终奖励值以及引导出奖励值的思维链CoT数据,这样可以降低大模型过度利用奖励值的 风险 •
**模型集成缓解过优化问题：**
通过模型集成ensemble,可以降低奖励模型的方差,有助于缓解overoptimization问题 均值集成 Mean:均值优化不是一种保守的优化技术;如果集成中的至少一个成员高估了奖励(即使其他成员准确 估计了奖励),均值优化也无法阻止策略利用有错误的奖励模型

R =

R

k i ∑ i 最差例子集成 Worst-Case:主要优点是没有任何可能需要调整的超参数.然而,由于其高度保守的性质,有时可 能会导致性能下降

R = R

i min i 不确定性加权集成 Uncertainty-Weighted:奖励是通过将所有模型的平均奖励与方差相结合来计算的.主要远离 是对ensemble内奖励模型之间存在高度分歧的响应的策略进行惩罚,这有助于防止利用单个有错误的奖励模型

R :=

R − k i ∑ i λ

(R − k i ∑ i

R ) k i ∑ i 2
