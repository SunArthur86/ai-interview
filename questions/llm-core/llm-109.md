---
id: "llm-109"
difficulty: "L2"
category: "llm-core"
subcategory: "评估"
feynman:
  essence: "*PPL (Perplexity):** 衡量模型对文本的「困惑程度」。"
  analogy: "大语言模型就像读过整个互联网的学者——通过预测「下一个词」生成文本，积累了海量语言模式和知识。"
  key_points:
    - "PPL (Perplexity): 衡量模型对文本的「困惑程度」."
    - "PPL=1:模型完美预测每个token"
    - "PPL=10:模型每步平均在10个token间犹豫"
first_principle:
  problem: "为什么需要 PPL(Perplexity,困惑度)?如何用于评估大模型？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
follow_up:
  - "PPL和Cross-Entropy Loss有什么关系?"
  - "不同模型的PPL能直接比较吗?"
---

# PPL(Perplexity,困惑度)是什么?如何用于评估大模型

- **PPL (Perplexity):** 衡量模型对文本的「困惑程度」.

- **数学定义:**
```
PPL = exp(-1/N * Σ log P(x_i | x_{<i}))

PPL越低 → 模型越确信 → 质量越好
```

- **直觉理解:**
- PPL=1:模型完美预测每个token
- PPL=10:模型每步平均在10个token间犹豫
- PPL=100:模型非常不确定

- **用途:**
1. **模型对比** - 同一测试集上PPL越低越好
2. **训练监控** - 训练过程中PPL应持续下降
3. **语言质量** - 衡量生成文本的流畅性

- **局限性:**
- PPL低不代表回答准确(只衡量语言建模能力)
- 不同Tokenizer的PPL不可直接比较
- 对长文本不敏感(平均效应)

- **实践:** 预训练用PPL监控,下游任务用任务特定指标(accuracy/F1/BLEU)
