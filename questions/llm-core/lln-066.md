---
id: "lln-066"
difficulty: "L2"
category: "llm-core"
subcategory: "推理优化"
feynman:
  essence: "- *Prefix Caching** 是缓存重复前缀的 KV Cache 以加速推理的技术. - *原理:** 1. 计算 prompt 的 KV Cache"
  analogy: "语义缓存就像客服知识库——相似的问题不用每次重新查（LLM推理），直接从历史回答中找最相似的复用，省钱又快。"
  key_points:
    - "Prefix Caching 是缓存重复前缀的 KV Cache 以加速推理的技术."
    - "计算 prompt 的 KV Cache 后将其缓存"
    - "后续请求如果共享相同前缀,直接复用缓存的 KV"
first_principle:
  problem: "剥离所有术语：Prefix Caching(前缀缓存)的原理?在什么场景下效果最好 底层在做什么？为什么这样做是最优的？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "Prefix Caching 如何处理缓存失效?"
  - "vLLM 的 Automatic Prefix Caching 如何实现?"
---

# Prefix Caching(前缀缓存)的原理是什么?在什么场景下效果最好

- **Prefix Caching** 是缓存重复前缀的 KV Cache 以加速推理的技术.

- **原理:**
1. 计算 prompt 的 KV Cache 后将其缓存
2. 后续请求如果共享相同前缀,直接复用缓存的 KV
3. 只需要计算新增部分的 KV

- **典型场景:**
- 多轮对话:system prompt + 历史对话是重复前缀
- Few-shot:示例部分是重复前缀
- RAG 系统:检索到的文档可能被多次引用

- **效果:** 首 token 延迟降低 50-90%,特别适合 Agent 场景(每轮都带大量上下文).

- **注意事项:** 缓存淘汰策略(LRU/TTL)、显存管理、缓存一致性.
