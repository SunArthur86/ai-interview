---
id: "llm-070"
difficulty: "L2"
category: "llm-core"
subcategory: "Transformer架构"
tags:
  - "IO"
  - "IOC"
  - "索引"
  - "Elasticsearch"
feynman:
  essence: "1 计算量FLOPs估计方法 FLOPs,floating point operations,表示浮点数运算次数,衡量了计算量的大小.如何计算矩阵乘法 的FLOPs呢?对于。"
  analogy: "Transformer 就像高效的读书小组——每个人（注意力头）同时读不同段落，然后交流关键信息，不像 RNN 逐字读。"
  key_points:
    - "75. 1 计算量FLOPs估计方法 FLOPs,floating point operations,表示浮点数运算次数,衡量了计算量的大小.如何计算矩阵乘法 的FLOPs呢?对于 ,计算AB需要进行"
    - "实际中非线性激活ReLU等不计入太多额外 FLOPs) 16bsd model 2 将Self-Attention和MLP计算量相加 一个典型的Transformer层计算量为: • **Attent"
    - "当 较大时, 项(注意力的 和 )会 成为计算瓶颈. s d model 2 s s2 **QKT** QKT Attn ∗V 🌽Output层 (将隐藏向量映射为词表大小) 在训练语言模型或进行推断"
first_principle:
  problem: "为什么需要 Transformer模型中计算量(FLOPs)的分析？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
follow_up:
  - "什么是覆盖索引？有什么优势？"
  - "索引失效的场景有哪些？"
---

# Transformer模型中计算量(FLOPs)的分析

75. 1 计算量FLOPs估计方法
FLOPs,floating point operations,表示浮点数运算次数,衡量了计算量的大小.如何计算矩阵乘法 的FLOPs呢?对于 ,计算AB需要进行 次乘法运算和 次加法运算,共计 次浮点运算,需要 的FLOPS,对于 ,计算AB需要的浮点运算次数为 . A ∈R , B ∈ 1×n Rn×1 n n 2n 2n A ∈R , B ∈ m×n Rn×k 2mnk 🌵计算AB需要的浮点运算次数推算

**对于矩阵相乘：**
则

A ∈R , B ∈ m×n Rn×k C = AB ∈Rm×k 其中 .对于计算一个元素 : C = ij A B

r=1 ∑ n ir rj C

ij • 需要进行 次乘法运算(将 与 相乘) n A

ir B

rj • 需要进行 次加法运算(将这 个乘积相加) n −1 n 整个结果矩阵 有 个元素,每个元素需要 次乘法和

**次加法,因此：**
C m × k n n −1 •
**总的乘法次数为：**

m × k × n •
**总的加法次数为：**

m × k × (n −1) 在大多数情况下,为了简化近似,人们会认为加法次数与乘法次数相当(当 很大时,

),因此总的浮点运算量(FLOPs)大约为 (其中包含约 次乘法和

次加法). n n −1 ≈n 2mnk mkn mkn
75. 2 Transformer结构中FLOPs计算
下面将对所提及的计算步骤进行更为详细的解释与展开.这些步骤来源于Transformer中一层(单 层)的计算分析,包括Self-Attention部分和MLP(Feed-Forward)部分,以及最终输出logits的计

**算.假设我们有以下记号：**
•

: 批次大小 (batch size) b •

: 序列长度 (sequence length) s •

: 隐藏层维度 d

model •

: 前馈层扩张后的维度,一般为4倍

d

ff d

model •

: 注意力头数 (heads) h

•

: 每个注意力头的维度 d = k d /h model •

: 词表大小 (vocab size) V 在实际实现中,这些计算往往合并在一起执行,但为方便计算FLOPs和参数量,我们将各个步骤独立 拆开说明. 🥥Input输入层分析 💎 在一次训练迭代中,假设输入数据的形状为 ,经embedding层得 ,矩阵乘法 的输入和输出的形状为 ,计算量为

[b, s] [b, s, h] [b, s, V ] × [V , h] →[b, s, h] 2bshV 假设输入数据的形状为

**.其中：**
[b, s] 是batch size,即一次输入的样本数. b 是序列长度(例如处理一句话中有s个token). s 每个输入样本中的每个位置(token)对应着一个词表索引,这个索引指向词典中大小为 的词表,即词表有 个可能的词. V V

**Embedding层的本质：**
通常,embedding层会将每个词(token)的索引映射到一个长度为 的向量(embedding 向量). h 词嵌入矩阵

W ∈ emb RV ×h 是词表大小. V 是embedding的维度(隐藏维度). h 直观来看,embedding矩阵中的每一行是一个词的向量表示.当给定一个词的索引时,就直 接取出该行作为该词的embedding向量. 将Embedding表示为矩阵乘法: 在实际实现中,Embedding查表是通过索引取行完成的,这个操作本身不像传统的矩阵相乘 那样显式地执行 次乘法和加法.但如果我们将"取embedding"的过程用矩阵乘法的等价形 式来表示,就可以得到一个FLOPs的估计方法: V 将输入的词表索引用独热(one-hot)编码表示: 对于输入的 中的每个元素(一个token ID),用维度为 的one-hot向量来表 示. 则输入可以看作是 的三维张量,每个位置都是一个one-hot向量(这里原本 的 映射为 后会很大,但这是一种理论上的表示方法,用于FLOPs计算 近似). [b, s] V [b, s, V ] [b, s] [b, s, V ] **在这种表示下：**
▪

表示batch中所有tokens的独热表示 [b, s, V ] ▪

是embedding矩阵 W ∈ emb [V , h] 将 与 相乘,就得到

**的输出：**

[b, s, V ] [V , h] [b, s, h] [b, s, V ] × [V , h] →[b, s, h] 在这个运算中,每个 位置的结果是由长度为 的向量(one-hot编码)与 矩 阵中的对应列进行点积得到的. [b, s, h] V V × h

**计算FLOPs的细节：**
对于矩阵乘法 和 ,计算结果 的形状是 . A ∈Rb×s×V W ∈ emb RV ×h C = AW

emb [b, s, h] 对于产生 的每个元素(这里 对应batch内第几个样本与序列位置组合, 对应 embedding维度索引,共有 个元素),需要进行: C

ij i j b × s × h ▪

次乘法 V ▪

次加法(将这 V个乘积相加) V −1 当 很大时, ,为简化计算,往往取总运算约为 FLOPs( 次乘法 + 次加法 ≈ ).因此,每计算输出矩阵中的一个元素,需要约 次浮点运算.整个 [b, s, h] 的输出有 个元素.对每个元素进行约

**次运算：**

V V −1 ≈V 2V V V 2V 2V b × s × h 2V 总FLOPs ≈b × s × h × 2V = 2bshV 🍋Self-Attention部分计算展开 计算 Q, K, V 矩阵 **操作：** 输入形状为 ,通过三个独立的线性层映射得到 三个张量. [b, s, d ] model Q, K, V • 每个线性变换将 映射到 (实际上是 ,但合并后 仍为 ). [b, s, d ] model [b, s, d ] model [b, s, h × d ] k [b, s, d ] model • 对于每个线性层(如计算Q的那一层),矩阵乘法的形状为

**.转换为FLOPs计算时：**

(2倍是因为矩阵相乘需考虑乘法+加法两类操 作) [b × s, d ] × model [d , d ] → model model [b × s, d ] model F LOP s ≈2 × b × s × d × model d

model • 因为Q、K、V各自有一套这样的映射,所以总计约为:

3 × (2 ⋅b ⋅s ⋅d ) = model 2 6bsd

model 2 计算注意力权重

A = QKT

**操作：**
•

形状为 (通常实现中会reshape Q为这种四维形式,但总元素不变),

同理 . Q [b, s, h, d ] k K [b, s, h, d ] k • 为计算注意力分数,需要对每个头计算 ,输出形状为 .
**QKT**
QKT [b, h, s, s] • 对单个头而言, 与

**相乘时：**

Q ∈[b, h, s, d ] k K ∈[b, h, s, dk] [s, d ] × k [d , s] → k [s, s] • FLOPs约为 (对单头).有 个头并

**个batch：**

2 × s × s × d k h b

**FLOPs**
FLOPs ≈
**QKT**
QKT 2 ⋅b ⋅h ⋅s ⋅s ⋅d k

• 由于 ,代入可得到约 FLOPs. d = k d /h model 2bs d

2 model 将注意力分数与V相乘 (即计算 ) Attn(Q, K, V ) = softmax(QK )V T

**操作：**
• 已获得的注意力矩阵 与 相乘,输出形状为

[b, h, s, s] V ∈[b, h, s, dk] [b, h, s, d ]. k •
**对单个头而言：**
FLOPs约为

[s, s] × [s, d ] → k [s, d ] k 2 × s × s × d k • 考虑 :

b, h

**FLOPs**
FLOPs ≈ Attn∗V 2bhs d = 2 k 2bs d

2 model 注意力输出的线性映射

**操作：**
• 将拼接后的多头注意力输出 通过一层线性层映射回 . [b, s, d ] model [b, s, d ] model • 矩阵乘法 ,

[bs, d ] × model [d , d ] → model model [bs, d ] model FLOPs ≈2 ⋅b ⋅s ⋅d

model 2 小结Self-Attention计算量 将上述四个部分加在一起,典型总FLOPs约为: •

**投影：**

Q, K, V 6bsd

model 2 •

:

**QKT**
QKT 2bs d

2 model •

:

Attn ∗V 2bs d

2 model •
**输出线性映射：**

2bsd

model 2 当序列长度 很长时, 项( 和 )往往会主导计算复杂度. s s2

**QKT**
QKT Attn ∗V 🥥MLP (Feed-Forward)部分计算展开

,这里常用设定

FFN(x) = max(0, xW + 1 b )W + 1 2 b 2 d = ff 4d

model 第一个线性层 ( ) d → model d

ff

**操作：**

[b, s, d ] × model [d , d ] → model ff [b, s, d ] ff

**FLOPs约为：**

2 ⋅b ⋅s ⋅d ⋅ model d = ff 2bsd (4d ) = model model 8bsd

model 2 第二个线性层 ( ) d → ff d

model

**操作：**

[b, s, d ] × ff [d , d ] → ff model [b, s, d ] model

**FLOPs约为：**

2 ⋅b ⋅s ⋅d ⋅ ff d = model 8bsd

model 2 (两次线性映射加起来约 FLOPs;实际中非线性激活ReLU等不计入太多额外 FLOPs) 16bsd

model 2

将Self-Attention和MLP计算量相加 一个典型的Transformer层计算量为: • **Attention部分：** 约

+ Q,K,V

6bsd

model 2 +
**QKT**
QKT

2bs d

2 model + Attn∗V

2bs d

2 model

out linear

2bsd

model 2 • **MLP部分：** 约

16bsd

model 2 当 不大时, 项占主导;当 较大时, 项(注意力的 和 )会 成为计算瓶颈. s d

model 2 s s2

**QKT**
QKT Attn ∗V 🌽Output层 (将隐藏向量映射为词表大小) 在训练语言模型或进行推断时,最后往往需要将每个位置的隐状态向量 映射到 词表维度 来得到每个词的预测分布(logits): [b, s, d ] model [b, s, V ]
**操作：**

[b, s, d ] × model [d , V ] → model [b, s, V ]
**FLOPs约为：**
,这在词表 很大时也可能相当可观. 2 ⋅b ⋅s ⋅d ⋅ model V V 全部合计(一次训练迭代) 对于一个拥有 LL 层的Transformer,输入数据为

**的tokens：**
[b, s]
1. 首先经过Embedding
这部分FLOPs已在之前解释过(约 ,仅用于理 论比较). [b, s] →[b, s, d ] model 2bshV
2. 每层包括Attention和MLP的计算,将
层的计算相加得到总计算量. L
3. 最终输出层( logits计算 ):
, 约

FLOPs. [b, s, d ] × model [d , V ] → model [b, s, V ] [b, s, V ] 2bsd V model 所以整次训练迭代(不考虑反向传播和优化器更新)中,前向计算FLOPs会是Embedding、LL层 Transformer block、以及最终输出层的综合. 上面展示了各个步骤背后的矩阵乘法形状变化和FLOPs数量估计的原因.这种拆解有助于理解大模型 (如GPT系列)在训练和推理时的计算开销来源,以及在实践中为什么注意力机制对长序列输入的计 算成本如此高昂.
