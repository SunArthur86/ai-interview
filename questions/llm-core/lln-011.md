---
id: "lln-011"
difficulty: "L2"
category: "llm-core"
subcategory: "大模型架构"
tags:
  - "IO"
  - "CAS"
  - "Elasticsearch"
feynman:
  essence: "论文Efficient Training of Language Models to Fill in the Middle中提出了FIM方法,通过简单数据转换使自回归语言模型具备填"
  analogy: "FIM 就像做填空题——以前的模型只能从头写到尾（从左到右自回归），FIM 让模型在已知前后文的情况下补全中间缺失的部分，这对代码补全至关重要。"
  key_points:
    - "论文Efficient Training of Language Models to Fill in the Middle中提出了FIM方法,通过简单数据转换使自回归语言模型具备填"
    - "**如果有一个填空题：** To be or not to be, _ _ _ question Encoder模型主导的时代,注意力是双向的,所以可以非常"
    - "方便的填充中间 mask 的内容.Decoder-only模型,token只能从左往右生成,就没法填充中间的内容了.但是填充中间内容的需"
first_principle:
  problem: "为什么需要 FIM(Fill in the Middle)？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "CAS 存在什么问题？如何解决？"
  - "ABA 问题是什么？如何避免？"
---

# 什么是FIM(Fill in the Middle)

论文Efficient Training of Language Models to Fill in the Middle中提出了FIM方法,通过简单数据转换使自回归语言模型具备填
充中间文本能力
**如果有一个填空题：** To be or not to be, _ _ _ question Encoder模型主导的时代,注意力是双向的,所以可以非常
方便的填充中间 mask 的内容.Decoder-only模型,token只能从左往右生成,就没法填充中间的内容了.但是填充中间内容的需
求一直是存在的,比如有一段代码,想在函数名和函数体中间生成注释,Decoder-only模型就无法做到
**FIM的核心思想：** 通过对训练数据的简单修改,在不改变模型架构的情况下,casual自回归语言模型能学习填充中间文本,且不影
响其正常的从左到右生成能力
会将原始文本划分为prefix、suffix和middle三部分,上面的例子就可以表示成如下的格式:
<PRE>To be or not to be, <SUF> question<MID>This is a<EOS>
<PRE> 代表前缀,<SUF> 代表后缀,<MID> 代表填充开始,<EOS> 表示填充结束,训练的时候用上面的样本,预测的时候则
输入到<MID> 为止,然后进行预测.同时论文中也发现这样的FIM机制堆原来的模型性能几乎没有影响:

FIM数据的casual attention pattern:
