---
id: "xhw-049"
difficulty: "L4"
category: "llm-core"
subcategory: "Transformer"
tags:
  - "Transformer"
---

# Transformer计算量(FLOPs)分析

将Self-Attention和MLP计算量相加
一个典型的Transformer层计算量为：
- Attention部分：约 

 +
Q,K,V

6bsd

model
 +
QKT

2bs d
model
 +
Attn∗V

2bs d
model

out linear

2bsd

model
- MLP部分：约

16bsd

model
当 
 不大时，
 项占主导；当 
 较大时，
项（注意力的
和
）会
成为计算瓶颈。
s
d

model
s
s2
QKT
Attn ∗V
🌽Output层 (将隐藏向量映射为词表大小)
在训练语言模型或进行推断时，最后往往需要将每个位置的隐状态向量 
 映射到
词表维度 
 来得到每个词的预测分布(logits)：
[b, s, d
]
model
[b, s, V ]
操作：

[b, s, d
] ×
model
[d
, V ] →
model
[b, s, V ]
FLOPs约为：
，这在词表 
 很大时也可能相当可观。
2 ⋅b ⋅s ⋅d
 ⋅
model
V
V
全部合计（一次训练迭代）
对于一个拥有 LL 层的Transformer，输入数据为 
 的tokens：
[b, s]
1. 首先经过Embedding 
 这部分FLOPs已在之前解释过(约 
 ，仅用于理
论比较)。
[b, s] →[b, s, d
]
model
2bshV
2. 每层包括Attention和MLP的计算，将 
 层的计算相加得到总计算量。
L
3. 最终输出层( logits计算 )： 
,
 约 
 
FLOPs。
[b, s, d
] ×
model
[d
, V ] →
model
[b, s, V ] [b, s, V ]
2bsd
V
model
所以整次训练迭代（不考虑反向传播和优化器更新）中，前向计算FLOPs会是Embedding、LL层
Transformer block、以及最终输出层的综合。
上面展示了各个步骤背后的矩阵乘法形状变化和FLOPs数量估计的原因。这种拆解有助于理解大模型
（如GPT系列）在训练和推理时的计算开销来源，以及在实践中为什么注意力机制对长序列输入的计
算成本如此高昂。
76. Transformer模型中计算量(FLOPs)和参数的表格分析
下面的表格总结了Transformer中各个结构（以decoder-only框架为例）的参数量与计算量(FLOPs)估
计。为简化起见，表中给出的参数量与计算量为典型情形下的近似值，实际数值可能会因具体实现与
超参数变化而有所调整。
符号说明：
- : 层数
L

- : 隐藏层维度
d

model
- : FFN的扩张维度（通常为
）
d

ff
4 × d

model
- : 词表大小
V
- : 批次大小（batch size）
B
- : 序列长度（sequence length）
n
- : 注意力头数（通常满足
 ）
h
d
 =
model
h ⋅d
k
- 所有计算量(FLOPs)为前向传播的近似值（未包含反向传播和梯度计算），并使用典型的
乘加
运算计数方式（一次点积计算为2次FLOPs：一次乘法和一次加法）。
2×
1. 各组件参数量与计算量对比表
组件
参数量 (约)
前向计算 FLOPs (约)
备注
词嵌入
(Embeddin
g)

V × d

model
Embedding查表本身计算量极低；输出
logits阶段: 
FLOPs
2 × B × n × d
×
model
V
输入token索引转换为向量时
几乎无矩阵乘法；最后输出层
（logits计算）需要大规模矩
阵乘法。
SelfAttention

≈4 × d

model
1. Q,K,V投影:

 
3 × 2 × B × n × d
  ≈
model
 6 × B × n ×
d

model
2.
求注意力得分: 

QKT
≈2 × B × n ×
d

model
3. 将注意力得分与V相乘: 

≈2 × B × n ×
d

model
4. 输出投影: 

≈2 × B × n × d

model
注意力计算中，
和

是随序列长度 
二
次增长的主要来源。当序列较
长时，注意力部分的计算量
(O(n²))可能成为主要瓶颈。
QKT
Attn ∗V
n
MLP(FFN)

 
(若

)
≈8 × d

model
d
=
ff
4d

model
前两层全连接：
1.
:

d
→
model
d

ff
2 × B × n × d
×
model
d

ff
2.

d
→
ff
d
:
model
2 × B × n × d
×
ff
d

model
合计 

≈16 × B × n × d

model
FFN的计算量与序列长度 
成
线性比例增长
，参数量
通常比注意力部分更大，但计
算复杂度对比注意力的
关
系更为温和。
n
O(n)
n2
LayerNor
m (每层2
个)

 参
数量（可忽略）
2 × d

model
FLOPs可忽略不计
相比Attention和MLP，
LayerNorm的参数和计算成本
都很小，一般不计入主要开
销。
