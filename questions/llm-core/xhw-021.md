---
id: "xhw-021"
difficulty: "L4"
category: "llm-core"
subcategory: "LLM前沿"
tags:
  - "llm-core"
---

# BPE和BBPE Tokenization方法

- 合并策略：每次合并频率最高的符号对。
- 优点：简单高效。
- 缺点：只考虑了符号对的频率，可能合并出一
些不具有语言学意义的子词。
WordPiece
- 合并策略：每次合并能最大化语料库概率的符
号对。
- 优点：考虑了子词对整体语言模型的贡献，生
成的子词更有意义。
- 缺点：计算复杂度较高，需要计算每个合并候
选的 
。
DeltaL
🍋总结
- WordPiece算法通过考虑语言模型的概率，在合并子词时选择能最大化语料库总概率的合
并。
- 这种方法使得生成的子词更符合语言习惯，更有利于下游任务的处理。
- 与只考虑频率的BPE相比，WordPiece生成的词汇表在实践中往往表现更好。
31. 介绍一下BPE&BBPE这两种Tokenization方法
BPE（Byte Pair Encoding）和 BBPE（Byte-Level BPE）是自然语言处理中的两种常用的子词分割算
法。它们旨在解决词汇表规模和模型泛化能力之间的平衡问题，通过将单词分割为较小的子词单元来
降低词汇表的大小，同时保持良好的语言表示能力。
1. BPE（Byte Pair Encoding）
BPE是一种基于频率的贪心算法，最初用于数据压缩。它的核心思想是通过反复合并出现频率最高的
字符对（或子词对），从而生成一组子词单元。BPE特别适合处理低频词汇的问题，因为它可以有效
减少词汇表中的稀有词。
算法步骤
- 初始化时，将每个单词分解为字符序列。
- 计算所有字符对的出现频率，找到频率最高的字符对，并将其合并为一个新子词。
- 重复这个过程，直到达到预设的子词个数。
BPE的伪代码
📌Input: 单词列表
 合并次数
 
Output: 子词表
1. 将每个单词表示为字符序列
2. 初始化子词表为单个字符的集合
3. for i in 1 to N:
      3.1 计算每个字符对的频率
W
N

      3.2 找到频率最高的字符对 
 
      3.3 合并字符对 
 ，更新子词表
4. 返回子词表
(x, y)
(x, y)
数学表示
设字符对为 
，其频率为
 ，则每次迭代选择频率最高的字符对

 ，并将其替换为一个新子词
：

(x
, x
)
i
i+1
f(x
, x
)
i
i+1
argmax(f(x
, x
))
i
i+1
z
x
, x
 →
i
i+1
z
BPE通过多次这样的操作，最终形成一组有效的子词集合，可以同时处理频繁词汇和低频词汇。
BPE的优点
- 能够有效处理词汇稀疏性，尤其是低频词。
- 动态生成子词单元，使模型的词汇表规模可控。
2. BBPE（Byte-Level BPE）
BBPE是BPE的字节级扩展版本，主要应用于多语言模型（如GPT等）和处理Unicode字符的场景。与
标准BPE不同，BBPE操作在字节层面，而不是字符层面，这允许它以更加通用的方式处理任意编码的
文本（包括非拉丁字母）。
BBPE的主要区别
- BBPE对文本进行字节级别的操作，而不是字符级别。文本首先被转换为字节序列，再使用BPE进
行子词分割。
- 这种方法特别适合处理复杂的字符集，如汉字、阿拉伯字母等。
BBPE的算法步骤
与BPE类似，不同之处在于：
- 输入的不是字符序列，而是UTF-8字节序列。
- 在字节层面合并频繁出现的字节对。
3.  BPE & BBPE的实际应用
- BPE在各种自然语言处理任务中都有广泛应用，如机器翻译（如Google的神经机器翻译系统）。
- BBPE在多语言模型中使用频繁，特别是在跨语言预训练模型中（如GPT、BERT等）。
4. BPE 和 BBPE 的优缺点对比
