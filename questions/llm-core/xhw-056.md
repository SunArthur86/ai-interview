---
id: "xhw-056"
difficulty: "L4"
category: "llm-core"
subcategory: "Transformer"
tags:
  - "Transformer"
---

# Linear Attention线性注意力

Cross-Attention 步骤： 
◦
Latent 当作 Q，输入当作 K/V： 

 后得到更新后的 
Latent。
Q = ZW ,
K =
Q
XW
,
V =
K
XW
, Attention(Q, K, V )
V
重复多层：再让 Latent 与自身或与输入继续交互；在每一层中，Latent 会逐渐聚合输入信
息，变得更加“浓缩”或“抽象”。
输出：最终在 Latent 上做分类、回归或其他下游任务，这样就无需对庞大的输入序列逐 
Token 做大规模计算。
应用与趋势
高维输入场景：图像、视频、音频、3D 点云等，每一帧/每个 patch 都是一个“Token”，
可能有数万 Token；用“latent”做中间表示，可显著减少注意力计算规模。
多模态、跨模态：可让一组 latent 向量充当“融合容器”，将不同模态的信息集中。
强化学习、记忆型模型：通过外部隐变量或记忆槽，让模型在长时间跨度或复杂环境中保留
更抽象的状态表征。
85. Linear Attention（线性注意力）是什么？
85.1 概念与公式
🥥
概念与公式
概念：尝试将注意力的计算复杂度从
 降低到 
或接近线性的方法，通常通过引入
核技巧（kernel trick）或因式分解将
 转换为可分离的形式。
O(n )
O(n)
softmax(QK )
⊤
公式： 
, 其中
是某种
核映射函数，通过这种“先合并 
，再与 
 相乘”的方式将原本的 
 矩阵相乘转化
为两个 
 的操作（在理想情况下）。
Attention(Q, K, V ) ≈(ϕ(Q) [ϕ(K) V ]) / (ϕ(Q) [ϕ(K) 1])
⊤
⊤
ϕ(⋅)
V
Q
O(n )
O(n)
应用：
- 仍然是解决长序列问题：希望把注意力的时间/空间复杂度降低到可接受范围。
- 文本、语音、基因序列、图像 patch 等长序列场景下都可尝试。
85.2 Linear Attention（线性注意力）
🥥
为什么要做“线性化”？
标准注意力计算复杂度：
，当序列长度 
 很大时（数千、上万 Token），开销（时
间、显存）非常惊人。
O(n )
n

线性注意力目标：将标准注意力
 通过某种近似或重构，使其计算复杂度
约为 
或 
，从而能在长序列场景下更高效地应用。
softmax(QK )V
⊤
O(n)
O(n log n)
核心思路：将 
 近似为可分解形式
softmax(QK )
⊤
标准注意力公式： 

Attention(Q, K, V ) =
V
⟹
n×n

softmax(
)

d
k
QK⊤
O(n ) 计算
线性注意力则尝试把上式中的 
 用某种
函数分解为： 

, 这样，就能把原本的 
分解成若
干个向量乘法，再配合一些巧妙操作，使得计算不再需要显式构建 
 矩阵。
softmax(
)

d
k
QK⊤
ϕ(⋅)
softmax(QK ) ≈
⊤
ϕ(Q) ϕ(K)⊤
QK （n ×
⊤
n级别）
n × n
典型示例

 是一种核映射 / 随机特征近似
ϕ(⋅)
Performer (Choromanski et al. 2021) 中：通过 随机特征映射（Random Feature 
Method）来近似 
，从而把 
 变为分解形式。
exp(x)
softmax(QK )
⊤
其它方法：也可能使用可学习的核函数
，或者使用 ReLU/ELU 的正变换等，核心都在于
将庞大的矩阵相乘改写为可分解的形式。
ϕ(⋅)
公式展开（以 Performer 为例，示意）
假设
,其中 
 是某种 
（
 为中间核维度）的映
射，那么
softmax(QK ) ≈
⊤
ϕ(Q) ϕ(K)⊤
ϕ(⋅)
d →r
r
                             

Attention(Q, K, V ) =
V
≈
n×n

softmax(QK )
⊤

ϕ(Q) [ϕ(K) 1]
⊤
ϕ(Q) [ϕ(K) V ]
⊤
- 的计算： 
ϕ(K) V
⊤
◦
先把所有 Key 经过
 变成一个 
 矩阵（若有 
 个 Token），再与 
 (

) 相乘，有时可以通过前向累加的方式在 
时间内就得到聚合结果。
ϕ(K)
n × r
n
V
n × d
O(n)
- 的计算： 
ϕ(Q) [ϕ(K) V ]
⊤
◦
每个 Query 向量 
 只需与
 的结果做一次矩阵/向量乘，即不再是与所
有 Key 两两计算。
ϕ(Q
)
i
ϕ(K) V
⊤
- 整体上，若设计得当，可以将原本需要显式构造 n×nn\times n 的步骤，替换为两次

 的操作（一个是
 的预先计算，一个是对每个 Query 做乘法）。
O(n)
ϕ(K) V
⊤
这样就达到了近似线性化的目的（虽然要注意，核映射维度 
 可能也会影响实际速度和内
存，但大体能显著降低针对 
 的二次方开销）。
r
n
实际操作中的流程
1. 输入序列
，投影得到 
。
x
, x
, … , x
2
n
Q, K, V
