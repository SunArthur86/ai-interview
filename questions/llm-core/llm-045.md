---
id: "llm-045"
difficulty: "L2"
category: "llm-core"
subcategory: "对齐技术"
tags:
  - "IO"
  - "IOC"
feynman:
  essence: "DPO(Direct Preference Optimization)和 PPO(Proximal Policy Optimization)是两种用于优化 策略"
  analogy: "微调就像给通才毕业生做岗前培训——已有基础能力（预训练），再针对具体岗位做专项训练。"
  key_points:
    - "DPO(Direct Preference Optimization)和 PPO(Proximal Policy Optimization)是两种用于优化 策略的算法,尽管它们的应用场景和优化目标不同,但都属于策略优化方法.以下从目标函数、"
    - "PPO PPO是一种用于强化学习(Reinforcement Learning, RL)的策略优化算法.其目的是通过 与环境交互,在累计长期奖励的基础上优化策 略.PPO主要用于训练智能体如何通过连续的动 作获得最大的累计奖励."
    - "DPO DPO是一种偏好优化算法,主要用于偏好学习任 务(如大模型的偏好微调).它的目标是通过直 接从人类偏好数据中优化策略,使得输出更加符 合用户偏好,而不是通过显式的奖励模型进行优 化."
first_principle:
  problem: "它们本质上为什么不同？各自的设计目标和适用场景是什么？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
---

# 分析一下DPO和PPO的区别

DPO(Direct Preference Optimization)和 PPO(Proximal Policy Optimization)是两种用于优化 策略的算法,尽管它们的应用场景和优化目标不同,但都属于策略优化方法.以下从目标函数、算法 思路、优化方法、以及公式推导等方面进行对比,帮助理解它们的核心区别.
1. 目标和应用场景
**PPO**
PPO PPO是一种用于强化学习(Reinforcement Learning, RL)的策略优化算法.其目的是通过 与环境交互,在累计长期奖励的基础上优化策 略.PPO主要用于训练智能体如何通过连续的动 作获得最大的累计奖励.
**DPO**
DPO DPO是一种偏好优化算法,主要用于偏好学习任 务(如大模型的偏好微调).它的目标是通过直 接从人类偏好数据中优化策略,使得输出更加符 合用户偏好,而不是通过显式的奖励模型进行优 化.
2. 优化目标的对比
PPO的优化目标 PPO通过限制策略更新的幅度,来保证策略的稳定性, 避免策略发生过大的变化.其目标函数通过引入一个剪 辑函数来实现这一点.PPO的优化目标可以写作:

**PPO**
t [ ( t t t t)]
是策略更新比率, 是当前 策略, 是旧策略. r (θ) = t

old •

是优势函数(Advantage Function),用于衡量 某一动作的优劣. A t DPO的优化目标 DPO跳过了显式奖励模型的训练过程,直 接从偏好数据中优化策略.它的目标函数
**是最大化偏好分布下的对数似然：**

**DPO**
i,1 y

是一个超参数,用于限制策略更新的幅度,避免 策略发生过大的变化. ϵ PPO的核心思路是通过对策略更新的比例进行裁剪,限 制策略更新幅度,确保策略不会离旧策略过远,从而保 证训练的稳定性. •

DPO 的核心思想是直接根据人类偏好数据 来调整策略,使策略更符合人类的期望.

3. 策略更新的约束
PPO的策略约束 PPO通过引入剪辑机制控制策略的变化幅度,防 止策略在单次更新中偏离太多.通过限制策略更 新的比率 ,PPO避免了策略不稳定现象. 其目标函数中的clip函数对策略更新做了以下限 制: r (θ) t

old t t π (a ∣s ) θ t t 如果 超出了 范围,目标函数 就会强制剪裁,限制更新幅度,从而实现稳健的 更新过程. r (θ) t [1 −ϵ, 1 + ϵ] DPO的策略约束 DPO 通过KL散度进行约束,以确保新策略不会与 参考策略偏离过远.DPO 的策略更新形式如下:

Z(x) π (y∣x) exp r(x, y) ref ( β 1 ) 这里的 是参考策略,它通常是一个预训 练的语言模型.通过控制 ,DPO在参考策略和 优化后的策略之间找到平衡,确保策略的变化不 会过大. π (y∣x) ref β
4. 应用任务的区别
**PPO**
PPO PPO 适用于典型的强化学习任务,如在游戏环境 中训练智能体、机器人控制等.PPO算法通常在 奖励信号稀疏的情况下表现出色,通过不断交互 和调整策略,逐步提高累计奖励. DPO的策略约束 DPO 更适用于偏好学习任务,如在大型语言模型 上进行人类偏好微调.它不需要通过环境交互来 优化,而是通过人类提供的偏好数据直接优化生 成模型的输出.相比于PPO的强化学习场景, DPO避免了复杂的奖励模型构建.
5. 对比总结
**PPO**

**DPO**
DPO 目标函数 最大化长期奖励,加入KL约 束和剪裁机制 最大化人类偏好,直接优化策 略不需要显式奖励模型
**适用场景**
适用场景 强化学习,智能体交互场景 偏好学习,语言模型微调等 策略更新方式 基于剪裁的策略比率更新 基于KL散度和人类偏好直接 调整策略 优化方法 通过策略梯度法更新 通过对数似然优化偏好数据下 的策略 复杂性 依赖环境交互和奖励模型 不依赖奖励模型,通过偏好数 据直接学习 控制策略变化的方式 剪裁策略更新幅度 KL散度约束,控制策略偏离 参考策略
6. 总结与结论
🌟PPO 是一种基于强化学习的策略优化方法,通过引入剪裁机制来防止策略更新过大,确保策 略的稳定性.PPO主要用于长期累计奖励的优化,适用于典型的RL场景. DPO 是一种基于人类偏好直接优化策略的方法,通过对人类偏好数据的对数似然优化策略, 而不依赖于显式的奖励模型.DPO常用于大型语言模型的微调等任务. 两者在应用场景和优化目标上差异明显,PPO侧重于基于环境奖励的强化学习任务,而DPO则更适合 偏好学习的场景.
