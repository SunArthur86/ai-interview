---
id: "xhw-048"
difficulty: "L4"
category: "llm-core"
subcategory: "Transformer"
tags:
  - "Transformer"
---

# Transformer中MLP层参数分析

◦
将 
与 
相乘，就得到 
的输出：

[b, s, V ]
[V , h]
[b, s, h]
[b, s, V ] × [V , h] →[b, s, h]
在这个运算中，每个 
位置的结果是由长度为
的向量（one-hot编码）与
矩
阵中的对应列进行点积得到的。
[b, s, h]
V
V × h
计算FLOPs的细节：
 对于矩阵乘法 
和 
，计算结果 
 的形状是 
。
A ∈Rb×s×V
W
 ∈
emb
RV ×h
C = AW

emb
[b, s, h]
◦
对于产生
 的每个元素（这里
对应batch内第几个样本与序列位置组合，
对应
embedding维度索引，共有
个元素），需要进行： 
C

ij
i
j
b × s × h
- 次乘法
V
- 次加法（将这 V个乘积相加）
V −1
当 
 很大时，
，为简化计算，往往取总运算约为 
 FLOPs（
次乘法 + 
次加法 ≈ 
）。因此，每计算输出矩阵中的一个元素，需要约
次浮点运算。整个 [b, 
s, h] 的输出有
个元素。对每个元素进行约
次运算：

V
V −1 ≈V
2V
V
V
2V
2V
b × s × h
2V
总FLOPs ≈b × s × h × 2V = 2bshV
🍋Self-Attention部分计算展开
计算 Q, K, V 矩阵
操作：输入形状为 
，通过三个独立的线性层映射得到 
 三个张量。
[b, s, d
]
model
Q, K, V
- 每个线性变换将 
 映射到
（实际上是 
 ，但合并后
仍为 
）。
[b, s, d
]
model
[b, s, d
]
model
[b, s, h × d
]
k
[b, s, d
]
model
- 对于每个线性层（如计算Q的那一层），矩阵乘法的形状为 

。转换为FLOPs计算时：
 
（2倍是因为矩阵相乘需考虑乘法+加法两类操
作）
[b × s, d
] ×
model
[d
, d
] →
model
model
[b × s, d
]
model
F LOP s ≈2 × b × s × d
 ×
model
d

model
- 因为Q、K、V各自有一套这样的映射，所以总计约为：

3 × (2 ⋅b ⋅s ⋅d
) =
model
6bsd

model
计算注意力权重

A = QKT
操作：
- 形状为 
（通常实现中会reshape Q为这种四维形式，但总元素不变），
 
同理 
。
Q
[b, s, h, d
]
k
K
[b, s, h, d
]
k
- 为计算注意力分数，需要对每个头计算 
，输出形状为
。
QKT
[b, h, s, s]
- 对单个头而言，
与
相乘时：

Q ∈[b, h, s, d
]
k
K ∈[b, h, s, dk]
[s, d
] ×
k
[d
, s] →
k
[s, s]
- FLOPs约为 
（对单头）。有 
 个头并 
 个batch：

2 × s × s × d
k
h
b
FLOPs
 ≈
QKT
2 ⋅b ⋅h ⋅s ⋅s ⋅d
k

- 由于 
，代入可得到约 
FLOPs。
d
 =
k
d
/h
model
2bs d
model
将注意力分数与V相乘 (即计算 
)
Attn(Q, K, V ) = softmax(QK )V
T
操作：
- 已获得的注意力矩阵 
 与 
相乘，输出形状为 

[b, h, s, s]
V ∈[b, h, s, dk]
[b, h, s, d
]。
k
- 对单个头而言：
 FLOPs约为 

[s, s] × [s, d
] →
k
[s, d
]
k
2 × s × s × d
k
- 考虑 
：

b, h
FLOPs
 ≈
Attn∗V
2bhs d
 =
k
2bs d
model
注意力输出的线性映射
操作：
- 将拼接后的多头注意力输出 
通过一层线性层映射回
。
[b, s, d
]
model
[b, s, d
]
model
- 矩阵乘法 
，

[bs, d
] ×
model
[d
, d
] →
model
model
[bs, d
]
model
FLOPs ≈2 ⋅b ⋅s ⋅d

model
小结Self-Attention计算量
将上述四个部分加在一起，典型总FLOPs约为：
- 投影: 

Q, K, V
6bsd

model
- : 

QKT
2bs d
model
- : 

Attn ∗V
2bs d
model
- 输出线性映射: 

2bsd

model
当序列长度 
很长时，
 项（
和
）往往会主导计算复杂度。
s
s2
QKT
Attn ∗V
🥥MLP (Feed-Forward)部分计算展开

，这里常用设定 

FFN(x) = max(0, xW
 +
b
)W
 +
2
b
d
 =
ff
4d

model
第一个线性层 (
)
d
 →
model
d

ff
操作：

[b, s, d
] ×
model
[d
, d
] →
model
ff
[b, s, d
]
ff
FLOPs约为：

2 ⋅b ⋅s ⋅d
 ⋅
model
d
 =
ff
2bsd
(4d
) =
model
model
8bsd

model
第二个线性层 (
)
d
 →
ff
d

model
操作：

[b, s, d
] ×
ff
[d
, d
] →
ff
model
[b, s, d
]
model
FLOPs约为：

2 ⋅b ⋅s ⋅d
 ⋅
ff
d
 =
model
8bsd

model
(两次线性映射加起来约 
FLOPs；实际中非线性激活ReLU等不计入太多额外
FLOPs）
16bsd

model
