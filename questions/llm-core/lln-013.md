---
id: lln-013
difficulty: L2
category: llm-core
subcategory: 大模型架构
images:
- llmnotes_p590_img1.jpg
feynman:
  essence: 模型过度复制输入文本，缺乏创造性回应。
  analogy: 像学生只背诵课本答案，不会灵活应用。
  first_principle: 训练数据和生成策略共同决定模型输出多样性。
  key_points:
  - 数据偏差导致复制常见模式
  - 自监督学习目标限制生成多样性
  - 调整参数和过滤减少重复
follow_up:
- 如何从数据预处理角度解决复读机问题?
- repetition penalty的具体实现原理?
---

# LLMs复读机问题

**LLMs复读机问题**是指模型在生成文本时过度依赖复制输入内容，出现高频重复或缺乏创造性的现象，常表现为无限循环输出某短语。

### 核心成因与原理
1.  **曝光偏差**：训练时使用真实的上一词预测下一词，而推理时使用模型生成的上一词。如果模型生成一个概率稍低的词，后续分布可能偏移回常见的高频词，导致循环。
2.  **目标函数局限**：标准交叉熵损失鼓励模型拟合训练数据的共现频率。训练数据中若存在大量重复模式，模型会倾向于模仿这些高频但低信息量的序列。
3.  **解码策略缺陷**：贪婪搜索易陷入局部最优；Beam Search虽然保留多个候选，但若长度归一化处理不当，模型倾向于生成短且通用（甚至重复）的高概率序列以降低累积损失。

### 解决方案与参数细节
1.  **采样策略调整**：
    *   **温度参数**：提高温度（如 >1.0）拉平概率分布，增加低频词被选中的概率。
    *   **Top-k / Top-p (Nucleus) Sampling**：从概率最高的 k 个词或累积概率达 p 的词池中随机采样，打破确定性循环。
2.  **惩罚机制**：
    *   **重复惩罚**：对已经生成过的 token 在计算 logits 时减去一个惩罚值（penalty_alpha），降低其再次出现的概率。
    *   **频率惩罚**：根据 token 在当前回答中的出现频率进行线性惩罚。
3.  **训练阶段优化**：使用更高质量的去重数据，或在 RLHF 阶段对重复输出给予极低的奖励值。

## 常见考点
1.  **温度参数对多样性和准确性的权衡影响**（温度过高导致幻觉，过低导致复读）。
2.  **对比 Beam Search 和 Sampling 在解决复读问题上的本质区别**。
3.  **Exposure Bias 的具体含义及其在模型生成长文本时的副作用**。
