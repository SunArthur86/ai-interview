---
id: "xhw-028"
difficulty: "L5"
category: "llm-core"
subcategory: "LLM前沿"
tags:
  - "llm-core"
---

# DeepSpeed ZeRO系列原理

•
效果： 稳定了激活值的分布，防止梯度爆炸，提高训练的稳定性和速度。
3. 优化的激活函数
- 使用ReLU或其变体： ReLU函数的导数为0或1，避免了Sigmoid/Tanh函数导数过小的问题。
- 效果： 防止梯度消失，加速收敛。
4. 梯度裁剪（Gradient Clipping）
- 原理： 在反向传播时，对梯度进行裁剪，限制其最大范数。
- 效果： 防止梯度爆炸，保持数值稳定性。
梯度裁剪示意图
梯度计算 ---> [梯度裁剪模块] ---> 梯度更新
5. 权重初始化
- 原理： 使用合适的方法（如Xavier初始化）设置初始权重。
- 效果： 防止初始梯度过大或过小，提供良好的训练开端。
6. 优化器的选择：
- 使用自适应学习率优化器（如Adam）： 根据梯度的一阶和二阶矩动态调整学习率。
- 效果： 适应不同参数的更新需求，稳定训练过程。
39.4 总结
Llama模型通过结合Transformer架构的优势，有效地应对了梯度消失和梯度爆炸的问题：
- 残差连接为梯度提供了直接通路，防止梯度消失。
- 层归一化和梯度裁剪技术稳定了训练过程，防止梯度爆炸。
- 优化的激活函数和权重初始化确保了梯度的合理范围。
这些方法的综合应用，使得Llama模型能够在深层网络中有效学习，捕捉复杂的语言模式和特征。
40. DeepSpeed中ZeRO系列的原理
《DeepSpeed之ZeRO系列：将显存优化进行到底》
DeepSpeed的ZeRO系列是专为优化大规模深度学习模型训练而设计的技术，主要包括ZeRO-1、
ZeRO-2和ZeRO-3，每个版本在内存效率和训练速度上都有显著提升。
1. 常用的并行策略
数据并行（data parallelism, DP）

参考图
数据并行
假设有
张卡，每张卡都保存一个模
型，每一次迭代（iteration/step）都将
batch数据分割成
个等大小的microbatch，每张卡根据拿到的micro-batch
数据独立计算梯度，然后调用
AllReduce计算梯度均值，每张卡再独
立进行参数更新。
N
N
Step1 输入数据切分
输入数据切分实现上比较简单，一般有两种常用的实现方式：
方式一
在每个训练Epoch开始前，将整个训练数据集根
据并行进程数划分，每个进程只读取自身切分的
数据。
方式二
数据的读取仅由具体某个进程负责(假设为
rank0)。rank0在数据读取后同样根据并行进程
数将数据切分成多块，再将不同数据块发送到对
应进程上。
Step2 模型参数同步
数据并行实现的关键问题在于如何保证训练过程中每个进程上模型的参数相同。
因为训练过程的每一个step 都会更新模型参数，每个进程处理不同的数据会得到不同的Loss。由Loss
计算反向梯度并更新模型参数后，如何保证进程间模型参数正确同步，是数据并行需要解决的最主要
问题。根据下面中的梯度更新公式，只要保证以下两点就能解决这个问题：
数学公式：

W
 =
t+1
W
 −
t
lr × ΔW
这个公式表示在优化过程中，模型的参数
 在时间步
的值是由当前时间步
的参数
减去
学习率
乘以梯度
来更新的。这是一种常见的梯度下降法的参数更新规则。
W
t + 1
t
W
t
lr
ΔW
1. 每个进程上模型初始化参数
 相同。
W
2. 每个进程上每次更新时的梯度
相同。
ΔW
保证每个进程模型参数初始相同有两种常用的实现方法：
方法一
所有进程在参数初始时使用相同的随机种子并以
相同的顺序初始化所有参数。
方法二
通过个具体进程初始化全部模型参数，之后由该
进程向其他所有进程广播模型参数。
基于上述任意一种方法使每个进程得到一份相同的模型初始化参数后，梯度同步的数据并行训练就可
以进一步拆解为如下三个部分：
Step2.1 前向计算

每个进程根据自身得到的输入数据独立前向计算，因为输入数据不同每个进程会得到不同的Loss。
Step2.2 反向计算
每个进程根据自身的前向计算独立进行反向计
算，因为每个进程上的Loss不同，每个进程上在
反向中会计算出不同的梯度。这时一个关键的操
作是要在后续的更新步骤之前，对所有进程上的
梯度进行同步，保证后续更新步骤中每个进程使
用相同的全局梯度更新模型参数。
这一个梯度同步过程是用一个Allreduce sum同步通信操作实现的，对梯度使用Allreduce sum操作后
每个进程上得到的梯度是相同的，这时候的梯度值等于所有进程上梯度对应位置相加的和，然后每个
进程用Allreduce后的梯度和除以数据并行中的进程数，这样得到的梯度是同步之前所有进程上梯度的
平均值。
Step2.3 反向计算
每个进程经过上述步骤后得到相同全局梯度，然后各自独立地完成参数更新。因为更新前模型各进程
间的参数是相同的，更新中所使用的梯度也是相同的，所以更新后各进程上的参数也是相同的。
上述是主流框架中数据并行的实现过程。和单卡训练相比，最主要的区别在于反向计算中的梯度需要
在所有进程中进行同步，保证每个进程上最终得到的是所有进程上梯度的平均值。
模型并行（model parallelism/）
流水线并行和张量并行都可以看作是模型并行的一种，只是对模型切分的维度不同，流水线并行可以
看作是层间并行，将模型不同的层放到不同的GPU上，张量并行看作是层内并行，是对层内具体的矩
阵运算进行拆分。
A. 张量并行（tensor parallelism, MP/TP) 
 x为输入，A为权重参数，y为输出。
按列拆分

按列拆分是保持x不变，将A按列拆分，分别计算
后再拼接在一起。
按行拆分
按行拆分是将x按列拆分，A按行拆分，分别计算
后，再相加得到最终的输出。

在两张GPU上的具体的并行操作思想就是将进行运算的矩阵拆分到不同的GPU上，然后将每个GPU
上的运算结果进行汇总（GPU之间通信）。
B. 流水并行（pipeline parallelism, PP）
流水线并行PP（Pipeline Parallelism）
把一个参数量较大的模型按照不同的层进行划
分，将多个模型的层尽可能均匀的分布在不同的
GPU显存上，来起到装载更大模型的能力。
2. 显存消耗
混合精度训练（mixed precision training）和
Adam优化器基本上已经是训练语言模型的标
配。

Adam在SGD基础上，为每个参数梯度增加了一
阶动量（momentum）和二阶动量
（variance）。
混合精度训练是同时存在fp16和fp32两种格式的
数值，其中模型参数、模型梯度都是fp16，此外
还有fp32的模型参数，如果优化器是Adam，则
还有fp32的momentum和variance。
ZeRO将模型训练阶段，每张卡中显存内容分为两类：
1. 模型状态（model states）:
模型参数（fp16）、模型梯度（fp16）和Adam状态（fp32的模型参数备份，fp32的momentum和
fp32的variance）。假设模型参数量 
 ：
Φ
模型参数（fp16格式）
Φ
模型参数的总量为
，每个参数用16位（2字
节）来存储。因此模型参数共占用2
字节。
Φ
Φ
模型梯度（fp16格式）
Φ
梯度和模型参数的数量相同，都是
，每个梯度
值也用16位（2字节）存储。因此，梯度共占用2

字节。
Φ
Φ
Adam状态
Φ
Adam优化器是一种常用的自适应梯度优化算法，使用动量（momentum）和方差（variance）来动
态调整学习率。在训练中，为了保持数值稳定性，Adam优化器的状态通常以fp32（32位浮点数，4字
节）存储。
- 模型参数备份（fp32格式）
◦
Adam优化器会维护模型参数的fp32备份（通常用于更精确的权重更新）。这是
个fp32参
数，每个占4字节。
Φ
◦
存储开销： 4
字节。
Φ
- Momentum（动量，fp32格式）
◦
Adam的动量用于追踪模型参数的变化趋势。Adam为每个参数维护一个动量值，这些动量值以
fp32格式存储。
◦
存储开销： 4
字节。
Φ
- Variance（方差，fp32格式）
◦
Adam的方差用于调整学习率。每个参数有一个对应的方差值，存储为fp32格式。
◦
存储开销： 4
 字节。
Φ

假设模型参数量 
 ，则共需要 
  字节存储，可以
看到，Adam状态占比  75%  。
Φ
2Φ + 2Φ + (4Φ + 4Φ + 4Φ) = 4Φ + 12Φ = 16Φ
2. 剩余状态（residual states）
 除了模型状态之外的显存占用，包括激活值（activation）、各种临时缓冲区（buffer）以及无法使用
的显存碎片（fragmentation）。
3. DeepSpeed针对模型状态的存储优化（去除冗余）
ZeRO（Zero Redundancy Optimizer）通过 分区（partition） 技术优化模型状态的存储，即每张设
备（卡）只存储
的模型状态，从而系统内仅维护一份完整的模型状态。以下描述了分区操作在不同
模型状态上的应用：

N
1. 优化器状态（Optimizer States）的分区
首先进行分区的是模型状态中的 Adam状态，下图中的
表示分区的优化器状态，其中 os  指的是 
optimizer states。此时，模型参数（parameters）和 梯度（gradients） 仍然在每张设备上保持一
份拷贝。
P

os
- 每张卡的模型状态所需显存 为：

4Φ +
 字节
N
12Φ
- 当
较大时，显存占用趋向于：
，也就是原来 
的
。
N
4Φ 字节
16Φ 字节
1
2. 梯度的分区
如果继续对 模型梯度 进行分区，即图中的 
（其中 
代表梯度），模型参数仍然在每张卡保持
一份拷贝。
P

os+g
g
- 每张卡的模型状态所需显存 为：

2Φ +
 字节
N
2Φ + 12Φ
- 当 
较大时，显存占用趋向于：
，也就是原来
 的
 。
Φ
2Φ 字节
16Φ 字节
1
3. 模型参数的分区
如果再进一步对 模型参数 进行分区，即图中的
 （其中 
 代表参数），则：
P

os+g+p
P
- 每张卡的模型状态所需显存 为：
，当 
较大时，显存占用趋向于：

 字节
N
16Φ
N
0 字节
通过这种分区方法，ZeRO能够显著减少每张卡上的模型状态存储需求，极大地提升了训练大规模模型
的效率和可扩展性。

模型状态分区
ZeRO-1
它通过将优化器状态分散到多
个设备上，减少了单个设备的
内存占用。这意味着每个设备
只需要存储它所负责的一部分
参数和状态，从而支持更大的
模型。
ZeRO-2
在ZeRO-1的基础上，进一步分
散了梯度。每个设备不仅存储
其负责的优化器状态，还可以
共享其他设备的梯度。这种方
式显著提高了内存利用率，使
得训练更大模型成为可能。
ZeRO-3
结合了参数分片、梯度分片和
优化器状态分片，几乎实现
了“无状态”训练。通过将所
有训练所需的数据分散到不同
的设备，ZeRO-3使得即使在有
限的GPU内存中也能训练数百
亿参数的模型。
4. 通信数据量分析
常用的集合通信（collective communication）函数Collective Operations。
Reduce
操作描述：将多个处理器上的数据聚合成一个结
果。例如，对每个处理器的数据执行求和操作，
并将结果放到某个指定处理器上。
用法：常用于在多个GPU或节点上训练时，汇总
各个设备的梯度或参数。
数据流动：从多个设备到一个设备。
Broadcast
操作描述：将某个处理器上的数据发送到所有处
理器上，确保每个处理器都有相同的数据副本。
用法：通常用于分布式训练的初始化阶段，将模
型参数从一个主节点广播到所有参与计算的设备
上。
数据流动：从一个设备到所有设备。

ALLGather
操作描述：每个处理器都参与数据的汇总（如求
和或求平均），并将汇总后的结果分发给所有处
理器。即所有设备的输入都被聚合为一个结果，
并在每个设备上都有一份相同的结果。
用法：这是数据并行训练的核心通信操作，通常
用于在每一轮迭代之后计算全局梯度的均值，所
有设备都获取相同的更新后的梯度。
数据流动：每个设备互相通信，得到相同的结
果。
AllReduce
操作描述：每个处理器都参与数据的汇总（如求
和或求平均），并将汇总后的结果分发给所有处
理器。即所有设备的输入都被聚合为一个结果，
并在每个设备上都有一份相同的结果。
用法：这是数据并行训练的核心通信操作，通常
用于在每一轮迭代之后计算全局梯度的均值，所
有设备都获取相同的更新后的梯度。
数据流动：每个设备互相通信，得到相同的结
果。
ReduceScatter
操作描述：所有处理器将各自的数据发送给其他所有处理器，并在每个处理器上收集所有其他处理器
的数据。
用法：常用于分布式训练的参数收集或数据分片操作。它允许所有设备共享它们独有的数据或参数，
以便同步或更新。
数据流动：每个设备向所有设备发送并接收数据。
📌
结论
- 和 
的通信量与传统数据并行相同。
P

os
P

os+g

- 会增加通信量。
P

os+g+p
在传统的数据并行中，每一步（iteration）计算梯度后，需要进行一次 AllReduce 操作来计算梯度的
均值。常用的 Ring AllReduce 分为 ReduceScatter 和 AllGather 两步完成。每张卡的通信数据量
（发送+接收）近似为 
字节。
2Φ
  😈
的通信量
P

os+g
在 
方案中，每张卡只存储 
 的优化器状态和梯度。为了计算每张卡上的 
梯度的均值，通
信量的具体分析如下：
P

os+g

N
N
- 对于 gpu0 来说，它存储的是 
 的梯度。为了计算该部分的均值，需要进行一次 Reduce 操作，
通信数据量是 
 。

N
Φ ×
N
N = Φ
- 其余显卡不需要保存这部分梯度值。为了优化，系统使用了 bucket策略，保证 
 的梯度只发送
一次。

N
📌注意：  
如果模型的最后两层梯度保存在 gpu0 上，为了节省显存，其他卡会将这两层的梯度删除。
为了计算倒数第三层的梯度，仍然使用 bucket策略。这样，其他卡可以在发送梯度的同时计
算倒数第三层的梯度。当这两者都完成后，后两层的梯度可以删除。
- 当 gpu0 计算完梯度均值后，可以更新它的局部优化器状态。反向传播结束后，再进行一次 
Gather 操作来更新模型参数，通信数据量为  
。
Φ ×
N
N = Φ
从全局来看，这个过程相当于通过 ReduceScatter 和 AllGather 两步完成，通信量和数据并行一致。
  😈 
的通信量
P

os+g+p
在
方案中，每张卡只存储了 
的模型参数。因此，不论是在前向计算还是反向传播，都涉
及到一次 Broadcast 操作，这增加了通信量。
P

os+g+p

N
参考文章（强烈推荐阅读）
https://www.paddlepaddle.org.cn/support/news?action=detail&id=2762
数据并行:提升训练吞吐的高效方法 |深度学习分布式训练专题
飞桨PaddlePaddle-源于产业实践的开源深度学习平台
