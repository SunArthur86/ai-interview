---
id: "xhw-014"
difficulty: "L3"
category: "llm-core"
subcategory: "Transformer"
tags:
  - "Transformer"
---

# Transformer位置编码作用及局限性

缩放因子 
的引入可以防止随着 
 增加，点积值变得过大。点积的期望值大约与向量维度 
 
成正比，因此通过除以 
，可以将点积值缩放到一个合适的范围，确保数值稳定。理论上，两个独
立随机向量的点积的标准差大约与向量维度 
的平方根成比例，因此通过缩放可以将点积值控制在合
理范围内。

d
k
d
k
d
k

d
k
d
k
保持数值稳定性
缩放因子确保了 Softmax 的输入不会过大或过小，保证了注意力机制的数值稳定性。大幅度缩放的点
积会使 Softmax 输出接近 0 或 1，这导致注意力集中在特定位置，而通过缩放，注意力分布更加平
滑，模型能够更稳定地训练。
避免梯度消失
通过引入缩放，点积值进入 Softmax 后，其值不会过于极端，进而避免了梯度消失问题。梯度消失通
常出现在激活函数的输出值接近 0 或 1 时，通过缩放可以确保 Softmax 的输入位于合理范围内，确保
反向传播过程中梯度正常传播。
3. 数值举例
假设
 ，那么查询向量 
 和键向量 
 的点积结果的期望值可能在 64 量级。如果不进行缩
放，Softmax 的输入可能非常大，导致其输出值集中在某些位置（接近 1），其他位置则接近 0。此时
模型难以有效学习。通过将点积结果除以
  ，点积结果被缩放至较小范围，使得 Softmax 的
输出更加平滑，注意力分布更加合理。
d
 =
k
Q
K
 =
8
4. 总结
在 Softmax 之前对点积注意力分数进行缩放（除以
 ）是为了应对高维向量下点积值过大的问
题。这一操作可以：

d
k
1.
防止数值不稳定，避免注意力权重极端化，确保模型能够有效学习；
2.
避免梯度消失，通过限制输入值的范围，确保 Softmax 梯度不会过小，保障模型训练效率；
3.
加快模型收敛，缩放使得多头注意力机制在高维度下保持数值稳定性，从而提高模型的训练速度
和表现。
这个缩放机制在实际应用中证明是有效的，能够显著提升模型的稳定性和性能。
22. Transformer的位置编码作用及局限性？
1. Transformer的位置编码介绍
在Transformer模型中，位置编码（Position Encoding）用于引入序列中各个词的位置信息。由
于Transformer模型的自注意力机制（Self-Attention）不具备处理序列顺序的能力，因此需要位置编
码来补充这一信息，也是苏剑林大神在博客中写的打破Attention的置换不变性，位置编码的主要作用
是为模型提供词在序列中的相对位置或绝对位置，是加入对Attention的先验认知，或者赋予Attention
学习到这些先验认知性质的能力。从而帮助模型理解词语的顺序和语法结构。
