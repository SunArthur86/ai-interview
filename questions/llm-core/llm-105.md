---
id: "llm-105"
difficulty: "L2"
category: "llm-core"
subcategory: "训练优化"
feynman:
  essence: "- *SFT 后的五大问题:** 1. **过拟合(Overfitting)** - 模型在小数据集上过度拟合 - 缓解:数据增强、Dropout、早停、减小训"
  analogy: "过拟合就像死记硬背的学生——训练题完美，新题就懵。"
  key_points:
    - "SFT 后的五大问题:"
    - "过拟合(Overfitting)"
    - "模型在小数据集上过度拟合"
first_principle:
  problem: "为什么需要 SFT(监督微调)后会出现哪些常见问题?如何缓解？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
follow_up:
  - "如何判断是否发生了灾难性遗忘?"
  - "LoRA为什么能减少遗忘?"
---

# SFT(监督微调)后会出现哪些常见问题?如何缓解

- **SFT 后的五大问题:**

1. **过拟合(Overfitting)**
- 模型在小数据集上过度拟合
- 缓解:数据增强、Dropout、早停、减小训练轮数

2. **灾难性遗忘(Catastrophic Forgetting)**
- 忘记预训练知识
- 缓解:低学习率(2e-5)、LoRA(冻结主干)、混合预训练数据

3. **模型偏差增加**
- SFT数据集的偏差被放大
- 缓解:数据均衡、多样化来源

4. **泛化能力下降**
- 只在SFT任务上表现好,其他任务退化
- 缓解:通用+领域混合训练、指令多样化

5. **鲁棒性降低**
- 对输入扰动敏感
- 缓解:对抗训练、输入增强

- **最佳实践:** LoRA微调 + 混合数据(领域20%+通用80%) + 低学习率 + 监控验证集loss
