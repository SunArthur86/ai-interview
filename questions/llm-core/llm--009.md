---
id: llm--009
difficulty: L2
category: llm-core
subcategory: 对齐训练
tags:
- IO
feynman:
  essence: GSPO和DAPO是对GRPO的改进，分别从序列级采样和动态自适应角度提升稳定性。
  analogy: GRPO给小组评分，GSPO直接给整组作业评分，DAPO根据作业难度动态调整评分标准。
  first_principle: 如何优化强化学习中的信用分配机制以适应不同任务特性？
  key_points:
  - GRPO基础：组内相对优势，无需奖励模型
  - GSPO改进：序列级采样，降低方差，更稳定
  - DAPO改进：动态调整权重与clip范围，适合长推理
follow_up:
- 为什么 token 级 KL 散度不如序列级稳定?
- DAPO 的自适应 clip 如何设计?
---

# GSPO 和 DAPO 是什么?它们与 GRPO 有什么区别

- **GSPO (Group Sequence Policy Optimization)**

是对 GRPO 的改进，核心变化:
1. **序列级重要性采样**: GRPO 在 token 级别做重要性采样，梯度更新粒度细但方差大；GSPO 改为序列级别，将整个序列作为一个更新单元。
2. **降低方差**: 序列级归一化减少训练不稳定性，利用 Group 内其他序列作为基准，计算相对优势。
3. **更稳定的 KL 控制**: 用序列级 KL 散度(新旧策略在整个输出分布上的差异)替代 token 级，防止策略漂移过快。

- **DAPO (Dynamic Adaptive Policy Optimization)**

另一个 GRPO 变体，针对长文本生成优化:
1. **动态优势权重**: 根据生成质量动态调整正负样本的权重。对于 Reward 极高的样本增加其梯度的贡献度，低分样本降低贡献度。
2. **自适应 clip 范围**: PPO 的 clip 范围 ε 固定，DAPO 根据训练进度和 Reward 方差自适应调整 ε，初期收敛快，后期稳。
3. **长序列友好**: 对长推理链(如数学证明)的奖励信号更敏感，解决了长序列中后期 Token 梯度消失的问题。

```text
GRPO / GSPO / DAPO 核心流程对比：

┌─────────────────────────────────────────────────────────────┐
│ 输入 Prompt                                                 │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
         ┌──────────────────────────────┐
         │ 生成 G 个输出序列 (y1...yG)   │
         └─────────────┬────────────────┘
                       │
           ┌───────────┴───────────┐
           ▼                       ▼
┌──────────────────┐     ┌──────────────────────┐
│   GRPO (Token)   │     │ GSPO / DAPO (Seq)    │
├──────────────────┤     ├──────────────────────┤
│ 计算 Token 级    │     │ 计算 Seq 级 Reward   │
│ 优势 A_i,j       │     │ 优势 A_i (整体)      │
│ (基于 Value/组内)│     │ (Group 归一化/动态)  │
└────────┬─────────┘     └───────────┬──────────┘
         │                          │
         ▼                          ▼
┌──────────────────┐     ┌──────────────────────┐
│ Clip (固定 ε)    │     │ Clip (动态 ε) / Seq  │
│ Token 级更新     │     │ 级更新               │
└──────────────────┘     └──────────────────────┘
```

- **三者核心对比**

| 维度 | GRPO | GSPO | DAPO |
|------|------|------|------|
| 采样粒度 | Token 级 | 序列级 | 序列级 (动态加权) |
| KL 控制 | Token 级 | 序列级 | 自适应 |
| 优势估计 | 组内相对 (GAE风格) | 序列归一化 (整体排名) | 动态加权 (质量敏感) |
| 适用场景 | 通用短文本 | 稳定性优先/长文本 | 长推理链/数学证明 |

- **总结**: 从 GRPO → GSPO/DAPO 的演进，本质上是在解决“如何更稳定、更有效地将序列级奖励分配到 token 级别”的信用分配问题。Token 级更新灵活但噪声大，序列级更新稳定但可能损失细节。

## 常见考点
1. **为什么 GRPO 不需要 Critic (Value) 模型？**（它利用 Group 内样本的相对分数作为优势估计，去掉了对 Value Model 的依赖）
2. **GSPO 的“序列级”具体是指什么？**（指计算 Importance Ratio 和 KL 时是对整个序列的概率求和/平均，而不是逐 token 计算）
3. **DAPO 的动态 Clip 是如何实现的？**（通常根据 Reward 的统计量或训练 Step 数来调整 PPO 的 epsilon 参数）
