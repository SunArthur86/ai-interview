---
id: "xhw-022"
difficulty: "L5"
category: "llm-core"
subcategory: "Transformer"
tags:
  - "Transformer"
---

# RoPE位置编码

目前，主流的大模型（如GPT系列、BERT、T5等）大多数采用的是BBPE（Byte-Level BPE）或类似
的字节级tokenization方法。这种方法广泛应用的原因包括：
处理多语言文本的能力： 大模型通常需要处理大量不同语言的文本，而BBPE在字节级别操作，能够
处理所有Unicode字符，特别是对于包含复杂字符集的语言（如中文、韩语、阿拉伯语）表现良好。
统一的tokenization ： BBPE方法不依赖于语言特定的字符结构，因此可以在不增加额外复杂性
的情况下，统一应用于多语言任务，简化了预训练和下游任务中的tokenization流程。
兼容表情符号和特殊字符： 现代大语言模型需要处理大量互联网数据，这些数据中包含许多表情符
号、特殊字符以及非标准符号，而BBPE可以更好地支持这类符号。
6. 为什么主流大模型使用BBPE而非BPE？
多语言与统一性： 随着模型规模的扩大，支持多种语言和字符集成为必然需求。BBPE通过在字节层
面的操作，能够避免各语言tokenization不一致的问题。
处理多种字符： 由于BBPE可以处理所有Unicode字符，能够无缝处理非拉丁字符和特殊符号，具备
更好的通用性。
7. 总结
BPE 和 BBPE 通过将词分割成子词的方式来平衡词汇表的大小与语言建模的泛化能力。BPE适合处理
较为常规的语言任务，而BBPE适合多语言和字符集复杂的场景。
32. 介绍一下RoPE位置编码
在Transformer中，位置编码（Positional Encoding, PE）是为了让模型感知序列中元素的顺序。
传统Transformer使用的是绝对位置编码，即对序列中每个位置赋予一个唯一标识符，例如
Sinusoidal位置编码，通过一组固定的正弦和余弦函数编码每个位置。然而，绝对位置编码存在局限
性：无法直接捕捉元素之间的相对位置信息，而后者对很多自然语言任务至关重要。相对位置编码能
让模型知道两个词之间的距离，这在如句法分析等任务中非常有用。

1. RoPE的动机
RoPE的目标是： 通过绝对位置编码的方式实现相对位置编码，即不改变现有Transformer框架下绝对
位置编码的方式，但能让模型自动捕捉元素之间的相对位置信息。
RoPE的理论基础是通过在查询（query）和键（key）向量上施加一个旋转变换，将位置信息嵌入这
些向量中。RoPE对位置编码的设计不仅适用于传统的Attention机制，还能扩展到线性Attention，这
一点是其创新所在。
2. RoPE的核心思想
假设序列的查询和键向量分别为 
 和 
，我们需要设计一个函数 
和 
，将查询和键
向量转换为带有位置信息的形式。RoPE通过将位置信息以旋转变换的形式嵌入向量，实现相对位置编
码。
q
k
f(q, m)
f(k, n)
我们期望Attention机制的核心运算内积运算（dot product）带有相对位置信息。因此，需要满足以
下恒等式：

⟨f(q, m), f(k, n)⟩= g(q, k, m −n)
📌m：表示查询向量 
 在序列中的位置索引。例如，如果你有一个序列
，那么查询 
对应于某个特定的序列元素，它的索引是 
，即 
，表示第 
 个位
置的查询向量。
q
[w
, w
, w
, … , w
]
2
n
q
m
q = q

m
m
n：表示键向量 
在序列中的位置索引。同理，键向量 
 对应序列中的第 
 个元素。
k
k = k
n
n
m - n：表示查询和键之间的相对位置差，这正是RoPE希望通过旋转变换捕捉的位置信息。
即在内积运算中，查询和键向量的相对位置 
 影响最终的Attention权重。这要求 
 和

  的设计不仅带有绝对位置信息，还能让它们的内积反映出相对位置信息。
m −n
f(q, m)
f(k, n)
3. 推导过程
二维情形下的推导 
📌通俗解释：
假设我们有两个向量，一个是查询向量 
，一个是键向量 
，我们希望它们不仅包含内容的
信息，还能够通过某种方式带上它们的位置（位置编码）。通过将每个向量旋转一定角度，
我们就可以让这些向量携带它们在序列中的位置信息。
q
k
旋转角度由它们的序列位置决定（位置越靠后，旋转的角度越大）。当我们计算它们的内积
时，复数乘法让旋转后的向量与原始向量结合起来，最终使得Attention计算中能够反映出查
询和键的相对位置。
总结：
- 用复数是为了方便表达旋转，而旋转是用来嵌入位置编码的方式。

- 用复数是为了方便表达旋转，而旋转是用来嵌入位置编码的方式。
- 复数的旋转特性使得我们可以用一个很简洁的方式在向量中加入位置信息，从而让模型不
仅知道查询和键的内容，还知道它们在序列中的相对位置。
为了简化推导，首先考虑查询和键向量在二维空间的形式。可以用复数表示它们之间的内积：

⟨f(q, m), f(k, n)⟩= Re[f(q, m)f (k, n)]
∗
其中  
是复数的共轭， 
代表取实部。我们假设函数 
的形式为一个旋转变换：
f ∗
Re[⋅]
f(q, m)
 

f(q, m) = qeimθ
即将查询向量  
乘以一个旋转角度
 的复数因子。类似地，键向量 
 也会乘以相应的旋转因子。
q
mθ
k
为什么使用复数？
复数在数学中有一个重要的特性——它可以自然地表示旋转。复数的形式为  
 （其中 
 是虚
数单位），而当我们用极坐标表示复数时，形式为 
 
a + bi
i
reiθ
其中 
  是复数的模（代表大小），而  
是角度（代表方向）。
r
θ
当我们乘以 
  时，本质上是在做一个旋转操作，将复数绕原点旋转 
 角度。这是利用复数来表示
旋转的关键原因。
eiθ
θ
矩阵形式的旋转变换： 复数旋转可以推广到高维情形。对于偶数维的查询和键向量，RoPE通过一个旋
转矩阵来编码位置。二维情况下的旋转矩阵为：
 

R
 =
m

( cos(mθ)
−sin(mθ)
sin(mθ)
cos(mθ))
这个矩阵用于变换每对相邻的维度，以嵌入位置 
 的信息。对于高维向量，RoPE将多个二维旋转矩
阵组合成一个大矩阵，分块处理每两个相邻的维度。
m
RoPE的高维实现： 假设查询和键向量的维度为 
，我们可以将它们的每两个相邻维度作为一组，应
用二维旋转矩阵。这样，RoPE的编码过程可以描述为对每个位置 
，将向量 
 乘以如下的旋转矩
阵：
d
m
q
 

R
 =
m
diag

⎝
⎛
⎝
⎛
cos(mθ
)
−sin(mθ
)
cos(mθ
)
d/2−1
−sin(mθ
)
d/2−1
sin(mθ
)
cos(mθ
)
⋮
sin(mθ
)
d/2−1
cos(mθ
)
d/2−1
⎠
⎞
⎠
⎞
其中，每个 
是与维度
 相关的角度偏移量。一般选择  
，类似于Sinusoidal位置
编码中的设计。
θ
i
i
θ
 =
i
10000−2i/d
例子：假设有一个 6 维查询向量
假设我们有一个查询向量  
，它的维度是 6，也就是  
，其中每个 
都
是一个标量。RoPE 的做法是将这个 6 维向量分成多个2维的小块，对每一个小块应用二维旋转。
q
q = [q
, q
, q
, q
, q
, q
]
2
4
6
q
i

1. 将向量分组
我们把 6 维向量分成 3 组，每组2个维度：
- 第1组： 

(q
, q
)
2
- 第2组： 

(q
, q
)
4
- 第3组： 

(q
, q
)
6
2. 应用二维旋转矩阵
对每一组应用二维旋转矩阵。假设位置  
 是 1（代表第1个位置），旋转角度 
 是一个固定值，那
么对于第1组 
 ，应用旋转矩阵：
m
θ
(q
, q
)
2
 

R
 =
m

=
( cos(mθ)
−sin(mθ)
sin(mθ)
cos(mθ))

( cos(θ)
−sin(θ)
sin(θ)
cos(θ))
假设  
，那么矩阵变为：
θ = 30∘
 

R
 =
=
( cos(30 )
∘
−sin(30 )
∘
sin(30 )
∘
cos(30 )
∘)

(
3
−
1
1
3 )
接着用这个旋转矩阵去变换向量 
 ，得到新的值：
(q
, q
)
2
 

=
(q
′
q
′ )
R
 ⋅
=
(q
q
2)

⋅
(
3
−
1
1
3 )

(q
q
2)
通过矩阵乘法，可以得到新的 
 和 
：
q
′
q
′
 

q
 =
′
q
 +
3
q
1
q
 =
′
−
q
 +
1
2
同样的操作对第2组  
和第3组  
分别应用旋转矩阵：
(q
, q
)
4
(q
, q
)
6
 

=
(q
′
q
′ )
R
 ⋅
以及

=
(q
q
4)
(q
′
q
′ )
R
 ⋅
(q
q
6)
这样处理之后，查询向量变成了一个带有位置信息的新向量：
 
 
q =
′
[q
, q
, q
, q , q
, q
]
′
′
′
′
′
′
📌总结：
1. 二维旋转矩阵的性质保证了对局部维度的操作不会破坏向量的整体特征。
2. 相对位置信息嵌入是自注意力机制的核心，而RoPE通过旋转的方式实现了这一点。
3. 局部两两分组处理可以减少计算复杂度，同时保持高效的位置信息编码。
自动实现相对位置编码： 通过在查询和键向量上施加旋转变换，RoPE能够保证在计算Attention时，
内积运算会自动引入相对位置信息。这是因为：
