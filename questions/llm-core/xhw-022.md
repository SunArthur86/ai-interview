---
id: xhw-022
difficulty: L5
category: llm-core
subcategory: Transformer
tags:
- Transformer
feynman:
  essence: 通过旋转向量注入位置信息，使点积结果包含相对距离。
  analogy: 把向量当成时钟指针，不同位置转不同角度，指针间的夹角就代表距离。
  first_principle: 如何在不破坏自注意力结构的情况下，高效地注入相对位置信息？
  key_points:
  - 通过绝对位置的旋转实现相对位置感知
  - 利用复数乘积的几何性质注入位置
  - 具有良好的外推性和远程衰减特性
---

# RoPE位置编码

RoPE (Rotary Positional Embedding，旋转位置编码) 通过**绝对位置编码的形式实现了相对位置编码**的效果。

**1. 核心思想**
通过将位置索引 $m$ 体现为旋转矩阵，对 Query 和 Key 向量进行旋转变换。变换后的向量内积等价于加入了相对位置 $m-n$ 的信息，即：
$$ \langle f(q, m), f(k, n) \rangle = g(q, k, m-n) $$

**2. 实现原理**
*   **复数视角**：将向量维度两两分组视为复数。位置 $m$ 的编码对应于乘以 $e^{im\theta}$。
*   **几何旋转**：随着位置 $m$ 的增加，向量在高维空间中发生旋转。当计算 $Q$ 和 $K$ 的点积（Attention 分数）时，旋转角度的差值（即相对位置）自然地影响了最终结果。
*   **相对位置感知**：模型不需要显式地学习相对位置矩阵，而是通过几何性质自然获得相对位置信息。

**3. 优势**
*   **远距离衰减**：RoPE 具有良好的外推性，能够自然地随着相对距离增加而降低关注度（符合语言直觉）。
*   **通用性**：不仅适用于标准的 Attention，也能无缝迁移到 Linear Attention 等变体。
*   **无需额外参数**：位置信息通过确定性的旋转注入，不增加可训练参数量。
