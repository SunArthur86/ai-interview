---
id: xhw-057
difficulty: L4
category: llm-core
subcategory: Transformer
tags:
- Transformer
feynman:
  essence: 用一组查询向量去关注另一组特征序列，实现不同模态或序列间的信息融合。
  analogy: 考试时看试卷（Query），不会做的题就去翻课本（Key/Value）找答案，试卷和课本是两样东西。
  first_principle: 如何让模型在处理当前序列时，动态地从另一个独立的信息源中获取相关上下文？
  key_points:
  - Q来自一个序列，K/V来自另一个序列
  - 是Encoder-Decoder架构的核心组件
  - 用于跨模态交互或源序列信息引用
  - 解决了如何让解码器结合编码信息的问题
---

# Cross-Attention交叉注意力

### Cross-Attention (交叉注意力)

#### 1. 定义
Cross-Attention 是指 Query (Q) 来自一个序列，而 Key (K) 和 Value (V) 来自另一个序列的注意力机制。它主要用于融合两个不同模态或不同来源的信息。

#### 2. Transformer 中的应用
在标准的 Transformer Encoder-Decoder 架构（如原始的 Transformer 用于机器翻译）中：

- **Decoder 端**：使用 Masked Self-Attention 处理已生成的目标序列。
- **Cross-Attention 层**：
  - **Q**：来自 Decoder 的上一层输出（代表目标语言）。
  - **K, V**：来自 Encoder 的最终输出（代表源语言）。

这使得 Decoder 在生成每一个词时，都能去“查阅”源语言句子中最相关的部分。

#### 3. 计算公式
$$Attention(Q_{dec}, K_{enc}, V_{enc}) = \text{softmax}(\frac{Q_{dec}K_{enc}^T}{\sqrt{d_k}})V_{enc}$$

#### 4. 应用场景
- **机器翻译**：源语言 -> 目标语言。
- **图像描述 (Image Captioning)**：图像特征 (CNN 输出) 作为 K/V，文本作为 Q。
- **多模态大模型**：文本 Q 去查询图像/视频编码后的 K/V。
