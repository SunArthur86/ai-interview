---
id: lln-021
difficulty: L2
category: llm-core
subcategory: 手撕代码
tags:
- IO
- IOC
- 索引
- Elasticsearch
feynman:
  essence: 通过对特征维度标准化，稳定数据分布，加速模型收敛。
  analogy: 就像给不同科目的考试成绩（分布不同）进行「Z-score」标准化，把它们拉到同一水平线上比较，防止某些特征因数值过大或过小主导了模型的学习。
  first_principle: 如何消除特征间的数值差异，使梯度传播更稳定？
  key_points:
  - LayerNorm计算均值方差做中心化和缩放，RMSNorm仅做缩放
  - 针对每个样本的特定维度计算统计量，不依赖Batch
  - 引入可学习参数gamma和beta增强模型表达能力
follow_up:
- 什么是覆盖索引？有什么优势？
- 索引失效的场景有哪些？
---

# 手撕Normalization

题目要求手撕RMSNorm和LayerNorm，代码只实现了LayerNorm且不完整。

### LayerNorm 实现

LayerNorm 对单个样本的所有特征进行归一化，常用于 NLP 领域（如 Transformer）。它不依赖 Batch 大小。

```python
import torch
from torch import nn

class LN(nn.Module):
    def __init__(self, normalized_shape, eps=1e-5, elementwise_affine=True):
        super().__init__()
        self.normalized_shape = normalized_shape
        self.eps = eps
        self.elementwise_affine = elementwise_affine
        if self.elementwise_affine:
            self.gain = nn.Parameter(torch.ones(normalized_shape)) # gamma (scale)
            self.bias = nn.Parameter(torch.zeros(normalized_shape)) # beta (shift)

    def forward(self, x):
        # 计算归一化维度，通常对最后一个或几个维度求均值
        dims = [-(i + 1) for i in range(len(self.normalized_shape))]
        mean = x.mean(dim=dims, keepdim=True)
        # unbiased=False 表示使用有偏估计（除以N），PyTorch LayerNorm 默认行为
        var = x.var(dim=dims, keepdim=True, unbiased=False) 
        x_norm = (x - mean) / torch.sqrt(var + self.eps)
        if self.elementwise_affine:
            x_norm = self.gain * x_norm + self.bias
        return x_norm
```

### RMSNorm 实现

RMSNorm (Root Mean Square Layer Normalization) 是 LayerNorm 的一种变体，去掉了均值中心化的重校准步骤。它在 LLaMA 等大模型中被广泛使用，因为其在保持性能的同时减少了计算量。

核心公式：$\bar{x}_i = \frac{x_i}{\sqrt{\frac{1}{n} \sum_{j=1}^{n} x_j^2} + \epsilon} \cdot \gamma_i$

```python
class RMSNorm(nn.Module):
    def __init__(self, normalized_shape, eps=1e-5, elementwise_affine=True):
        super().__init__()
        self.normalized_shape = normalized_shape
        self.eps = eps
        self.elementwise_affine = elementwise_affine
        if self.elementwise_affine:
            self.gain = nn.Parameter(torch.ones(normalized_shape)) # gamma
            # RMSNorm 通常没有 beta (bias)，因为它去掉了均值项

    def forward(self, x):
        dims = [-(i + 1) for i in range(len(self.normalized_shape))]
        # 计算均方根
        ms = (x ** 2).mean(dim=dims, keepdim=True)
        x_norm = x * torch.rsqrt(ms + self.eps)
        if self.elementwise_affine:
            x_norm = self.gain * x_norm
        return x_norm
```

### 细节对比

| 特性 | LayerNorm | RMSNorm |
| :--- | :--- | :--- |
| **计算公式** | $\frac{x - \mu}{\sqrt{\sigma^2 + \epsilon}}$ | $\frac{x}{\sqrt{mean(x^2) + \epsilon}}$ |
| **中心化** | 有 (减去均值 $\mu$) | 无 |
| **参数** | $\gamma, \beta$ | 仅 $\gamma$ (通常) |
| **稳定性** | 在某些 RNN 结构中更稳定 | 计算更快，LLM 中表现良好 |

### ## 常见考点
1.  **LayerNorm vs BatchNorm**：为什么 Transformer 使用 LayerNorm 而不是 BatchNorm？（BatchNorm 依赖 batch size，对小 batch 或变长序列不友好；LN 具有平移不变性，适合 NLP）
2.  **unbiased 参数**：在 `var` 计算中 `unbiased=False` 的含义是什么？（False 表示除以 N，True 表示除以 N-1，PyTorch LayerNorm 默认 False 以匹配 TensorFlow 和原始论文）
3.  **RMSNorm 的优势**：RMSNorm 相比 LayerNorm 省去了哪里计算？为什么在 LLM 中流行？（省去了减去均值的操作，理论依据是重校准均值对模型表达能力的提升有限，且在推理时更省显存和算力）
4.  **hidden states 维度**：如果输入是 `[batch, seq_len, hidden_dim]`，`normalized_shape` 通常填什么？(`hidden_dim`)
