---
id: xhw-039
difficulty: L5
category: llm-core
subcategory: LLM前沿
tags:
- llm-core
feynman:
  essence: 多头共享KV以显存加速推理。
  analogy: 多位专家共用同一套参考书库。
  first_principle: 如何在保持多头表达能力的同时降低KV存储开销？
  key_points:
  - Q拆分为多个头独立计算
  - K和V在所有头间共享（单头维度）
  - 大幅减少KV Cache显存占用
  - 推理速度显著提升
---

# 手撕MQA

# 手撕 MQA (Multi-Query Attention)

## 核心原理
MQA（Multi-Query Attention）是 GQA（Grouped-Query Attention）的极端特例。标准 MHA 中，每个 Head 都有独立的 Q、K、V；而在 MQA 中，所有 Head 共享同一份 K 和 V，只有 Q 是分头的。这种机制极大地减少了显存占用（尤其是 KV Cache）并提升了推理速度。

## 实现代码

```python
import torch
import torch.nn as nn
import math

class MultiQueryAttention(nn.Module):
    def __init__(self, d_model, num_heads):
        super().__init__()
        self.d_model = d_model
        self.num_heads = num_heads
        self.head_dim = d_model // num_heads

        # MQA 关键：
        # Q: [d_model, d_model] -> 输出拆分为 num_heads
        # K, V: [d_model, head_dim] -> 所有 heads 共享
        self.q_linear = nn.Linear(d_model, d_model) 
        self.k_linear = nn.Linear(d_model, self.head_dim)
        self.v_linear = nn.Linear(d_model, self.head_dim)
        
        self.out_linear = nn.Linear(d_model, d_model)

    def forward(self, x, mask=None):
        batch_size, seq_len, _ = x.size()

        # 1. 线性变换
        # Q: [Batch, SeqLen, d_model] -> [Batch, SeqLen, num_heads * head_dim]
        q = self.q_linear(x)
        # K, V: [Batch, SeqLen, head_dim] (注意这里没有 heads 维度)
        k = self.k_linear(x)
        v = self.v_linear(x)

        # 2. reshape Q 以便多头计算
        # [Batch, SeqLen, num_heads, head_dim]
        q = q.view(batch_size, seq_len, self.num_heads, self.head_dim)
        # [Batch, num_heads, SeqLen, head_dim] (转置适配矩阵乘法)
        q = q.transpose(1, 2)

        # 3. 扩展 K 和 V 以匹配 Q 的 head 数量
        # [Batch, 1, SeqLen, head_dim] -> [Batch, num_heads, SeqLen, head_dim]
        # 使用 expand 实现内存共享，不增加数据拷贝
        k = k.unsqueeze(1).expand(-1, self.num_heads, -1, -1)
        v = v.unsqueeze(1).expand(-1, self.num_heads, -1, -1)

        # 4. Scaled Dot-Product Attention
        # Score = Q * K^T / sqrt(d_k)
        scores = torch.matmul(q, k.transpose(-2, -1)) / math.sqrt(self.head_dim)

        if mask is not None:
            scores = scores.masked_fill(mask == 0, -1e9)

        attn = torch.softmax(scores, dim=-1)

        # 5. 加权求和
        # context: [Batch, num_heads, SeqLen, head_dim]
        context = torch.matmul(attn, v)

        # 6. 拼接 Heads
        # [Batch, SeqLen, num_heads, head_dim]
        context = context.transpose(1, 2).contiguous()
        # [Batch, SeqLen, d_model]
        context = context.view(batch_size, seq_len, self.d_model)

        # 7. 输出投影
        return self.out_linear(context)
```

## MQA 架构图

```text
标准 MHA               MQA (本题目标)
┌─────────────┐       ┌─────────────┐
│   Input X   │       │   Input X   │
└──────┬──────┘       └──────┬──────┘
       │                     │
   ┌───┴────┐             ┌───┴────┐
   │Linear  │             │Linear  │
   └───┬────┘             └───┬────┘
       │                     │
  ┌────┴─────┐           ┌────┴─────┐
  │Q  K  V   │           │    Q     │ <--- 多头投影
  │(各不相同) │           │  (多头)   │
  └────┬─────┘           └────┬─────┘
       │                     │
       │              ┌──────┴──────┐
       │              │ Linear      │
       │              │ K, V (共享) │ <--- 单头投影 (所有 head 共享)
       │              └──────┬──────┘
       │                     │
  [Multi-Head Attn]     [Broadcast K,V to Heads]
       │                     │
  ┌────┴─────┐           ┌────┴─────┐
  │Concat    │           │Concat    │
  └────┬─────┘           └────┬─────┘
       │                     │
  [Output Linear]      [Output Linear]
```

## 常见考点
1.  **MQA vs GQA vs MHA**：三者的区别是什么？MQA 是 GQA 的 group_size=1 的特例，MHA 是 group_size=num_heads 的特例。
2.  **KV Cache 节省**：MQA 在推理阶段能节省多少显存？显存占用从 O(2 * num_heads * seq_len * d) 降至 O(2 * 1 * seq_len * d)，极大节省了显存。
3.  **性能影响**：MQA 会导致精度下降吗？通常有轻微下降，但可以通过增加参数或微调来弥补；推理吞吐量通常显著提升。
