---
id: "llm--012"
difficulty: "L3"
category: "llm-core"
subcategory: "训练优化"
tags:
  - "IO"
  - "CAS"
feynman:
  essence: "- *损失缩放 (Loss Scaling)** 是混合精度训练中解决 FP16 梯度下溢(underflow)的关键技术. - *问题背景** - FP16"
  analogy: "混合精度就像厨房混用工具——切菜用快刀（FP16快），称量用精密秤（FP32准），省显存不损精度。"
  key_points:
    - "损失缩放 (Loss Scaling) 是混合精度训练中解决 FP16 梯度下溢(underflow)的关键技术."
    - "FP16 的最小正常数约为 6e-5,很多梯度值小于此值会被截断为 0"
    - "梯度为 0 → 参数不更新 → 模型停止学习"
first_principle:
  problem: "为什么需要 损失缩放(Loss Scaling)?为什么混合精度训练需要它？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "如何判断梯度是否发生了下溢?"
  - "bfloat16 在哪些 GPU 上支持?"
---

# 什么是损失缩放(Loss Scaling)?为什么混合精度训练需要它

- **损失缩放 (Loss Scaling)** 是混合精度训练中解决 FP16 梯度下溢(underflow)的关键技术.

- **问题背景**
- FP16 的最小正常数约为 6e-5,很多梯度值小于此值会被截断为 0
- 梯度为 0 → 参数不更新 → 模型停止学习

- **损失缩放原理**

1. **前向传播**:正常计算,得到 loss
2. **损失放大**:loss × scale_factor(如 2^16 = 65536)
3. **反向传播**:放大后的 loss 产生放大的梯度,避免下溢
4. **梯度缩小**:更新前,梯度 ÷ scale_factor 恢复真实值

```python
# PyTorch AMP (自动混合精度)
scaler = torch.cuda.amp.GradScaler()

with torch.cuda.amp.autocast():
output = model(inputs)
loss = criterion(output, targets)

scaler.scale(loss).backward()  # 放大 loss
scaler.step(optimizer)          # 更新(自动缩小梯度)
scaler.update()                 # 动态调整 scale_factor
```

- **动态损失缩放**
- 如果梯度出现 inf/nan → scale_factor 减半
- 连续 N 步无 inf/nan → scale_factor 翻倍
- 自动找到合适的 scale,无需手动调参

- **bfloat16 vs float16**
- float16: 指数位 5bit,尾数位 10bit → 动态范围小,需要 loss scaling
- bfloat16: 指数位 8bit,尾数位 7bit → 动态范围与 float32 相同,**不需要 loss scaling**
- 但 bfloat16 精度略低,某些任务可能略差

- **经验**:现代训练推荐用 bfloat16(如果 GPU 支持),省去 loss scaling 的复杂性.
