---
id: llm-037
difficulty: L2
category: llm-core
subcategory: 注意力机制
tags:
- IO
images:
- llm100_p081_img1.png
- llm100_p082_img1.png
- llm100_p082_img2.png
feynman:
  essence: 分组查询注意力，平衡MHA的性能与MQA的速度。
  analogy: 多人共用几本参考书，比每人各带一本书省空间，比所有人共用一本书效率高。
  first_principle: 如何在保证模型精度的前提下，大幅降低推理时的显存带宽消耗？
  key_points:
  - 通过分组让多个Query头共享Key和Value矩阵
  - 减少推理时的KV Cache显存占用
  - 解决显存带宽瓶颈，提升推理吞吐
  - 是MHA和MQA的折中方案
---

# 为什么大模型用GQA(Group Query Attention)

大模型使用 **GQA (Grouped-Query Attention)** 是为了在 **推理速度** 和 **模型性能** 之间取得更好的平衡，主要解决推理时的显存带宽瓶颈。

### 1. 背景机制
*   **MHA (Multi-Head Attention)**：每个注意力头都有独立的 Key (K) 和 Value (V) 矩阵。
    *   *优点*：表达能力强，捕捉特征丰富。
    *   *缺点*：KV Cache 占用显存大，读取 KV 带宽开销高，推理慢。
*   **MQA (Multi-Query Attention)**：所有头共享同一组 K 和 V 矩阵，仅 Query (Q) 独立。
    *   *优点*：KV Cache 极小，推理速度极快。
    *   *缺点*：表达能力受限，模型性能下降明显。

### 2. GQA 的原理
GQA 是 MHA 和 MQA 的折中方案。它将查询头分为若干组，**组内**的所有头共享同一组 K 和 V 矩阵。

*   *机制*：假设有 $H$ 个 Query 头，将其分为 $G$ 组。每组包含 $H/G$ 个头，它们共享同一个 KV 矩阵对。
*   *特例*：当 $G=1$ 时，退化为 MQA；当 $G=H$ 时，退化为 MHA。

### 3. GQA 的优势
*   **平衡性能与速度**：相比 MQA，保留了更多的 KV 独立性，性能更接近 MHA；相比 MHA，大幅减少了 KV Cache 大小和内存访问次数。
*   **灵活性**：可以通过调整分组数量 $G$ 来灵活权衡显存占用和模型精度。
*   **推理加速**：显著降低了自回归推理时的显存带宽压力，提高吞吐量。

### 4. 架构对比

```text
MHA (Multi-Head Attention)        GQA (Grouped-Query Attention)      MQA (Multi-Query Attention)
┌─────────────────────┐           ┌─────────────────────┐           ┌─────────────────────┐
│ Q1 Q2 Q3 Q4 ... QH  │           │ Q1 Q2 │ Q3 Q4 ... QH │           │ Q1 Q2 Q3 Q4 ... QH  │
│  ↓  ↓  ↓  ↓        │           │  ↓  ↓ │  ↓  ↓        │           │  ↓  ↓  ↓  ↓        │
│ K1 K2 K3 K4 ... KH  │           │  K1   │  K2   ... KG │           │  K  (Shared)       │
│ V1 V2 V3 V4 ... VH  │           │  V1   │  V2   ... VG │           │  V  (Shared)       │
└─────────────────────┘           └─────────────────────┘           └─────────────────────┘
```

### 5. 关键细节补充
*   **KV Cache 显存计算**：对于 Batch Size=1，序列长度 $L$，Hidden Size $d$，层数 $L_{layer}$。
    *   MHA 显存占用：$2 \times L_{layer} \times H \times L \times d_{k}$ ($H$ 为头数，$d_k$ 为每头维度)。
    *   GQA 显存占用：$2 \times L_{layer} \times G \times L \times d_{k}$ ($G$ 为组数，通常 $G \ll H$)。
    *   MQA 显存占用：$2 \times L_{layer} \times 1 \times L \times d_{k}$。
*   **Upsampling 机制**：在计算 Attention 分数时，共享的 K 和 V 需要复制（Repeat） $H/G$ 次以匹配 Q 的头数，或者在矩阵乘法时利用广播机制实现。
*   **性能回归**：从 MHA 迁移到 GQA 时，通常需要进行 **Up-training（增量训练）** 以恢复模型性能，直接使用 GQA 权重可能会导致效果略有下降。

## 常见考点
1.  **KV Cache 的具体大小计算**：给定 Batch Size、Context Length、Hidden Size 等参数，如何估算 MHA vs GQA 的显存占用？
2.  **从 MHA 到 GQA 的迁移策略**：如何在预训练好的 MHA 模型上高效切换到 GQA？（通常是线性投影合并 KV 头，然后继续增量训练）。
3.  **分组数 $G$ 的选择**：如何决定 $G$ 的大小？（例如 Llama 2 70B 中 $G=8$，Llama 3 中 $G=8$，通常权衡性能和吞吐量）。
