---
id: "slt2-010"
difficulty: "L3"
category: "llm-core"
subcategory: "Transformer架构"
images:
  - "svg_quantization.svg"
feynman:
  essence: "量化（Quantization）将FP16/BF16权重压缩到低精度（INT8/INT4），减少显存和加速推理"
  analogy: "量化就像把高清图压缩成标清——参数从 32 位降到 8/4 位，体积小速度快，但精度略降，是部署优化的关键手段。"
  key_points:
    - "GPTQ（后训练量化）："
    - "基于二阶Hessian矩阵信息逐层量化"
    - "INT4量化效果好，速度快"
first_principle:
  problem: "为什么需要 大模型推理中的量化有哪些方案？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 大模型推理中的量化有哪些方案？

量化（Quantization）将FP16/BF16权重压缩到低精度（INT8/INT4），减少显存和加速推理。

主要方案：

1. GPTQ（后训练量化）：
- 基于二阶Hessian矩阵信息逐层量化
- INT4量化效果好，速度快
- 适合服务端部署

2. AWQ（Activation-aware Weight Quantization）：
- 保护对激活值贡献大的权重通道（salient channels）
- 引入缩放因子s，使重要权重的量化误差最小
- INT4量化效果优于GPTQ

3. BitsAndBytes（NF4/INT8）：
- QLoRA使用的NF4量化
- 适合训练场景（QLoRA微调）

4. GGUF/GGML（llama.cpp）：
- 面向CPU推理的量化方案
- 支持Q4_K_M、Q5_K_M等多种精度

效果：INT4量化使13B模型从26GB降到约7GB，RTX 4090可跑。精度损失通常<1%。
