---
id: "llm-089"
difficulty: "L2"
category: "llm-core"
subcategory: "对齐技术"
tags:
  - "IO"
  - "IOC"
images:
  - "llm100_p242_img1.png"
feynman:
  essence: "DPO(Direct Preference Optimization)是一种用于优化语言模型(LLMs)的算法,旨在通过直接优 化人类偏好数据来训练模型.DPO"
  analogy: "微调就像给通才毕业生做岗前培训——已有基础能力（预训练），再针对具体岗位做专项训练（指令/偏好对齐）。"
  key_points:
    - "1 DPO 损失函数的基本形式"
    - "3 损失函数的进一步解释"
    - "Logistic 函数的作用:"
first_principle:
  problem: "如果要解决这个问题，最本质的方法论是什么？先理解问题约束，再找最优路径。"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
---

# 怎么理解DPO的损失函数

DPO(Direct Preference Optimization)是一种用于优化语言模型(LLMs)的算法,旨在通过直接优 化人类偏好数据来训练模型.DPO 的核心思想是简化传统的 RLHF(Reinforcement Learning from Human Feedback)过程,避免复杂的奖励模型训练,直接通过偏好数据来优化策略.
95. 1 DPO 损失函数的基本形式
🫐DPO 的损失函数定义如下:

**DPO**
(x,y ,y )∼D w l [ ( π (y ∣x) ref w π (y ∣x) θ w π (y ∣x) ref l π (y ∣x) θ l )]

是我们要训练的模型(policy model),目标是让它学会根据人类的偏好生成更好的 输出. π θ •

是参考模型(reference model),通常是一个预训练的语言模型,它的行为是固定 的,用来“约束”策略模型,防止策略模型偏离正常行为太远. π

ref •

是人类偏好的输出(chosen completion). y

是人类不喜欢的输出(rejected completion). y l •

是包含偏好数据的数据集,每个样本由 组成. D (x, y , y ) w l •

是 DPO Loss = -log σ(β · (log π(y_w|x)/π_ref(y_w|x) - log π(y_l|x)/π_ref(y_l|x))) l π (y ∣x) θ l yl 通过计算这两个比率的对数差值,我们可以衡量策略模型在偏好数据上的表现.如果策略模 型更倾向于选择 而不是 ,那么这个差值会变大,损失函数会变小. y

w y l 策略模型和参考模型的相除是什么意思? 公式中的 和
**是关键部分.它们的含义是：**

w x •

:参考模型认为 是输入 的合理输出的概率. π (y ∣x) ref w y

w x •

:策略模型对 的偏好程度 相对于 参考模型对 的偏好程度.

**这个比率的意义是：**
• 如果策略模型比参考模型更喜欢 ,那么这个比率会大于 1. y

w • 如果策略模型比参考模型更不喜欢 ,那么这个比率会小于 1. y

w 同理, 表示策略模型对 的偏好程度相对于参考模型.

为什么要用这个比率? DPO 的目标是让策略模型更倾向于选择 ,而不是
**.通过计算这两个比率的差值：**

w y l

π (y ∣x) ref l π (y ∣x) θ l 我们可以衡量策略模型对 和 的偏好差异.如果策略模型更喜欢 ,这个差值会变 大;如果策略模型更喜欢 ,这个差值会变小. y

w y l y

w y l
95. 3 损失函数的进一步解释
🫐为了更清楚地理解损失函数,我们可以将其分解为以下几个部分:
1. Logistic 函数的作用:
函数将差值映射到 之间,表示策略模型选择 而不是 的概率. σ (0, 1) y

2. 超参数
**的作用：**
控制策略模型与参考模型之间的差异.较大的 会使策略模型更倾向于偏离参考模 型,而较小的 会使策略模型更接近参考模型. β β β
3. 参考模型的作用: 参考模型 用于约束策略模型的行为,防止策略模型过度偏离预训练的语言模型. 这样可以避免模型生成不合理或不安全的输出. π

95. 4 损失函数的简化形式
🫐为了进一步简化损失函数,我们可以对公式进行如下转换:

**利用对数的性质,可以将其改写为：**

β log π (y ∣x) −log π (y ∣x) − ( θ w ref w ) β log π (y ∣x) −log π (y ∣x) ( θ l ref l )
**进一步简化后,可以得到：**

β log π (y ∣x) −log π (y ∣x) − ( θ w θ l ) β log π (y ∣x) −log π (y ∣x) ( ref w ref l ) 这个形式与源代码中的计算函数一致.其中: • 左半部分表示策略模型对 和 的偏好差异. y

w y l • 右半部分表示参考模型对 和 的偏好差异. y

w y l 损失函数的最终目标

DPO 的最终目标是最大化策略模型对偏好数据的偏好程度,同时保持策略模型与参考模型之 间的差异在可控范围内.通过这种方式,DPO 能够有效地学习人类偏好,并生成符合人类期 望的输出 🫐DPO 的损失函数通过直接优化人类偏好数据,简化了传统的 RLHF 过程.它利用参考模型来 约束策略模型的行为,并通过 logistic 函数将偏好差异映射到概率空间.通过这种方式, DPO 能够有效地学习人类偏好,并生成符合人类期望的输出.
