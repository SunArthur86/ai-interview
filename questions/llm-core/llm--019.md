---
id: "llm--019"
difficulty: "L2"
category: "llm-core"
subcategory: "推理与部署"
images:
  - "svg_beam_search.svg"
feynman:
  essence: "**推理 (Inference)** 是将输入文本送入模型,生成输出的过程."
  analogy: "推理就像模型在考试答题——训练是学习，推理是应用知识回答问题。"
  key_points:
    - "推理 (Inference) 是将输入文本送入模型,生成输出的过程."
    - "输入:一段文本序列(Token IDs)"
    - "输出:模型预测的下一个 Token(逐个生成)"
first_principle:
  problem: "为什么需要 大模型推理的输入和输出?Temperature 参数有什么作用？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "Top-p 和 Temperature 有什么区别?"
  - "什么时候用 Beam Search?"
---

# 大模型推理的输入和输出是什么?Temperature 参数有什么作用

- **推理 (Inference)** 是将输入文本送入模型,生成输出的过程.

- **输入**:一段文本序列(Token IDs)
- **输出**:模型预测的下一个 Token(逐个生成)

- **Temperature 参数**
- **Temperature = 0**:每次选概率最高的 token(确定性输出)
- **Temperature = 1**:按原始概率分布采样(有创造性)
- **Temperature > 1**:概率分布更平坦(更随机,可能不连贯)

- **实用建议**
- 代码生成/事实问答:Temperature = 0~0.3
- 创意写作/头脑风暴:Temperature = 0.7~1.0
- 超过 1.2 通常不建议

- **一句话理解**:Temperature 低 = 严谨(选最优),Temperature 高 = 创意(加点随机性).
