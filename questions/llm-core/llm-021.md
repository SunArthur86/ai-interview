---
id: "llm-021"
difficulty: "L2"
category: "llm-core"
subcategory: "Transformer架构"
tags:
  - "IO"
feynman:
  essence: ")? d k 📌在 Transformer 的多头注意力机制中,缩放点积注意力(Scaled Dot-Product Attention) 是核心计算方法之一."
  analogy: "注意力机制就像在聚会上听人说话——自动聚焦到感兴趣的声音，给不同人不同关注度，数学上就是加权的加权求和。"
  key_points:
    - ")? d k 📌在 Transformer 的多头注意力机制中,缩放点积注意力(Scaled Dot-Product Attention) 是核心计算方法之一.计算过程中,查询向量 和键向量 进行点积"
    - "2. 避免梯度消失,通过限制输入值的范围,确保 Softmax 梯度不会过小,保障模型训练效率"
    - "3. 加快模型收敛,缩放使得多头注意力机制在高维度下保持数值稳定性,从而提高模型的训练速度 和表现. 这个缩放机制在实际应用中证明是有效的,能够显著提升模型的稳定性和性能."
first_principle:
  problem: "追根溯源：为什么在attention中要进行scaled(为什么除以 的根本原因是什么？背后的设计哲学是什么？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
follow_up:
  - "如果不除以√d_k会导致什么问题?"
  - "dk很大时scaled的影响有多大?"
---

# 为什么在attention中要进行scaled(为什么除以

)?

d k 📌在 Transformer 的多头注意力机制中,缩放点积注意力(Scaled Dot-Product Attention) 是核心计算方法之一.计算过程中,查询向量 和键向量 进行点积操作,得到注意力分 数,然后通过 Softmax 函数将这些分数转换为概率分布.然而,直接进行点积运算时,随着 向量维度 的增大,点积的值可能会变得非常大,进而影响 Softmax 的梯度以及模型的训 练稳定性. Q K d k
1. 问题背景
在注意力机制中,点积计算会通过以下公式得出注意力分数:

Attention(Q, K, V ) = Softmax

V (

d k

**QKT**
QKT ) 其中 是查询向量, 是键向量, 是值向量, 是键向量的维度. Q K V d k **问题1：** 点积值过大 查询向量 和键向量 是高维向量,点积的期望值会随着维度 的增加而增大.具体来说,两个 随机独立的向量的点积期望值与向量维度成正比,导致点积结果会非常大.进入 Softmax 函数后,这 些大值会使得 Softmax 输出变得极端化:某些位置的输出非常接近 1,而其他位置接近 0.此时,注 意力机制的注意力分布过于集中,模型难以有效地从多个位置学习信息. Q K d k **问题2：** 梯度消失 当点积值过大时,Softmax 函数的梯度可能会变得非常小,导致模型的反向传播过程中梯度消失,使 得模型训练速度变慢或难以收敛.
2. 缩放的解决方案
为了避免这些问题,Transformer 引入了一个缩放因子,将点积的结果除以

**的平方根,即：**

d k

d k

**QKT**
QKT

**这样做的原因主要有以下几点：**
正则化效果

缩放因子 的引入可以防止随着 增加,点积值变得过大.点积的期望值大约与向量维度

成正比,因此通过除以 ,可以将点积值缩放到一个合适的范围,确保数值稳定.理论上,两个独 立随机向量的点积的标准差大约与向量维度 的平方根成比例,因此通过缩放可以将点积值控制在合 理范围内.

d k 1 d k d k

d k d k 保持数值稳定性 缩放因子确保了 Softmax 的输入不会过大或过小,保证了注意力机制的数值稳定性.大幅度缩放的点 积会使 Softmax 输出接近 0 或 1,这导致注意力集中在特定位置,而通过缩放,注意力分布更加平 滑,模型能够更稳定地训练. 避免梯度消失 通过引入缩放,点积值进入 Softmax 后,其值不会过于极端,进而避免了梯度消失问题.梯度消失通 常出现在激活函数的输出值接近 0 或 1 时,通过缩放可以确保 Softmax 的输入位于合理范围内,确保 反向传播过程中梯度正常传播.
3. 数值举例
假设 ,那么查询向量 和键向量 的点积结果的期望值可能在 64 量级.如果不进行缩 放,Softmax 的输入可能非常大,导致其输出值集中在某些位置(接近 1),其他位置则接近 0.此时 模型难以有效学习.通过将点积结果除以 ,点积结果被缩放至较小范围,使得 Softmax 的 输出更加平滑,注意力分布更加合理. d = k 64 Q K = 64 8
4. 总结
在 Softmax 之前对点积注意力分数进行缩放(除以 )是为了应对高维向量下点积值过大的问

**题.这一操作可以：**

d k 1. 防止数值不稳定,避免注意力权重极端化,确保模型能够有效学习; 2. 避免梯度消失,通过限制输入值的范围,确保 Softmax 梯度不会过小,保障模型训练效率; 3. 加快模型收敛,缩放使得多头注意力机制在高维度下保持数值稳定性,从而提高模型的训练速度 和表现. 这个缩放机制在实际应用中证明是有效的,能够显著提升模型的稳定性和性能.
