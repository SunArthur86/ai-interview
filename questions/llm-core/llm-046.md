---
id: llm-046
difficulty: L2
category: llm-core
subcategory: 对齐技术
tags:
- IO
- IOC
images:
- llm100_p109_img1.png
feynman:
  essence: 通过人类对比生成结果，构造优于和劣于的偏好对。
  analogy: 老师把两个学生的作业拿出来比较，好的当范文，差的当反面教材。
  first_principle: 如何将人类模糊的好坏偏好转化为模型可训练的数值化正负样本？
  key_points:
  - 对同一Prompt生成多个候选回复
  - 人工标注选出优胜者和失败者
  - 优胜者作为正样本，失败者作为负样本
  - 确保成对样本基于同一输入具有可比性
---

# DPO的正负样本怎么构造

在 DPO (Direct Preference Optimization) 训练中，正负样本的构造是关键步骤，本质上是基于人类反馈构建“偏好对”。

### 1. 数据收集阶段
*   **Prompt (提示词)**：给定一个输入 $x$（如用户问题）。
*   **Generation (生成)**：让模型针对 $x$ 生成多个不同的回复。例如，生成 $y_1$ 和 $y_2$。

### 2. 人类标注与偏好形成
*   人类标注员对生成的回复进行比较，判断哪个更好。
*   假设标注员认为 $y_1$ 优于 $y_2$，则形成一个偏好对 $(y_w, y_l)$，其中 $y_w$ 是偏好回答（Winner），$y_l$ 是拒绝回答（Loser）。

### 3. 正负样本定义
在 DPO 的 Loss 计算上下文中：
*   **正样本**：指 **偏好回答 $y_w$**。在训练中，模型需要提升生成该答案的概率。
*   **负样本**：指 **拒绝回答 $y_l$**。在训练中，模型需要降低生成该答案的概率（或者相对于 $y_w$ 降低其概率）。

### 4. 构造细节
*   **Pairwise (成对)**：必须保证 $y_w$ 和 $y_l$ 是基于同一个 Prompt $x$ 生成的，具有可比性。
*   **格式**：通常数据集格式为 `{ "prompt": x, "chosen": y_w, "rejected": y_l }`。
*   **来源**：除了生成后比较，也可以收集历史数据中的用户点赞（正样本）和点踩/重新生成（负样本）作为偏好对。

### 5. 使用方式
在 DPO 的前向传播中，Prompt 分别输入模型生成 $y_w$ 和 $y_l$ 的 Logits，通过计算两者在策略模型和参考模型下的对数概率差来构建 Loss，从而实现“让正样本得分高于负样本”的目标。
