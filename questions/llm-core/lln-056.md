---
id: lln-056
difficulty: L2
category: llm-core
subcategory: LoRA与微调
tags:
- IO
- Elasticsearch
images:
- llmnotes_p403_img1.jpg
- llmnotes_p404_img1.jpg
- llmnotes_p404_img2.jpg
feynman:
  essence: GPTQ利用数学误差补偿，AWQ依据激活重要性保护关键权重
  analogy: GPTQ像精算师做平衡，AWQ像保镖重点保护大人物(大激活通道)
  first_principle: 如何在不重训模型的前提下，最小化因低比特量化带来的精度损失？
  key_points:
  - GPTQ基于Hessian矩阵进行误差感知的逐层量化
  - AWQ发现1%的权重贡献了大部分激活，需重点保护
  - AWQ通过缩放实现更好的硬件并行计算效率
follow_up:
- 为什么AWQ比GPTQ推理速度更快?
- SmoothQuant和AWQ有什么相似之处?
- 如何评估量化后模型的质量损失?
---

# GPTQ和AWQ量化算法的原理和区别是什么

两者都是训练后量化(PTQ)方法,将模型权重从FP16压缩到INT4/INT8.

- **GPTQ (Generative Pre-trained Transformer Quantization):**

**核心思想：** 基于二阶信息(Hessian矩阵)逐层最优量化.

1. 计算权重矩阵的Hessian矩阵 H = Xᵀ·X (X是校准数据)
2. 按列量化权重,每量化一列就用剩余信息补偿前面量化引入的误差
3. 使用Cholesky分解加速Hessian求逆
4. 效果:INT4量化后几乎无损,速度提升3-4倍

- **AWQ (Activation-aware Weight Quantization):**

**核心思想：** 不是所有权重同等重要,激活值大的通道对应的权重量化误差影响更大.

1. 观察到:激活幅度大的通道对应的权重对量化误差更敏感
2. 引入缩放因子s:在量化前将重要通道的权重放大,量化后缩小
3. 通过网格搜索找到最优的缩放比例
4. 效果:比GPTQ更快的推理速度(更好的硬件友好性)

- **对比:**

| 维度 | GPTQ | AWQ |
|------|------|-----|
| 量化依据 | 二阶信息 | 激活感知 |
| 校准数据 | 需要(~128样本)| 需要(~128样本)|
| 推理速度 | 快 | 更快(INT4 GEMM优化好)|
| 显存节省 | ~75% | ~75% |
| 量化效果 | 优秀 | 优秀 |
| 硬件兼容性 | 较好 | 更好(标准INT4操作)|

- **选择建议:** 追求速度选AWQ,追求量化精度选GPTQ,两者都远优于简单的Round-to-Nearest量化.

```text
      AWQ 算法核心流程示意图

┌─────────────────────────────────────────────────────────────┐
│                     Calibration Set (Input)                  │
└─────────────────────────────┬───────────────────────────────┘
                              │
                              ▼
                 ┌─────────────────────────┐
                 │  Measure Activation     │
                 │  (Get magnitude per ch) │
                 └─────────────┬───────────┘
                               │
                               ▼
                 ┌─────────────────────────┐
                 │  Find Scaling Factor(s) │◄─── Grid Search
                 │  (Retain 1% max weight) │     (Best Accuracy)
                 └─────────────┬───────────┘
                               │
                               ▼
┌──────────────────┐    ┌──────────────────────┐
│   FP16 Weights   │───►│  Divide by Scale(s)  │
└──────────────────┘    └──────────┬───────────┘
                                  │
                                  ▼
                         ┌──────────────────────┐
                         │   Quantize to INT4   │
                         └──────────┬───────────┘
                                    │
                                    ▼
                         ┌──────────────────────┐
                         │ Save Scale(s) + INT4 │
                         └──────────────────────┘
```

## 常见考点
1. **校准集的作用是什么？**：用来计算激活值的统计信息（如AWQ中的幅度）或Hessian矩阵（如GPTQ中的二阶导数），通常128-512个样本即可，无需标注。
2. **GPTQ为何推理较慢？**：GPTQ在反量化时通常需要处理非对称量化和特殊的权重重组，部分硬件（如早期GPU）对INT4 GEMM支持不如AWQ的Smooth Quant方案高效。
3. **量化通常发生在哪些层？**：主要针对Linear层的权重，Attention中的KV Cache也可以量化（如Int8 KV Cache），激活量化通常较难保持精度。
