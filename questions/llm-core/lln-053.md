---
id: lln-053
difficulty: L2
category: llm-core
subcategory: SFT与RLHF
tags:
- IO
images:
- llmnotes_p308_img1.jpg
- llmnotes_p308_img2.jpg
- llmnotes_p308_img3.jpg
feynman:
  essence: 用组内样本相对排序替代绝对分值评估
  analogy: 不给学生打绝对分，而是看他在小组里是前几名还是后几名
  first_principle: 如何在移除价值函数估计的同时，保持强化学习的训练稳定性？
  key_points:
  - 移除Critic模型，显存占用减半
  - 通过同批次采样的G个回复计算优势值
  - 利用相对优势替代绝对优势，降低方差
follow_up:
- GRPO如何处理没有明确答案的开放性任务?
- GRPO中的group size G如何设置?
- DeepSeek-R1的RL训练流程是怎样的?
---

# GRPO(Group Relative Policy Optimization)的原理是什么?相比PPO有哪些改进

GRPO是DeepSeek提出的PPO改进算法,核心创新:去掉Critic网络,用组内相对奖励估计baseline.

- **PPO的问题:**
1. 需要训练Critic网络估计V(s),增加训练复杂度和显存
2. Critic估计不准会导致策略梯度方差大
3. 4个模型同时训练,工程复杂

- **GRPO的核心改进:**

**PPO的Advantage：** A(s,a) = r(s,a) + gamma*V(s') - V(s) 需要Critic
**GRPO的Advantage：** 对每个prompt采样G个回复,组内归一化
A_i = (r_i - mean(r_1,...,r_G)) / std(r_1,...,r_G)

- **GRPO流程:**
1. 对每个prompt采样一组G个回复
2. 用规则奖励(如数学正确性)或RM打分
3. 组内归一化得到相对优势
4. 用PPO-style目标函数(带clip和KL惩罚)更新策略

- **优势:**
- 去掉Critic网络,只需2个模型(Policy/Ref)
- 组内相对比较更稳定
- 特别适合有明确正确答案的任务(数学、代码)
- DeepSeek-R1使用GRPO实现了强大的推理能力

- **算法架构补充:**
GRPO 的关键假设是：对于同一个 Query，生成的多个 Outputs 构成一个组，该组内奖励的均值可以作为一个很好的 Baseline 替代 Critic 的 $V(s)$ 估计。
优势函数计算公式标准化：
$$ A_i = \frac{r_i - \text{mean}(\mathbf{r})}{\text{std}(\mathbf{r}) + \epsilon} $$
这种 Group Normalization 方式天然去除了 Reward Model 可能存在的偏差（只要 RM 对同一组输出的排序或相对打分是准的，绝对值偏差会被均值抵消）。

```text
PPO Training Loop               GRPO Training Loop

┌────────────────┐             ┌────────────────┐
│ Query: q       │             │ Query: q       │
└───────┬────────┘             └───────┬────────┘
        │                              │
        ▼                              ▼
┌───────────────┐             ┌───────────────────┐
│ Sample: a1    │             │ Group Sample:     │
│ (Single pass) │             │ [a1, a2, ..., aG] │
└───────┬───────┘             └─────────┬─────────┘
        │                              │
        ▼                              ▼
┌───────────────┐             ┌───────────────────┐
│ Critic Net    │             │ Reward/Rules      │
│ Estimate V(s) │             │ Score: [r1..rG]   │
└───────┬───────┘             └─────────┬─────────┘
        │                              │
        ▼                              ▼
┌───────────────┐             ┌───────────────────┐
│ A = r + V' - V│             │ A_i = (r_i - mean)│
│ (Need V)      │             │       / std       │
└───────┬───────┘             └─────────┬─────────┘
        │                              │
        ▼                              ▼
┌───────────────┐             ┌───────────────────┐
│ Policy Update │             │ Policy Update     │
│ (Clip + Ent)  │             │ (Clip + KL)       │
└───────────────┘             └───────────────────┘
```

## 常见考点
1. **为什么 GRPO 适合 DeepSeek-R1**：DeepSeek-R1 的目标是强化推理能力，这类任务（数学、代码）通常有可验证的 Reward Model（如编译检查、答案比对），或者规则奖励。规则奖励往往输出单一数值，Critic 很难学习到这种稀疏奖励的 Value，而 GRPO 的组内比较不需要绝对值，更稳健。
2. **采样开销**：GRPO 需要每组采样 G 个输出（如 G=64），这会不会导致推理速度变慢？（训练阶段确实会增加采样计算量，但减少了 Critic 的前向和反向传播计算，且在大规模并行训练中，采样的开销相对可控；关键是 GRPO 极大降低了显存占用，使得更大 Batch Size 成为可能）。
3. **与 PPO-Clip 的关系**：GRPO 的目标函数仍保留了 PPO 的 Clipping 机制，用于限制新旧策略的更新幅度，保证训练稳定性。
