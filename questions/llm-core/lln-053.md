---
id: "lln-053"
difficulty: "L2"
category: "llm-core"
subcategory: "SFT与RLHF"
tags:
  - "IO"
images:
  - "llmnotes_p308_img1.jpg"
  - "llmnotes_p308_img2.jpg"
  - "llmnotes_p308_img3.jpg"
feynman:
  essence: "GRPO是DeepSeek提出的PPO改进算法,核心创新:去掉Critic网络,用组内相对奖励估计baseline. - *PPO的问题:** 1. 需要训练C"
  analogy: "大语言模型就像读过整个互联网的学者——通过预测「下一个词」生成文本，积累了海量语言模式和知识。"
  key_points:
    - "需要训练Critic网络估计V(s),增加训练复杂度和显存"
    - "Critic估计不准会导致策略梯度方差大"
    - "4个模型同时训练,工程复杂"
first_principle:
  problem: "剥离所有术语：GRPO(Group Relative Policy Optimization)的原理?相比PPO有哪些改进 底层在做什么？为什么这样做是最优的？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "GRPO如何处理没有明确答案的开放性任务?"
  - "GRPO中的group size G如何设置?"
  - "DeepSeek-R1的RL训练流程是怎样的?"
---

# GRPO(Group Relative Policy Optimization)的原理是什么?相比PPO有哪些改进

GRPO是DeepSeek提出的PPO改进算法,核心创新:去掉Critic网络,用组内相对奖励估计baseline.

- **PPO的问题:**
1. 需要训练Critic网络估计V(s),增加训练复杂度和显存
2. Critic估计不准会导致策略梯度方差大
3. 4个模型同时训练,工程复杂

- **GRPO的核心改进:**

**PPO的Advantage：** A(s,a) = r(s,a) + gamma*V(s') - V(s) 需要Critic
**GRPO的Advantage：** 对每个prompt采样G个回复,组内归一化
A_i = (r_i - mean(r_1,...,r_G)) / std(r_1,...,r_G)

- **GRPO流程:**
1. 对每个prompt采样一组G个回复
2. 用规则奖励(如数学正确性)或RM打分
3. 组内归一化得到相对优势
4. 用PPO-style目标函数(带clip和KL惩罚)更新策略

- **优势:**
- 去掉Critic网络,只需2个模型(Policy/Ref)
- 组内相对比较更稳定
- 特别适合有明确正确答案的任务(数学、代码)
- DeepSeek-R1使用GRPO实现了强大的推理能力
