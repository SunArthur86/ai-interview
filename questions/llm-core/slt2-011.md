---
id: "slt2-011"
difficulty: "L3"
category: "llm-core"
subcategory: "Transformer架构"
images:
  - "svg_kvcache.svg"
feynman:
  essence: "投机采样用一个小模型（Draft Model）快速生成候选token，大模型（Target Model）并行验证。"
  analogy: "大语言模型就像读过整个互联网的学者——通过预测「下一个词」生成文本，积累了海量语言模式和知识。"
  key_points:
    - "小模型自回归生成k个候选token（速度快）"
    - "大模型一次性并行计算这k个token的logits（并行，只需一次forward）"
    - "如果小模型生成的token与大模型一致 → 接受"
first_principle:
  problem: "为什么需要 投机采样（Speculative Decoding）？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 投机采样（Speculative Decoding）是什么？

投机采样用一个小模型（Draft Model）快速生成候选token，大模型（Target Model）并行验证。

流程：
1. 小模型自回归生成k个候选token（速度快）
2. 大模型一次性并行计算这k个token的logits（并行，只需一次forward）
3. 对比大小模型输出：
   - 如果小模型生成的token与大模型一致 → 接受
   - 如果不一致 → 从第一个不一致处拒绝，用大模型的分布重新采样

优势：
- 数学上等价于直接用大模型采样（无损）
- 小模型猜对的部分不需要大模型自回归计算
- 典型加速2-3倍

变体：
- Medusa：多个头同时预测多个未来token（不需要小模型）
- EAGLE：用LLM自身的隐状态预测
- Lookahead Decoding：基于Jacobi迭代并行解码
