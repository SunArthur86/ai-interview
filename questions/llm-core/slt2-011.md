---
id: slt2-011
difficulty: L3
category: llm-core
subcategory: Transformer架构
images:
- svg_kvcache.svg
feynman:
  essence: 小模型草拟，大模型并行审核，提速且无损。
  analogy: 实习生写好草稿，主编一次性审阅全稿，对的留，错的改，比主编逐字写快。
  first_principle: 如何在不改变模型输出的前提下加速自回归生成？
  key_points:
  - 小模型快速草拟Token
  - 大模型并行验证
  - 分布完全一致
  - 大幅减少串行计算
---

# 投机采样（Speculative Decoding）是什么？

投机采样用一个小模型（Draft Model）快速生成候选token，大模型（Target Model）并行验证。

流程：
1. 小模型自回归生成k个候选token（速度快）
2. 大模型一次性并行计算这k个token的logits（并行，只需一次forward）
3. 对比大小模型输出：
   - 如果小模型生成的token与大模型一致 → 接受
   - 如果不一致 → 从第一个不一致处拒绝，用大模型的分布重新采样

优势：
- 数学上等价于直接用大模型采样（无损）
- 小模型猜对的部分不需要大模型自回归计算
- 典型加速2-3倍

变体：
- Medusa：多个头同时预测多个未来token（不需要小模型）
- EAGLE：用LLM自身的隐状态预测
- Lookahead Decoding：基于Jacobi迭代并行解码
