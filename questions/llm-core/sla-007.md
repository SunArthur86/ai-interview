---
id: "sla-007"
difficulty: "L2"
category: "llm-core"
feynman:
  essence: "幻觉是指LLM生成看似合理但事实上不正确的内容。分为事实性幻觉（与客观事实不符）和忠实性幻觉（与输入/上下文不符）。。"
  analogy: "控制幻觉就像给 AI 装事实核查员——通过 RAG 引用来源、设置置信度阈值、多路验证，减少一本正经的胡说八道。"
  key_points:
    - "🎯 本质：幻觉是指LLM生成看似合理但事实上不正确的内容"
    - "分为事实性幻觉（与客观事实不符）和忠实性幻觉（与输入/上下文不符）"
    - "📊 幻觉产生原因： 1. 训练数据包含错误信息、统计偏差、知识过时 2. 自回归生成基于概率采样，可能偏离事实 3. 长尾知识分布不均 4. 高温度采样引入随机性 缓解方案： 1. RAG（检索增强生"
first_principle:
  problem: "为什么需要 LLM的幻觉(Hallucination)问题？有哪些缓解方案？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 什么是LLM的幻觉(Hallucination)问题？有哪些缓解方案？

🎯 本质：幻觉是指LLM生成看似合理但事实上不正确的内容。分为事实性幻觉（与客观事实不符）和忠实性幻觉（与输入/上下文不符）。

📊 幻觉产生原因：
1. 训练数据包含错误信息、统计偏差、知识过时
2. 自回归生成基于概率采样，可能偏离事实
3. 长尾知识分布不均
4. 高温度采样引入随机性

缓解方案：

1. RAG（检索增强生成）——最有效
  提供外部知识源，模型基于检索到的真实文档生成答案
  减少对模型内部记忆的依赖

2. 对齐训练
  RLHF/DPO：训练模型在不确定时承认不知道
  Constitutional AI：模型自我审查输出

3. 解码策略优化
  降低temperature（事实性任务用0-0.3）
  Contrastive Search：对比搜索
  对比不同层的输出（DoLa）

4. 后处理验证
  Self-Check：模型自我检查事实准确性
  External Verification：用知识库验证关键事实
  FActScore：细粒度事实评估

5. 架构改进
  多模态输入辅助验证
  Tool Use：让模型调用搜索引擎/计算器
  内置安全检查

6. Prompt工程
  明确要求引用来源
  要求标注不确定性
  分步推理(CoT)减少跳步错误
