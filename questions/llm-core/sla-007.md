---
id: sla-007
difficulty: L2
category: llm-core
feynman:
  essence: 模型生成看似合理但不符合事实或上下文的内容。
  analogy: 学生考试时不会做，为了拿分编造了一个看似合理的错误答案。
  first_principle: 如何让模型在不确定时保持诚实并减少编造？
  key_points:
  - 主要因数据偏差、概率采样和知识盲区导致
  - RAG（外挂知识库）是最有效的缓解方案
  - RLHF可训练模型"知之为知之，不知为不知"
---

# 什么是LLM的幻觉(Hallucination)问题？有哪些缓解方案？

🎯 本质：幻觉是指LLM生成看似合理但事实上不正确的内容。分为事实性幻觉（与客观事实不符）和忠实性幻觉（与输入/上下文不符）。

📊 幻觉产生原因：
1. 训练数据包含错误信息、统计偏差、知识过时。
2. 自回归生成基于概率采样，可能偏离事实。
3. 长尾知识分布不均。
4. 高温度采样引入随机性。

缓解方案：

1. **RAG（检索增强生成）——最有效**
   - 提供外部知识源，模型基于检索到的真实文档生成答案。
   - 减少对模型内部记忆的依赖。

2. **对齐训练**
   - RLHF/DPO：训练模型在不确定时承认不知道。
   - Constitutional AI：模型自我审查输出。

3. **解码策略优化**
   - 降低temperature（事实性任务用0-0.3）。
   - Contrastive Search：对比搜索。
   - 对比不同层的输出（DoLa）。

4. **后处理验证**
   - Self-Check：模型自我检查事实准确性。
   - External Verification：用知识库验证关键事实。
   - FActScore：细粒度事实评估。

5. **架构改进**
   - 多模态输入辅助验证。
   - Tool Use：让模型调用搜索引擎/计算器。
   - 内置安全检查。

6. **Prompt工程**
   - 明确要求引用来源。
   - 要求标注不确定性。
   - 分步推理(CoT)减少跳步错误。
