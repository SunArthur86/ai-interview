---
id: "lln-055"
difficulty: "L2"
category: "llm-core"
subcategory: "LoRA与微调"
tags:
  - "IO"
  - "IOC"
images:
  - "llmnotes_p388_img1.jpg"
  - "llmnotes_p389_img1.jpg"
feynman:
  essence: "LoRA变体通过不同角度改进原始LoRA的局限性."
  analogy: "微调就像给通才毕业生做岗前培训——已有基础能力（预训练），再针对具体岗位做专项训练（指令/偏好对齐）。"
  key_points:
    - "DoRA详解(最值得关注):"
    - "LoRA: W = W0 + BA"
    - "DoRA: W = m · (W0 + BA) / ||W0 + BA||"
first_principle:
  problem: "为什么需要 LoRA的变体有哪些?DoRA、AdaLoRA、LoRA+各自有什么改进？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "DoRA为什么比LoRA效果更好?数学上如何解释?"
  - "GaLore如何实现全参数训练但只占用LoRA级别的显存?"
---

# LoRA的变体有哪些?DoRA、AdaLoRA、LoRA+各自有什么改进

LoRA变体通过不同角度改进原始LoRA的局限性.

- **主要变体对比:**

| 变体 | 核心改进 | 解决的问题 |
|------|---------|-----------|
| AdaLoRA | 动态分配rank | 固定rank不合理 |
| DoRA | 分解为大小和方向 | LoRA缺少权重幅值调整 |
| LoRA+ | A和B使用不同学习率 | A/B学习不均衡 |
| GaLore | 梯度低秩投影,全参数训练 | LoRA只能训练少量参数 |
| LongLoRA | Shifted Sparse Attention | 原始LoRA不支持长上下文 |

- **DoRA详解(最值得关注):**
- LoRA: W = W0 + BA
- DoRA: W = m · (W0 + BA) / ||W0 + BA||
- 将权重分解为大小(magnitude)和方向(direction)
- 更接近全参数微调的更新模式
- 效果显著优于标准LoRA,尤其在低rank时

- **AdaLoRA详解:**
- 基于重要性分数动态调整每层的rank
- 使用SVD分解替代随机初始化
- 训练过程中裁剪不重要的奇异值,分配给重要层
