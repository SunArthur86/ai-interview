---
id: "llm--001"
difficulty: "L2"
category: "llm-core"
subcategory: "训练理论"
feynman:
  essence: "- *On-policy vs Off-policy** - *On-policy**:策略更新只能使用当前策略自己采集的数据.代表:PPO、REINFORCE"
  analogy: "on-policy vs off-policy 就像「自己试 vs 看别人试」——on-policy 用自己当前策略的数据学习（更新即丢弃），off-policy 用别人的经验也能学（数据可复用）。"
  key_points:
    - "- *On-policy vs Off-policy** - *On-policy**:策略更新只能使用当前策略自己采集的数据.代表:PPO、REINFORCE.优点:理论清晰"
    - "缺点:数据利用率低(采一批用一次就丢). - *Off-policy**:可以利用其他策略(或旧策略)采集的数据来更新当前策略.代表:DQN、SAC、Q-learning.优点:数据可复用(exper"
    - "缺点:需要重要性采样或函数近似修正偏差. **核心区别：** 是否允许用旧数据训练新策略. - *Online vs Offline RL** - **Online RL**:训练过程中可以与环境持续"
first_principle:
  problem: "它们本质上为什么不同？各自的设计目标和适用场景是什么？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "为什么 PPO 不能用 experience replay?"
  - "DPO 为什么不需要在线采样?"
---

# 请解释 on-policy 和 off-policy 的区别,以及 online 和 offline RL 的含义.

- **On-policy vs Off-policy**

- **On-policy**:策略更新只能使用当前策略自己采集的数据.代表:PPO、REINFORCE.优点:理论清晰;缺点:数据利用率低(采一批用一次就丢).

- **Off-policy**:可以利用其他策略(或旧策略)采集的数据来更新当前策略.代表:DQN、SAC、Q-learning.优点:数据可复用(experience replay);缺点:需要重要性采样或函数近似修正偏差.

**核心区别：** 是否允许用旧数据训练新策略.

- **Online vs Offline RL**
- **Online RL**:训练过程中可以与环境持续交互采集新数据
- **Offline RL**:只有固定的历史数据集,不能与环境交互

在 RLHF 场景中:
- PPO 是 on-policy + online
- DPO 可视为 off-policy(直接利用偏好数据,不需要在线采样)
- GRPO 也是 on-policy,但通过组内相对优势减少方差
