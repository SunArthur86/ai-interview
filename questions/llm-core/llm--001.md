---
id: llm--001
difficulty: L2
category: llm-core
subcategory: 训练理论
feynman:
  essence: On-policy只能用新数据，Off-policy能用旧数据；Online可交互，Offline只能用存量。
  analogy: On-policy像只吃现做的菜；Off-policy像吃罐头，随时可吃但要过保质期检查。
  first_principle: 智能体在训练时应该使用什么样的数据分布来更新策略？
  key_points:
  - On-policy数据利用率低，Off-policy支持经验回放
  - Online RL边学边探索，Offline RL从固定数据集中学习
  - PPO需在线采样，DPO直接利用离线偏好数据
  - Off-policy通常需要重要性采样修正分布偏差
follow_up:
- 为什么 PPO 不能用 experience replay?
- DPO 为什么不需要在线采样?
---

# 请解释 on-policy 和 off-policy 的区别,以及 online 和 offline RL 的含义.

- **On-policy vs Off-policy**

- **On-policy**:策略更新只能使用当前策略自己采集的数据.代表:PPO、REINFORCE.优点:理论清晰;缺点:数据利用率低(采一批用一次就丢).

- **Off-policy**:可以利用其他策略(或旧策略)采集的数据来更新当前策略.代表:DQN、SAC、Q-learning.优点:数据可复用(experience replay);缺点:需要重要性采样或函数近似修正偏差.

**核心区别：** 是否允许用旧数据训练新策略.

- **Online vs Offline RL**
- **Online RL**:训练过程中可以与环境持续交互采集新数据
- **Offline RL**:只有固定的历史数据集,不能与环境交互

在 RLHF 场景中:
- PPO 是 on-policy + online
- DPO 可视为 off-policy(直接利用偏好数据,不需要在线采样)
- GRPO 也是 on-policy,但通过组内相对优势减少方差
