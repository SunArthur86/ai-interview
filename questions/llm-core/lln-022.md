---
id: lln-022
difficulty: L2
category: llm-core
subcategory: 手撕代码
tags:
- Optional
- IO
- IOC
- Elasticsearch
images:
- llmnotes_p599_img1.jpg
feynman:
  essence: 推理时缓存K、V，避免历史token重复计算。
  analogy: 就像聊天记录一样，每次只输入新话，但自动加载之前的对话历史，不用每次都重新打字。
  first_principle: 如何利用空间换时间，避免推理过程中的重复计算？
  key_points:
  - 初始化预分配固定大小的K、V缓存张量
  - 推理时根据位置索引将当前步K、V写入缓存
  - 计算注意力时从缓存中读取完整历史K、V
---

# 手撕在MHA中加入KV Cache

在MHA中，预分配K、V缓存张量，推理时将当前步的K、V写入缓存，并从缓存中读取历史K、V进行注意力计算，避免重复计算。
