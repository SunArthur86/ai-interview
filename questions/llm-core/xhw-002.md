---
id: "xhw-002"
difficulty: "L4"
category: "llm-core"
subcategory: "训练与微调"
tags:
  - "LoRA"
---

# LoRA是怎样进行初始化矩阵的

LoRA 通过引入低秩矩阵分解，在微调时不再调整原始模型的所有权重，而是通过将权重矩阵分解成两
个较小的低秩矩阵来表示权重的变化。尽管预训练模型拥有大量的参数，但许多参数在特定任务的微
调过程中实际上是不活跃的。LoRA通过低秩分解的方式，只更新那些对特定任务最重要的参数，这样
做可以在保持模型性能的同时，显著减少微调所需的计算资源和时间。
假设某个层的权重矩阵为 
，LoRA 不微调 
 本身，而是通过两个低秩矩阵  
和 
来近似微调权重矩阵。LoRA 的权重更新公式为：
W ∈Rd×k
W
A ∈Rd×r
B ∈Rr×k
           

W
 =
new
W +
ΔW =
r
α
W +
(A ×
r
α
B)
- ：微调后的权重矩阵。
W

new
- ：预训练模型的原始权重矩阵，保持不变。
W
- 和 
 ：低秩矩阵， 
 近似于权重变化量
 。
A
B
A × B
ΔW
- ：一个缩放因子，用来调节低秩矩阵的影响。
α
- ：秩的大小，表示低秩矩阵
 和 
 的维度，通常 
，从而大大减少需要训练的参数数
量。
r
A
B
r ≪d, k
LoRA 的核心思想是将权重的变化
通过两个小型矩阵的乘积进行表示，利用低秩矩阵分解的方式
有效降低参数量。这样只需要训练低秩矩阵的参数，而不是完整的权重矩阵。
ΔW
3. LoRA 的优点
🎁高效性：
通过低秩矩阵分解，
大幅减少了需要微调
的参数量。在大规模
预训练模型中，LoRA 
仅引入一小部分新的
参数，节省了存储空
间和计算开销。
📍冻结主干模型：LoRA 
保持原始模型的主干
权重 
不变，仅调
整 
 和 
 矩阵。这
使得可以在多个任务
之间共享同一个预训
练模型，而不同任务
只需要微调少量的低
秩矩阵。
W
A
B
🌟通用性强：
LoRA 可以应用在模型
的多种层次结构（如
自注意力层、前馈网
络层等），广泛适用
于各类 Transformer 
模型。

3. Lora是怎样进行初始化矩阵的
在 LoRA 的实现中，矩阵
 初始化为零矩阵，矩阵 
使用随机高斯分布初始化，这样做的主要目的是
在微调开始时确保权重调整
是零矩阵，从而稳定训练过程并保持模型在初始阶段的表现与预
训练模型一致。下面详细解释这种初始化方式的原因和优点：
B
A
ΔW = 0
1. 为什么初始化时需要B初始化为零矩阵

LoRA 的目标是在不改变预训练模型原始权重 
的情况下，通过微调来对模型进行轻量级的适配。如
果在微调一开始，矩阵 
和 
 的初始值导致较大的权重变化（即  
 ），模型的表现可能会
发生突变，偏离原本的预训练模型表现，从而导致不稳定的训练过程。
W
A
B
ΔW =0
通过将
 初始化为零矩阵， 
  一开始为零矩阵，这样确保了在微调开始时：
B
ΔW = A × B

Wnew = W + ΔW = W + 0 = W
即，初始时微调模型的权重与预训练模型的权重保持一致。这确保了模型不会一开始就因为过大的权
重变化而导致性能波动。
2.  随机初始化A的原因
尽管 
 初始化为零，但 
 使用随机高斯分布进行初始化。这是为了确保在训练过程中，当 
 不再
为零矩阵时，
 能够提供多样的参数更新方向。具体原因包括：
B
A
B
A
📌
增加表示能力：随机初始化 
 可以确保它具有丰富的表示能力，使得低秩矩阵
 能
够探索广泛的权重调整空间。随机初始化提供了不同的起始点，从而能够在训练过程中捕捉
更多的特征。
A
A × B
避免对称性问题：如果 
 初始为零或相同的值，可能会限制模型在调整过程中学习到多样
化的特征。随机初始化打破了这种对称性，使得矩阵 
能够在训练过程中学到更复杂的特征
组合。
A
A
提供有效的梯度信息：随机初始化 
可以防止梯度更新过程中出现梯度消失的问题。初始
化时提供的非零梯度信息可以加速收敛，帮助模型更快地找到适应任务的最优参数。
A
3. 好处与优点
📌
稳定性：在训练开始时，保证权重矩阵的变化是零，能够稳定训练过程，使模型在初始阶段
的表现与预训练模型一致。这样可以避免模型性能在训练初期因为过大的权重调整而大幅波
动。
渐进调整：通过将 
 初始化为零矩阵，
  从零开始逐渐增加，权重调整逐渐介入模
型，而不是一次性强烈干扰模型的权重。这样能够让 LoRA 模块在稳定的基础上学习新的特
征。
B
ΔW
保护预训练模型的效果：因为 LoRA 的初始更新为零矩阵，这样在刚开始的几个训练步骤
中，模型仍然依赖于预训练权重，保护了预训练模型在下游任务中的基础表现能力。
减少梯度爆炸或消失的风险：如果 
 初始过大，可能导致梯度计算异常，进而引发梯度
爆炸或消失问题。通过保证初始时 
 为零，能够避免这些梯度异常情况的发生。
ΔW
ΔW
4. 总结
