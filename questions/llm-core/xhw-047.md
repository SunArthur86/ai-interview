---
id: "xhw-047"
difficulty: "L4"
category: "llm-core"
subcategory: "LLM前沿"
tags:
  - "llm-core"
---

# 长尾问题和解决方法

小黄搞AI-LLM面试问题
75. Transformer模型中计算量(FLOPs)的分析
75.1 计算量FLOPs估计方法
FLOPs，floating point operations，表示浮点数运算次数，衡量了计算量的大小。如何计算矩阵乘法
的FLOPs呢？对于
，计算AB需要进行
次乘法运算和
次加法运算，共计
次浮点运算，需要
的FLOPS,对于
，计算AB需要的浮点运算次数为
。
A ∈R
, B ∈
1×n
Rn×1
n
n
2n
2n
A ∈R
, B ∈
m×n
Rn×k
2mnk
🌵计算AB需要的浮点运算次数推算
对于矩阵相乘：
则

A ∈R
,
B ∈
m×n
Rn×k
C = AB ∈Rm×k
其中 
。对于计算一个元素 
：
C
 =
ij
A
B

r=1
∑
n
ir
rj
C

ij
- 需要进行 
 次乘法运算（将 
 与 
 相乘）
n
A

ir
B

rj
- 需要进行 
 次加法运算（将这 
 个乘积相加）
n −1
n
整个结果矩阵 
 有 
 个元素，每个元素需要 
 次乘法和 
 次加法，因此：
C
m × k
n
n −1
- 总的乘法次数为：

m × k × n
- 总的加法次数为：

m × k × (n −1)
在大多数情况下，为了简化近似，人们会认为加法次数与乘法次数相当(当 
 很大时，

 )，因此总的浮点运算量(FLOPs)大约为 
（其中包含约 
 次乘法和 

 次加法）。
n
n −1 ≈n
2mnk
mkn
mkn
75.2 Transformer结构中FLOPs计算
下面将对所提及的计算步骤进行更为详细的解释与展开。这些步骤来源于Transformer中一层（单
层）的计算分析，包括Self-Attention部分和MLP（Feed-Forward）部分，以及最终输出logits的计
算。假设我们有以下记号：
- : 批次大小 (batch size)
b
- : 序列长度 (sequence length)
s
- : 隐藏层维度
d

model
- : 前馈层扩张后的维度，一般为4倍 

d

ff
d

model
- : 注意力头数 (heads)
h

- : 每个注意力头的维度
d
 =
k
d
/h
model
- : 词表大小 (vocab size)
V
在实际实现中，这些计算往往合并在一起执行，但为方便计算FLOPs和参数量，我们将各个步骤独立
拆开说明。
🥥Input输入层分析
💎 在一次训练迭代中，假设输入数据的形状为
,经embedding层得
，矩阵乘法
的输入和输出的形状为
,计算量为

[b, s]
[b, s, h]
[b, s, V ] × [V , h] →[b, s, h]
2bshV
假设输入数据的形状为 
。其中： 
[b, s]
◦

 是batch size，即一次输入的样本数。
b
◦

 是序列长度（例如处理一句话中有s个token）。
s
每个输入样本中的每个位置（token）对应着一个词表索引，这个索引指向词典中大小为
的词表，即词表有
个可能的词。
V
V
Embedding层的本质：
 通常，embedding层会将每个词（token）的索引映射到一个长度为
的向量（embedding
向量）。
h
◦
词嵌入矩阵 

W
 ∈
emb
RV ×h
◦

 是词表大小。
V
◦

 是embedding的维度（隐藏维度）。
h
直观来看，embedding矩阵中的每一行是一个词的向量表示。当给定一个词的索引时，就直
接取出该行作为该词的embedding向量。
将Embedding表示为矩阵乘法：
 在实际实现中，Embedding查表是通过索引取行完成的，这个操作本身不像传统的矩阵相乘
那样显式地执行
次乘法和加法。但如果我们将"取embedding"的过程用矩阵乘法的等价形
式来表示，就可以得到一个FLOPs的估计方法：
V
◦
将输入的词表索引用独热(one-hot)编码表示：
 对于输入的 
 中的每个元素（一个token ID），用维度为
的one-hot向量来表
示。
 则输入可以看作是 
的三维张量，每个位置都是一个one-hot向量（这里原本
的 
 映射为 
 后会很大，但这是一种理论上的表示方法，用于FLOPs计算
近似）。
[b, s]
V
[b, s, V ]
[b, s]
[b, s, V ]
◦
在这种表示下：
- 表示batch中所有tokens的独热表示
[b, s, V ]
- 是embedding矩阵
W
 ∈
emb
[V , h]
