---
id: xhw-070
difficulty: L5
category: llm-core
subcategory: LLM前沿
tags:
- llm-core
feynman:
  essence: 利用强化学习通过组内相对优势估计优化策略，无需价值模型，驱动模型自发探索推理路径。
  analogy: 老师不教具体解法，只公布全班作业的平均分和方差，让学生根据自己相对于平均分的表现自我调整解题思路。
  first_principle: 如何在没有人工示范和复杂价值模型的情况下，仅依靠结果奖励引导模型优化其推理策略？
  key_points:
  - 采用 GRPO 算法，不需要训练价值模型。
  - 对同一问题生成多个答案计算组内相对优势。
  - 使用规则奖励（答案正确性、格式规范）作为反馈。
  - 加入 KL 散度约束保持模型稳定性。
  - 模型在奖励驱动下自发涌现反思和长链式思考能力。
---

# DeepSeek-R1-Zero中RL的原理

### DeepSeek-R1-Zero 中 RL 的原理

#### 1. 核心机制：Group Relative Policy Optimization (GRPO)
DeepSeek-R1-Zero 采用了一种基于强化学习的优化策略，核心特点是不训练显式的价值模型，而是通过**组内相对优势**来进行策略优化。

#### 2. 关键步骤

**2.1 采样与分组**
对于同一个输入提示，模型采样生成 $G$ 个不同的输出序列。

**2.2 奖励计算**
利用基于规则的奖励系统对每个输出进行打分。主要包含两类奖励：
- **准确率奖励**：检查最终答案是否正确（如数学题结果匹配）。
- **格式奖励**：检查推理过程是否包含在指定的标签（如 `<think>`）之间。

**2.3 优势估计**
计算每个输出相对于组内平均水平的优势：
$$A_i = \frac{r_i - \text{mean}(r)}{\text{std}(r)}$$
其中 $r_i$ 是第 $i$ 个输出的奖励，通过减去均值并除以标准差，实现奖励的归一化，消除不同任务奖励量纲的影响。

**2.4 策略更新 (GRPO)**
使用类似 PPO 的目标函数进行优化，但利用组内其他输出来替代基准策略。优化目标包含两部分：
1. **最大化期望奖励**：利用计算出的优势 $A_i$ 更新策略，使高分输出的生成概率增加。
2. **KL 散度约束**：引入 KL 散度惩罚项 $D_{KL}(\pi_\theta || \pi_{ref})$，防止模型在训练过程中偏离原始模型太远，避免语言模式崩塌。

#### 3. 行为涌现
在上述 RL 机制的驱动下，为了最大化奖励（即获得正确答案），模型逐渐学会了生成更长的推理链，并自发地出现了**自省**和**重新评估**的行为。这表明强化学习能够驱动模型自主发展出有效的解决问题的策略。
