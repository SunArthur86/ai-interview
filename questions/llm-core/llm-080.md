---
id: "llm-080"
difficulty: "L2"
category: "llm-core"
subcategory: "注意力机制"
tags:
  - "IO"
feynman:
  essence: "1 概念与公式 🥥 概念与公式 **概念：** 在计算注意力时,Query(查询)来自一个序列(或特征集),Key / Value(键/值) 来自另一个序列(或特征集).常见情形是。"
  analogy: "注意力机制就像在聚会上听人说话——自动聚焦到感兴趣的声音，给不同人不同关注度。"
  key_points:
    - "86. 1 概念与公式 🥥 概念与公式 **概念：** 在计算注意力时,Query(查询)来自一个序列(或特征集),Key / Value(键/值) 来自另一个序列(或特征集).常见情形是 Decod"
    - "Y 编码器输出 H Decoder 也有若干层,每一层通常包含: a. Masked Self-Attention(针对 本身做自注意力,用 mask 来防止看见后续词)"
    - "Y b. Cross-Attention(也叫 Source-Attention,用 的表示去“查询” ,从而访问 源序列信息)"
first_principle:
  problem: "为什么需要 Cross-Attention(交叉注意力)？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
---

# Cross-Attention(交叉注意力)是什么

86. 1 概念与公式
🥥 概念与公式 **概念：** 在计算注意力时,Query(查询)来自一个序列(或特征集),Key / Value(键/值) 来自另一个序列(或特征集).常见情形是 Decoder 使用 Cross-Attention 来“关注” Encoder 输出. **公式：** 如果我们将 Encoder 的输出记为 ,Decoder 的先前状态记为 ,那么 Cross- Attention 可能类似: 其余部分与自注意力相 同,仍是

H Y Q = Y W , K = Q HW , V = K

**HW V**
HW V Attention(Q, K, V ) = softmax( ) V

d k QKᵀ
86. 2 Transformer中的Cross-Attention

🥥 Transformer中的Cross-Attention 在 Transformer 的 Encoder-Decoder 结构里,所谓的 Cross-Attention(交叉注意力)指

**的是：**
Query(Q) 来自 Decoder 端(即目标序列的表示). Key(K) 和 Value(V) 则来自 Encoder 端(即源序列的表示). 因此,在做 Cross”的其实就是:Decoder 的隐藏状态(或 Decoder 端的输入)和 Encoder 输出的隐藏表示(即源序列经过 Encoder 后得到的一组特征) Encoder-Decoder 结构概览 在标准的 Transformer 中,分为 Encoder 和 Decoder 两个主要部分: **Encoder：** 输入为源序列 (例如外语句子),经过若干层自注意力与前馈网络后,输出一 个隐藏表示矩阵 . ,其中 是源序列的长度,

是隐藏维度. X H H = Encoder(X) (大小约为 n × d) n d **Decoder：** 输入通常包括 目标序列(或已生成部分序列) 的嵌入; Y 编码器输出

H Decoder 也有若干层,每一层通常包含: a. Masked Self-Attention(针对 本身做自注意力,用 mask 来防止看见后续词); Y b. Cross-Attention(也叫 Source-Attention,用 的表示去“查询” ,从而访问 源序列信息); Y H c. 前馈网络(Feed-Forward). Cross-Attention 的输入与输出 来自 Decoder 端的 Query • Decoder 在第 ll 层(或任意一层)会先用 Masked Self-Attention 得到一批隐藏向量

. D(l) • 这批隐藏向量会经过线性变换成为 , 这里 对应目标序列(或已生成 的目标 Token)在 Decoder 第 ll 层的表示. Q = D(l)
**W Q*
W Q D(l) 来自 Encoder 端的 Key、Value • Encoder 的输出 (大小为 )会经过线性变换分别得到 和 ,

H n × d K V K = H W , V = K

**H W V**
H W V • 这里 包含了源序列(输入句子)在 Encoder 端的上下文信息;经过投影后,就成为用 来匹配 Query 的 “Key” 以及最终提供“内容信息”的 “Value”. H Cross-Attention 的计算

一旦得到 ,就跟普通的 Scaled Dot-Product Attention 一样:

Q, K, V Attention(Q, K, V ) = softmax( )V

d k QKᵀ • 这里, 的行数对应目标序列的长度(Decoder 时间步数), Q •

的行数对应源序列的长度(Encoder 输出长度). K, V 为什么称作“Cross-Attention”? Self-Attention(自注意力)是 同一个序列 内部的各个位置之间相互计算注意力; Cross-Attention 则是 目标序列(Decoder 侧) 和 源序列(Encoder 侧) 之间的注意力交 互.
**应用**
应用 • **机器翻译：** Decoder 根据源语言(Encoder 输出)生成目标语言. • 多模态任务(Vision+Language、Speech+Text 等):图像特征和文本特征通过 Cross- Attention 交互. 在机器翻译中,最开始的“attention”就是源序列和目标序列间的注意力,实际上就属于 Cross- Attention 的范畴. 在 Transformer 中,Encoder-Decoder 结构里,Decoder 部分显式地包含一个 Cross-Attention 模 块,用来获取源序列信息.
