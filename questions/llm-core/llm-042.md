---
id: "llm-042"
difficulty: "L2"
category: "llm-core"
subcategory: "强化学习"
tags:
  - "IO"
feynman:
  essence: "在强化学习中,策略函数和值函数是两个核心的概念,它们分别用于描述智能体如何选择动作以及这 些动作的长期回报. 策略函数(Policy Function) 策略函"
  analogy: "RLHF 就像给 AI 请人类老师——人类给回答打分排序，训练奖励模型，再用强化学习让 AI 越来越符合人类偏好。"
  key_points:
    - "在强化学习中,策略函数和值函数是两个核心的概念,它们分别用于描述智能体如何选择动作以及这"
    - "策略函数(Policy Function)"
    - "策略函数 描述了在给定状态 下,智能 体选择某个动作 的概率或确定性规则.它决定"
first_principle:
  problem: "为什么需要 强化学习中策略函数和值函数？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
---

# 强化学习中策略函数和值函数是什么

在强化学习中,策略函数和值函数是两个核心的概念,它们分别用于描述智能体如何选择动作以及这
些动作的长期回报.
策略函数(Policy Function)
策略函数 描述了在给定状态 下,智能 体选择某个动作 的概率或确定性规则.它决定
了智能体的行为方式,指导智能体在不同状态下
应采取的动作. π (a∣s) θ s a •
**随机策略：**
如果智能体根据概率来选择动作,策略函数

定义了智能体在状态 下以概率

选择动作 的分布. π (a∣s) θ s π (a∣s) θ a
**例如,在随机策略下：**
, 表示给定状态 ,动作 的选择概率. π (a∣s) = θ P (a∣s) s a •
**确定性策略：**
如果智能体在每个状态下总是选择一个固定 的动作.
**策略函数就是一个确定性的函数：**

a = π (s) θ
在这种情况下,策略函数直接输出智能体在状态

下所采取的动作 . s a
在策略优化算法(如 A3C、PPO)中,策略函数
通常由神经网络近似表示,网络的参数是 ,通
过优化这些参数使得策略能在不同状态下选择最
优的动作. θ 值函数(Value Function) 值函数 描述了在状态 下,按照策略
行动时能够获得的期望累积回报.它反映了智能
体在当前状态下的“好坏”,即从状态 开始, 智能体期望未来能够获得的奖励总和. V (s) π s π s
**值函数的定义为：**

V (s) = π E γ R(s , a ) s = s, a ∼π [ t=0 ∑ ∞ t t t ∣ ∣ 0 t ] • **期望累积回报：** 这里, 是折扣因子,

是智能体在时间步 采取动作 后 获得的即时奖励.值函数是这些奖励的加权 和,表示从状态 开始,按照策略 行动后 未来能够获得的总奖励的期望值. γ R(s , a ) t t t a t s π •
**状态值函数和动作值函数：** 状态值函数 描述的是在状态 下, 期望未来的累积回报. V (s) π s 动作值函数 则是考虑在状态 下 选择某个特定动作 时的期望累积回报, 即:

Q (s, a) π s a Q (s, a) = π E γ R(s , a ) s = s, a = a, a ∼π [ t=0 ∑ ∞ t t t ∣ ∣ 0 0 t ]
在 Q-Learning 等算法中,动作值函数
是
核心,用于指导智能体在每个状态下选择最佳动
作. Q(s, a)

**总结**
总结 • 策略函数 :定义了在状态 下选择动作 的概率,决定了智能体的行为. π (a∣s) θ s a • 值函数 :定义了在状态 下,遵循策略
所能获得的期望累积回报,反映了状态的价值.
V (s) π s π
这两者结合起来为强化学习算法提供了强大的工具,策略函数指导行动,值函数评估行动的长期效
果.
