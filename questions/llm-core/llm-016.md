---
id: "llm-016"
difficulty: "L2"
category: "llm-core"
subcategory: "Transformer架构"
tags:
  - "IO"
  - "IOC"
  - "Elasticsearch"
images:
  - "svg_transformer_blocks.svg"
feynman:
  essence: "么区别,怎么运用? 1. Encoder&Decoder 模型是什么: 标准的 Transformer 模型主要由两个模块构成: • Encoder(左边):负"
  analogy: "Transformer 就像高效的读书小组——每个人（注意力头）同时读不同段落，然后交流关键信息，不像 RNN 逐字读。"
  key_points:
    - "么区别,怎么运用? 1. Encoder&Decoder 模型是什么: 标准的 Transformer 模型主要由两个模块构成: • Encoder(左边):负责理解输入文本,为每个输入构造对应的语义"
    - "• Decoder(右边):负责生成输出,使用 Encoder 输出的语义表示结合其他输入来生成目标序 列. 2. Encoder-only 模型: • **架构特点：** Encoder-only "
    - "解码器则根据编码器的输出生成目标序列.这个结构能够同时处理输入和输出序列的关 联. • **典型模型：** T5(Text-to-Text Transfer Transformer)和 Transfo"
first_principle:
  problem: "为什么需要 Encoder-only、Decoder-only和Encoder-Decoder的模型分别有什？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Transformer 架构是 LLM 的基础——Attention 机制让长距离依赖成为 O(1)"
    - "预训练+微调+对齐是 LLM 训练三步曲——各有各的目标和挑战"
    - "推理优化的核心是减少计算和访存——KV Cache、量化、 batching 三板斧"
  rebuild: "从模型结构出发：① Transformer 为什么有效？② 训练三阶段各自解决什么？③ 推理优化有哪些方向？④ 如果设计新架构你会怎么做？"
---

# Encoder-only、Decoder-only和Encoder-Decoder的模型分别有什

么区别,怎么运用?
1. Encoder&Decoder 模型是什么:
标准的 Transformer 模型主要由两个模块构成: • Encoder(左边):负责理解输入文本,为每个输入构造对应的语义表示(语义特征); • Decoder(右边):负责生成输出,使用 Encoder 输出的语义表示结合其他输入来生成目标序 列.
2. Encoder-only 模型:
• **架构特点：** Encoder-only 模型只使用编码器部分.编码器的核心是处理输入序列,并生成该序列 的上下文向量(即隐藏状态),它能够很好地捕捉输入序列的全局信息. • **典型模型：** BERT(Bidirectional Encoder Representations from Transformers)是一个典 型的 Encoder-only 模型.

• **应用场景：** 这种架构适合于理解类任务,例如: **文本分类：** 根据输入的文本进行分类(如情感分析、新闻分类). 问答任务(抽取式):通过输入问题和上下文,模型从上下文中抽取出准确的答案. 命名实体识别(NER):标注文本中的特定实体(如人名、地名等).
3. Decoder-only 模型:
• **架构特点：** Decoder-only 模型只使用解码器部分.解码器主要用于生成序列,它依赖于自回归机 制,每次生成一个单词,并根据之前生成的单词继续预测下一个单词. • **典型模型：** GPT(Generative Pretrained Transformer)是 Decoder-only 架构的代表. • **应用场景：** 这种架构适合生成类任务,例如: **文本生成：** 根据提示生成一段连贯的文本(如对话生成、自动写作). **对话系统：** 逐步生成自然语言响应,回答用户提问. 机器翻译(部分场景下):逐词生成目标语言的翻译结果.
4. Encoder-Decoder 模型:
• **架构特点：** Encoder-Decoder 模型包含编码器和解码器两个部分.编码器处理输入序列,生成上 下文向量;解码器则根据编码器的输出生成目标序列.这个结构能够同时处理输入和输出序列的关 联. • **典型模型：** T5(Text-to-Text Transfer Transformer)和 Transformer(Vaswani et al. 提出 的基础模型)是典型的 Encoder-Decoder 模型. • **应用场景：** 这种架构适合需要输入和输出匹配的任务,例如: **机器翻译：** 将一个句子从源语言翻译成目标语言,编码器负责理解源语言句子,解码器负责生 成目标语言句子. **文本摘要：** 编码器处理长文本,解码器生成简短摘要. 文本生成(条件生成):如文本填空或根据输入生成对应的完整文本.
5. 主要区别总结:
• Encoder-only :用于理解任务,适合文本分类、问答、信息抽取等场景. • Decoder-only :用于生成任务,擅长文本生成、对话系统、续写等. • Encoder-Decoder :用于处理输入-输出映射的任务,特别适合机器翻译、文本摘要等.
6. 如何运用:
**📌任务选择：** 根据具体任务选择合适的架构:

• 文本分类等理解任务优先使用 Encoder-only 模型如 BERT. • 文本生成等生成任务使用 Decoder-only 模型如 GPT. • 输入输出序列间有强关联的任务如翻译和摘要,采用 Encoder-Decoder 模型如 T5.
