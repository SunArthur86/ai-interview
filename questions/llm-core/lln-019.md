---
id: lln-019
difficulty: L2
category: llm-core
subcategory: 大模型应用
tags:
- 泛型
- IO
- IOC
- 索引
- Elasticsearch
images:
- llmnotes_p596_img1.jpg
- llmnotes_p597_img1.jpg
feynman:
  essence: 通过检索增强和生成优化提升工业界RAG的准确性和稳定性
  analogy: 像图书馆管理员先整理书架，再精准推荐书籍，最后用AI摘要关键内容
  first_principle: 如何让RAG系统在工业场景中更精准、稳定地满足用户需求？
  key_points:
  - 工业界关注query改写、意图分流、向量召回等检索优化
  - 生成优化注重内容全面性、格式和稳定性
  - 微调通过负例、监督信号等增强模型鲁棒性
follow_up:
- RAG中如何处理多轮对话的上下文?
- 向量检索和全文检索在RAG中如何结合?
---

# 讲讲工业界RAG具体如何优化

工业界 RAG (Retrieval-Augmented Generation) 优化是一个系统工程，涉及 Index、Retriever、Reranker、Generator 全链路。

**1. 检索阶段优化**
*   **Query 理解与改写**：
    *   *Query Expansion*：利用 LLM 生成同义词、子问题，弥补语义匹配不足。
    *   *HyDE (Hypothetical Document Embeddings)*：让 LLM 先生成一个「假设性回答」，再用该回答的向量去检索，解决 Query 和 Document 语义空间不匹配问题。
    *   *Step-back Prompting*：让模型先退一步抽象出高层概念，再进行检索，适合复杂推理问题。

**2. 排序与过滤优化**
*   **Rerank (精排)**：Retriever（如BM25/Dense）召回 Top-50/100 后，使用 Cross-encoder（如 BERT-Rerank, Cohere Rerank）进行精准打分，截取 Top-5/10。
*   **去重**：对检索结果进行语义去重，避免上下文中充满重复信息。

**3. 生成阶段优化**
*   **上下文压缩**：检索出的文档可能包含噪声，使用 LLMLingua 等模型压缩 Context，去除非关键信息，保留 Prompt 的语义完整性。
*   **引用归因**：在 Prompt 中要求模型回答时标注来源 ID，提升可信度并便于 Debug。
*   **矛盾检测**：如果检索结果间存在冲突，训练模型或设计 Prompt 识别并指出矛盾，而非胡乱拼凑。

**4. 微调优化**
*   **RAFT (Retrieval-Augmented Fine Tuning)**：训练 LLM 在检索到的「干扰文档」中识别出「金标准文档」并据此回答。增强模型的抗噪和检索辨别能力。
*   **AAR (Attributed to Retrieved)**：微调模型使其生成内容严格基于检索内容，减少幻觉。

**系统架构图**：
```text
User Query
    │
    ▼
┌───────────────┐    Query Expansion / HyDE
│  Query Rewrite│ ──────────────────────────────┐
└───────┬───────┘                             │
        │                                     │
        ▼                                     │
┌───────────────┐ (Top-K Docs)    ┌───────────────────────┐
│  Bi-Encoder   │ ──────────────> │       Reranker        │ (Cross-encoder)
│  Retriever    │ (Vector/BM25)   │ (Re-score & Filter)   │
└───────────────┘                 └───────────┬───────────┘
                                              │
                 Context Compression           ▼
                 (LLMLingua)          ┌───────────────────────┐
                       ───────────────│   Final Context (Top-N)│
                                      └───────────┬───────────┘
                                                  │
                                                  ▼
                                       ┌───────────────────────┐
                                       │ LLM Generator (w/ Ref)│
                                       └───────────────────────┘
```

**常见考点**：
1. 检索准确率低怎么排查？（检查 Embedding 模型是否适合领域，切分 Chunk 大小是否合理，是否需要混合检索 BM25+Vector）
2. Rerank 为什么效果好但慢？（Cross-encoder 需要将 Query 和每个 Doc 拼接输入 BERT，计算复杂度 $O(N \cdot L^2)$，无法并行处理 Docs，只能用于少量精排）
3. 如何缓解长文本导致的 Lost in the Middle 现象？（将关键信息放在 Prompt 的开头或结尾，或使用模型重新排序检索结果的顺序）
