---
id: "lln-050"
difficulty: "L2"
category: "llm-core"
subcategory: "大模型架构"
images:
  - "llmnotes_p062_img1.jpg"
  - "llmnotes_p063_img1.jpg"
  - "llmnotes_p063_img2.jpg"
feynman:
  essence: "为保持总参数量不变,通常将隐藏层维度缩小(如从4d变为8d/3)。"
  analogy: "激活函数就像神经元的开关——ReLU 是硬开关（<0 关 >0 开），GELU/SwiGLU 是带门控的旋钮，更平滑更精细。"
  key_points:
    - "引入门控机制:xV作为门控信号调制Swish(xW1)的输出"
    - "需要三个权重矩阵(W1, V, W2),参数量增加约50%"
    - "为保持总参数量不变,通常将隐藏层维度缩小(如从4d变为8d/3)"
first_principle:
  problem: "为什么需要 大模型中常用的激活函数有哪些?为什么LLaMA选择SwiGLU？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "GLU家族还有哪些变体(GEGLU, ReGLU等)?"
  - "SwiGLU中隐藏层维度如何设置?"
---

# 大模型中常用的激活函数有哪些?为什么LLaMA选择SwiGLU

- **主流激活函数演进:**

| 激活函数 | 公式 | 代表模型 |
|---------|------|----------|
| ReLU | max(0,x) | 早期模型 |
| GELU | x·Phi(x) | BERT, GPT-2 |
| SiLU/Swish | x·sigma(x) | 部分现代模型 |
| SwiGLU | Swish(xW1)·(xV) | LLaMA, Qwen |

- **SwiGLU详解:**
**传统FFN：** FFN(x) = ReLU(xW1)W2
**SwiGLU：** FFN(x) = (Swish(xW1) ⊙ xV)W2

- 引入门控机制:xV作为门控信号调制Swish(xW1)的输出
- 需要三个权重矩阵(W1, V, W2),参数量增加约50%
- 为保持总参数量不变,通常将隐藏层维度缩小(如从4d变为8d/3)

- **为什么选择SwiGLU:**
1. 效果好 - 多个基准测试上优于ReLU和GELU
2. 门控机制 - 允许模型动态选择信息通路,增强表达能力
3. 平滑梯度 - SiLU的平滑性有助于深层网络训练
