---
id: lln-050
difficulty: L2
category: llm-core
subcategory: 大模型架构
images:
- llmnotes_p062_img1.jpg
- llmnotes_p063_img1.jpg
- llmnotes_p063_img2.jpg
feynman:
  essence: 引入门控机制的平滑激活函数，提升非线性表达能力
  analogy: 像水龙头(门控)控制水流(Swish)，动态调节信息通过量
  first_principle: 如何设计激活函数以平衡计算成本与模型的非线性表达能力？
  key_points:
  - SwiGLU包含线性变换、门控和激活三部分
  - 参数量增加50%，需配合隐藏层维度缩小使用
  - 门控机制让模型具备动态选择信息的能力
follow_up:
- GLU家族还有哪些变体(GEGLU, ReGLU等)?
- SwiGLU中隐藏层维度如何设置?
---

# 大模型中常用的激活函数有哪些?为什么LLaMA选择SwiGLU

- **主流激活函数演进:**

| 激活函数 | 公式 | 代表模型 |
|---------|------|----------|
| ReLU | max(0,x) | 早期模型 |
| GELU | x·Phi(x) | BERT, GPT-2 |
| SiLU/Swish | x·sigma(x) | 部分现代模型 |
| SwiGLU | Swish(xW)⊙(xV) | LLaMA, Qwen |

- **SwiGLU详解:**
**传统FFN：** FFN(x) = ReLU(xW₁)W₂
**SwiGLU：** FFN(x) = (Swish(xW) ⊙ xV)W₂

- 引入门控机制: xV作为门控信号调制Swish(xW)的输出
- 需要三个权重矩阵(W, V, W₂),参数量增加约50%
- 为保持总参数量不变,通常将隐藏层维度缩小(如从4d变为${8d \over 3}$)

- **为什么选择SwiGLU:**
1. 效果好 - 多个基准测试上优于ReLU和GELU
2. 门控机制 - 允许模型动态选择信息通路,增强表达能力
3. 平滑梯度 - SiLU的平滑性有助于深层网络训练

- **架构细节补充:**
SwiGLU 本质上是一个带有门控的 FFN 变体。
在标准 Transformer 中，维度变化通常为：d_model -> 4*d_model -> d_model。
在 LLaMA 使用 SwiGLU 时，为了补偿增加的参数量（3个矩阵 vs 2个矩阵），隐藏层维度通常调整为 $d_{ff} = \frac{8}{3} d_{model}$ 或 $\frac{2}{3} \times 4d_{model}$，这样总参数量与使用 ReLU 的标准 FFN 持平。

```text
Standard FFN Block        LLaMA SwiGLU Block
┌──────────────┐          ┌───────────────────────────────┐
│   Input (d)  │          │         Input (d)              │
└──────┬───────┘          └───────────┬───────────────────┘
       │                              │
       ▼                              ▼
┌──────────────┐          ┌─────────────────────┐   ┌───────────────────┐
│  Linear (d→4d)│          │ Linear (d→8d/3) [W] │   │ Linear (d→8d/3) [V]│
└──────┬───────┘          └──────────┬──────────┘   └──────────┬──────────┘
       │                             │                         │
       ▼                             ▼                         ▼
┌──────────────┐               ┌──────────┐              ┌──────────┐
│    ReLU      │               │  Swish   │              │ Identity │
└──────┬───────┘               └─────┬────┘              └────┬─────┘
       │                             │                         │
       └──────────┬──────────────────┘                         │
                  │ (Element-wise Multiply / Gate)             │
                  ▼                                            │
            ┌─────────────────────┐                           │
            │ Element-wise Prod   │◄───────────────────────────┘
            └──────────┬──────────┘
                       │
                       ▼
            ┌─────────────────────┐
            │ Linear (8d/3→d) [W₂]│
            └──────────┬──────────┘
                       │
                       ▼
              ┌────────────────┐
              │ Output (d)     │
              └────────────────┘
```

## 常见考点
1. **参数量计算**：为什么 LLaMA 使用 SwiGLU 后要将隐藏层维度设为 $8d/3$？（为了抵消由 2 个矩阵变为 3 个矩阵带来的参数量增加，保持总计算量和参数量与标准 Transformer 相当）。
2. **GeGLU vs SwiGLU**：两者非常相似，区别仅在于门控的激活函数，GeGLU 使用 GELU，SwiGLU 使用 Swish (SiLU)，LLaMA 选 SwiGLU 主要是因为 Swish 的非单调性和平滑性在特定场景下收敛更好。
3. **上界性**：SiLU 是非单调且有下界的，这对于梯度传播有何影响？（非单调性允许负梯度流通，有助于解决“死神经元”问题）。
