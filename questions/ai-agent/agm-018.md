---
id: "agm-018"
difficulty: "L1"
category: "ai-agent"
subcategory: "记忆系统"
tags:
  - "IO"
  - "IOC"
images:
  - "svg_embedding_training.svg"
feynman:
  essence: "旧向量与新型不在同一空间,不可混比.做法:双写期(新旧模型并行写)、离线全量重"
  analogy: "Agent 记忆就像人的记忆——短期记忆是当前对话（工作记忆），长期记忆是过去经验（向量存储），摘要是对话的压缩笔记。"
  key_points:
    - "旧向量与新型不在同一空间,不可混比.做法:双写期(新旧模型并行写)、离线全量重"
    - "嵌入、检索时标明 embedding_model_version ,查询与库版本一致."
first_principle:
  problem: "为什么需要 Embedding 模型升级后旧向量怎么办？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "重 Embedding 全量成本高，能否只重 Embed「热点」记忆？"
  - "新旧向量混用会导致检索质量下降吗？"
---

# Embedding 模型升级后旧向量怎么办

旧向量与新型不在同一空间,不可混比.做法:双写期(新旧模型并行写)、离线全量重
嵌入、检索时标明 embedding_model_version ,查询与库版本一致.
