---
id: "agr-015"
difficulty: "L2"
category: "ai-agent"
subcategory: "RAG进阶"
tags:
  - "IOC"
feynman:
  essence: "- *Semantic Chunking** 是根据内容语义边界进行文档切分的技术. - *三种 Chunk 策略对比:** - *1. 固定长度切分(Fixe"
  analogy: "RAG 就像开卷考试——先翻书找到相关段落（检索），再结合理解写出答案（生成），不靠死记硬背（模型参数），知识可随时更新。"
  key_points:
    - "Semantic Chunking 是根据内容语义边界进行文档切分的技术."
    - "三种 Chunk 策略对比:"
    - "1. 固定长度切分(Fixed Size): 每 N 个 token 切一刀.简单但可能截断句子,破坏语义完整性."
first_principle:
  problem: "为什么需要 Semantic Chunking(语义切分)?它比固定长度切分好在哪里？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "语义切分的阈值如何设定?"
  - "如何处理代码块和表格等非文本内容?"
---

# Semantic Chunking(语义切分)是什么?它比固定长度切分好在哪里

- **Semantic Chunking** 是根据内容语义边界进行文档切分的技术.

- **三种 Chunk 策略对比:**

- **1. 固定长度切分(Fixed Size):** 每 N 个 token 切一刀.简单但可能截断句子,破坏语义完整性.

- **2. 递归切分(Recursive):** 按段落 -> 句子 -> 词的层级递归切分.比固定长度好,但仍然基于规则.

- **3. 语义切分(Semantic):** 计算相邻句子的语义相似度,在相似度骤降的位置切分.确保每个 Chunk 内语义连贯.

- **语义切分的实现:**
1. 将文档按句子分割
2. 计算相邻句子的 Embedding 相似度
3. 设置阈值,相似度低于阈值的位置即为切分点
4. 合并过短的 Chunk,拆分过长的 Chunk

- **效果:** 检索准确率提升 10-20%,因为每个 Chunk 语义完整,不会出现半个概念被截断的情况.

- **代价:** 需要额外的 Embedding 计算,预处理时间更长.
