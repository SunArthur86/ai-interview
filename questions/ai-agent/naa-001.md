---
id: "naa-001"
difficulty: "L3"
category: "ai-agent"
subcategory: "Agent稳定性"
tags:
  - "IO"
  - "索引"
feynman:
  essence: "- *Agent 工具调用失败常见原因** 1. API 超时/网络错误 2. 参数格式不匹配 3. 工具返回异常结果 4. 权限不足 5. 工具被限流 - *"
  analogy: "工具调用就像给 AI 配了瑞士军刀——根据任务需要灵活选择搜索、计算、代码执行等工具，LLM 决定何时用、怎么用。"
  key_points:
    - "Agent 工具调用失败常见原因"
    - "API 超时/网络错误"
    - "Level 1 - 重试:"
first_principle:
  problem: "为什么需要 Agent 工具调用失败后如何降级处理?设计一个容错策略.？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "如何设计工具调用的 schema 校验?"
  - "多 Agent 协作中一个 Agent 失败如何处理?"
---

# Agent 工具调用失败后如何降级处理?设计一个容错策略.

- **Agent 工具调用失败常见原因**
1. API 超时/网络错误
2. 参数格式不匹配
3. 工具返回异常结果
4. 权限不足
5. 工具被限流

- **多级降级策略**

- **Level 1 - 重试**:
- 指数退避重试(3次,间隔 1s/2s/4s)
- 仅对幂等操作重试

- **Level 2 - 参数修正**:
- 解析失败信息 → LLM 重新生成参数
- Schema 校验 → 自动补全缺失字段

- **Level 3 - 替代工具**:
- 注册 fallback 工具链(如搜索引擎A → 搜索引擎B → 内置知识库)
- 工具优先级排序

- **Level 4 - 优雅降级**:
- 跳过失败步骤,用已有信息继续
- 向用户透明地报告失败并请求手动输入

- **Level 5 - 人工兜底**:
- 关键操作失败 → 转人工审核
- 积累失败模式 → 离线优化

- **实现模式**
```python
class ToolExecutor:
async def execute(self, tool, params):
**try：** return await tool.run(params)
except TimeoutError:
for attempt in range(3):
await asyncio.sleep(2**attempt)
**try：** return await tool.run(params)
**except：** pass
return await self.fallback(tool, params)
```
