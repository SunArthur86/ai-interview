---
id: "agm-012"
difficulty: "L1"
category: "ai-agent"
subcategory: "记忆系统"
feynman:
  essence: "会漏掉仍然有效但表述不相似的硬约束;也会过度偏向「像」但错误的片段.需要 时间与重要 性补足. - *追问应对:**若问「三路召回怎么融合?」--答:RRF(倒"
  analogy: "Agent 记忆就像人的记忆——短期记忆是当前对话（工作记忆），长期记忆是过去经验（向量存储），摘要是对话的压缩笔记。"
  key_points:
    - "追问应对"
    - "会漏掉仍然有效但表述不相似的硬约束"
    - "也会过度偏向「像」但错误的片段.需要 时间与重要"
first_principle:
  problem: "为什么需要 只做强相关性检索会有什么问题？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "强相关但过时的信息会误导 Agent 吗？"
  - "如何引入「时效性」权重？"
---

# 只做强相关性检索会有什么问题

会漏掉仍然有效但表述不相似的硬约束;也会过度偏向「像」但错误的片段.需要 时间与重要
性补足.
- **追问应对:**若问「三路召回怎么融合?」--答:RRF(倒数排名融合)、或统一打分后 线性
加权 / Learning to Rank,线上 AB 调参.
