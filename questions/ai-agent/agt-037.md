---
id: agt-037
difficulty: L5
category: ai-agent
subcategory: 记忆系统
tags:
- Agent
feynman:
  essence: 通过向量检索实现跨会话的语义信息持久化与召回。
  analogy: 像图书馆，新书入库并打标签，想找相关内容时去搜索引。
  first_principle: 如何让 AI 跨越时间边界，记住并召回非即时的语义知识？
  key_points:
  - 基于向量数据库存储与语义检索。
  - 需结合元数据过滤（用户隔离等）。
  - 需引入重要性评分与时间衰减机制。
---

# 长期记忆

长期记忆用于跨会话、跨任务保留信息，典型实现是「向量数据库 + 元数据」。

### 核心原理
1.  **存储**：文本经 Embedding 模型转为向量，与 user_id、timestamp 等元数据一同存入数据库。
2.  **检索**：查询文本转向量 -> 计算 Top-K 相似度 -> 按元数据过滤 -> 注入 Prompt。
3.  **管理**：包括 CRUD 操作、重要性评分（加权保留）与衰减机制（时间遗忘）。

### 局限与应对
*   **局限**：语义相似不代表事实正确；难以精确匹配 ID/订单号；更新一致性难保障。
*   **应对**：混合检索（BM25+向量）、人工/模型校验、主键版本控制、冷热数据分离。

### 架构图：长期记忆 RAG 流程
```text
┌──────────────┐      ┌──────────────────────┐      ┌──────────────┐
│   User Query │ ──>  │  Retrieval Strategy  │ ──>  │ Vector DB    │
└──────────────┘      │  (Filter + Search)   │      │ (ANN Index)  │
                      └──────────────────────┘      └──────┬───────┘
                                                             │
                                                             ▼
┌──────────────┐      ┌──────────────────────┐      ┌──────────────┐
│   Context    │ <─── │  Reranking & Merge   │ <─── │  Top-K Docs  │
│ (Prompt +    │      │  (Score Normalization)│     │ + Metadata   │
│  Memories)   │      └──────────────────────┘      └──────────────┘
└──────┬───────┘
       │
       ▼
┌──────────────┐
│     LLM      │
└──────────────┘
```

### 数据流细节
*   **写入**：数据切分 -> Embedding -> 写入 Vector DB 同时写入 Metadata Store（如 SQL/Redis）。
*   **读取**：Vector Search 获取粗排结果 -> Metadata 过滤（如 `user_id=123` AND `date>2024`） -> 精排。

### 代码逻辑（示意）
```python
import time, math

class LongTermMemory:
    def search(self, user_id, query, top_k=5):
        q_vec = fake_embed(query)
        now = time.time()
        scores = []
        for item in self.items:
            # 1. 元数据过滤 (硬性条件)
            if item.user_id != user_id: continue
            
            # 2. 多因子打分
            # 相似度 + 重要性 + 时间衰减
            rel = cosine_sim(q_vec, item.vector)
            recency = math.exp(-0.1 * (now - item.last_accessed))
            score = 0.6 * rel + 0.2 * item.importance + 0.2 * recency
            scores.append((score, item))
        return sorted(scores, reverse=True)[:top_k]
```

### ## 常见考点
1.  **混合检索 是什么？**：结合关键词检索（BM25/ES）和向量检索。BM25 擅长精确匹配（如人名、专有名词），向量检索擅长语义匹配。两者结果通过倒排融合（RRF）或加权合并。
2.  **如何解决召回内容的准确幻觉？**：引入citation机制，让 LLM 在回答时标注引用来源；或者在 Prompt 中明确要求 "If the retrieved information is irrelevant, ignore it"。
3.  **向量数据库选型依据？**：考虑 QPS、延迟、数据量、是否支持过滤。Pinecone/Milvus 适合云原生大规模，Pgvector 适合中小规模及强事务一致性需求。
4.  **Embedding 模型的更新与迁移？**：当更换 Embedding 模型时，历史数据需要重新向量化，成本较高。通常采用双写策略或渐进式迁移。
