---
id: agf-004
difficulty: L1
category: ai-agent
subcategory: Agent核心框架
feynman:
  essence: 防止计划频繁变更导致的系统震荡。
  analogy: 频繁改方案会让团队无所适从，定好大方向微调细节才稳。
  first_principle: 如何在动态环境变化中保持计划执行的稳定性？
  key_points:
  - 过度重规划会导致执行轨迹不稳定
  - 应限制重规划频率并优先采用局部调整
  - 保留已验证事实以避免重复探索
---

# Re-planning 会不会导致「计划抖动」?怎么缓解

会。频繁重规划可能让执行轨迹不稳定。缓解方式包括：限制重规划次数、局部重规划优先、在 state 中保留已验证事实、对计划变更加一致性检查（新旧计划差异说明）。

伪代码示例：
```python
if needs_replan(state):
    plan = replanner(task, plan, state)
    if error or inconsistency:
        new_plan = LLM_replanner(original_task, old_plan, state, error_trace)
        continue with new_plan
```
