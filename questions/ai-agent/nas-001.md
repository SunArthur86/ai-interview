---
id: nas-001
difficulty: L2
category: ai-agent
subcategory: RAG进阶
tags:
- IO
- IOC
- 索引
feynman:
  essence: 通过严格的版本控制和更新策略，确保检索到的数据始终最新。
  analogy: 像整理书架，新书来了要移走旧版，否则可能借到过期的书。
  first_principle: 如何在数据频繁变动的情况下保证索引与数据源的一致性？
  key_points:
  - 为每个文档分配唯一 ID 并支持覆盖更新
  - 利用版本号过滤过时的向量数据
  - 建立自动化的增量更新管道
  - 生产环境推荐使用向量库的 Upsert 机制
follow_up:
- 如何检测当前索引中是否存在污染?
- 增量更新和全量重建的成本如何权衡?
---

# RAG 知识库更新后如何避免旧索引污染

- **索引污染**指知识库更新后,旧版本的向量仍残留在索引中,导致检索到过期或错误信息.

- **污染来源**
1. 文档更新后旧向量未删除
2. 文档删除后向量未清理
3. 版本冲突:新旧版本同时被检索
4. 元数据未同步更新

- **解决方案**
1. **文档级 ID 管理**:每个文档/切片分配唯一 ID,更新时先删旧 ID 再插入新 ID
2. **版本号标注**:向量存储时带 version 字段,检索时过滤旧版本
3. **增量更新管道**:监听文档变更 → diff 检测 → 自动更新索引
4. **TTL 过期机制**:给向量设过期时间,定期清理
5. **软删除 + 定期重建**:标记删除,低峰期全量重建索引
6. **双索引策略**:新旧索引并存,灰度切换后删除旧索引

- **工程实践**:使用 Milvus/Qdrant 的 upsert 语义(按主键覆盖),配合文档版本号管理.
