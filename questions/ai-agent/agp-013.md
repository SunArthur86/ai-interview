---
id: agp-013
difficulty: L1
category: ai-agent
subcategory: Prompt工程
feynman:
  essence: 边走边看 vs 谋定后动
  analogy: ReAct像“摸着石头过河”，Plan-and-Execute像“按攻略图通关”。
  first_principle: 如何处理不确定性环境下的任务执行
  key_points:
  - ReAct 适合动态环境
  - Plan-and-Execute 适合清晰任务
  - 复杂系统混合使用
follow_up:
- ReAct 死循环 vs Plan-Execute 计划僵化，哪个更致命？
- 能否混合两种模式？
---

# Agent 里 ReAct 和 Plan-and-Execute 怎么选

环境动态、需频繁反馈用 ReAct；任务步骤清晰、可一次规划用 Plan-and-Execute；复杂系统常混合。

**关键细节与原理：**
1. **ReAct (Reason + Act)**：
   - **流程**：Thought → Action → Observation（循环）。
   - **特点**：逐步探索，每一步都根据上一步的观察调整。适合无法预知下一步结果的场景（如网页浏览、数据库查询）。缺点是容易陷入循环或累积 Token 成本高。
2. **Plan-and-Execute (Plan & Solve)**：
   - **流程**：先一次性生成完整计划，然后按步骤执行。
   - **特点**：全局视野强，步骤之间并行度高。适合任务明确、逻辑链条固定的场景（如“写一篇包含A、B、C要素的文章”）。缺点是如果计划第一步错了，后续全盘皆输。
3. **混合模式**：先用 Plan-and-Solve 拆解大任务为子任务，每个子任务内部使用 ReAct 动态执行。

**架构流程对比图：**
```text
ReAct (Looping):           Plan-and-Execute (Sequential):
┌───────┐                  ┌───────┐
│ Query │                  │ Query │
└───┬───┘                  └───┬───┘
    │                          │
    ▼                          ▼
┌─────────┐              ┌──────────┐
│ Thought │              │ Planner  │──┐
└───┬─────┘              └──────────┘  │ Plan List
    │                                  │ [Step1, Step2...]
    ▼                                  ▼
┌─────────┐              ┌──────────┐ ┌──────────┐
│ Action  │              │Executor 1│ │Executor 2│ ...
└───┬─────┘              └────┬─────┘ └────┬─────┘
    │                          │           │
    ▼                          ▼           ▼
┌───────────┐              ┌─────────────────────┐
│ Observation│              │ Final Output Aggregation│
└─────┬─────┘              └─────────────────────┘
      │ (Loop back if needed)
      ▼
┌─────────┐
│ Thought │ ...
└─────────┘
```

**常见考点：**
1. 如何解决 ReAct 中的死循环问题？（设置最大步数限制、通过 Prompt 指导“何时停止”）
2. Plan-and-Execute 生成的计划如果执行失败，如何自我修正？（引入 ReAct 模块进行局部重试或触发 Re-planning）
3. 在多 Agent 系统中，哪种模式更适合协同？（通常 ReAct 适合互相讨论的动态协作）
