---
id: "ai-a-011"
difficulty: "L2"
category: "ai-agent"
subcategory: "工具使用"
tags:
  - "String"
  - "Integer"
  - "IO"
  - "Elasticsearch"
feynman:
  essence: "- *Function Calling流程:** 1. 定义工具schema(name/description/parameters) 2. LLM根据用户意图"
  analogy: "工具调用就像给 AI 配了瑞士军刀——根据任务需要灵活选择搜索、计算、代码执行等工具，LLM 决定何时用、怎么用。"
  key_points:
    - "Function Calling流程:"
    - "定义工具schema(name/description/parameters)"
    - "LLM根据用户意图选择工具并生成参数"
first_principle:
  problem: "剥离所有术语：Function Calling的工作原理?如何设计好的工具描述 底层在做什么？为什么这样做是最优的？"
  axioms:
    - "Agent = LLM + 规划 + 记忆 + 工具——LLM 是大脑，其余是手脚"
    - "ReAct 的本质是「思考-行动-观察」循环——显式推理比隐式更可靠"
    - "Agent 的上限不取决于 LLM 能力，而取决于工具集和记忆系统的设计"
  rebuild: "从 Agent 架构出发：① Agent 为什么比纯 LLM 强？② 规划/记忆/工具各自解决什么问题？③ 如何评估 Agent 效果？④ 理想的 Agent 架构是什么样？"
follow_up:
  - "工具太多时如何做工具选择?"
  - "并行Function Calling如何实现?"
---

# Function Calling的工作原理是什么?如何设计好的工具描述

- **Function Calling流程:**

1. 定义工具schema(name/description/parameters)
2. LLM根据用户意图选择工具并生成参数
3. 应用执行工具调用
4. 将结果返回给LLM
5. LLM基于结果生成最终回复

- **设计好的工具描述:**

```json
{
**"name"：** "search_docs",
**"description"：** "搜索内部文档库.当用户询问产品文档、API文档或技术规范时使用.不要用于搜索网页.",
**"parameters"：** {
**"query"：** {"type": "string", "description": "搜索关键词,英文优先"},
**"limit"：** {"type": "integer", "description": "返回结果数,默认5", "default": 5}
}
}
```

- **最佳实践:**
1. **描述明确** - 说明何时用、何时不用
2. **参数语义** - 每个参数描述清楚期望值
3. **工具粒度** - 不太粗(一个工具做太多)也不太细(太多小工具)
4. **错误提示** - 工具返回错误时给LLM可操作的信息
5. **少即是多** - 工具数量控制在5-15个为佳
