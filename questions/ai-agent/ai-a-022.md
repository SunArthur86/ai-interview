---
id: "ai-a-022"
difficulty: "L2"
category: "ai-agent"
subcategory: "规划与推理"
tags:
  - "反射"
  - "IO"
feynman:
  essence: "批评prompt要具体(不要说「改进」,要说「检查边界条件、错误处理、代码风格」)。"
  analogy: "Agent 反思就像做完题后的自我检查——回顾推理过程，发现错误就修正。"
  key_points:
    - "Self-Refine流程:"
    - "Reflection vs Self-Refine:"
    - "Self-Refine: 同一模型自我改进"
first_principle:
  problem: "为什么需要 Self-Refine和Reflection机制如何提升Agent质量?具体怎么实现？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Agent = LLM + 规划 + 记忆 + 工具——LLM 是大脑，其余是手脚"
    - "ReAct 的本质是「思考-行动-观察」循环——显式推理比隐式更可靠"
    - "Agent 的上限不取决于 LLM 能力，而取决于工具集和记忆系统的设计"
  rebuild: "从 Agent 架构出发：① Agent 为什么比纯 LLM 强？② 规划/记忆/工具各自解决什么问题？③ 如何评估 Agent 效果？④ 理想的 Agent 架构是什么样？"
follow_up:
  - "多少轮反思效果最优?"
  - "自我批评会不会产生相同偏见?"
---

# Self-Refine和Reflection机制如何提升Agent质量?具体怎么实现

- **Self-Refine流程:**

```
生成初稿 → 自我批评 → 修改 → 自我批评 → 修改 → ... → 最终版
```

- **实现:**
```python
# 1. 生成
output = llm(task)

# 2. 批评
feedback = llm(f"审查以下输出的问题:\n{output}")

# 3. 修改
output = llm(f"根据反馈改进:\n反馈:{feedback}\n原文:{output}")

# 重复2-3直到满意或达到最大次数
```

- **Reflection vs Self-Refine:**
- **Self-Refine:** 同一模型自我改进
- **Reflection:** 可以用不同模型/角色来批评

- **效果:**
- 代码生成:通过率提升10-20%
- 推理任务:准确率提升5-15%
- 写作任务:质量提升但增加2-3倍Token

- **关键:** 批评prompt要具体(不要说「改进」,要说「检查边界条件、错误处理、代码风格」)
