---
id: agt-043
difficulty: L4
category: ai-agent
subcategory: 多智能体
tags:
- ai-agent
feynman:
  essence: 通过分工协作提升复杂任务的稳定性、并行度与专业能力。
  analogy: 像公司，有产品、开发、测试各司其职，比一个人单干效率高。
  first_principle: 如何通过系统性的分工协作来解决单一模型在复杂任务中的能力瓶颈？
  key_points:
  - 解决长上下文中的注意力分散问题。
  - 实现子任务的并行处理。
  - 提供更细粒度的容错与权限控制。
---

# 为什么需要多智能体

多智能体系统指多个相对独立的 Agent 在协作协议下共同完成复杂任务。

### 为什么需要多智能体？
**单 Agent 的瓶颈**：
1.  **注意力漂移**：长提示词下，模型易忽略中间的约束。
2.  **推理链断裂**：复杂任务中，单次调用难以进行长程规划与回溯。
3.  **能力局限**：单一 System Prompt 难以同时兼顾严谨规划、创意发散与代码执行。

**多 Agent 的优势**：
1.  **专业化**：每个 Agent 专注于特定领域（如代码审查），Prompt 更短更稳。
2.  **并行化**：独立子任务可并行执行，缩短墙钟时间。
3.  **容错性**：某一 Agent 失败可隔离重试，不致全盘崩溃。

### 代码逻辑（示意）
```python
roles = [
    ("analyst", "你只输出需求要点。"),
    ("coder", "你只输出代码。"),
    ("reviewer", "你只审查代码安全。")
]
for name, hint in roles:
    agent = SimpleAgent(name, hint)
    result = agent.run(task)
```
