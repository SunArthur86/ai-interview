---
id: trd-005
difficulty: L2
category: ai-agent
subcategory: Tool Use
feynman:
  essence: 模型一次下单批量并发执行任务，独立执行后汇总结果。
  analogy: 像餐厅服务员一次性给后厨下五张不同桌子的菜单，厨师们同时做菜，做完后再按菜单编号准确端回给对应的桌子，而不是做完一道再下一道。
  first_principle: 如何让 LLM 将单步串行的决策过程转化为并发的执行策略。
  key_points:
  - 输出结构：模型一次性生成 JSON 数组
  - 执行方式：服务端并发执行（如 asyncio）
  - 独立性：并行调用间不可有参数依赖
  - 容错与聚合：需处理乱序结果和部分异常
---

# Function Calling 的并行调用是如何实现的？它在工程实现上有哪些挑战？

Function Calling 并行调用通过让模型一次输出包含多个 tool_calls 的 JSON 数组实现，服务端解析后并发执行这些函数。工程挑战包括：1. 参数依赖问题（需确保各调用参数独立，不可依赖前序结果）；2. 异常处理策略（需设计部分失败时的容错机制）；3. 结果聚合与重排序（需根据 ID 将乱序返回的结果映射回原始请求）；4. 超时控制（防止单个慢速请求阻塞整体流程）。
