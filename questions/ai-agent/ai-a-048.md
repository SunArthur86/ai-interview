---
id: ai-a-048
difficulty: L3
category: ai-agent
subcategory: Agent评估
tags:
- Elasticsearch
feynman:
  essence: 通过分级权限和审批机制平衡自动化与安全风险。
  analogy: 就像电脑系统的UAC弹窗，危险操作必须人工点头。
  first_principle: 如何在赋予Agent执行力的同时确保它不造成破坏？
  key_points:
  - 将操作分为L0-L4五个风险等级，越危险越需要人工介入
  - 实现suggest、auto、yolo三种模式适应不同场景
  - 代码中通过异常或审批流阻断未授权的高危操作
follow_up:
- 如何设计安全的yolo模式?
- Agent如何处理用户拒绝?
---

# Agent的安全权限如何设计?不同敏感级别的操作如何分级管理

- **操作安全分级:**

| 级别 | 操作类型 | 示例 | 控制策略 |
|------|---------|------|---------|
| L0 安全 | 只读操作 | 读取文件、搜索 | 自动执行 |
| L1 低风险 | 可逆写操作 | 写入临时文件 | 自动执行+日志 |
| L2 中风险 | 不可逆写操作 | 修改文件、git commit | **需确认** |
| L3 高风险 | 系统级操作 | 删除文件、安装包 | **需确认+审计** |
| L4 极高风险 | 外部影响 | 发送邮件、支付、部署 | **需双重确认** |

- **审批机制设计:**

```python
class ToolExecutor:
    def __init__(self, auto_approve_levels={0, 1}):
        self.auto_approve = auto_approve_levels

    async def execute(self, tool, params):
        level = tool.security_level
        if level in self.auto_approve:
            return await tool.run(params)
        else: 
            approved = await self.ask_user(
                f"工具 {tool.name} 需要审批\n"
                f"参数：{params}\n"
                f"风险级别：L{level}\n"
                f"是否允许?"
            )
            if approved:
                return await tool.run(params)
            return {"error": "用户拒绝执行"}
```

- **安全模式:**
- **suggest模式:** 所有写操作都需确认
- **auto模式:** L0-L2自动,L3+需确认
- **yolo模式:** 全部自动(仅限安全沙箱)

- **最佳实践:** 默认suggest模式,用户信任后逐步提升自动化级别

- **安全沙箱架构图:**
```text
┌───────────────────────────────────────────────┐
│                Host OS / Env                  │
│  ┌─────────────────────────────────────────┐  │
│  │        Agent Runtime (Python/Node)      │  │
│  │  ┌───────────────────────────────────┐  │  │
│  │  │    Permission / Policy Engine     │  │  │
│  │  │  (RBAC, Rate Limit, Input Filter) │  │  │
│  │  └───────────────┬───────────────────┘  │  │
│  │                  │                       │  │
│  │                  ▼                       │  │
│  │  ┌───────────────────────────────────┐  │  │
│  │  │      Tool Execution Layer         │  │  │
│  │  └───────────────┬───────────────────┘  │  │
│  └──────────────────┼───────────────────────┘  │
│                     │                          │
│   ┌─────────────────┼─────────────────┐       │
│   │  ▼               ▼                 ▼       │
│   │ Container      DB                API     │
│   │ (Docker/Firecracker)               │       │
└───┴─────────────────────────────────────────────┘
```

- **高级安全策略:**
   - **输入清洗**: 在参数传递给系统命令前，必须进行 Shell 转义，防止命令注入。
   - **资源配额**: 限制 CPU/RAM/网络使用，防止 Agent 死循环消耗资源。
   - **审计日志**: 所有敏感操作必须记录不可篡改的日志，包含操作人、时间、参数及结果。

## 常见考点
- **如何防止 Prompt Injection 导致绕过安全限制？** (答案: 使用 Post-Processing 验证工具调用，不盲目信任 LLM 输出的参数)
- **如何实现「最小权限原则」？** (答案: 动态生成临时凭证，仅授予完成任务所需的最小读写范围，任务即销毁)
- **Agent 误操作删除了系统文件怎么办？** (答案: 运行在容器或虚拟机中，通过 Snapshot 机制快速回滚；禁止直接操作宿主机文件系统)
