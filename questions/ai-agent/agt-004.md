---
id: agt-004
difficulty: L2
category: ai-agent
subcategory: Agent基础概念
tags:
- Agent
feynman:
  essence: 从目标到结果的闭环执行过程，包含感知、规划、执行与修正。
  analogy: 像员工接到任务后，先列计划，再逐项执行，中间核对进度，最后交报告。
  first_principle: 如何通过结构化流程保证 Agent 在复杂任务中的执行连贯性与可控性？
  key_points:
  - 流程：意图识别 → 任务拆解 → 工具调用 → 结果整合 → 输出。
  - 防止迷失：限制步数、维护状态清单、强制结构化输出。
  - 关键在于环境反馈后的动态调整。
---

# Agent 的工作流程

### Agent 的工作流程

可理解为：**听懂 → 拆活 → 动手 → 对账 → 交卷**。

#### 详细步骤
1. **输入处理**
   意图识别、指代消解、安全过滤、加载相关记忆与文档。

2. **任务分解**
   生成子任务列表或决策树；复杂任务可配合 Human-in-the-loop 确认里程碑。

3. **工具选择与调用**
   由模型或路由模块选择工具；执行器做鉴权、限流与结果规范化。

4. **结果整合**
   合并多源信息，解决冲突（例如时间更新的数据优先）。

5. **输出生成**
   面向用户的自然语言答案 + 可选引用与操作轨迹（便于审计）。

#### 常见问题：如何避免 Agent 迷失？
- **明确**停止条件与最大步数。
- **维护**任务清单与当前子目标。
- 对每步输出要求**结构化**（JSON Mode）。
- 关键步骤**强制验证**（单元测试式检查、二次 LLM 审核）。

#### 代码示例：任务清单状态
```json
{
  "goal": "生成上季度销售报告",
  "todos": [
    {"id": 1, "task": "拉取销售数据", "done": false},
    {"id": 2, "task": "计算同比", "done": false},
    {"id": 3, "task": "写成摘要", "done": false}
  ],
  "notes": []
}
```
