---
id: "agf-013"
difficulty: "L2"
category: "ai-agent"
subcategory: "Agent核心框架"
tags:
  - "IO"
  - "IOC"
  - "Elasticsearch"
feynman:
  essence: "ReAct = Reasoning + Acting，让模型交替输出「思考」和「行动」，行动调用工具后把 Observation 喂回模型继续推理，形成边想边做、用外部结果纠错的闭环。"
  analogy: "ReAct 框架就像「边想边做」的工作方式——先思考（Thought）→ 行动（Action）→ 观察（Observation）→ 再思考，循环往复直到完成。"
  key_points:
    - "让模型交替输出推理与可执行行动,并用工具返回的 Observation 闭环纠错,从而把推理接"
    - "thread = f\"Task: {task}\\n\""
    - "order = [\"researcher\", \"writer\", \"reviewer\"]"
first_principle:
  problem: "为什么需要 请用一句话解释 ReAct.？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 请用一句话解释 ReAct.

让模型交替输出推理与可执行行动,并用工具返回的 Observation 闭环纠错,从而把推理接
地到外部环境.
def run_crew(task: str, roles: dict, llm, rounds: int = 2) -> str:
thread = f"Task: {task}\n"
order = ["researcher", "writer", "reviewer"]
for _ in range(rounds):
for r in order:
prompt = roles[r] + "\n\n" + thread
reply = llm(prompt)
thread += f"\n[{r}]:\n{reply}\n"
return thread
python
