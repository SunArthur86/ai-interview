---
id: ai-a-003
difficulty: L3
category: ai-agent
subcategory: Agent架构
tags:
- GC
- Elasticsearch
feynman:
  essence: 将任务拆解为计划，再分步执行，而非边做边想。
  analogy: 装修房子，ReAct 是走一步看一步，Plan-and-Execute 是先画图纸再施工。
  first_principle: 如何将复杂任务结构化分解，以提高执行效率和稳定性？
  key_points:
  - 包含规划、执行和再规划三个阶段
  - 相比 ReAct 减少了单步推理的上下文消耗
  - 支持子任务并行执行，提高效率
  - 遇到错误更易通过局部重试恢复
follow_up:
- Replanner何时触发重新规划?
- 如何处理子任务之间的依赖?
---

# Plan-and-Execute Agent架构是什么?相比ReAct有什么优势

- **Plan-and-Execute:** 先全局规划,再逐步执行.

- **架构:**
```
用户任务 → Planner(全局规划) → 任务列表
↓
Executor(逐步执行) ← 逐个任务
↓
Replanner(重新规划) ← 执行结果
```

- **vs ReAct:**

| | ReAct | Plan-and-Execute |
|--|-------|-----------------|
| 规划 | 每步即时决策 | **先全局规划** |
| 上下文 | 完整历史 | **只需当前任务** |
| Token消耗 | 高(线性增长) | **低(分段)** |
| 适用 | 简单/探索性 | **复杂/多步骤** |
| 错误恢复 | 重新推理 | **重新规划** |

- **LangChain实现:**
```python
planner = ChatOpenAI().with_structured_output(Plan)
executor = AgentExecutor(agent=tool_agent)
replanner = ChatOpenAI().with_structured_output(Response)
```

- **优势:**
1. 减少Token消耗(每步只需子任务上下文)
2. 更适合复杂任务
3. 可以并行执行独立子任务
