---
id: "ai-a-018"
difficulty: "L2"
category: "ai-agent"
subcategory: "记忆系统"
tags:
  - "IO"
  - "GC"
  - "IOC"
  - "索引"
feynman:
  essence: "Agent 短期记忆（工作记忆）常见策略：① 滑动窗口（只保留最近 K 轮）；② 摘要压缩（把旧对话摘要成一段）；③ 实体记忆（提取关键实体单独存）；④ 状态变量（todo list、当前子目标）。上下文满了的应对：摘要旧上下文 + 检索长期记忆 + 丢弃低相关性内容，优先保留「当前任务 + 最近决策」。"
  analogy: "Agent 记忆就像人的记忆——短期记忆是当前对话（工作记忆），长期记忆是过去经验（向量存储），摘要是对话的压缩笔记。"
  key_points:
    - "上下文满了的处理策略:"
    - "滑动窗口 - 只保留最近K轮对话"
    - "摘要压缩 - 定期总结旧对话"
first_principle:
  problem: "为什么需要 Agent的短期记忆(工作记忆)如何设计?上下文窗口满了怎么办？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Agent = LLM + 规划 + 记忆 + 工具——LLM 是大脑，其余是手脚"
    - "ReAct 的本质是「思考-行动-观察」循环——显式推理比隐式更可靠"
    - "Agent 的上限不取决于 LLM 能力，而取决于工具集和记忆系统的设计"
  rebuild: "从 Agent 架构出发：① Agent 为什么比纯 LLM 强？② 规划/记忆/工具各自解决什么问题？③ 如何评估 Agent 效果？④ 理想的 Agent 架构是什么样？"
follow_up:
  - "摘要压缩的时机如何确定?"
  - "实体记忆如何提取实体?"
---

# Agent的短期记忆(工作记忆)如何设计?上下文窗口满了怎么办

- **Agent记忆层级:**

```
即时记忆 = 当前prompt上下文窗口(如128K tokens)
↓ 溢出
短期记忆 = 最近N轮对话摘要
↓ 归档
长期记忆 = 向量数据库中的历史交互
```

- **上下文满了的处理策略:**

1. **滑动窗口** - 只保留最近K轮对话
- 简单但丢失早期上下文

2. **摘要压缩** - 定期总结旧对话
- 每N轮将前N-1轮总结为一段摘要
- LangChain的ConversationSummaryMemory

3. **向量检索** - 将历史存入向量库
- 当前问题embedding检索相关历史
- 只注入相关的历史片段

4. **实体记忆** - 提取并记住关键实体
- 记住用户提到的人名/项目名/偏好
- LangChain的EntityMemory

5. **分层压缩**
- 最近3轮:完整保留
- 3-10轮前:摘要
- 10轮前:只存向量索引

- **最佳实践:** 滑动窗口 + 摘要压缩 + 向量检索的组合
