---
id: agt-044
difficulty: L5
category: ai-agent
subcategory: 多智能体
tags:
- ai-agent
feynman:
  essence: 定义 Agent 间的组织架构与交互流程，以适应不同类型的任务。
  analogy: 像乐团，有指挥家模式（中心化）、接力赛（流水线）或即兴 jam（民主）。
  first_principle: 如何设计组织结构以最优的方式协调多个智能体完成任务？
  key_points:
  - 中心化适合强控制与审计场景。
  - 流水线适合线性处理任务。
  - 民主讨论适合创意发散，需防无限循环。
---

# 三大协作模式

协作模式决定了 Agent 之间的控制流与信息流向，常见三种模式：中心化、流水线、民主讨论。

### 1. 中心化模式
*   **结构**：一个 Manager 负责任务分解、分配与汇总；多个 Worker 执行子任务。
*   **优点**：决策路径清晰，易于审计和权限控制。
*   **缺点**：Manager 是单点瓶颈，规划错误会全局放大。

### 2. 流水线模式
*   **结构**：Agent A -> B -> C，上游输出作为下游输入。
*   **优点**：适合固定 SOP（如数据处理），易于分段测试。
*   **缺点**：错误会逐级传递，难以处理大范围回溯。

### 3. 民主协作模式
*   **结构**：多个对等 Agent 多轮发言，可能由协调者维持流程。
*   **优点**：适合头脑风暴、多角度审稿。
*   **缺点**：易空转，需设置硬终止条件（最大轮数、Token 预算）。

### 代码逻辑（示意）
```python
class BossWorker:
    def run(self, task):
        subtasks = self.boss.plan(task)
        return {name: self.workers[name](st) for name, st in subtasks}

class Pipeline:
    def run(self, input_data):
        for stage in self.stages:
            input_data = stage(input_data)
        return input_data
```
