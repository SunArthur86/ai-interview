---
id: "ai-a-009"
difficulty: "L3"
category: "ai-agent"
subcategory: "Agent架构"
tags:
  - "IO"
  - "GC"
  - "IOC"
  - "Elasticsearch"
feynman:
  essence: "- *LangGraph:** 用有向图(StateGraph)编排Agent工作流. - *核心概念:** 1. **State** - 全局状态对象(Typ"
  analogy: "LangGraph 就像状态机工厂——用图（Graph）定义 Agent 的流转逻辑，节点是处理步骤，边是条件跳转，状态在节点间传递。"
  key_points:
    - "LangGraph: 用有向图(StateGraph)编排Agent工作流."
    - "State - 全局状态对象(TypedDict/Pydantic)"
    - "Node - 图节点,每个节点是一个函数/Agent"
first_principle:
  problem: "为什么需要 LangGraph的核心概念?它如何用图结构管理Agent状态？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Agent = LLM + 规划 + 记忆 + 工具——LLM 是大脑，其余是手脚"
    - "ReAct 的本质是「思考-行动-观察」循环——显式推理比隐式更可靠"
    - "Agent 的上限不取决于 LLM 能力，而取决于工具集和记忆系统的设计"
  rebuild: "从 Agent 架构出发：① Agent 为什么比纯 LLM 强？② 规划/记忆/工具各自解决什么问题？③ 如何评估 Agent 效果？④ 理想的 Agent 架构是什么样？"
follow_up:
  - "LangGraph的Checkpoint如何工作?"
  - "Conditional Edge和Router有什么区别?"
---

# LangGraph的核心概念是什么?它如何用图结构管理Agent状态

- **LangGraph:** 用有向图(StateGraph)编排Agent工作流.

- **核心概念:**

1. **State** - 全局状态对象(TypedDict/Pydantic)
2. **Node** - 图节点,每个节点是一个函数/Agent
3. **Edge** - 边,定义节点间转移
4. **Conditional Edge** - 根据状态动态选择下一节点

- **示例架构:**
```
START → research → draft → review
↑       ↓
revise ← critique
```

- **代码示例:**
```python
graph = StateGraph(AgentState)
graph.add_node("research", research_node)
graph.add_node("draft", draft_node)
graph.add_node("review", review_node)
graph.add_edge("research", "draft")
graph.add_conditional_edges("review", should_revise,
{True: "draft", False: END})
graph.add_edge(START, "research")
```

- **优势:**
1. **循环支持** - LangChain Chain不支持循环,LangGraph原生支持
2. **状态管理** - 全局状态自动传递
3. **可持久化** - Checkpoint支持中断/恢复
4. **人机协作** - Interrupt机制支持人工审批
