---
id: "agr-009"
difficulty: "L2"
category: "ai-agent"
subcategory: "工程化"
tags:
  - "GC"
feynman:
  essence: "- *RecursiveCharacterTextSplitter 核心思想:** 递归尝试分隔符列表,优先使用「更自然」的边界. - *默认分隔符顺序:**"
  analogy: "RAG 就像开卷考试——先翻书找到相关段落（检索），再结合理解写出答案（生成），不靠死记硬背（模型参数），知识可随时更新。"
  key_points:
    - "RecursiveCharacterTextSplitter 核心思想:"
    - "语义完整性 - 先按段落(\\n\\n)切分,保持语义完整"
    - "递归回退 - 段落太长 → 按句子(\\n)切 → 句子太长 → 按词( )切"
first_principle:
  problem: "为什么需要 LangChain的RecursiveCharacterTextSplitter的分隔符顺序为什么重要？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "chunk_overlap的作用是什么?"
  - "如何为代码文件设计分隔符?"
---

# LangChain的RecursiveCharacterTextSplitter的分隔符顺序为什么重要

- **RecursiveCharacterTextSplitter 核心思想:**

递归尝试分隔符列表,优先使用「更自然」的边界.

- **默认分隔符顺序:**
```
['\n\n', '\n', ' ', '']
```

- **为什么顺序重要:**

1. **语义完整性** - 先按段落(\n\n)切分,保持语义完整
2. **递归回退** - 段落太长 → 按句子(\n)切 → 句子太长 → 按词( )切
3. **避免硬截断** - 不会在一个句子/词中间切断

- **自定义分隔符示例:**
```python
splitter = RecursiveCharacterTextSplitter(
chunk_size=500,
chunk_overlap=50,
separators=[
'\n## ',      # Markdown H2
'\n### ',     # Markdown H3
'\n\n',       # 段落
'\n',         # 行
'. ',         # 句子
' ',          # 词
''            # 字符
]
)
```

- **为什么比Fixed-size好:** 固定长度切分会在句子中间断裂,Recursive优先在自然边界切分
