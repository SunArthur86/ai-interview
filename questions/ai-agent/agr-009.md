---
id: agr-009
difficulty: L2
category: ai-agent
subcategory: 工程化
tags:
- GC
feynman:
  essence: 按语义边界而非固定长度切分
  analogy: 像切蛋糕，按花纹切（递归）比按固定刀数切（固定长度）更好看。
  first_principle: 如何切碎文档而不破坏语义完整性
  key_points:
  - 优先使用自然分隔符
  - 递归回退机制
  - 保持句子语义完整
follow_up:
- chunk_overlap的作用是什么?
- 如何为代码文件设计分隔符?
---

# LangChain的RecursiveCharacterTextSplitter的分隔符顺序为什么重要

- **RecursiveCharacterTextSplitter 核心思想:**

递归尝试分隔符列表,优先使用「更自然」的边界.

- **默认分隔符顺序:**
```
['\n\n', '\n', ' ', '']
```

- **为什么顺序重要:**

1. **语义完整性** - 先按段落(\n\n)切分,保持语义完整
2. **递归回退** - 段落太长 → 按句子(\n)切 → 句子太长 → 按词( )切
3. **避免硬截断** - 不会在一个句子/词中间切断

- **自定义分隔符示例:**
```python
splitter = RecursiveCharacterTextSplitter(
chunk_size=500,
chunk_overlap=50,
separators=[
'\n## ',      # Markdown H2
'\n### ',     # Markdown H3
'\n\n',       # 段落
'\n',         # 行
'. ',         # 句子
' ',          # 词
''            # 字符
]
)
```

- **为什么比Fixed-size好:** 固定长度切分会在句子中间断裂,Recursive优先在自然边界切分
