---
id: agt-021
difficulty: L4
category: ai-agent
subcategory: RAG
tags:
- ai-agent
feynman:
  essence: 专门存和查“数字坐标”的数据库，能从海量数据里秒级找相似。
  analogy: 像图书馆的智能索引卡，不用读完每本书就能找到相关内容。
  first_principle: 如何在高维空间中，从海量向量中快速找到与查询最相似的 Top-K 结果？
  key_points:
  - ANN 算法牺牲少许精度换取百倍速度。
  - HNSW 适合低延迟，IVF+PQ 适合大规模省内存。
  - Milvus 等数据库解决 FAISS 缺乏的持久化和运维问题。
  - efSearch 参数越大召回越高但越慢。
  - 支持元数据过滤对生产环境至关重要。
---

# 向量数据库

### 向量数据库

**5.1 什么是向量数据库**
面向高维向量的存储与近似最近邻（ANN）查询系统，支持元数据过滤。

**5.2 主流选型**
- **FAISS**: 单机库，极致性能，需自建服务。
- **Milvus**: 云原生、分布式，适合大规模。
- **Pinecone**: 全托管 SaaS，零运维。
- **Qdrant/Weaviate**: 功能丰富，过滤能力强。

**面试 Q10：FAISS 和 Milvus 本质区别？**
A：FAISS 是 ANN 算法库，需自建存储、服务、容灾；Milvus 是向量数据库系统，提供分布式存储、运维能力与数据管理接口。

**5.3 ANN 算法**
- **HNSW**: 图索引，低延迟高召回，内存占用高。
- **IVF**: 聚类分桶，适合海量数据。
- **PQ**: 乘积量化，压缩向量省内存，有损精度。

### 代码示例：FAISS
```python
import faiss
import numpy as np

index = faiss.IndexFlatIP(dim)  # 内积索引
index.add(vectors)  # vectors 为 numpy array
D, I = index.search(query_vec, k=5)
```
