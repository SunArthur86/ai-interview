---
id: "agt-021"
difficulty: "L3"
category: "ai-agent"
subcategory: "Function Calling"
tags:
  - "String"
  - "注解"
  - "IO"
  - "GC"
  - "IOC"
  - "Elasticsearch"
feynman:
  essence: "LangChain 中 @tool 装饰器用 docstring + 类型注解自动生成 schema，简单直观适合简单工具；StructuredTool 显式构造（name/description/args_schema），控制力强适合复杂工具；选型：简单工具用 @tool，需要精细控制 schema、复用 Pydantic 模型或动态生成工具时用 StructuredTool。"
  analogy: "工具调用就像给 AI 配了瑞士军刀——根据任务需要灵活选择搜索、计算、代码执行等工具，LLM 决定何时用、怎么用。"
  key_points:
    - "1. @tool装饰器(快速简洁):"
    - "自动从docstring + 类型注解生成schema"
    - "适合: 原型开发、简单工具"
first_principle:
  problem: "它们本质上为什么不同？各自的设计目标和适用场景是什么？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "如何处理工具返回的大数据?"
  - "工具的return_direct是什么意思?"
---

# LangChain中@tool装饰器和StructuredTool有什么区别?如何选择

- **两种定义工具的方式:**

- **1. @tool装饰器(快速简洁):**
```python
from langchain.tools import tool

@tool
def search(query: str, limit: int = 5) -> str:
"""搜索文档库.

**Args：** 
**query：** 搜索关键词
**limit：** 返回数量
"""
return db.search(query, limit)
```
- 自动从docstring + 类型注解生成schema
- 简单快速
- **适合:** 原型开发、简单工具

- **2. StructuredTool(完全控制):**
```python
from langchain.tools import StructuredTool
from pydantic import BaseModel

class SearchInput(BaseModel):
**query：** str = Field(description="搜索关键词")
**limit：** int = Field(default=5, ge=1, le=100)

tool = StructuredTool.from_function(
func=search,
name="search_docs",
description="搜索内部文档库",
args_schema=SearchInput,
return_direct=False
)
```
- 完全控制名称、描述、参数schema
- 支持Pydantic验证
- **适合:** 生产环境、复杂工具

- **选择:**
- 快速原型 → @tool
- 生产环境 → StructuredTool
