---
id: agt-033
difficulty: L4
category: ai-agent
subcategory: 工具调用
tags:
- Agent
feynman:
  essence: 实现基础能力的函数，需兼顾功能与安全。
  analogy: 给模型配备瑞士军刀，但要锁住危险的刀刃。
  first_principle: 如何安全地将外部系统能力暴露给不可信的模型？
  key_points:
  - 数据库操作必须参数化防注入
  - 代码执行必须运行在沙箱中
  - 计算器严禁 eval，使用 AST 解析
  - 文件操作需限制访问路径
---

# 常见工具实现

### 常见工具实现

**1. 搜索工具**：封装 Search API，返回摘要与链接，避免大量 HTML 进上下文。

**2. 数据库工具**：
*   **核心**：永远参数化查询，禁止字符串拼接 SQL。
*   **权限**：只读账号 + 白名单表。

**3. 代码执行**：
*   **风险**：极高，可执行任意代码。
*   **防护**：必须在沙箱（Docker/WASM）中运行，限制网络与资源。

**4. 文件操作**：限制根目录（`chroot`），路径规范化，防止任意文件读写。

**5. 计算器**：禁止使用 `eval()`，应使用 AST 解析或安全数学库（如 `numexpr`）。

**代码示例（安全计算器）**
```python
import ast
import operator

_ALLOWED_OPS = {
    ast.Add: operator.add, ast.Sub: operator.sub,
    ast.Mult: operator.mul, ast.Div: operator.truediv,
}

def safe_eval(expr: str) -> float:
    node = ast.parse(expr, mode='eval').body
    if isinstance(node, ast.Constant): return node.value
    if isinstance(node, ast.BinOp) and type(node.op) in _ALLOWED_OPS:
        return _ALLOWED_OPS[type(node.op)](safe_eval(node.left), safe_eval(node.right))
    raise ValueError("Unsafe operation")
```
