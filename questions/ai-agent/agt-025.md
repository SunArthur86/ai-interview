---
id: agt-025
difficulty: L4
category: ai-agent
subcategory: RAG
tags:
- RAG
feynman:
  essence: 用模型考模型，量化检查 RAG 系统是“瞎编”还是“有据可依”。
  analogy: 像老师批改作业，不仅看答案对不对，还要看解题步骤有没有抄错。
  first_principle: 如何科学地量化 RAG 系统的性能，定位问题出现在检索还是生成环节？
  key_points:
  - 评估是 RAG 落地不可缺的一环。
  - 忠实度比答案正确性更重要，防止幻觉。
  - RAGAS 是主流的自动化评估框架。
  - 需构建覆盖各类场景的黄金数据集。
  - LLM 裁判本身可能有偏差，需人工抽检。
---

# RAG 评估

### RAG 评估

**9.1 核心指标**
- **忠实度**: 答案是否由上下文推出（不幻觉）。
- **上下文相关性**: 检索到的块是否相关。
- **答案正确性**: 答案是否正确。

**9.2 RAGAS 框架**
利用 LLM 作为裁判，自动计算上述指标。

**面试 Q15：为什么说「只看最终答案对错」不够？**
A：可能猜对或上下文不相关仍生成。需同时评检索质量与忠实度，定位瓶颈在检索还是生成。

### 代码示例：RAGAS 调用
```python
from ragas import evaluate
from datasets import Dataset

data = {
    "question": ["公司总部在哪？"],
    "answer": ["上海。"],
    "contexts": [["公司总部位于上海浦东新区……"]],
    "ground_truth": ["上海"],
}

ds = Dataset.from_dict(data)
result = evaluate(ds, metrics=["faithfulness", "answer_relevancy"])
```
