---
id: "agr-017"
difficulty: "L2"
category: "ai-agent"
subcategory: "RAG进阶"
feynman:
  essence: "- *Rerank** 是在初步检索后对候选结果进行精细排序的步骤. - *解决的问题:** - 向量检索(ANN)速度快但精度有限,top-k 中可能混入不相"
  analogy: "RAG 就像开卷考试——先翻书找到相关段落（检索），再结合理解写出答案（生成），不靠死记硬背（模型参数），知识可随时更新。"
  key_points:
    - "Rerank 是在初步检索后对候选结果进行精细排序的步骤."
    - "向量检索(ANN)速度快但精度有限,top-k 中可能混入不相关结果"
    - "向量相似度高不等于与查询真正相关"
first_principle:
  problem: "为什么需要 Rerank(重排序)在 RAG 中解决什么问题?如何选择重排序模型？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "Rerank 增加多少延迟?值得吗?"
  - "如何评估 Rerank 的效果?"
---

# Rerank(重排序)在 RAG 中解决什么问题?如何选择重排序模型

- **Rerank** 是在初步检索后对候选结果进行精细排序的步骤.

- **解决的问题:**
- 向量检索(ANN)速度快但精度有限,top-k 中可能混入不相关结果
- 向量相似度高不等于与查询真正相关

- **Rerank 工作原理:**
1. 向量检索召回 top-50 或 top-100 候选
2. Rerank 模型对每个候选与查询做 cross-encoder 计算(而非 bi-encoder)
3. Cross-encoder 能捕获查询和文档的深层交互关系
4. 输出精确的相关性分数,重排后取 top-5

- **为什么 Cross-encoder 更准:**
- Bi-encoder(向量检索):查询和文档独立编码,最后算余弦相似度.快但浅.
- Cross-encoder:查询和文档拼在一起送入模型,能捕获细粒度交互.慢但准.

- **选型:**
- Cohere Rerank v3:商用效果最好
- bge-reranker-large:开源首选
- jina-reranker:轻量级方案
