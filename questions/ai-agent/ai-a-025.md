---
id: "ai-a-025"
difficulty: "L2"
category: "ai-agent"
subcategory: "规划与推理"
feynman:
  essence: "Forward Planning（前向规划）从当前状态向目标推演，直观但组合爆炸；Backward Planning（后向规划）从目标倒推所需前提，聚焦相关操作、搜索空间小。Agent 选型：目标明确、操作多时用 Backward 减少分支；状态明确、路径探索时用 Forward；复杂任务常混合双向搜索 + 启发式。"
  analogy: "Plan-and-Execute 就像先做计划再执行的项目经理——先整体规划任务分解，再逐步执行，比 ReAct 的走一步看一步更有全局观。"
  key_points:
    - "Forward Planning(前向规划):"
    - "从当前状态出发,向目标推进"
    - "「我现在有什么 → 下一步做什么 → 最终达到目标」"
first_principle:
  problem: "它们本质上为什么不同？各自的设计目标和适用场景是什么？"
  axioms:
    - "Agent = LLM + 规划 + 记忆 + 工具——LLM 是大脑，其余是手脚"
    - "ReAct 的本质是「思考-行动-观察」循环——显式推理比隐式更可靠"
    - "Agent 的上限不取决于 LLM 能力，而取决于工具集和记忆系统的设计"
  rebuild: "从 Agent 架构出发：① Agent 为什么比纯 LLM 强？② 规划/记忆/工具各自解决什么问题？③ 如何评估 Agent 效果？④ 理想的 Agent 架构是什么样？"
follow_up:
  - "何时需要重新规划?"
  - "规划的粒度如何控制?"
---

# Agent如何做多步推理规划?Forward Planning vs Backward Planning的区别

- **Forward Planning(前向规划):**
- 从当前状态出发,向目标推进
- 「我现在有什么 → 下一步做什么 → 最终达到目标」
- 适合:步骤较少、目标明确的任务

- **Backward Planning(反向规划):**
- 从目标倒推需要的步骤
- 「要达到X → 需要先完成Y → 需要先完成Z」
- 适合:目标复杂但路径需要推理的任务

- **对比:**

| | Forward | Backward |
|--|---------|----------|
| 起点 | 当前状态 | 目标状态 |
| 方向 | 向前推 | 向后推 |
| 适用 | 简单线性 | **复杂多步** |
| 效率 | 快 | 需更多推理 |
| 示例 | 「写完代码→测试→部署」 | 「要部署→需要测试→需要代码」 |

- **混合策略:**
1. 先Backward:分解目标到子目标
2. 再Forward:按依赖顺序执行
3. 执行中动态调整(Replan)

- **LLM规划prompt设计:**
```
**目标：** {goal}
**当前状态：** {state}
**可用工具：** {tools}

**请制定详细计划：** 
1. 倒推实现目标需要哪些步骤
2. 标注每个步骤的依赖关系
3. 标注每步使用的工具
```
