---
id: "agr-001"
difficulty: "L2"
category: "ai-agent"
subcategory: "RAG技术"
tags:
  - "索引"
feynman:
  essence: "RAG 两步流水线：离线阶段做 解析→清洗→分块→嵌入→建索引（把知识库预处理成可检索的向量）；在线阶段做 Query（可选改写）→检索→（可选重排）→拼 Prompt→生成；增量更新通过文档哈希/时间戳检测变更，只重建变更部分。"
  analogy: "RAG 就像开卷考试——先翻书找到相关段落（检索），再结合理解写出答案（生成），不靠死记硬背（模型参数），知识可随时更新。"
  key_points:
    - "**离线：** 解析→清洗→分块→嵌入→建索引"
    - "在线:Query(可选改写)→检索→(可选重排)"
    - "→拼 Prompt→生成.追问:增量更新怎么做?(见 10.3)"
first_principle:
  problem: "为什么需要 RAG 两步流水线(离线与在线).？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "什么是覆盖索引？有什么优势？"
  - "索引失效的场景有哪些？"
---

# RAG 两步流水线(离线与在线).

**离线：** 解析→清洗→分块→嵌入→建索引;在线:Query(可选改写)→检索→(可选重排)
→拼 Prompt→生成.追问:增量更新怎么做?(见 10.3)
