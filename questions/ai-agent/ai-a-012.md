---
id: "ai-a-012"
difficulty: "L2"
category: "ai-agent"
subcategory: "工具使用"
tags:
  - "IOC"
feynman:
  essence: "当工具多达几十上百个时，全部塞进 Prompt 会导致选择困难和幻觉；解法是分层策略：① 工具检索（Tool RAG）先按 query 检索 Top-K 相关工具；② 工具聚类/分类先选大类再选具体工具；③ 两阶段路由（小模型粗筛 + 大模型精选）；④ 动态加载与 few-shot。"
  analogy: "工具调用就像给 AI 配了瑞士军刀——根据任务需要灵活选择搜索、计算、代码执行等工具，LLM 决定何时用、怎么用。"
  key_points:
    - "工具过载问题: LLM上下文有限,工具太多导致选择困难和幻觉."
    - "解决方案(分层策略):"
    - "工具检索(Tool RAG)"
first_principle:
  problem: "为什么需要 当Agent有几十上百个工具时,如何高效选择正确的工具？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Agent = LLM + 规划 + 记忆 + 工具——LLM 是大脑，其余是手脚"
    - "ReAct 的本质是「思考-行动-观察」循环——显式推理比隐式更可靠"
    - "Agent 的上限不取决于 LLM 能力，而取决于工具集和记忆系统的设计"
  rebuild: "从 Agent 架构出发：① Agent 为什么比纯 LLM 强？② 规划/记忆/工具各自解决什么问题？③ 如何评估 Agent 效果？④ 理想的 Agent 架构是什么样？"
follow_up:
  - "工具embedding用什么模型?"
  - "如何评估工具选择的准确性?"
---

# 当Agent有几十上百个工具时,如何高效选择正确的工具

- **工具过载问题:** LLM上下文有限,工具太多导致选择困难和幻觉.

- **解决方案(分层策略):**

1. **工具检索(Tool RAG)**
- 将工具描述做embedding
- 根据用户意图检索Top-K相关工具
- 只将相关工具传给LLM

2. **工具分类路由**
- 先用LLM/分类器选择工具类别
- 再在类别内选择具体工具

3. **层级工具**
- 粗粒度工具作为入口
- 调用后动态暴露细粒度工具

4. **工具使用历史**
- 记录哪些工具组合在什么场景下被使用
- 用历史数据辅助选择

5. **MCP动态发现**
- MCP Server按需暴露工具
- Agent先列出可用工具,再选择

- **实践建议:**
- <15个工具:直接放系统prompt
- 15-50个工具:Tool RAG
- 50+个工具:分类路由 + Tool RAG
