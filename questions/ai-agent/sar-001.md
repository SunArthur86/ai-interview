---
id: "sar-001"
difficulty: "L2"
category: "ai-agent"
feynman:
  essence: "🎯 本质：GraphRAG是微软提出的RAG增强方案，通过构建知识图谱来捕获文档间的实体关系，解决传统RAG只能回答局部问题、无法做全局推理的局限。"
  analogy: "RAG 就像开卷考试——先翻书找到相关段落（检索），再结合理解写出答案（生成），不靠死记硬背（模型参数），知识可随时更新。"
  key_points:
    - "🎯 本质：GraphRAG是微软提出的RAG增强方案，通过构建知识图谱来捕获文档间的实体关系，解决传统RAG只能回答局部问题、无法做全局推理的局限"
    - "🧒 类比：传统RAG像在一堆散落的文件里按关键词搜索（找到相关文件但看不到文件之间的联系）"
    - "GraphRAG像先画了一张关系图（谁认识谁、谁在哪工作），然后基于关系图回答问题"
first_principle:
  problem: "它们本质上为什么不同？各自的设计目标和适用场景是什么？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# GraphRAG是什么？它和传统RAG有什么区别？

🎯 本质：GraphRAG是微软提出的RAG增强方案，通过构建知识图谱来捕获文档间的实体关系，解决传统RAG只能回答局部问题、无法做全局推理的局限。

🧒 类比：传统RAG像在一堆散落的文件里按关键词搜索（找到相关文件但看不到文件之间的联系）；GraphRAG像先画了一张关系图（谁认识谁、谁在哪工作），然后基于关系图回答问题。

📊 GraphRAG vs 传统RAG：
| 特性 | 传统RAG(Naive) | GraphRAG |
|------|---------------|----------|
| 数据结构 | 向量索引 | 知识图谱+向量 |
| 擅长 | 具体事实查询 | 全局/关系推理 |
| 示例 | "X的定义是什么" | "整个文档集的主要主题" |
| 构建 | 快(切分+向量化) | 慢(LLM提取实体关系) |
| 更新 | 增量容易 | 需重建社区 |

GraphRAG工作流程：
1. Indexing阶段
  - 用LLM从文档中提取实体和关系
  - 构建知识图谱（节点=实体，边=关系）
  - 用Leiden算法进行社区检测（将图分为主题社区）
  - 为每个社区生成摘要（LLM总结）

2. Query阶段
  - 全局查询：从社区摘要中汇总答案
  - 局部查询：从相关实体/关系出发回答
  - 混合查询：结合两者

优势：能回答跨文档的全局性问题、能做多跳推理、答案更有结构
劣势：索引构建慢且贵（大量LLM调用）、增量更新困难、存储成本高

适用场景：法律文档分析、学术论文综述、企业知识库全局分析
