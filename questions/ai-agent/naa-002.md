---
id: naa-002
difficulty: L2
category: ai-agent
subcategory: Agent安全
feynman:
  essence: 通过清晰定义、校验和对齐训练抑制LLM的工具幻觉。
  analogy: 给工具贴上详细标签，过安检时还要核对身份证。
  first_principle: 如何防止模型在不确定时“脑补”出错误的工具指令？
  key_points:
  - 幻觉源于描述模糊、训练偏差和上下文混淆
  - 用Schema校验和白名单拦截明显的错误调用
  - 通过Few-shot和SFT教模型什么时候不该用工具
follow_up:
- 如何评估 Agent 工具调用的准确率?
- Function Calling 和 ReAct 哪个幻觉率更低?
---

# Agent 产生幻觉式工具调用(hallucinated tool calls)的原因是什么?如何减少

- **幻觉式工具调用**指 LLM 调用不存在的工具、传递错误参数、或在不该调用工具时调用.

- **常见原因**
1. **工具描述模糊**:LLM 不理解何时使用、参数含义不清
2. **过度调用倾向**:模型训练时见过大量工具调用,倾向'用工具'而非'直接回答'
3. **上下文混淆**:对话历史中提到工具名,被误认为调用指令
4. **幻觉函数名**:编造不存在的 API 名称
5. **参数类型错误**:传字符串给期望数字的参数

- **减少策略**

1. **工具描述工程**:
   - 用 JSON Schema 严格定义参数类型和约束
   - 添加 'when to use' 和 'when NOT to use' 说明
   - 提供调用示例

2. **调用前校验**:
   - 函数名白名单检查
   - 参数 schema 校验(类型、范围、必填)
   - 调用频率限制

3. **Few-shot 引导**:
   - 在 system prompt 中展示正确和错误的调用模式
   - 包含'不需要工具'的示例

4. **置信度过滤**:
   - 检查 tool_call 的 logprob
   - 低置信度时不执行,要求重新推理

5. **微调对齐**:
   - 用真实工具调用数据做 SFT
   - 负样本训练:错误调用 + 惩罚
