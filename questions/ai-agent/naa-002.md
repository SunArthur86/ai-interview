---
id: naa-002
difficulty: L2
category: ai-agent
subcategory: Agent安全
feynman:
  essence: 通过清晰定义、校验和对齐训练抑制LLM的工具幻觉。
  analogy: 给工具贴上详细标签，过安检时还要核对身份证。
  first_principle: 如何防止模型在不确定时“脑补”出错误的工具指令？
  key_points:
  - 幻觉源于描述模糊、训练偏差和上下文混淆
  - 用Schema校验和白名单拦截明显的错误调用
  - 通过Few-shot和SFT教模型什么时候不该用工具
follow_up:
- 如何评估 Agent 工具调用的准确率?
- Function Calling 和 ReAct 哪个幻觉率更低?
---

# Agent 产生幻觉式工具调用(hallucinated tool calls)的原因是什么?如何减少

- **幻觉式工具调用**指 LLM 调用不存在的工具、传递错误参数、或在不该调用工具时调用.

- **常见原因**
1. **工具描述模糊**:LLM 不理解何时使用、参数含义不清
2. **过度调用倾向**:模型训练时见过大量工具调用,倾向'用工具'而非'直接回答'
3. **上下文混淆**:对话历史中提到工具名,被误认为调用指令
4. **幻觉函数名**:编造不存在的 API 名称
5. **参数类型错误**:传字符串给期望数字的参数

- **减少策略**

1. **工具描述工程**:
   - 用 JSON Schema 严格定义参数类型和约束
   - 添加 'when to use' 和 'when NOT to use' 说明
   - 提供调用示例

2. **调用前校验**:
   - 函数名白名单检查
   - 参数 schema 校验(类型、范围、必填)
   - 调用频率限制

3. **Few-shot 引导**:
   - 在 system prompt 中展示正确和错误的调用模式
   - 包含'不需要工具'的示例

4. **置信度过滤**:
   - 检查 tool_call 的 logprob
   - 低置信度时不执行,要求重新推理

5. **微调对齐**:
   - 用真实工具调用数据做 SFT
   - 负样本训练:错误调用 + 惩罚

- **详细执行流程图**
```text
┌───────────────────────────────────────┐
│            LLM Generation             │
└───────────────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │  1. Syntax Validation │
        │  (Valid JSON?)        │
        └───────────┬───────────┘
                    │ No
                    ▼
            ┌───────────────┐
            │  Reject/Reask │
            └───────────────┘
                    │ Yes
                    ▼
        ┌───────────────────────┐
        │  2. Semantic Check    │
        │  - Name Whitelist      │
        │  - Arg Schema          │
        │  - Logic Constraints   │
        └───────────┬───────────┘
                    │ Fail
                    ▼
            ┌───────────────────┐
            │ Auto-fix / Error  │
            │ Feedback to LLM   │
            └───────────────────┘
                    │ Pass
                    ▼
        ┌───────────────────────┐
        │  3. Confidence Filter │
        │  (Logprobs > Thresh)  │
        └───────────┬───────────┘
                    │ Low
                    ▼
            ┌───────────────────┐
            │  Manual Review    │
            │  or Skip          │
            └───────────────────┘
                    │ High
                    ▼
            ┌───────────────┐
            │   Execute     │
            └───────────────┘
```

- **## 常见考点**
  1. **Forced Tool Calling**：如果业务强制要求 Agent 必须调用某个工具（如意图识别后），如何防止 LLM 产生幻觉调用非该组的工具？（在 Prompt 中严格限定作用域，或使用 fine-tuned 的小模型做强制路由）
  2. **Schema 复杂度**：当工具参数非常复杂（如几十个字段的嵌套对象）时，LLM 容易出错，如何解决？（使用 `pydantic` 等库将复杂结构拆解为多步生成，或描述中简化结构）
  3. **ReAct 模式下的循环**：如果 LLM 幻觉了一个不存在的工具并尝试调用，系统报错后，LLM 可能会陷入“尝试-报错-再尝试同个错误”的死循环，如何打破？（在 Error Message 中明确告知工具不存在，并强制要求停止或重选）
  4. **Logprob 阈值设定**：置信度的阈值如何确定？（需根据特定模型在验证集上的分布曲线进行统计设定，通常设在对数概率 -0.1 或 -0.2 左右）
