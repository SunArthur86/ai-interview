---
id: "ai-a-024"
difficulty: "L2"
category: "ai-agent"
subcategory: "规划与推理"
tags:
  - "IO"
  - "IOC"
feynman:
  essence: "选择(Selection) - 用UCB公式选择最有前景的节点。"
  analogy: "LATS 就像下棋时的多步推演——不只看眼前一步（ReAct），而是展开多条路径探索，选最优分支，探索能力强但计算成本高。"
  key_points:
    - "MCTS + LLM = 有方向的试错推理"
    - "选择(Selection) - 用UCB公式选择最有前景的节点"
    - "扩展(Expansion) - 扩展一个新子节点"
first_principle:
  problem: "为什么需要 MCTS(蒙特卡洛树搜索)如何与LLM结合用于Agent推理？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Agent = LLM + 规划 + 记忆 + 工具——LLM 是大脑，其余是手脚"
    - "ReAct 的本质是「思考-行动-观察」循环——显式推理比隐式更可靠"
    - "Agent 的上限不取决于 LLM 能力，而取决于工具集和记忆系统的设计"
  rebuild: "从 Agent 架构出发：① Agent 为什么比纯 LLM 强？② 规划/记忆/工具各自解决什么问题？③ 如何评估 Agent 效果？④ 理想的 Agent 架构是什么样？"
follow_up:
  - "UCB公式如何平衡探索和利用?"
  - "MCTS的搜索次数如何确定?"
---

# MCTS(蒙特卡洛树搜索)如何与LLM结合用于Agent推理

- **MCTS + LLM = 有方向的试错推理**

- **MCTS四步骤:**
1. **选择(Selection)** - 用UCB公式选择最有前景的节点
2. **扩展(Expansion)** - 扩展一个新子节点
3. **模拟(Simulation)** - 用LLM快速模拟到终点
4. **回传(Backpropagation)** - 更新路径上的胜率

- **LLM在MCTS中的角色:**

| 角色 | 作用 |
|------|------|
| 策略网络 | 生成候选动作(扩展) |
| 价值网络 | 评估节点前景(模拟) |
| 快速模拟 | Rollout到终局 |

- **优势:**
- 结合探索(exploration)和利用(exploitation)
- 可以发现LLM直接推理找不到的非直觉路径
- 适合数学竞赛、程序合成等需要搜索的任务

- **代表工作:** rStar-Math、AlphaCodium、ToT

- **代价:** 计算量大(需要大量LLM调用),适合离线推理
