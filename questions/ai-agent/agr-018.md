---
id: "agr-018"
difficulty: "L2"
category: "ai-agent"
subcategory: "RAG进阶"
tags:
  - "IO"
  - "Elasticsearch"
feynman:
  essence: "- *Query Rewrite** 是在检索前对用户查询进行改写增强的技术. - *常见策略:** - *1. 查询扩展(Query Expansion):*"
  analogy: "Transformer 就像高效的读书小组——每个人（注意力头）同时读不同段落，然后交流关键信息，不像 RNN 逐字读。"
  key_points:
    - "Query Rewrite 是在检索前对用户查询进行改写增强的技术."
    - "1. 查询扩展(Query Expansion): 将一个查询扩展为多个相关查询.如 Transformer 原理 -> Transformer 自注意力机制 + Transformer 位置编码 + Transformer 训练方法."
    - "2. 查询分解(Query Decomposition): 将复杂问题拆分为子问题.如比较 A 和 B 的优劣 -> A 的优势是什么 + B 的优势是什么 + 两者对比."
first_principle:
  problem: "为什么需要 Query Rewrite(查询重写)有哪些策略?什么场景需要用？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "多查询扩展的结果如何合并?"
  - "Query Rewrite 会引入噪声吗?"
---

# Query Rewrite(查询重写)有哪些策略?什么场景需要用

- **Query Rewrite** 是在检索前对用户查询进行改写增强的技术.

- **常见策略:**

- **1. 查询扩展(Query Expansion):** 将一个查询扩展为多个相关查询.如 Transformer 原理 -> Transformer 自注意力机制 + Transformer 位置编码 + Transformer 训练方法.

- **2. 查询分解(Query Decomposition):** 将复杂问题拆分为子问题.如比较 A 和 B 的优劣 -> A 的优势是什么 + B 的优势是什么 + 两者对比.

- **3. 回指消解(Coreference Resolution):** 多轮对话中的代词消解.如它 -> 指代上一轮提到的具体名词.

- **4. HyDE:** 生成假设文档作为查询.

- **适用场景:**
- 用户查询模糊或过短
- 多轮对话场景
- 复杂推理类问题

- **代价:** 每次检索多一次 LLM 调用,增加延迟.需要在效果和延迟间权衡.
