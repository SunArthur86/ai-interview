---
id: "ai-a-002"
difficulty: "L2"
category: "ai-agent"
subcategory: "Agent架构"
tags:
  - "IO"
feynman:
  essence: "- *ReAct = Reasoning + Acting** 让LLM交替进行推理(Thought)和行动(Action),使用工具后观察结果(Observa"
  analogy: "ReAct 框架就像「边想边做」的工作方式——先思考（Thought）→ 行动（Action）→ 观察（Observation）→ 再思考，循环往复直到完成。"
  key_points:
    - "ReAct = Reasoning + Acting"
    - "减少幻觉 - 基于工具返回的真实数据"
    - "可追溯 - 推理链可审查"
first_principle:
  problem: "为什么需要 ReAct框架的核心思想?Thought-Action-Observation循环如何工作？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Agent = LLM + 规划 + 记忆 + 工具——LLM 是大脑，其余是手脚"
    - "ReAct 的本质是「思考-行动-观察」循环——显式推理比隐式更可靠"
    - "Agent 的上限不取决于 LLM 能力，而取决于工具集和记忆系统的设计"
  rebuild: "从 Agent 架构出发：① Agent 为什么比纯 LLM 强？② 规划/记忆/工具各自解决什么问题？③ 如何评估 Agent 效果？④ 理想的 Agent 架构是什么样？"
follow_up:
  - "ReAct和Function Calling有什么区别?"
  - "工具调用失败时如何处理?"
---

# ReAct框架的核心思想是什么?Thought-Action-Observation循环如何工作

- **ReAct = Reasoning + Acting**

让LLM交替进行推理(Thought)和行动(Action),使用工具后观察结果(Observation).

- **工作流程:**
```
**用户：** 北京明天天气如何?

**Thought：** 我需要查询北京的天气预报
**Action：** weather_api(city="北京", date="tomorrow")
**Observation：** 晴, 25°C, 东南风3级

**Thought：** 我拿到了天气信息,可以回答了
**Action：** finish(answer="北京明天晴,25°C,东南风3级")
```

- **优势:**
1. **减少幻觉** - 基于工具返回的真实数据
2. **可追溯** - 推理链可审查
3. **可纠错** - 工具失败后可重试或换方案

- **局限:**
- Token消耗大(每次都带完整推理历史)
- 延迟高(多轮工具调用)
- 需要精心设计工具描述
