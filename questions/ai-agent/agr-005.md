---
id: "agr-005"
difficulty: "L2"
category: "ai-agent"
subcategory: "向量检索"
tags:
  - "IO"
  - "索引"
feynman:
  essence: "FAISS 中 IndexFlatIP 是暴力内积搜索（精确、无参数、慢、O(n)），IndexHNSW 基于分层小世界图近似搜索（快、有精度损失、需建图、可控参）；数据量小（<10万）或要求精确用 FlatIP，数据量大且接受少量精度损失用 HNSW（M 控制连接数，越大越精确越耗内存）。"
  analogy: "数据库索引就像书的目录——没目录要逐页翻（全表扫描），有目录直接跳到对应页，但目录也占空间。"
  key_points:
    - "FAISS索引类型对比:"
    - "IndexHNSW关键参数:"
    - "M:每个节点的连接数(16-48,越大越精确越耗内存)"
first_principle:
  problem: "它们本质上为什么不同？各自的设计目标和适用场景是什么？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "IVF-PQ的精度如何?"
  - "FAISS如何做GPU加速?"
---

# FAISS中IndexFlatIP和IndexHNSW有什么区别?如何选择

- **FAISS索引类型对比:**

| | IndexFlatIP | IndexHNSW |
|--|------------|-----------|
| 原理 | 暴力搜索(计算所有点) | 图结构近似搜索 |
| 精度 | **100%精确** | 近似(95%+ recall) |
| 速度 | 慢(O(n)) | **快(O(log n))** |
| 内存 | 低(只存向量) | 高(存图结构) |
| 构建时间 | 无需构建 | 需要构建 |
| 适合规模 | **<10万** | **>10万** |

- **选择建议:**
```
数据量 < 10万 → IndexFlatIP(暴力搜索,精度最高)
数据量 10万-100万 → IndexHNSW(图搜索,速度快)
数据量 > 100万 → IVF-PQ(聚类+量化,内存友好)
```

- **IndexHNSW关键参数:**
- **M**:每个节点的连接数(16-48,越大越精确越耗内存)
- **efConstruction**:构建搜索宽度(200-500)
- **efSearch**:查询搜索宽度(50-200,运行时可调)
- efSearch调大 → 精度提升但速度下降
