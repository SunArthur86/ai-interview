---
id: agt-034
difficulty: L4
category: ai-agent
subcategory: 工具调用
tags:
- ai-agent
feynman:
  essence: 工具调用的系统设计模式与风险控制。
  analogy: 就像组装一个复杂的自动化工厂，既要考虑流水线（编排），又要考虑安全阀（安全）。
  first_principle: 如何构建一个健壮、可扩展且安全的工具调用系统？
  key_points:
  - FC 是模型与代码交互的标准协议
  - 路由与编排解决规模化问题
  - 安全是工具调用的生命线
  - MCP 是提升复用性的未来趋势
---

# 综合面试题精选（≥15 题）

### 综合面试题精选

1.  **Q：Function Calling 和纯 JSON 输出区别？**
    **A**：FC 是原生通道，自动绑定对话 ID 和工具定义，解析更稳、支持多轮对话；JSON 依赖 Prompt，易混入闲聊。

2.  **Q：`tool_calls` 和 `tool` 消息怎么对应？**
    **A**：`assistant` 消息里的 `tool_calls[i].id` 对应后续 `role=tool` 消息里的 `tool_call_id`，实现多调用结果的匹配。

3.  **Q：MCP 解决什么痛点？**
    **A**：工具集成碎片化、重复建设。MCP 提供标准协议，让工具即插即用。

4.  **Q：工具路由何时必须上？**
    **A**：工具过多导致上下文膨胀、误选率上升或成本增加时（通常 >50 个工具）。

5.  **Q：并行调用要注意什么？**
    **A**：幂等性、后端并发限制、数据竞争、部分失败重试策略。

6.  **Q：审计日志要记什么？**
    **A**：时间、TraceID、用户、工具名、参数摘要（脱敏）、结果状态、耗时、模型版本。

7.  **Q：为什么 Calculator 禁止 `eval`？**
    **A**：`eval` 可执行任意 Python 代码，等同于远程代码执行（RCE）漏洞，必须使用 AST 解析等安全方式。
