---
id: "naa-005"
difficulty: "L2"
category: "ai-agent"
subcategory: "Agent架构"
tags:
  - "IO"
feynman:
  essence: "LATS（Language Agent Tree Search）把 MCTS 引入 Agent：每一步不再是单条 Thought-Action，而是展开一棵搜索树，用 LLM 自评估做价值估计、UCB 选择扩展节点、回溯更新价值，从而系统性探索多条路径；比 ReAct 的线性「走错就错」强在能探索多个方案并择优，代价是 token 消耗大。"
  analogy: "ReAct 框架就像「边想边做」的工作方式——先思考（Thought）→ 行动（Action）→ 观察（Observation）→ 再思考，循环往复直到完成。"
  key_points:
    - "LATS (Language Agent Tree Search) 将蒙特卡洛树搜索 (MCTS) 与 LLM Agent 结合,通过系统性探索多条路径来提升决策质量."
    - "核心思想:不是线性执行 ReAct(Thought→Action→Observation),而是构建搜索树,探索多个可能的行动分支."
    - "四个阶段(类比 MCTS)"
first_principle:
  problem: "为什么需要 LATS(Language Agent Tree Search)是如何将 MCTS 引入 Agent 的?它比 ReAct 强在哪里？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Agent = LLM + 规划 + 记忆 + 工具——LLM 是大脑，其余是手脚"
    - "ReAct 的本质是「思考-行动-观察」循环——显式推理比隐式更可靠"
    - "Agent 的上限不取决于 LLM 能力，而取决于工具集和记忆系统的设计"
  rebuild: "从 Agent 架构出发：① Agent 为什么比纯 LLM 强？② 规划/记忆/工具各自解决什么问题？③ 如何评估 Agent 效果？④ 理想的 Agent 架构是什么样？"
follow_up:
  - "UCT 公式在 Agent 场景如何适配?"
  - "LATS 的成本如何控制?"
---

# LATS(Language Agent Tree Search)是如何将 MCTS 引入 Agent 的?它比 ReAct 强在哪里

- **LATS (Language Agent Tree Search)** 将蒙特卡洛树搜索 (MCTS) 与 LLM Agent 结合,通过系统性探索多条路径来提升决策质量.

- **核心思想**:不是线性执行 ReAct(Thought→Action→Observation),而是构建搜索树,探索多个可能的行动分支.

- **四个阶段(类比 MCTS)**

1. **Selection(选择)**:用 UCT 公式选择最有潜力的节点
2. **Expansion(扩展)**:LLM 生成 K 个候选行动
3. **Simulation(模拟)**:执行行动,获取环境反馈
4. **Backpropagation(回传)**:用 LLM 自评更新路径价值

- **与 ReAct 的对比**

| 维度 | ReAct | LATS |
|------|-------|------|
| 探索方式 | 线性(单条路径) | 树搜索(多条路径) |
| 回溯 | ❌ 不回溯 | ✅ 可回溯到更优分支 |
| 成本 | 1x | ~5-10x(多路径探索) |
| 效果 | 基线 | 显著提升(尤其难任务) |

- **适用场景**
- 复杂推理(数学证明、代码调试)
- 决策空间大的任务
- 允许较高延迟的场景

- **局限**:计算成本高(多轮 LLM 调用),不适合实时对话.
