---
id: ai-scen-040
difficulty: L3
category: ai-scenario
subcategory: AI推荐与搜索
tags:
- AI搜索
- Perplexity
- RAG
- 引用溯源
- 多源检索
- Query理解
feynman:
  essence: 检索增强生成（RAG）+引用溯源，直接生成答案。
  analogy: 像助理帮你读完资料并标注出处，直接给你结论。
  first_principle: 如何从信息检索升级为知识合成与交付？
  key_points:
  - Query分解与多源检索
  - 内容提取与去重
  - 引用标注抑制幻觉
  - 时效性与多源交叉验证
follow_up:
- AI搜索引擎的时效性如何保证？
- 引用溯源的准确性如何验证？
- 如何处理搜索结果中的信息冲突？
---

# 如何设计一个AI搜索引擎（类似Perplexity）？直接给出答案而非链接列表，支持引用溯源。

【场景分析】
AI搜索引擎（如Perplexity）= 传统搜索 + LLM理解 + RAG + 引用溯源。核心价值：直接给出答案而非链接列表，解决信息过载。

【系统架构】
1. **Query理解层**：
   - **意图识别**：分类为导航型（去哪）、信息型（是什么）、交易型（买什么）。
   - **Query改写与分解**：
     - 利用LLM将复杂Query拆解为多个Sub-Query（例如："比较iPhone 15和华为Mate 60" -> 拆解为："iPhone 15 参数"、"Mate 60 参数"、"两者对比评测"）。
     - 利用HyDE（Hypothetical Document Embeddings）生成假设性答案文档，以此进行向量检索，提高语义召回率。

2. **多源检索层**：
   - **Web搜索**：调用Bing/Google/SerpAPI获取Top-K结果。
   - **垂直检索**：调用知识库、学术、GitHub、Reddit等特定API。
   - **混合检索**：BM25（关键词） + Dense Vector（语义），平衡精准度与泛化性。

3. **内容提取与处理层**：
   - **网页清洗**：Readability/Trafilatura去除广告、导航栏、CSS。
   - **切分**：按语义切分文本块（Chunk），保持上下文完整性。
   - **去重与冲突检测**：MinHash LSH去重；LLM检测多源信息冲突（如不同来源数据不一致）。

4. **答案生成层（RAG Pipeline）**：
   - **上下文构建**：将Query + Top-K检索到的Snippet拼装成Prompt。
   - **引用生成**：要求LLM在生成句子时标注引用ID [1][2]，利用COT思维链确保事实有据可依。
   - **结构化输出**：Summary（摘要） + Details（正文） + References（来源链接） + Follow-up Questions（推荐追问）。

5. **后处理校验**：
   - **事实一致性**：使用NLI（Natural Language Inference）模型校验生成的答案与Reference是否存在矛盾。
   - **无答案处理**：若检索结果置信度低，直接回答"未找到相关信息"，避免幻觉。

【RAG 处理流程图】
```text
User Query
   │
   ▼
┌──────────────┐
│ Query Plan   │ (LLM: 意图识别 & 拆解)
│   Rewriter   │
└──────┬───────┘
       │ (Sub-Queries)
       ▼
┌───────────────────────────────────────┐
│            Retrieval Layer            │
│  ┌──────────┐      ┌──────────┐      │
│  │ Web API  │      │ Vector DB│      │
│  │ (Link+Snippet)│   │ (Semantic)│     │
│  └────┬─────┘      └────┬─────┘      │
└───────┼────────────────┼──────────────┘
        │                │
        ▼                ▼
┌───────────────────────────────────────────┐
│           Context Builder                 │
│  (Deduplication, Relevance Scoring, Rank) │
└─────────────────────┬─────────────────────┘
                      │
                      ▼
              ┌───────────────┐
              │  LLM Generator│
              │  (Answer +    │
              │   Citations)  │
              └───────┬───────┘
                      │
                      ▼
           ┌────────────────────┐
           │ Hallucination Check│ (NLI/Verifier)
           └─────────┬──────────┘
                     │
                     ▼
             Final Answer Display
```

【与传统搜索的关键差异】
- 传统搜索：返回链接列表，用户自己阅读、整合信息。
- AI搜索：直接返回结构化答案 + 来源引用，信息密度高。
- **多轮追问**：系统能记住上下文，支持追问深入。
- **个性化**：基于用户画像调整答案深度和语言风格。

【时效性处理】
- **实时索引**：对于新闻类Query，强制添加时间过滤参数（如 "past 24 hours"）。
- **时间标注**：Prompt中明确要求："如果涉及时间敏感信息，请注明截止日期（如：截至2025年6月...）"。
- **多版本对比**：检测到信息冲突时，优先采用时间戳最新的来源，并在答案中标注"不同来源数据存在差异"。

【质量保障】
- **引用验证**：确保答案中的每个事实断言都能在Reference中找到对应文本（String Match 或 Embedding Similarity > 阈值）。
- **多数确认**：对于关键数据（如人数、金额），要求至少2个独立来源交叉验证。
- **幻觉检测**：NLI校验 + 低置信度标记（如"据推测"）。
- **用户反馈**：点赞/点踩/举报幻觉 → 收集Bad Case → 优化检索召回和Prompt。

【## 常见考点】
1. **如何降低AI搜索的延迟？**
   - 答：采用流式输出（Streaming）让用户先看到文字；同时进行异步检索和生成。还可以通过Cache常见问题的答案。
2. **如何处理检索内容不足导致LLM瞎编的问题？**
   - 答：设置严格的Prompt约束，例如"仅根据提供的上下文回答，如果上下文中没有答案，请直接说不知道"，并配合置信度评分截断。
3. **多源信息冲突时如何处理？**
   - 答：优先级策略：官方来源 > 权威媒体 > 一般博客；时间策略：越新越好；透明度策略：在答案中明确列出冲突点。
4. **引用索引不连续或错误怎么办？**
   - 答：后处理阶段通过正则匹配或微调一个专门的小模型来校准引用ID与上下文的对应关系。
