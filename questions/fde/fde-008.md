---
id: fde-008
difficulty: L3
category: fde
subcategory: AI部署实施
tags:
- IO
- IOC
- Redis
- Elasticsearch
- Docker
feynman:
  essence: 利用容器化技术屏蔽环境差异，实现一键交付。
  analogy: 像搬家把所有东西装进集装箱，到了新地方直接卸货就能用，不用管房子结构。
  first_principle: 如何消除客户异构环境带来的部署障碍，实现标准化交付？
  key_points:
  - 核心是Docker容器化，解决环境依赖问题
  - 使用docker-compose编排服务，实现一键启动
  - 提前预判并处理架构、网络及驱动等环境坑
follow_up:
- 如何做蓝绿部署?
- 客户没有 Docker 怎么办?
---

# 如何把一个 AI 应用快速部署到客户的服务器上

- **AI 应用客户部署 Checklist**

- **1. 容器化(最重要)**
```dockerfile
FROM python:3.11-slim
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

- **2. docker-compose 一键启动**
```yaml
version: '3'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY}
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
```

- **3. 部署步骤**
```bash
# 客户服务器上
git clone <repo>
cd <project>
cp .env.example .env  # 填入 API Key
docker-compose up -d
# 验证
curl localhost:8000/health
```

- **4. 常见坑**
- 客户服务器是 ARM 架构 → Docker buildx 跨平台构建
- 客户没有公网 → 用内网 IP + 反向代理
- 客户防火墙限制 → 只开放必要端口
- GPU 驱动版本不匹配 → 提前确认 CUDA 版本

- **FDE 经验**:永远准备一个 `install.sh` 脚本,客户只需一行命令就能装好.
