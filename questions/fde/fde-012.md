---
id: "fde-012"
difficulty: "L3"
category: "fde"
subcategory: "AI解决方案设计"
feynman:
  essence: "- *多模型 AI 网关架构** **请求流：** 用户请求 -> API Gateway(认证/限流) -> 模型路由器(场景选择) -> 模型适配层(统一接"
  analogy: "模型路由就像医院分诊——简单问题看全科（小模型），复杂问题看专家（大模型），按难度分配资源控成本。"
  key_points:
    - "多模型 AI 网关架构"
    - "模型路由器:根据 query 复杂度/预算/延迟要求选择模型"
    - "简单FAQ -> GLM-4-Flash(便宜快速)"
first_principle:
  problem: "如果要解决这个问题，最本质的方法论是什么？先理解问题约束，再找最优路径。"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "如何做模型 A/B 测试?"
  - "语义缓存命中率怎么提升?"
---

# 如何设计一个支持多模型的 AI 网关?让客户可以根据场景切换不同的 LLM

- **多模型 AI 网关架构**

**请求流：** 用户请求 -> API Gateway(认证/限流) -> 模型路由器(场景选择) -> 模型适配层(统一接口) -> 实际调用 -> 统一响应 -> 缓存层

- **核心组件**

1. **模型路由器**:根据 query 复杂度/预算/延迟要求选择模型
- 简单FAQ -> GLM-4-Flash(便宜快速)
- 代码问题 -> GPT-4(代码能力强)
- 长文档 -> Claude 3(长上下文)
- 默认 -> GLM-4-Plus(性价比)

2. **统一适配层**:不同模型 API 格式不同,统一为一致接口
3. **降级链**:主模型(GPT-4) -> 故障 -> 备用(Claude) -> 故障 -> 兜底(GLM-4)
4. **成本控制**:Token 预算管理 + 语义缓存 + 模型降级

- **客户价值**:一个 API 地址,背后自动选最优模型,降低成本同时保证效果.
