---
id: "ai-h-003"
difficulty: "L3"
category: "ai-harness"
subcategory: "LLM框架"
tags:
  - "IO"
  - "GC"
  - "Elasticsearch"
feynman:
  essence: "DSPy 与 LangChain 的本质区别：LangChain 是「Prompt 模板 + 链式编排」，Prompt 手写固定；DSPy 把 Prompt 当作可优化的参数，通过「声明式签名（Signature）+ 自动编译（teleprompter）」用少量标注数据自动优化 Prompt/示例/路由，更接近「编程」而非「提示工程」，适合需要系统性优化、可复现的场景。"
  analogy: "Harness Engineering 就像给 AI 搭建完整的施工脚手架——不只是调 LLM API，而是构建提示词管理、工具编排、错误处理、监控的完整工程框架。"
  key_points:
    - "DSPy vs 手写Prompt:"
    - "手写:依赖人工经验,换模型可能失效"
    - "DSPy:自动优化,换模型只需重新compile"
first_principle:
  problem: "它们本质上为什么不同？各自的设计目标和适用场景是什么？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "DSPy的BootstrapFewShot如何选择示例?"
  - "DSPy能优化非英语任务吗?"
---

# DSPy的编程模型和LangChain有什么本质区别

- **本质区别:**

| | LangChain | DSPy |
|--|-----------|------|
| 范式 | **命令式**(手写prompt) | **声明式**(定义输入输出) |
| Prompt | 手动编写和调优 | **自动搜索最优prompt** |
| 示例选择 | 手动 | **自动选择few-shot** |
| 适配模型 | 需重写prompt | **自动适配** |

- **DSPy核心模式:**
```python
# 1. 定义Signature(做什么)
class QA(dspy.Signature):
"""回答问题"""
question = dspy.InputField()
answer = dspy.OutputField(desc="简洁准确的答案")

# 2. 选择Module(怎么做)
qa = dspy.ChainOfThought(QA)

# 3. 编译优化(自动找最优prompt+示例)
optimizer = dspy.BootstrapFewShot(metric=accuracy)
compiled_qa = optimizer.compile(qa, trainset=data)

# 4. 使用
result = compiled_qa(question="什么是注意力机制?")
```

- **DSPy vs 手写Prompt:**
- 手写:依赖人工经验,换模型可能失效
- DSPy:自动优化,换模型只需重新compile
- 可版本管理、可复现、可A/B测试
