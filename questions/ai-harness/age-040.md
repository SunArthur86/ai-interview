---
id: age-040
difficulty: L2
category: ai-harness
subcategory: Agent工程化
tags:
- GC
feynman:
  essence: 通过全链路追踪和多维指标监控Agent运行状态。
  analogy: 给Agent装上“黑匣子”和“体检表”，随时查健康。
  first_principle: 如何确保不可预测的LLM应用在生产环境中可靠可控？
  key_points:
  - Trace记录完整执行链路和工具调用
  - 核心指标包括质量、性能、成本、安全
  - 采用结构化日志和采样率控制优化性能
follow_up:
- 如何设计 Trace 的采样策略?
- 如何用 LLM-as-Judge 自动评估 Agent 质量?
---

# 生产级 Agent 的可观测性(Observability)怎么做?需要追踪哪些指标

**Agent 可观测性** 是确保 Agent 在生产环境可靠运行的基础.

**Trace 轨迹追踪:**
*   记录每一轮的输入 prompt、模型输出、工具调用、工具返回.
*   构建完整的执行链路图,关联 Span ID.
*   支持回放:用历史数据重新执行以复现 Bug.

**核心指标:**

*   **1. 质量指标:** 任务完成率、工具调用成功率、用户满意度(CSAT).
*   **2. 性能指标:** 端到端延迟、首 token 延迟(TTFT)、每轮平均时间.
*   **3. 成本指标:** 每次 Token 消耗、API 调用费用、工具调用次数.
*   **4. 安全指标:** Prompt 注入检测次数、权限越界尝试.

**观测架构图:**
```text
┌──────────┐       ┌──────────┐       ┌──────────┐       ┌──────────┐
│  User    │──────▶│  Agent   │──────▶│  LLM/Tool│──────▶│ External │
│ Request  │ Trace │ Runtime  │ Span  │ Execution│ Span  │  APIs    │
└──────────┘       └────┬─────┘       └────┬─────┘       └──────────┘
                        │                   │
                        │ Generate/Collect  │
                        ▼                   ▼
                 ┌─────────────────────────────┐
                 │    Trace/Log Collector       │
                 │  (Async, Non-blocking)       │
                 └──────────────┬──────────────┘
                                │
                                ▼
                 ┌─────────────────────────────┐
                 │  Observability Platform      │
                 │  (LangSmith / Langfuse)      │
                 │  - Dashboard                 │
                 │  - Root Cause Analysis       │
                 │  - Dataset Evaluation        │
                 └─────────────────────────────┘
```

**工具生态:** LangSmith(LangChain)、Langfuse(开源)、Arize Phoenix、Helicone.

**关键设计:** 结构化日志(非纯文本,推荐 JSON)、采样率控制(高流量时只记录 1-10%)、异步上报(不阻塞主流程).

## 常见考点
*   如何在海量 Trace 中低成本地检索出“失败案例”？
*   为什么要区分 Trace 和 Log？它们在 Agent 系统中分别承载什么信息？
*   如何评估 Agent 的“推理质量”？（除了结果正确性，过程是否合理？）
