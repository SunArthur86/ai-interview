---
id: "age-040"
difficulty: "L2"
category: "ai-harness"
subcategory: "Agent工程化"
tags:
  - "GC"
feynman:
  essence: "生产 Agent 可观测性需要追踪的指标：任务成功率、平均步数、每次 LLM/工具调用的耗时与 Token、错误率与错误类型分布、P95 延迟、单位任务成本、缓存命中率、安全违规数；用 OpenTelemetry 打 Span 串 trace，配合 Prometheus + Grafana 做大盘与告警。"
  analogy: "Agent 可观测性就像飞机的黑匣子——记录每一次思考（Thought）、行动（Action）、观察（Observation），出问题时能回放完整链路定位根因。"
  key_points:
    - "Agent 可观测性 是确保 Agent 在生产环境可靠运行的基础."
    - "Trace 轨迹追踪:"
    - "记录每一轮的输入 prompt、模型输出、工具调用、工具返回"
first_principle:
  problem: "为什么需要 生产级 Agent 的可观测性(Observability)怎么做?需要追踪哪些指标？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "如何设计 Trace 的采样策略?"
  - "如何用 LLM-as-Judge 自动评估 Agent 质量?"
---

# 生产级 Agent 的可观测性(Observability)怎么做?需要追踪哪些指标

- **Agent 可观测性** 是确保 Agent 在生产环境可靠运行的基础.

- **Trace 轨迹追踪:**
- 记录每一轮的输入 prompt、模型输出、工具调用、工具返回
- 构建完整的执行链路图
- 支持回放(Replay):用历史数据重新执行

- **核心指标:**

- **1. 质量指标:** 任务完成率、工具调用成功率、用户满意度

- **2. 性能指标:** 端到端延迟、首 token 延迟、每轮平均时间

- **3. 成本指标:** 每次 Token 消耗、API 调用费用、工具调用次数

- **4. 安全指标:** Prompt 注入检测次数、权限越界尝试

- **工具生态:** LangSmith(LangChain)、Langfuse(开源)、Arize Phoenix、Helicone

- **关键设计:** 结构化日志(非纯文本)、采样率控制(高流量时只记录 1-10%)、异步上报(不阻塞主流程).
