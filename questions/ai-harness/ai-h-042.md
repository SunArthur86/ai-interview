---
id: ai-h-042
difficulty: L5
category: ai-harness
subcategory: 工程实践
tags:
- Elasticsearch
feynman:
  essence: 通过多供应商冗余、自动故障转移和分级降级策略，确保服务连续性。
  analogy: 给飞机装多台发动机，坏了一台其他的还能顶上，实在不行就滑翔降落。
  first_principle: 如何在外部依赖不稳定的情况下保证核心业务的可用性？
  key_points:
  - 设计多供应商 fallback 机制，避免单点依赖
  - 实施分级降级策略（切模型、切缓存、返回静态）
  - 建立缓存层作为最后的防线
  - 定期进行故障演练验证高可用性
follow_up:
- 如何做多供应商的负载均衡?
- 健康检查如何设计?
---

# LLM应用如何设计高可用架构?LLM API不稳定时如何保证服务

- **高可用架构设计:**

- **1. 多供应商冗余**
```
用户请求 → API网关
├─ 主供应商 ← 健康检查
├─ 备供应商 ← 自动切换
└─ 兜底(本地模型/Ollama) ← 极端情况
```

- **2. 请求级容错**
```python
async def call_llm_with_failover(prompt):
    providers = [
        ('openai', 'gpt-4', 0.7),      # 70%流量
        ('anthropic', 'claude-3.5', 0.2), # 20%
        ('zhipu', 'glm-4', 0.1),       # 10%
    ]
    for provider, model, weight in providers:
        try:
            return await call(provider, model, prompt)
        except (Timeout, RateLimit, ServerError):
            continue
    # 所有供应商都失败
    return fallback_response
```

- **3. 缓存兜底**
- 热门查询缓存结果
- API全部不可用时返回缓存

- **4. 降级策略**
| 级别 | 策略 |
|------|------|
| L0 | 正常服务 |
| L1 | 切换备用供应商 |
| L2 | 使用小模型(更快) |
| L3 | 返回缓存结果 |
| L4 | 返回静态回复(维护中) |

- **SLA目标:** 99.9%可用性 = 每月最多43分钟不可用
