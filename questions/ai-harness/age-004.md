---
id: age-004
difficulty: L2
category: ai-harness
subcategory: 工程化实践
feynman:
  essence: 根据语义相似度而非完全匹配来复用历史回答。
  analogy: 听懂意思相似的“你好”和“嗨”，都用同一句回复。
  first_principle: 如何减少重复计算以提升响应速度并降低成本？
  key_points:
  - 通过向量相似度匹配语义而非精确字符串
  - 适合高并发、意图重复的简单问答
  - 高精度要求场景需人工校验或禁用
  - 完全幂等场景优先使用精确缓存
---

# 语义缓存和精确缓存怎么选

低成本、高 QPS 的重复咨询场景适合语义缓存；对强合规与强正确性场景要谨慎，需提高阈值、加业务校验或禁用缓存。精确缓存适合完全幂等的调用（如参数批处理）。

**深入解析**：
语义缓存基于向量相似度匹配，核心在于**Embedding模型**和**距离阈值（Score）**的权衡。

1.  **技术实现细节**：
    *   **精确缓存**：通常使用 Redis 或 Memcached，Key 为 Prompt 的 Hash 值（如 MD5/SHA256）。适用于参数高度敏感的场景，如 SQL 生成、代码执行。
    *   **语义缓存**：需将输入转为向量存储于向量数据库。匹配时计算 Cosine Similarity，若 `similarity >= threshold` 则命中。

2.  **关键参数与边界**：
    *   **阈值设置**：阈值过低导致“答非所问”（幻觉），阈值过高则缓存命中率低。建议动态调整或引入元数据过滤（如用户 ID、时间窗口）。
    *   **输入长度**：语义缓存对超长上下文（如 32k+）的 Embedding 成本较高，需考虑截断策略。

**架构对比图**：
```text
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│   用户请求   │ ───> │  缓存拦截层   │ ───> │   LLM 服务   │
└─────────────┘      └──────┬───────┘      └─────────────┘
                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │  精确缓存    │ │  语义缓存    │ │    未命中    │
    │ (Key: Hash)  │ │ (Key: Vector)│ │              │
    └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
           │                │                │
           │ Exact Match    │ Sim >= Thresh  │
           ▼                ▼                ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │  瞬时返回    │ │ 相似度校验   │ │ 调用 LLM     │
    │  (100% 一致) │ │ (业务二次确认)│ │ (回填缓存)    │
    └──────────────┘ └──────────────┘ └──────────────┘
```

## 常见考点
1.  **缓存失效策略**：LLM 模型更新或 Prompt 模板变更后，如何清理或预热语义缓存？（通常加版本号或 Hash 作为命名空间）。
2.  **性能指标**：在引入 Embedding 计算开销的情况下，语义缓存通常能带来多少 QPS 提升？（实测通常能降低 30%-60% 的 Token 消耗）。
3.  **一致性问题**：如何处理用户修改了问题中的一个词（如“将5元改为10元”），但语义相似度依然很高导致返回旧结果的问题？（结合实体抽取或参数比对进行硬拦截）。
