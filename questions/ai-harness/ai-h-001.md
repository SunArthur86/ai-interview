---
id: ai-h-001
difficulty: L2
category: ai-harness
subcategory: LLM框架
tags:
- IO
- GC
- Elasticsearch
feynman:
  essence: Runnable 接口统一组件行为，LCEL 用管道符简化链路构建。
  analogy: 像水管工用标准接头连接不同管道，LCEL 让代码像搭积木一样简单。
  first_principle: 如何构建一套统一、可组合且易于观测的 LLM 应用开发范式？
  key_points:
  - Runnable 是所有组件的统一基类。
  - LCEL 使用 | 符号声明式连接组件。
  - 原生支持异步、流式和自动重试。
  - 内置可观测性，便于调试与追踪。
follow_up:
- LCEL的RunnableParallel如何使用?
- 如何实现自定义Runnable?
---

# LangChain的核心抽象是什么?LCEL相比传统Chain有什么优势

- **LangChain核心抽象:Runnable接口**

**所有组件都实现Runnable：** invoke/ainvoke/stream/astream/batch/abatch

- **LCEL (LangChain Expression Language):**
```python
# 声明式管道
chain = prompt | model | parser
result = chain.invoke({"input": "..."})

# 并行
parallel = RunnableParallel(a=chain_a, b=chain_b)
```

- **LCEL vs 传统Chain:**

| | 传统Chain | LCEL |
|--|-----------|------|
| 语法 | 嵌套构造 | **管道操作符** |
| 异步 | 需手动实现 | **原生支持** |
| 流式 | 需手动实现 | **内置流式** |
| 重试 | 需手动实现 | **自动重试** |
| 可观测 | 难 | **自动trace** |

- **LCEL核心能力:**
1. 自动异步/同步切换
2. 自动batching
3. 自动streaming(token级流式)
4. 内置fallback(链路容错)
5. 无缝集成LangSmith追踪
