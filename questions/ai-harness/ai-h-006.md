---
id: ai-h-006
difficulty: L3
category: ai-harness
subcategory: LLM框架
tags:
- IO
- Elasticsearch
feynman:
  essence: 把LLM输出强类型化为Pydantic对象。
  analogy: 像在代码和LLM之间加了一个翻译官，确保LLM吐出的东西一定能被代码识别。
  first_principle: 如何让非结构化的LLM输出直接变成代码可用的数据结构？
  key_points:
  - 利用Pydantic定义数据结构
  - 自动验证输出格式，失败自动重试
  - 支持流式传输和复杂数据嵌套
  - 封装了Function Calling的复杂细节
follow_up:
- Instructor的自动重试机制如何工作?
- BAML和Instructor有什么区别?
---

# Instructor库如何简化LLM结构化输出?它和Function Calling有什么关系

- **Instructor核心:用Pydantic模型定义输出,自动处理重试和验证**

```python
import instructor
from pydantic import BaseModel

class User(BaseModel):
    name: str
    age: int
    email: str

# 一行代码获取结构化输出
client = instructor.from_openai(openai_client)
user = client.chat.completions.create(
    model="gpt-4",
    response_model=User,
    messages=[{"role": "user", "content": "提取:张三,25岁,zs@mail.com"}]
)
# user.name = "张三", user.age = 25, user.email = "zs@mail.com"
```

- **Instructor vs 原生Function Calling:**

| | Function Calling | Instructor |
|--|-----------------|------------|
| Schema定义 | JSON Schema | **Pydantic类** |
| 验证 | 手动 | **自动** |
| 重试 | 手动 | **自动重试+错误注入** |
| 嵌套 | 难 | **原生支持** |
| 多语言 | Python/JS | **Python(主打)** |

- **Instructor优势:**
1. **类型安全** - IDE自动补全+类型检查
2. **自动验证** - Pydantic约束(min/max/regex)
3. **自动重试** - 验证失败自动把错误信息发回给LLM重试
4. **流式结构化** - 支持流式输出部分结果

- **类似工具:** Marvin, BAML, Outlines
