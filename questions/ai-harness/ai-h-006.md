---
id: "ai-h-006"
difficulty: "L3"
category: "ai-harness"
subcategory: "LLM框架"
tags:
  - "IO"
  - "Elasticsearch"
feynman:
  essence: "Instructor vs 原生Function Calling。"
  analogy: "Harness Engineering 就像给 AI 搭建完整的施工脚手架——不只是调 LLM API，而是构建提示词管理、工具编排、错误处理、监控的完整工程框架。"
  key_points:
    - "Instructor核心:用Pydantic模型定义输出,自动处理重试和验证"
    - "Instructor vs 原生Function Calling:"
    - "Instructor优势:"
first_principle:
  problem: "为什么需要 Instructor库如何简化LLM结构化输出?它和Function Calling有什么关系？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "Instructor的自动重试机制如何工作?"
  - "BAML和Instructor有什么区别?"
---

# Instructor库如何简化LLM结构化输出?它和Function Calling有什么关系

- **Instructor核心:用Pydantic模型定义输出,自动处理重试和验证**

```python
import instructor
from pydantic import BaseModel

class User(BaseModel):
**name：** str
**age：** int
**email：** str

# 一行代码获取结构化输出
client = instructor.from_openai(openai_client)
user = client.chat.completions.create(
model="gpt-4",
response_model=User,
messages=[{"role": "user", "content": "提取:张三,25岁,zs@mail.com"}]
)
# user.name = "张三", user.age = 25, user.email = "zs@mail.com"
```

- **Instructor vs 原生Function Calling:**

| | Function Calling | Instructor |
|--|-----------------|------------|
| Schema定义 | JSON Schema | **Pydantic类** |
| 验证 | 手动 | **自动** |
| 重试 | 手动 | **自动重试+错误注入** |
| 嵌套 | 难 | **原生支持** |
| 多语言 | Python/JS | **Python(主打)** |

- **Instructor优势:**
1. **类型安全** - IDE自动补全+类型检查
2. **自动验证** - Pydantic约束(min/max/regex)
3. **自动重试** - 验证失败自动把错误信息发回给LLM重试
4. **流式结构化** - 支持流式输出部分结果

- **类似工具:** Marvin, BAML, Outlines
