---
id: ai-h-043
difficulty: L3
category: ai-harness
subcategory: RAG工程化
tags:
- IO
- Elasticsearch
feynman:
  essence: 基于LLM的RAG效果自动评估框架。
  analogy: 像阅卷老师自动检查学生答题是否结合了课本资料。
  first_principle: 如何用LLM自动量化RAG系统的检索质量和生成质量？
  key_points:
  - 检索指标关注查准率和查全率
  - 生成指标关注忠实度和相关性
  - 支持LLM自动生成评测数据
  - 集成到CI/CD做持续回归测试
follow_up:
- Faithfulness如何自动计算?
- 如何减少评估的成本?
---

# Ragas框架如何评估RAG系统?有哪些核心指标?如何部署

**Ragas 评估框架核心解析**

**1. 评估指标体系与原理**

**检索质量指标：**
- **Context Precision (上下文精确度)**：检索到的上下文中相关信息的比例，衡量是否包含噪声。
- **Context Recall (上下文召回率)**：金标准答案所需的信息是否被检索到，实际上是判断 Ground Truth 中的断言能否由检索到的 Context 推导出来。
- **Context Relevancy (上下文相关性)**：检索到的上下文与用户问题的直接相关性，通过计算句子相似度得出。

**生成质量指标：**
- **Faithfulness (忠实度)**：答案是否严格基于检索到的文档，检测幻觉。原理是将Answer拆解为多个原子声明，逐一验证这些声明是否能在Context中找到依据（NLI任务）。
- **Answer Relevancy (答案相关性)**：生成的答案是否直接回应了用户的问题。通过将Answer反向生成问题，计算生成的问题与原始问题的Embedding相似度。

**Ragas 评估工作流图：**
```
[ 输入数据集 ]
    │ (Question, Context, Answer, Ground Truth)
    ▼
┌───────────────────────────────────────┐
│         LLM as a Judge (评估器)        │
├───────────────────────────────────────┤
│ 1. Faithfulness:  Answer -> Claims    │
│                 Context + Claims -> NLI│
│                                       │
│ 2. Recall:      GT -> Sentences       │
│                 Context + Sentences -> NLI│
└─────────────────────────────────────────┘
    │
    ▼
[ 评估分数报告 (0-1) ]
```

**2. 代码实现示例**

```python
from ragas import evaluate
from ragas.metrics import (
    faithfulness,
    answer_relevancy,
    context_precision,
    context_recall
)

# eval_dataset 需包含: question, contexts, answer, ground_truth
result = evaluate(
    dataset=eval_dataset,
    metrics=[
        faithfulness,
        answer_relevancy,
        context_precision,
        context_recall
    ]
)

print(result.scores)
```

**3. 评估数据集构建**
- **人工标注**：专家编写 Question + Ground Truth，质量最高但成本高。
- **LLM 自动生成**：基于文档通过大模型自动生成问答对，常用“Evolution”模式（增加问题难度和多样性）。
- **日志提取**：从真实用户日志中清洗并脱敏提取，需人工抽检质量。

**4. 最佳实践**
建议建立 CI/CD 集成的持续评估 Pipeline，每次 RAG 配置（如 Chunk 策略、Embedding 模型）变更后自动运行评估，确保效果不回退。

## 常见考点
1. **LLM作为裁判的偏差**：如何避免评估LLM（如GPT-4）因为自身偏好导致评分不公？（回答：使用多个模型投票，或使用与生成模型不同家族的模型进行评估）
2. **评估成本**：Ragas评估非常消耗Token，如何降低成本？（回答：使用小模型如Llama-3-8B作为评估器，或对数据进行分层抽样评估）
3. **Ground Truth缺失**：在没有人工标注数据的情况下如何评估？（回答：使用"answer correctness"指标，或者仅依靠Context Precision和Faithfulness等无监督指标）
