---
id: "age-018"
difficulty: "L1"
category: "ai-harness"
subcategory: "工程化实践"
tags:
  - "Kubernetes"
feynman:
  essence: "K8s 部署 Agent 服务时 HPA 可以按 CPU/内存扩，但更适合用自定义指标：请求队列长度、P95 延迟、429 比例、并发推理数（需 Prometheus Adapter / KEDA）；同时要留意冷启动和 LLM 长尾延迟，必要时结合预热和 PD 分离。"
  analogy: "Agent 发布就像新药临床试验——先小范围试用（金丝雀），没问题再全量推广（蓝绿切换），随时准备回滚（紧急叫停）。"
  key_points:
    - "CPU/内存、自定义指标如 请求队列长度、P95 延迟、429 比例(需 Prometheus"
    - "注意冷启动与 LLM 长尾延迟."
first_principle:
  problem: "为什么需要 K8s 部署 Agent 服务时 HPA 可以按什么指标扩？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# K8s 部署 Agent 服务时 HPA 可以按什么指标扩

CPU/内存、自定义指标如 请求队列长度、P95 延迟、429 比例(需 Prometheus
Adapter);注意冷启动与 LLM 长尾延迟.
