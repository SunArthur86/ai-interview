---
id: ai-h-010
difficulty: L3
category: ai-harness
subcategory: LLM框架
tags:
- IO
- 序列化
- GC
- 索引
feynman:
  essence: 框架带来便利，也增加了显著的延迟与内存开销。
  analogy: 像穿防护服，虽然安全（功能全），但跑步肯定比穿背心（原生API）慢。
  first_principle: 如何平衡开发效率与运行时的性能损耗？
  key_points:
  - 框架主要开销在于抽象层和序列化
  - 原生API性能最优，适合高并发核心链路
  - 可通过精简依赖（如langchain-core）减少开销
  - 复杂逻辑可接受框架开销换取开发效率
follow_up:
- 如何精简LangChain依赖?
- 原生API如何实现重试/流式等框架功能?
---

# LLM框架本身的性能开销有多大?如何选择性能最优的框架

- **框架开销来源:**
1. **抽象层开销** - 额外的类/函数调用
2. **序列化/反序列化** - Prompt构造/输出解析
3. **依赖膨胀** - 安装包大、import慢
4. **错误处理** - 重试/fallback逻辑

- **性能对比(简单RAG任务基准测试):**

| 方案 | 延迟 | 内存 | 依赖数 |
|------|---------|---------|--------|
| 原生API | **~50** | **~20** | **1** |
| LangChain | ~200 | ~150 | 200+ |
| LlamaIndex | ~180 | ~120 | 100+ |
| Haystack | ~160 | ~100 | 80+ |

- **性能优化建议:**

1. **延迟敏感 → 原生API**
- 去掉框架中间层
- 手动管理prompt和解析

2. **复杂逻辑 → 框架但精简依赖**
```python
# 只安装需要的
pip install langchain-core  # 而非 langchain
```

3. **批量处理 → 减少框架开销**
- 用batch API而非循环单次调用
- 异步并发(asyncio)

4. **缓存层**
- Prompt缓存(系统prompt复用)
- 结果缓存(相同输入直接返回)

- **结论:** 对于性能关键场景,推荐原生API + 轻量辅助库(如Instructor for structured output)
