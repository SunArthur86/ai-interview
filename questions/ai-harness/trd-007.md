---
id: trd-007
difficulty: L2
category: ai-harness
subcategory: Testing & Eval
feynman:
  essence: 通过场景库、Mock和确定性控制，在非确定性环境中构建可复测的Agent验证闭环。
  analogy: 像用剧本拍电影，不仅检查演员（模型）演技，还要确保每次都按剧本（工具链）走，且布景（外部环境）一成不变。
  first_principle: 如何在概率性和依赖性系统中，建立确定性的因果验证标准？
  key_points:
  - 建设包含真实Query与最优工具链的场景库
  - 利用Mock Server隔离外部环境波动
  - 结合结果正确性与中间过程合规性的多维评估
  - 固定参数以对抗模型非确定性
---

# 如何构建一套 AI Agent 的回归测试体系？核心难点在哪里？

Agent 的回归测试旨在确保在代码或模型更新后，原有的工具调用链路和逻辑依然正确。由于 Agent 的非确定性（随机采样）和外部依赖（API、环境），测试难度远超传统软件。

**构建体系**：
1. **场景库建设**：收集真实的用户 Query 和对应的最优工具调用序列（Golden Trace）。
2. **可重复性模拟**：构建 Mock Server，模拟外部工具的返回结果，确保测试环境不受外部 API 变动影响。
3. **评估指标**：除了最终答案的正确性（Exact Match 或 LLM-as-a-Judge），还要检查中间过程是否调用了正确的工具以及参数是否合规。
4. **确定性控制**：在测试时固定 Seed 和 Temperature，减少随机性波动。

**核心难点**：
1. **非确定性输出**：即使 Seed 固定，底层框架或模型版本的微小差异也可能导致输出路径改变。
2. **评估成本高**：需要真实运行多步推理和外部调用，测试耗时且消耗 Token。
3. **长尾场景覆盖**：Agent 面对复杂异常时的行为难以穷举。

### 测试闭环流程图
```text
┌──────────┐      ┌───────────────┐      ┌─────────────────┐
│   User   │ ───> │  Agent System │ ───> │  Execution Trace│
│  Query   │      │  (Under Test) │      │  (Tools/Steps)  │
└──────────┘      └───────┬───────┘      └────────┬────────┘
                          │                       │
                          ▼                       ▼
              ┌───────────────────────┐  ┌─────────────────┐
              │  Mock Server / VCR    │  │  LLM Evaluator │
              │  (Fix External Deps) │  │  (Comparison)  │
              └───────────────────────┘  └────────┬────────┘
                                                    │
                           ┌────────────────────────┼────────────────────────┐
                           │                        │                        │
                           ▼                        ▼                        ▼
                   ┌──────────────┐        ┌──────────────┐        ┌──────────────┐
                   │ Final Answer │        │ Tool Accuracy│        │ Error Rate  │
                   │  Similarity  │        │ (Precision)  │        │  (Safety)    │
                   └──────────────┘        └──────────────┘        └──────────────┘
```

## 常见考点
1. **Golden Trace 的维护**：当业务逻辑变更时，如何批量更新 Golden Trace？如何处理由于非确定性导致无法 100% 匹配 Trace 的情况（如模糊匹配）？
2. **Mock 粒度**：Mock 是应该做在 HTTP 层（录制回放）还是函数层？如何处理 Mock 数据与真实数据时效性不一致的问题？
3. **评估器选择**：在 LLM-as-a-Judge 中，如何解决 Judge 模型本身的偏见和幻觉？是否需要使用更强的模型（如 GPT-4）来评估小模型？
