---
id: ai-h-052
difficulty: L2
category: ai-harness
subcategory: AI编程
tags:
- IOC
feynman:
  essence: 基于CLI的AI编程助手，采用无状态Loop和工具调用架构。
  analogy: 一个拥有读写权限和终端操作能力的超级智能实习生。
  first_principle: 如何设计一个能够自主操作文件系统并编写代码的智能体？
  key_points:
  - 基于Loop的推理-行动循环
  - 依赖MCP协议扩展工具能力
  - 使用Prompt Caching优化长上下文
  - 强调安全和自动验证机制
follow_up:
- Claude Code 为什么不用 RAG 检索代码?
- Agent Loop 如何防止无限循环?
---

# Claude Code 的架构是怎样的?它的 Agent Loop 是如何设计的

**Claude Code 架构与 Agent Loop 设计**

**核心定位：**
Claude Code 是 Anthropic 推出的 CLI AI 编程助手，其架构是现代 Agent 设计的参考标杆。

**1. Agent Loop 设计**
- **循环逻辑**：接收用户指令 -> LLM 推理 -> 工具调用 -> 观察结果 -> 继续推理/结束。
- **迭代限制**：最大 25 轮迭代，防止无限循环。
- **上下文管理**：支持上下文压缩，超长历史时自动摘要。
- **请求模式**：每轮都带完整上下文（无状态请求），依赖 Prompt Caching 优化。

**2. 核心工具集**
- **文件操作**：Read (读取)、Write (写入)、Edit (编辑)、Glob (查找)。
- **命令执行**：Bash 命令执行环境。
- **代码搜索**：Grep 语义或文本搜索。
- **MCP 协议**：通过 Model Context Protocol 连接外部工具（如 GitHub、数据库）。

**3. 系统提示词 关键设计**
- **角色定义**：明确身份为 Claude Code，Anthropic 的 AI 助手。
- **工具使用规则**：优先搜索再修改、改动后必须验证。
- **安全约束**：禁止执行破坏性命令、高风险操作需用户确认。

**4. 上下文管理策略**
- **Prompt Caching**：利用缓存减少重复计算，降低成本和延迟。
- **自动压缩**：当上下文超过阈值时自动压缩历史记录，保留关键摘要。

**设计哲学：**
采用“无状态请求 + 前缀缓存 + 上下文压缩”的简单架构，而非复杂的有状态 Agent。

---

### Agent Loop 核心流程图

```text
┌──────────────────────────────────────────────────────────────┐
│                     用户输入指令                 │
└─────────────────────────────┬────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│              LLM 推理 (系统提示词 + 用户指令 + 上下文)           │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 思考: 分析需求，决定使用哪些工具，参数是什么              │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────────┬────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   生成 Tool Call │
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
        ┌───────────┐  ┌───────────┐  ┌───────────┐
        │ 文件操作   │  │ 执行 Bash  │  │ 搜索/外部  │
        │ (Read/Edit)│  │ (Run/Test) │  │ (MCP/Grep) │
        └─────┬─────┘  └─────┬─────┘  └─────┬─────┘
              │              │              │
              └──────────────┼──────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   工具执行结果   │
                    │   (Observation)  │
                    └────────┬────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │  判断: 是否达到目标 或 迭代 > 25?│
              └─────────────┬────────────────┘
                    是 /     \ 否
                   /          \
                  ▼            ▼
          ┌───────────┐  ┌───────────────┐
          │ 输出最终答案│  │ 回到 LLM 推理  │
          └───────────┘  │ (继续思考)     │
                         └───────────────┘
```

**## 常见考点**
1. **Prompt Caching 机制**：追问 Prompt Caching 的具体实现原理（如针对 System Prompt 和长期不变的代码库上下文如何命中缓存）以及对成本和延迟的具体影响。
2. **安全沙箱隔离**：Bash 命令执行是非常危险的，面试官会询问如何防止 Agent 执行 `rm -rf` 或泄露密钥，考察对容器化、权限控制或 Danger List 的了解。
3. **MCP 协议细节**：询问 MCP (Model Context Protocol) 的作用，以及它如何让 LLM 标准化地访问外部数据源（是本地应用还是云端服务）。
4. **状态管理**：为什么选择“无状态”设计而不是内存中维护 Session？考察对分布式扩展性、错误恢复和一致性的理解。
