---
id: "ai-h-015"
difficulty: "L2"
category: "ai-harness"
subcategory: "RAG工程化"
tags:
  - "索引"
feynman:
  essence: "生产 RAG 常见 10 类问题：① 检索质量差（→ Reranker + 查询扩展）；② 分块不合理（→ 语义/父子分块）；③ 检索不到（→ Hybrid 检索 向量+BM25）；④ 答案不引用来源（→ 强制 citation Prompt）；⑤ 幻觉（→ 拒答策略 + 置信度）；⑥ 冷启动（→ 预热缓存）；⑦ 多轮上下文丢失（→ 查询改写）；⑧ 文档更新滞后（→ 增量索引）；⑨ 多租户串数据（→ 权限过滤）；⑩ 成本高（→ 缓存 + 模型路由）。"
  analogy: "Harness Engineering 就像给 AI 搭建完整的施工脚手架——不只是调 LLM API，而是构建提示词管理、工具编排、错误处理、监控的完整工程框架。"
  key_points:
    - "数据质量(分块+解析)"
    - "检索质量(Reranker+查询扩展)"
    - "生成质量(Prompt+引用)"
first_principle:
  problem: "为什么需要 生产RAG系统的10个常见问题及解决方案？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "如何系统性地评估RAG效果?"
  - "增量索引如何实现?"
---

# 生产RAG系统的10个常见问题及解决方案

- **10大问题与方案:**

| # | 问题 | 症状 | 解决方案 |
|---|------|------|---------|
| 1 | 检索不准 | 答非所问 | 加Reranker + 查询改写 |
| 2 | 检索太多 | 上下文过长 | 减少Top-K + 压缩chunk |
| 3 | 检索太少 | 答案不全 | Multi-Query扩大召回 |
| 4 | 分块不合理 | 语义割裂 | 语义分块/结构化分块 |
| 5 | 幻觉 | 编造答案 | 提示「只基于文档」+ 引用标注 |
| 6 | 多语言 | 混合检索差 | BGE-M3多语言模型 |
| 7 | 实时更新 | 知识过时 | 增量索引 + 通知机制 |
| 8 | 大规模 | 检索慢 | 分区索引 + 缓存 |
| 9 | 多轮对话 | 上下文丢失 | 对话历史改写查询 |
| 10 | 评估难 | 不知效果如何 | Ragas/TruLens自动评估 |

- **优化优先级:**
1. 数据质量(分块+解析)
2. 检索质量(Reranker+查询扩展)
3. 生成质量(Prompt+引用)
4. 评估体系(Ragas+人工抽检)
