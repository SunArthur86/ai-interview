---
id: "ai-h-012"
difficulty: "L2"
category: "ai-harness"
subcategory: "RAG工程化"
tags:
  - "IO"
feynman:
  essence: "查询扩展（Query Expansion）最佳实践：Multi-Query（用 LLM 生成多个子查询并行检索再融合）、HyDE（生成假设答案再检索）、子问题分解、同义词扩展；平衡效果与延迟的方法：并行执行多查询、用小模型做扩展、对扩展结果做 RRF 融合去重、并对延迟敏感场景限制扩展数量（如 3~5 个）。"
  analogy: "Harness Engineering 就像给 AI 搭建完整的施工脚手架——不只是调 LLM API，而是构建提示词管理、工具编排、错误处理、监控的完整工程框架。"
  key_points:
    - "Multi-Query的多个查询并行检索"
    - "只增加1次LLM调用延迟(生成查询)"
    - "相同/相似查询的结果缓存"
first_principle:
  problem: "为什么需要 生产RAG中查询扩展的最佳实践?如何平衡效果和延迟？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "如何判断查询是否需要扩展?"
  - "RRF融合的k值如何选择?"
---

# 生产RAG中查询扩展的最佳实践是什么?如何平衡效果和延迟

- **查询扩展技术对比:**

| 技术 | 延迟增加 | 效果提升 | 适用 |
|------|---------|---------|------|
| Query Rewriting | +1次LLM | 中 | 模糊查询 |
| HyDE | +1次LLM | **高** | 事实型查询 |
| Multi-Query | +1次LLM | 高 | 覆盖面不足 |
| Step-Back | +1次LLM | 高 | 全局问题 |
| Query Decomposition | +1次LLM | **高** | 复杂多跳 |

- **延迟优化:**

1. **并行查询**
- Multi-Query的多个查询并行检索
- 只增加1次LLM调用延迟(生成查询)

2. **缓存**
- 相同/相似查询的结果缓存
- HyDE的假想文档缓存

3. **条件触发**
- 简单查询不做扩展
- 用分类器判断是否需要扩展

- **生产推荐组合:**
```
用户查询 → 分类(简单/复杂)
├─ 简单 → 直接向量检索
└─ 复杂 → Query Rewriting + Multi-Query(3个变体) → 并行检索 → RRF融合 → Rerank
```
