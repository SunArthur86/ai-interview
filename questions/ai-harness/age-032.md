---
id: "age-032"
difficulty: "L2"
category: "ai-harness"
subcategory: "工程化"
tags:
  - "IO"
feynman:
  essence: "Semaphore 大小设定原则：取 min(下游 API 并发限制, GPU/服务器资源容量, 数据库连接池大小, 合理延迟范围)；异步不一定提高吞吐——I/O 密集场景有效，但若受 GPU 算力或单线程推理限制，还需配合批处理、多副本、队列，并防止无界并发压垮下游。"
  analogy: "并发控制就像高速公路收费站——开太多口（高并发）系统扛不住，开太少（低限流）用户等太久，要找到吞吐和延迟的平衡。"
  key_points:
    - "Semaphore大小设定原则:"
    - "关键: 异步的收益来自IO等待期间的上下文切换,如果下游本身就慢(LLM推理3秒),异步让你在等待时处理其他请求."
    - "总QPS上限 = Semaphore / 平均响应时间"
first_principle:
  problem: "为什么需要 Agent系统中并发控制Semaphore应该设多大?异步一定提高吞吐吗？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "asyncio的Semaphore如何使用?"
  - "如何计算最优并发数?"
---

# Agent系统中并发控制Semaphore应该设多大?异步一定提高吞吐吗

- **Semaphore大小设定原则:**

```
Semaphore = min(
下游API并发限制,
GPU/服务器资源容量,
数据库连接池大小,
合理的延迟范围
)
```

- **实践值:**
| 组件 | 建议Semaphore |
|------|-------------|
| LLM API | 5-20(看限流) |
| 向量数据库 | 10-50 |
| Web搜索API | 3-10 |
| 代码执行沙箱 | 2-5 |

- **异步≠更高吞吐:**

异步(async/await)只是**不阻塞线程**,不等于更多并发.

| 场景 | 异步效果 |
|------|---------|
| IO密集(LLM API) | **显著提升**(线程不等待) |
| CPU密集(本地推理) | **无效果**(GIL限制) |
| 混合负载 | 部分提升 |

- **关键:** 异步的收益来自**IO等待期间的上下文切换**,如果下游本身就慢(LLM推理3秒),异步让你在等待时处理其他请求.

- **但:**
- 总QPS上限 = Semaphore / 平均响应时间
- 10并发 / 3秒 = ~3.3 QPS
- 提高吞吐必须增大Semaphore或降低延迟
