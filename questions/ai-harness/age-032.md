---
id: age-032
difficulty: L2
category: ai-harness
subcategory: 工程化
tags:
- IO
feynman:
  essence: 并发控制是受限资源、下游能力和延迟的权衡。
  analogy: 像餐厅只有5个上菜窗口（Semaphore），再多厨师（异步）也不能超过这个物理极限。
  first_principle: 如何在系统资源受限和外部依赖缓慢的情况下，最大化处理请求的吞吐量？
  key_points:
  - Semaphore取各环节瓶颈的最小值
  - 异步通过IO等待切换提升资源利用率
  - CPU密集型任务异步收益有限
  - 吞吐上限受限于并发数和响应时间
follow_up:
- asyncio的Semaphore如何使用?
- 如何计算最优并发数?
---

# Agent系统中并发控制Semaphore应该设多大?异步一定提高吞吐吗

- **Semaphore大小设定原则:**

```
Semaphore = min(
下游API并发限制,
GPU/服务器资源容量,
数据库连接池大小,
合理的延迟范围
)
```

- **实践值:**
| 组件 | 建议Semaphore |
|------|-------------|
| LLM API | 5-20(看限流) |
| 向量数据库 | 10-50 |
| Web搜索API | 3-10 |
| 代码执行沙箱 | 2-5 |

- **异步≠更高吞吐:**

异步(async/await)只是**不阻塞线程**,不等于更多并发.

| 场景 | 异步效果 |
|------|---------|
| IO密集(LLM API) | **显著提升**(线程不等待) |
| CPU密集(本地推理) | **无效果**(GIL限制) |
| 混合负载 | 部分提升 |

- **关键:** 异步的收益来自**IO等待期间的上下文切换**,如果下游本身就慢(LLM推理3秒),异步让你在等待时处理其他请求.

- **但:**
- 总QPS上限 = Semaphore / 平均响应时间
- 10并发 / 3秒 = ~3.3 QPS
- 提高吞吐必须增大Semaphore或降低延迟
