---
id: trd-006
difficulty: L2
category: ai-harness
subcategory: Protocol & Infra
feynman:
  essence: 连接AI与数据源的万能插座，统一通信标准实现即插即用。
  analogy: 就像USB接口统一了电脑和外设的连接一样，MCP让AI模型能像插U盘一样直接连接各种数据源，不用每次都专门做一根专用线。
  first_principle: 如何用统一的标准消除AI模型与异构数据源之间的连接重复开发成本？
  key_points:
  - 传输层：基于JSON-RPC 2.0统一底层通信（STDIO/SSE），解决语言环境差异
  - 会话层：管理连接生命周期与能力握手，确保交互稳定
  - 能力层：定义提示词、资源、工具等具体接口，作为实际操作API
---

# MCP (Model Context Protocol) 的三层架构分别是什么？它解决了什么痛点？

MCP (Model Context Protocol) 是一种开放标准，旨在连接 AI 模型与本地/远程数据源和工具。其三层架构设计如下：

1. **传输层**：定义了 Client（如 Desktop App）和 Host（如 MCP Server）之间的通信方式，通常是基于 JSON-RPC 2.0 的标准，支持 STDIO 或 SSE (Server-Sent Events) 传输，解决了不同语言和环境的底层通信问题。
2. **会话层**：管理连接的生命周期、初始化握手和资源隔离。它定义了如何建立连接、交换 Capabilities（能力列表），确保连接的稳定性和协议一致性。
3. **能力层**：定义了具体的接口类型，包括 Prompts（提示词模板）、Resources（读取文件/数据）、Tools（执行函数）等。这是模型实际交互的 API 表面。

**解决的痛点**：在 MCP 之前，每个 AI 应用都需要为每个数据源（如 Google Drive、Slack、本地文件）编写特定的 Connector（插件），导致生态碎片化。MCP 提供了统一的“即插即用”标准，使得任何兼容 MCP 的模型都能无缝接入任何实现了 MCP 协议的数据源，极大地降低了连接成本。

### 架构原理图
```text
┌───────────────────────────────────────────────────────────────┐
│                        MCP Client (e.g. Claude Desktop)        │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐      │
│  │  LLM Core     │──│ Orchestrator │──│  MCP Client   │      │
└──┴───────────────┴──┴───────────────┴──┴───────┬───────────┘
                                                       │
                  ┌────────────────────────────────────┼─────────────────────────────┐
                  │          Transport Layer           │                             │
                  │  (JSON-RPC 2.0 over STDIO / SSE)   │                             │
                  └────────────────────────────────────┼─────────────────────────────┘
                                                       │
        ┌──────────────────────────────────────────────┼──────────────────────────────┐
        │                                              │                              │
┌───────▼────────┐                          ┌──────────▼──────────┐
│ MCP Server A   │                          │  MCP Server B       │
│ (e.g., Local FS)│                          │  (e.g., PostgreSQL)  │
├────────────────┤                          ├─────────────────────┤
│ Session Layer  │                          │  Session Layer      │
│ - Init Handshake│                          │  - Init Handshake   │
├────────────────┤                          ├─────────────────────┤
│ Capability Layer│                         │  Capability Layer    │
│ - Resources    │                          │  - Tools            │
│ - Prompts      │                          │  - Resources        │
└────────────────┘                          └─────────────────────┘
```

## 常见考点
1. **传输方式的选择**：何时使用 STDIO（本地进程、简单配置），何时使用 SSE（远程服务、双向实时流）？
2. **资源与工具的区别**：Resources 主要用于读取静态或动态数据（如文件、数据库状态），而 Tools 用于执行操作（如发送邮件、修改数据），在权限设计上有什么区别？
3. **采样能力**：服务端如何通过 `initialize` 返回的 `capabilities` 字段告知客户端支持的功能，客户端如果不支持某种能力如何降级处理？
