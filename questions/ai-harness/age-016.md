---
id: age-016
difficulty: L1
category: ai-harness
subcategory: 工程化实践
feynman:
  essence: 将意图识别与权限校验解耦，确保执行前通过安全审查。
  analogy: 先填申请单说明意图，审批通过后才能盖章办事。
  first_principle: 如何在赋予模型操作能力的同时确保可控的安全性？
  key_points:
  - 意图与参数生成不直接操作
  - 策略层集中校验权限
  - 拒绝原因需闭环反馈
---

# 工具调用的「两步授权」怎么做

工具调用的「两步授权」旨在将「意图识别」与「实际执行」解耦，防止模型直接越权操作。具体流程如下：

1.  **第一步：意图生成（计划阶段）**
    *   模型仅根据用户输入生成「调用意图」与「参数结构」（JSON Schema），不直接执行操作。
    *   输出示例：`{"tool": "transfer", "args": {"to": "user_b", "amount": 100}}`。
2.  **第二步：策略校验（授权阶段）**
    *   **鉴权服务**介入，不直接调用工具，而是检查：
        *   **角色权限**：当前用户是否有权调用该工具（RBAC）。
        *   **资源边界**：参数中的资源 ID（如 `to: user_b`）是否在允许范围内。
        *   **速率限制**：该敏感操作是否超过频率限制。
    *   **人工确认（可选）**：对于高风险操作（如删除数据库、大额转账），触发二次确认（2FA 或人工审批）。
3.  **第三步：执行与反馈**
    *   校验通过后，执行器调用真实工具。
    *   如果拒绝，将具体的拒绝原因（如「余额不足」、「无权操作」）转化为自然语言反馈给模型，由模型组织话术回复用户。

```text
     用户
      │
      ▼
   ┌─────┐
   │ LLM │ (1. 生成意图: "Transfer $100 to B")
   └──┬──┘
      │
      ▼
 ┌──────────────┐
 │ 策略引擎     │ ◀─── (2. 检查: 限额? 权限? 风险?)
 │ (AuthZ)      │
 └──┬───────┬───┘
    │       │
    │ 否    │ 是
    ▼       ▼
 ┌────┐ ┌────────┐
 │反馈│ │执行器  │ (3. 调用真实 API)
 └────┘ └───┬────┘
            │
            ▼
         真实世界
```

## 常见考点
1.  **如果参数中包含注入攻击（如 `rm -rf`），策略引擎如何识别？**（追问：参数校验正则、沙箱环境）
2.  **模型生成的参数格式如果不合法怎么办？**（追问：Output Parsing 的异常处理与自动重试机制）
3.  **如何防止模型通过 Prompt 越狱绕过策略引擎？**（追问：策略引擎必须独立，且不依赖模型的自然语言解释，而是依赖结构化的参数检查）
