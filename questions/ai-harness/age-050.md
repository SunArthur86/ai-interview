---
id: age-050
difficulty: L2
category: ai-harness
subcategory: 生产工程化
tags:
- IO
feynman:
  essence: 用算法或编译思路自动寻找最佳Prompt指令与示例。
  analogy: 像给不会说话的机器人写说明书，自动改写直到它听懂为止。
  first_principle: 如何自动找到模型表现最好的输入描述？
  key_points:
  - OPRO让LLM当优化器改写Prompt措辞
  - DSPy将Prompt工程变为模块化编程
  - 通过评估反馈循环迭代最优解
follow_up:
- DSPy 的编译过程具体做了什么?
- 自动优化的 Prompt 可解释吗?
---

# OPRO 和 DSPy 是什么?它们如何实现自动 Prompt 优化

- **OPRO (Optimization by PROmpting)** 和 **DSPy** 是两种自动优化 Prompt 的方法。

- **OPRO (Google DeepMind):**
  - **核心思想**: 用 LLM 生成并优化 Prompt 文本本身，LLM 充当“优化器”。
  - **流程**: 输入元 Prompt（包含任务描述和评分历史） -> LLM 生成新的候选 Prompt -> 在测试集评估 -> 将最佳 Prompt 加入历史 -> 迭代。
  - **本质**: 元启发式搜索（类似进化算法），LLM 是变异/生成算子，适合优化自然语言指令的措辞。

- **DSPy (Stanford):**
  - **核心思想**: 将 Prompt Engineering 视为“模块化编程”问题，而非文本修改。
  - **流程**: 定义签名（Signature，输入/输出声明） -> 组合模块（Module） -> 编译器自动优化。
  - **编译器优化**: 自动尝试不同的 Prompt 策略（如 Few-shot 示例选择、Chain-of-Thought 指令）以提升指标。
  - **关键机制**: Bootstrap Few-shot（自动从训练集中挑选最有价值的示例）。

- **对比:**
  - **OPRO**: 优化单轮 Prompt 的自然语言措辞，适合简单任务。
  - **DSPy**: 优化整个程序的逻辑流和各模块的 Prompt，适合复杂 Agent 和多步流水线。

- **效果:** 在多数 benchmark 上，自动优化往往能超过人工经验调优 5-20%.

- **增强原理与技术细节:**
  - **DSPy 的 Teleprompter:** DSPy 中的优化器被称为 Teleprompter。它不仅仅选择示例，还可以自动决定是否启用 CoT（思维链），或者是否需要引入额外的子模块（如 `dspy.Retrieve`）来提升性能。
  - **评估指标:** 两者都依赖一个可执行的评估函数（Metric）。对于 OPRO，通常是简单的准确率或匹配分数；对于 DSPy，Metric 可以是更复杂的逻辑（例如：答案正确且引用了正确的上下文）。
  - **成本权衡:** 自动优化需要消耗大量的 Token 进行试错。通常在开发/离线阶段运行，将优化得到的“最佳 Prompt”或“Few-shot 示例列表”固化到生产环境中使用。

- **工作流程对比图:**

```text
OPRO 流程:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ 元 Prompt    │ -> │ LLM (优化器) │ -> │ 候选 Prompt  │
│ (Meta)       │    │              │    │ List         │
└──────────────┘    └──────────────┘    └──────┬───────┘
                                              │
                                              ▼
                                     ┌──────────────┐
                                     │  LLM (执行)  │ ◄─── 在测试集运行
                                     └──────┬───────┘
                                            │
                                            ▼
                                     ┌──────────────┐
                                     │  评估打分    │
                                     └──────┬───────┘
                                            │
             (Top prompts 加入 Meta Prompt) │
                                            └─> [循环]

DSPy 流程:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ 定义 Signature│ -> │ 组合 Module  │ -> │ 编译器       │
│ (Input/Output)│    │ (Chain)      │    │ (Teleprompter)│
└──────────────┘    └──────────────┘    └──────┬───────┘
                                              │
                     ┌────────────────────────┘
                     │
                     ▼
            ┌────────────────┐
            │ 训练集 + Metric │
            └────────┬───────┘
                     │
                     ▼
          ┌──────────────────────┐
          │ Bootstrap Few-shot   │ ◄─── 自动生成/挑选示例
          │ 或其他优化策略       │
          └──────────┬───────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │ 优化后的 Compiled    │
          │ Program (固化配置)   │ ──> 部署到生产环境
          └──────────────────────┘
```

## 常见考点
1. **DSPy 和 LangChain 的关系是什么？**
   - DSPy 不是 LangChain 的替代品，而是一种不同层次的抽象。LangChain 关注“链的编排”（如何调用工具、如何拼接字符串），DSPy 关注“程序的优化”（如何自动学习最佳的 Prompt 和 Few-shot 示例）。DSPy 可以运行在 LangChain 或原生 LLM 之上。
2. **当 LLM 底层模型切换时（如从 GPT-4 换到 Llama 3），如何处理？**
   - 这是 DSPy 的强项。由于 DSPy 将逻辑与 Prompt 文本解耦，只需重新运行“编译器”（Compiler），DSPy 会针对新模型重新寻找最佳的 Few-shot 示例和指令，而无需人工重写 Prompt。OPRO 也具备类似的模型无关性，只需重新运行优化循环。
3. **什么情况下不适合使用自动 Prompt 优化？**
   - 极其简单的任务（成本不划算）；对推理延迟极度敏感的场景（优化过程耗时）；或者任务指令本身需要极强的可解释性和安全性，自动生成的黑盒 Prompt 可能难以审计。
