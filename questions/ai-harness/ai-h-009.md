---
id: ai-h-009
difficulty: L3
category: ai-harness
subcategory: LLM框架
tags:
- IO
- IOC
- Elasticsearch
feynman:
  essence: React应用与LLM流式输出的标准化胶水。
  analogy: 像为React专门订做的水管，让大模型的文字像水流一样平滑地流进界面。
  first_principle: 如何在前端应用中无缝集成流式LLM响应？
  key_points:
  - 统一了不同模型商的调用接口
  - 提供useChat等React Hooks简化前端开发
  - 内置流式传输和RSC（服务端组件）支持
  - 主要服务于Next.js生态
follow_up:
- AI SDK的Tool Calling如何在前端使用?
- RSC集成有什么优势?
---

# Vercel AI SDK的核心设计是什么?它如何简化LLM应用的前端开发

- **Vercel AI SDK核心:**

前端(React/Vue/Svelte)与LLM API之间的桥接层.

- **三大模块:**
1. **AI SDK Core** - 统一的LLM调用接口(generateText/streamText)
2. **AI SDK UI** - 前端组件(useChat/useCompletion)
3. **AI SDK RSC** - React Server Components集成

- **核心优势 - 统一Provider:**
```typescript
// 换Provider只需改一行
import { openai } from '@ai-sdk/openai'
// import { anthropic } from '@ai-sdk/anthropic'

const result = await streamText({
  model: openai('gpt-4'),
  // model: anthropic('claude-3.5-sonnet'),
  prompt: 'Hello'
})
```

- **前端流式渲染:**
```tsx
const { messages, input, handleSubmit } = useChat()

return (
  <div>
    {messages.map(m => <div key={m.id}>{m.content}</div>)}
    <form onSubmit={handleSubmit}>
      <input value={input} onChange={handleInputChange} />
    </form>
  </div>
)
```

- **适合场景:**
- Next.js/React全栈LLM应用
- 需要流式UI的聊天/生成界面
- 快速原型开发
