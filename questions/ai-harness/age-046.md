---
id: age-046
difficulty: L2
category: ai-harness
subcategory: 生产工程化
feynman:
  essence: 用Golden Set定基准，用LLM-as-Judge自动化回归测试。
  analogy: 像自动化考试系统，有标准答案，机器帮老师改卷。
  first_principle: 如何在模型频繁迭代中快速保证服务质量不退化？
  key_points:
  - Golden Set提供人工标注的高质量基准数据
  - LLM-as-Judge实现快速自动化的效果评估
  - 结合Trace回放确保线上体验一致性
follow_up:
- Golden Set 需要多少条?
- 如何消除 LLM-as-Judge 的偏差?
---

# 什么是 Golden Set 和 LLM-as-Judge?如何在生产中构建 AI 评测体系

- **AI 评测体系** 是确保 Prompt 和模型变更不引入回归的核心手段.

- **Golden Set(黄金测试集):**
  - 人工标注的高质量测试用例(问题 + 标准答案 + 评分标准)
  - 覆盖核心场景和边界情况,通常 100-1000 条
  - 每次 Prompt 或模型变更后跑一遍
  - 用通过率衡量变更安全性

- **LLM-as-Judge:**
  - 用 GPT-4 或 Claude 评估另一个模型的输出质量
  - 评分维度:正确性、完整性、相关性、安全
  - 优势:比人工评估快 100 倍,比规则评估灵活
  - 风险:评判模型自身有偏差

- **评测流程:**
  1. 构建 Golden Set(人工标注)
  2. 每次变更跑 Golden Set
  3. 用 LLM-as-Judge 自动打分
  4. 低分样本人工复核
  5. 变更通过率 > 95% 才上线

- **Trace 回放:** 用历史真实请求的 Trace 数据,在新版本上回放,确保不影响线上体验.

- **系统架构示意:**
```text
┌──────────────────────────────────────────────────────┐
│            AI 评测流水线                      │
└──────────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        v               v               v
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ Golden Set  │ │  生产日志   │ │  在线AB测   │
│ (离线/标准) │ │ (Trace回放) │ │ (真实流量)  │
└──────┬──────┘ └──────┬──────┘ └──────┬──────┘
       │               │               │
       └───────────────┼───────────────┘
                       v
            ┌─────────────────────┐
            │   LLM Application   │
            │   (Prompt/Model V2) │
            └──────────┬──────────┘
                       │
          ┌────────────┼────────────┐
          v            v            v
   ┌──────────┐ ┌──────────┐ ┌──────────┐
   │ 人工评估 │ │规则评估  │ │LLM Judge │
   │(少量抽样)│ │(关键词/  │ │ (批量打分)│
   │          │ │ 格式校验)│ │          │
   └─────┬────┘ └─────┬────┘ └─────┬────┘
         └───────┬──────┴───────┬────┘
                 v              v
           ┌─────────────┐ ┌───────────┐
           │ 质量报告    │ │ 决策通过? │
           │ (分数/通过率)│ │(Pass/Fail)│
           └─────────────┘ └───────────┘
```

## 常见考点
1. **LLM-as-Judge 的准确性如何保证?**
   可以使用“黄金参考集”来校准 Judge 模型。即让 Judge 评估一批已知分数的数据,计算其与真实分数的相关性。此外,可采用“多数投票法”让多个不同的 Judge 模型评估同一条数据,取平均分.

2. **如何评估开放式生成任务(如创意写作)?**
   这类任务很难有唯一标准答案。通常采用“相对评估”模式,让 Judge 模型对比 V1 和 V2 的输出,判断哪个更好,而不是打绝对分数.

3. **自动化评测与真实用户体验的偏差?**
   自动评测可能过分关注“事实正确性”,而忽略了用户体验中的“语气”、“风格”或“互动感”。因此需要建立线上反馈闭环,收集用户点赞/点踩数据来校准评测权重.
