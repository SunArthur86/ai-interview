---
id: ai-h-034
difficulty: L2
category: ai-harness
subcategory: RAG工程化
tags:
- CMS
- IOC
- 索引
- Kafka
feynman:
  essence: 仅处理变更文档，通过Upsert机制实现向量库的局部高效更新。
  analogy: 像编辑文档一样，只修改有错别字的那一页，而不是把整本书重印一遍。
  first_principle: 如何在数据频繁变更时最小化索引构建的计算成本和时间开销？
  key_points:
  - 通过哈希或时间戳检测文档变更
  - 利用Upsert操作删除旧向量并插入新向量
  - 采用双缓冲或后台构建实现零停机更新
  - 大规模数据应使用分区索引和异步队列
follow_up:
- 向量库的upsert性能如何?
- 分区索引如何设计?
---

# RAG知识库如何实现增量更新?大规模文档变更时索引如何高效更新

- **增量更新策略:**

1. **文档变更检测**
- 文件哈希(MD5/SHA256)对比
- 修改时间戳检查
- Webhook通知(CMS集成)

2. **增量索引**
- 只对变更文档重新分块+embedding
- 删除旧向量，插入新向量
- 大多数向量数据库支持upsert

3. **批量更新Pipeline**
```
变更检测 → 增量分块 → 批量embedding → 向量库upsert → 索引刷新
```

4. **双缓冲更新**
- 新索引在后台构建
- 构建完成后原子切换
- 零停机更新

- **大规模场景(百万文档):**
- **分区索引** - 按时间/类别分区，只更新变更分区
- **异步队列** - Kafka/SQS排队处理
- **并行embedding** - 多GPU批量计算
- **Qdrant/Milvus的批量upsert API**

- **删除处理:**
- 软删除(标记deleted=true，检索时过滤)
- 定期硬删除(低峰期清理)
