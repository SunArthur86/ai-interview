---
id: "age-045"
difficulty: "L2"
category: "ai-harness"
subcategory: "生产工程化"
feynman:
  essence: "Token 成本管理：① 请求级追踪（每次请求记 input/output tokens × 单价）；② 按租户/功能/模型维度归因；③ 预算与配额（超额限流或降级）；④ 优化手段（模型路由、语义缓存、Prompt 精简、批处理）；⑤ 报表与告警（日/周成本、异常突增告警）。"
  analogy: "Agent 成本管控就像家庭理财——跟踪每笔支出（Token消耗），区分必需消费和浪费（缓存命中/模型路由优化），在预算内最大化效果。"
  key_points:
    - "Token 成本管理 是生产 AI 应用的核心运营指标."
    - "1. 请求级追踪: 每次请求记录 input_tokens、output_tokens、模型、单价,计算实际费用."
    - "2. 功能级分账: 按 API 端点或功能模块分组统计.如 RAG 检索花了多少、代码生成花了多少."
first_principle:
  problem: "从第一性原理看：生产 AI 应用中,如何做 Token 成本归因和优化 的根本优势/劣势来源于什么？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "语义缓存如何判断两个请求是否相似?"
  - "如何设定每个用户的 Token 配额?"
---

# 生产 AI 应用中,如何做 Token 成本归因和优化

- **Token 成本管理** 是生产 AI 应用的核心运营指标.

- **成本归因:**

- **1. 请求级追踪:** 每次请求记录 input_tokens、output_tokens、模型、单价,计算实际费用.

- **2. 功能级分账:** 按 API 端点或功能模块分组统计.如 RAG 检索花了多少、代码生成花了多少.

- **3. 用户级分账:** 按用户/租户统计用量,用于计费和配额管理.

- **4. 成本标签:** 给每个请求打标签(项目、环境、实验版本),支持多维度切片分析.

- **优化手段:**

- **1. 模型降级:** 简单任务用小模型(GPT-4o-mini、DeepSeek-V3),复杂任务才用大模型

- **2. Prompt 压缩:** 减少冗余上下文、精简 System Prompt

- **3. 缓存:** Prefix Caching、语义缓存(相似请求返回缓存结果)

- **4. 批处理:** Batch API 通常有 50% 折扣

- **5. 上下文管理:** 定期压缩对话历史、限制工具返回长度
