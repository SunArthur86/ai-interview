---
id: ai-h-025
difficulty: L2
category: ai-harness
subcategory: 可观测性
tags:
- IO
- GC
- Elasticsearch
feynman:
  essence: LLM应用的全链路调试、评估与可视化平台
  analogy: 软件的黑盒录像机，能回放每一步思考
  first_principle: 如何Debug和评估一个不可解释的黑盒模型？
  key_points:
  - 可视化完整的调用链路
  - 基于Trace构建测试数据集
  - 集成LLM-as-judge自动评估
  - 支持Prompt版本A/B测试
follow_up:
- 如何从Trace创建评估数据集?
- LangSmith的LLM-as-judge如何配置?
---

# LangSmith如何用于LLM应用的可观测性?核心功能有哪些

- **LangSmith核心功能:**

1. **Trace(追踪)** - 可视化每一步的执行链,每个LLM调用的完整input/output,Token消耗和延迟

2. **Datasets(数据集)** - 从生产Trace中创建评估数据集,手动标注/修正

3. **Evaluation(评估)** - 在数据集上运行应用,自动评估(LLM-as-judge),版本对比

4. **Playground(实验场)** - 修改prompt后立即测试,A/B对比效果

- **调用链路与数据流:**

```text
App (LangChain/LangGraph)
   │
   ├─> LangChain SDK (自动捕获上下文)
   │      │
   │      └─> POST /runs (发送到LangSmith API)
   │             │
   │             ▼
   ┌─────────────────────────────┐
   │      LangSmith Platform     │
   │  ┌───────┐ ┌───────┐ ┌─────┐│
   │  │ Traces│ │  Eval │ │Data ││
   │  └───────┘ └───────┘ └─────┘│
   └─────────────────────────────┘
```

- **集成方式:**
设置环境变量`LANGCHAIN_TRACING_V2=true`和`LANGCHAIN_API_KEY`,自动追踪所有LangChain/LangGraph调用

- **非LangChain应用:** 使用LangSmith SDK手动添加trace (需构建 RunTree 结构)

- **定价:** 免费5K traces/月,之后按量付费

- **## 常见考点**
1. LangSmith 和 OpenTelemetry 的关系是什么？（目前不兼容，需用专用SDK）
2. 如何在 Trace 中隐藏敏感信息（如 PII 数据）？（使用 `run_tree` 的 `extra` 参数或配置过滤器）
3. 评估器的“LLM-as-a-Judge”如何保证客观性？（使用自定义 Rubric 或 多模型投票）
