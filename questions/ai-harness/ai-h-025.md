---
id: "ai-h-025"
difficulty: "L2"
category: "ai-harness"
subcategory: "可观测性"
tags:
  - "IO"
  - "GC"
  - "Elasticsearch"
feynman:
  essence: "- *LangSmith核心功能:** 1. **Trace(追踪)** - 可视化每一步的执行链,每个LLM调用的完整input/output,Token消耗"
  analogy: "Agent 可观测性就像飞机的黑匣子——记录每一次思考（Thought）、行动（Action）、观察（Observation），出问题时能回放完整链路定位根因。"
  key_points:
    - "LangSmith核心功能:"
    - "Trace(追踪) - 可视化每一步的执行链,每个LLM调用的完整input/output,Token消耗和延迟"
    - "Datasets(数据集) - 从生产Trace中创建评估数据集,手动标注/修正"
first_principle:
  problem: "为什么需要 LangSmith如何用于LLM应用的可观测性?核心功能有哪些？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "如何从Trace创建评估数据集?"
  - "LangSmith的LLM-as-judge如何配置?"
---

# LangSmith如何用于LLM应用的可观测性?核心功能有哪些

- **LangSmith核心功能:**

1. **Trace(追踪)** - 可视化每一步的执行链,每个LLM调用的完整input/output,Token消耗和延迟

2. **Datasets(数据集)** - 从生产Trace中创建评估数据集,手动标注/修正

3. **Evaluation(评估)** - 在数据集上运行应用,自动评估(LLM-as-judge),版本对比

4. **Playground(实验场)** - 修改prompt后立即测试,A/B对比效果

- **集成方式:**
设置环境变量LANGCHAIN_TRACING_V2=true和LANGCHAIN_API_KEY,自动追踪所有LangChain/LangGraph调用

- **非LangChain应用:** 使用LangSmith SDK手动添加trace

- **定价:** 免费5K traces/月,之后按量付费
