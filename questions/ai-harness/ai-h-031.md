---
id: "ai-h-031"
difficulty: "L3"
category: "ai-harness"
subcategory: "工程实践"
tags:
  - "IO"
  - "Elasticsearch"
feynman:
  essence: "- *LiteLLM核心:统一API接口** 用一个接口调用所有LLM供应商(OpenAI/Anthropic/Zhipu/DeepSeek/本地Ollama等"
  analogy: "模型路由就像医院分诊——简单问题看全科（小模型），复杂问题看专家（大模型），按难度分配资源控成本。"
  key_points:
    - "LiteLLM核心:统一API接口"
    - "统一接口 - 100+供应商,一套SDK"
    - "负载均衡 - 多个API Key轮询"
first_principle:
  problem: "为什么需要 LiteLLM和模型网关的作用?如何管理多供应商LLM？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "LiteLLM如何做Fallback?"
  - "模型网关如何部署?"
---

# LiteLLM和模型网关的作用是什么?如何管理多供应商LLM

- **LiteLLM核心:统一API接口**

用一个接口调用所有LLM供应商(OpenAI/Anthropic/Zhipu/DeepSeek/本地Ollama等).

- **核心功能:**
1. **统一接口** - 100+供应商,一套SDK
2. **负载均衡** - 多个API Key轮询
3. **Fallback** - 主供应商失败自动切换
4. **成本追踪** - 自动计算Token费用
5. **限流** - 按用户/项目限流
6. **缓存** - 相同请求缓存结果

- **使用方式:**
```python
from litellm import completion

# 统一接口,只需改model名
response = completion(model="gpt-4", messages=[...])
response = completion(model="claude-3-5-sonnet", messages=[...])
response = completion(model="zhipu/glm-4", messages=[...])
```

- **模型网关(Proxy模式):**
- 部署为独立服务
- 所有应用通过网关访问LLM
- 统一计费/日志/限流/缓存

- **vs 直接调用各供应商API:**
- 优势:统一管理、故障转移、成本控制
- 劣势:增加一层延迟、额外运维
