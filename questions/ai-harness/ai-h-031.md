---
id: ai-h-031
difficulty: L3
category: ai-harness
subcategory: 工程实践
tags:
- IO
- Elasticsearch
feynman:
  essence: 屏蔽模型差异的统一接口与流量管理层
  analogy: 万能转接头，插哪个电器都能用，还能自动断电保护
  first_principle: 如何简化多模型供应商的集成与运维复杂度？
  key_points:
  - 统一不同模型的调用格式
  - 多Key轮询实现负载均衡
  - 主备切换保障服务可用性
  - 网关层统一管控成本与配额
follow_up:
- LiteLLM如何做Fallback?
- 模型网关如何部署?
---

# LiteLLM和模型网关的作用是什么?如何管理多供应商LLM

- **LiteLLM核心:统一API接口**

用一个接口调用所有LLM供应商(OpenAI/Anthropic/Zhipu/DeepSeek/本地Ollama等).

- **网关架构图:**
```text
Client Application
      │
      │ (Standard OpenAI Format: POST /v1/chat/completions)
      ▼
┌──────────────────────────────────────┐
│         LiteLLM Proxy Server         │
│  ┌────────────┐   ┌──────────────┐   │
│  │   Router   │──>│ Load Balancer│   │
│  └────────────┘   └──────────────┘   │
│       │                                  │
│       │  (Model Name Mapping)            │
│       ▼                                  │
│  ┌──────────┐  ┌──────────┐  ┌────────┐│
│  │ OpenAI   │  │ Anthropic│  │  Local ││
│  │ Provider │  │ Provider │  │  Ollama││
│  └──────────┘  └──────────┘  └────────┘│
└──────────────────────────────────────┘
```

- **核心功能:**
1. **统一接口** - 100+供应商,一套SDK (`completion(model="provider/model")`)
2. **负载均衡** - 多个 API Key 轮询,支持权重设置 (`litellm.set_weight`) 
3. **Fallback** - 主供应商失败自动切换 (配置 `fallbacks` 列表)
4. **成本追踪** - 自动计算 Token 费用,支持导出至 LangSmith/Datadog
5. **限流** - 按用户/项目限流 (`rpm_limit`, `tpm_limit`)
6. **缓存** - 相同请求缓存结果 (支持 Redis/S3)

- **使用方式:**
```python
from litellm import completion

# 统一接口,只需改model名
response = completion(model="gpt-4", messages=[...])
response = completion(model="claude-3-5-sonnet", messages=[...])
response = completion(model="zhipu/glm-4", messages=[...])
```

- **模型网关:**
- 部署为独立服务 (`litellm --config /path/to/config.yaml`)
- 所有应用通过网关访问LLM
- 统一计费/日志/限流/缓存

- **vs 直接调用各供应商API:**
- 优势:统一管理、故障转移、成本控制
- 劣势:增加一层延迟 (通常 <10ms),额外运维

- **## 常见考点**
1. 当多个 API Key 都达到限额时，LiteLLM 如何处理？（进入 `cooldown` 模式，暂停请求并记录日志，或者抛出 RateLimitError）
2. 如何处理非 OpenAI 格式的模型参数？（LiteLLM 会自动映射，例如将 `max_tokens` 转换为 `max_new_tokens` 给 HuggingFace 模型）
3. 如何保证网关的高可用？（启动多个网关实例前使用 Nginx/Envoy 做负载均衡，配置健康检查）
