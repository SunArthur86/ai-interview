---
id: "age-037"
difficulty: "L4"
category: "ai-harness"
subcategory: "Agent工程化"
tags:
  - "CAS"
feynman:
  essence: "- *模型路由** 是根据任务特征选择不同模型,平衡效果、成本和延迟. - *路由策略:** - *1. 规则路由:** 根据任务类型硬编码.简单任务(如格式转"
  analogy: "模型路由就像医院分诊——简单问题看全科（小模型），复杂问题看专家（大模型），按难度分配资源控成本。"
  key_points:
    - "模型路由 是根据任务特征选择不同模型,平衡效果、成本和延迟."
    - "1. 规则路由: 根据任务类型硬编码.简单任务(如格式转换)用小模型,复杂任务(如架构设计)用大模型.响应最快,但灵活性差."
    - "2. 模型路由(LLM Router): 用一个小模型先判断任务复杂度,再路由到对应模型.灵活但增加了一次推理延迟."
first_principle:
  problem: "为什么需要 生产环境如何设计 Agent 的模型路由和 fallback 策略？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "如何评估路由策略的效果?"
  - "级联路由如何设置置信度阈值?"
---

# 生产环境如何设计 Agent 的模型路由和 fallback 策略

- **模型路由** 是根据任务特征选择不同模型,平衡效果、成本和延迟.

- **路由策略:**

- **1. 规则路由:** 根据任务类型硬编码.简单任务(如格式转换)用小模型,复杂任务(如架构设计)用大模型.响应最快,但灵活性差.

- **2. 模型路由(LLM Router):** 用一个小模型先判断任务复杂度,再路由到对应模型.灵活但增加了一次推理延迟.

- **3. 级联路由(Cascade):** 先用便宜模型尝试,如果置信度低再升级到大模型.成本最优但延迟波动大.

- **Fallback 策略:**
- 模型超时 -> 切换到备用供应商
- API 限流 -> 排队重试 + 指数退避
- 输出质量差 -> 自动重试 + Prompt 调整
- 完全失败 -> 返回缓存结果或人工接管

- **关键指标:** 路由准确率、平均成本、P99 延迟、fallback 触发率.
