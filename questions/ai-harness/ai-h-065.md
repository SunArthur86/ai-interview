---
id: "ai-h-065"
difficulty: "L3"
category: "ai-harness"
subcategory: "工程化实践"
tags:
  - "IOC"
  - "Redis"
feynman:
  essence: "**语义缓存** 不做精确匹配,而是基于**语义相似度**判断是否命中缓存."
  analogy: "语义缓存就像客服知识库——相似的问题不用每次重新查（LLM推理），直接从历史回答中找最相似的复用，省钱又快。"
  key_points:
    - "语义缓存 不做精确匹配,而是基于语义相似度判断是否命中缓存."
    - "普通缓存 vs 语义缓存"
    - "用户 Query → Embedding Model → Query Vector"
first_principle:
  problem: "它们本质上为什么不同？各自的设计目标和适用场景是什么？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "如何处理'差一个字但意思完全不同'的情况?"
  - "语义缓存如何做 A/B 测试?"
---

# 语义缓存(Semantic Cache)如何实现?它和普通缓存有什么本质区别

- **语义缓存** 不做精确匹配,而是基于**语义相似度**判断是否命中缓存.

- **普通缓存 vs 语义缓存**

| 维度 | 普通缓存 | 语义缓存 |
|------|---------|---------|
| 匹配方式 | key 精确匹配 | 向量相似度 |
| 命中条件 | 完全相同 | 语义相同('年假几天' = '有多少天年假') |
| 存储 | KV Store (Redis) | 向量数据库 (Redis/Chroma) |
| 命中率 | 低(~5%) | 高(~20-40%) |

- **实现流程**

```
1. 用户 Query → Embedding Model → Query Vector
2. Query Vector → 向量数据库 → Top-K 相似搜索
3. 相似度 > 阈值(如 0.92)? → 缓存命中 → 返回
4. 相似度 < 阈值? → 缓存未命中 → 调 LLM → 存入缓存
```

- **关键设计决策**

1. **相似度阈值**
- 太高(0.98)→ 命中率低,省不了钱
- 太低(0.85)→ 可能返回错误答案
- 推荐:0.90~0.95,配合人工抽检调优

2. **缓存有效期 (TTL)**
- 事实型问题:长 TTL(7天)
- 时效型问题:短 TTL(1小时)或不缓存
- 用 LLM 分类 query 类型自动设置 TTL

3. **防止'串味'**
- 多租户隔离:不同用户的缓存分开
- 角色/Prompt 隔离:不同 system prompt 的缓存分开
- 方法:将 system_prompt_hash + user_query_vector 作为复合 key

4. **成本收益**
- Embedding 成本(~$0.0001/query)远低于 LLM 调用(~$0.02/query)
- 缓存命中率 30% → 节省 ~30% API 成本
- 典型 ROI:语义缓存的基建成本在 1-2 个月内回本
