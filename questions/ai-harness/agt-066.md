---
id: agt-066
difficulty: L3
category: ai-harness
subcategory: 生产工程化
tags:
- ai-harness
feynman:
  essence: 通过并发、流式和复用机制提升系统吞吐与响应速度。
  analogy: 像餐厅优化：多桌同时上菜（并行）、先上开胃菜（流式）、复用餐具（连接池）。
  first_principle: 如何最小化系统响应时间并最大化资源利用率？
  key_points:
  - 异步IO避免阻塞等待
  - 流式输出改善用户感知延迟
  - 多层缓存减少重复请求
---

# 性能优化

### 性能优化

**核心目标**：解决 Agent 系统中的串行 LLM 调用、高延迟网络 IO 和上下文膨胀问题。

**原理详解**：

1. **异步处理**：
   - 使用 `asyncio` 或消息队列，避免 I/O 等待阻塞线程。
   - **并行工具调用**：无依赖的工具调用并发执行，大幅降低总耗时。

2. **流式输出**：
   - 利用 SSE (Server-Sent Events) 边生成边返回，降低首字延迟 (TTFB)。
   - 需处理流式中断情况，保证最终状态一致性。

3. **并发控制**：
   - 使用 `Semaphore` 限制对同一模型的并发请求数，防止触发 429 (Rate Limit)。

4. **连接复用**：
   - HTTP 客户端开启 `keep-alive`，数据库使用连接池，减少握手开销。

5. **缓存分层**：
   - **L1 (进程内)**：LRU 缓存，极低延迟。
   - **L2 (Redis)**：分布式共享，支持 TTL。
   - 读路径：L1 -> L2 -> 回源模型。
