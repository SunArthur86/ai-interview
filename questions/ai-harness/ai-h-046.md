---
id: "ai-h-046"
difficulty: "L2"
category: "ai-harness"
subcategory: "工程实践"
tags:
  - "IO"
  - "Elasticsearch"
feynman:
  essence: "LLM 应用 A/B 测试：① 明确指标（任务成功率、准确率、用户满意度、延迟、成本）；② 随机分流（按用户/请求哈希，保证组间可比）；③ 样本量（用功效分析算最小样本，避免假显著）；④ 同期对照（避免时间偏差）；⑤ 多重比较校正；⑥ 不仅看均值还要看分布（长尾）；统计显著性用 t 检验/卡方，效应量看 Cohen d。"
  analogy: "Harness Engineering 就像给 AI 搭建完整的施工脚手架——不只是调 LLM API，而是构建提示词管理、工具编排、错误处理、监控的完整工程框架。"
  key_points:
    - "主指标:任务成功率 / 用户满意度 / 准确率"
    - "辅助指标:延迟 / 成本 / Token消耗"
    - "护栏指标:幻觉率 / 安全违规率"
first_principle:
  problem: "为什么需要 LLM应用的A/B测试如何设计?统计显著性如何保证？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "序贯检验如何提前终止实验?"
  - "如何处理多变量测试?"
---

# LLM应用的A/B测试如何设计?统计显著性如何保证

- **A/B测试设计:**

- **1. 定义指标**
- 主指标:任务成功率 / 用户满意度 / 准确率
- 辅助指标:延迟 / 成本 / Token消耗
- 护栏指标:幻觉率 / 安全违规率

- **2. 样本量计算**
```
# 最小样本量(近似公式)
n = 16 * σ² / Δ²

σ = 指标标准差
Δ = 最小可检测效应

例:σ=0.2, Δ=0.05 → n=1280 per group
```

- **3. 流量分配**
- 50/50均分(统计功效最强)
- 90/10金丝雀(先小流量验证)
- 基于用户ID的确定性分桶

- **4. 统计检验**
```python
from scipy import stats

# 比例检验(成功率)
z_stat, p_value = stats.proportions_ztest(
[success_a, success_b],
[total_a, total_b]
)

if p_value < 0.05:
print("显著差异")
```

- **5. 常见陷阱**
- 多重检验问题(同时测多个指标→Bonferroni校正)
- 新鲜效应(新版本短期效果不代表长期)
- 幸存者偏差(只看完成用户)
- Simpson's Paradox(子群体结论与总体矛盾)
