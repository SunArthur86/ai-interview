---
id: age-023
difficulty: L2
category: ai-harness
subcategory: 工程化实践
feynman:
  essence: 按业务风险等级分配资源与策略
  analogy: VIP通道：重要的事找专家盯着，不重要的事找实习生随便弄。
  first_principle: 如何平衡成本、速度与业务风险？
  key_points:
  - 关键路径用强模型+人工介入+审计
  - 非关键路径用小模型+重缓存
  - 根据失败容忍度差异化降级
---

# 如何做「关键路径」与「非关键路径」分级

### 核心理念：风险分级，差异化治理

#### 1. 定义分级标准
- **关键路径**：涉及资金交易、数据删除、权限变更、核心业务决策（如医疗诊断、法律建议）。
  - *特征*：错误容忍度极低，不可逆，合规性要求高。
- **非关键路径**：闲聊、草稿生成、内容摘要、内部知识检索、辅助性代码生成。
  - *特征*：允许一定偏差，用户体验为主，追求速度和低成本。

#### 2. 关键路径：强一致性与人机协同
- **模型选择**：最强推理能力模型（如 GPT-4, Claude 3.5 Sonnet），低 Temperature (0~0.1) 确保确定性与逻辑性。
- **HITL (Human In The Loop)**：
  - **Pre-check**：关键操作前要求用户二次确认。
  - **Post-review**：生成结果后人工审核（或仅标记为待审核状态）。
- **审计**：全量记录输入、输出、中间推理步骤，保存至不可篡改存储（如 WORM 存储），用于合规审计。

#### 3. 非关键路径：高吞吐与容错
- **模型选择**：小模型或低成本模型（如 GPT-4o-mini, Llama-3-8B），较高 Temperature (0.7~1.0) 增加创造性。
- **超时策略**：设置宽松但合理的超时，避免阻塞用户，超时则直接降级或返回占位符。
- **缓存策略**：对高频相似 Query 开启激进的语义缓存。
- **降级策略**：服务不可用时返回友好的兜底话术，而非错误堆栈。

```text
┌──────────────────────┐
│      业务请求进来      │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│   业务类型分类器      │
└──────┬─────────┬──────┘
       │         │
  关键  │         │  非关键
       ▼         ▼
┌──────────────────┐ ┌──────────────────────┐
│ 关键路径处理     │ │ 非关键路径处理        │
│ ---------------- │ │ -------------------- │
│ 1. 高精模型      │ │ 1. 轻量/小模型       │
│ 2. 低 Temp       │ │ 2. 高 Temp/Creative  │
│ 3. HITL 审核流   │ │ 3. 语义缓存          │
│ 4. 强制审计日志  │ │ 4. 快速失败/降级     │
└────────┬─────────┘ └──────────┬───────────┘
         │                     │
         ▼                     ▼
┌──────────────────┐ ┌──────────────────────┐
│  高置信度/高风险  │ │   低置信度/低成本     │
│  结果输出        │ │   结果输出           │
└──────────────────┘ └──────────────────────┘
```

## 常见考点
1. **如何自动判断请求属于哪条路径**？
   - 答：可以通过 Prompt 分类器（一个小模型）先行判断意图；或者在路由层通过关键字或元数据（如 `category: 'finance'`）规则匹配。
2. **如果关键路径模型太慢导致超时怎么办**？
   - 答：采用异步处理模式。前端先返回“处理中”，后台通过 WebSocket 或回调通知结果；若长时间未出结果，转人工介入。
3. **非关键路径的缓存如何保证一致性**？
   - 答：通常非关键路径对一致性要求不高，可以设置较短的 TTL（如 5-10 分钟）。或者采用基于 Embedding 的语义缓存，设置相似度阈值。
