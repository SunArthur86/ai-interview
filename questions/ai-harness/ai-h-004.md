---
id: ai-h-004
difficulty: L3
category: ai-harness
subcategory: LLM框架
tags:
- GC
- IOC
- 索引
feynman:
  essence: 基于强类型DAG的工业级检索管道。
  analogy: 像搭乐高积木，用特定形状的接口（类型）强行保证拼装不出错，还能用图纸（YAML）描述。
  first_principle: 如何构建模块化、可验证且易于维护的流水线系统？
  key_points:
  - 核心设计是DAG（有向无环图）Pipeline
  - 强类型检查保证组件连接安全
  - 支持YAML配置便于非代码部署
  - 内置评估与监控，适合生产环境
follow_up:
- Haystack的YAML配置如何写?
- 组件如何自定义?
---

# Haystack框架的核心设计是什么?它和LangChain/LlamaIndex有什么区别

- **Haystack (deepset) 核心设计:Pipeline + Component**

```python
# 声明式Pipeline
pipeline = Pipeline()
pipeline.add_component("embed", embedder)
pipeline.add_component("retriever", retriever)
pipeline.add_component("reader", reader)
pipeline.connect("embed.embedding", "retriever.query_embedding")
pipeline.connect("retriever.documents", "reader.documents")
```

- **核心区别:**

| | LangChain | LlamaIndex | Haystack |
|--|-----------|------------|----------|
| 设计 | 链式调用 | 数据索引 | **DAG Pipeline** |
| 类型安全 | 弱 | 弱 | **强** |
| 生产就绪 | 中 | 中 | **高** |
| 评估 | 需外接 | 内置基础 | **内置评估** |
| 配置 | 代码 | 代码 | **YAML配置** |

- **Haystack优势:**
1. **类型安全** - 组件间连接有类型检查
2. **YAML配置** - Pipeline可用YAML定义
3. **生产就绪** - 日志/监控/错误处理完善
4. **评估集成** - 内置评估pipeline

- **适合场景:** 企业级RAG系统、需要严格类型检查的生产环境
