---
id: "ai-h-005"
difficulty: "L3"
category: "ai-harness"
subcategory: "LLM框架"
tags:
  - "IO"
  - "GC"
  - "IOC"
  - "Elasticsearch"
feynman:
  essence: "LangChain 的 LCEL（LangChain Expression Language）是线性 DAG，难以表达循环、条件、状态；LangGraph 把 Agent 建模成「状态机/图」——节点是函数、边是条件跳转，原生支持循环（ReAct 的多轮）、人工介入、持久化状态、子图。当你需要 Agent 多轮推理、带记忆、需人工审批、或复杂分支循环时该用 LangGraph。"
  analogy: "Harness Engineering 就像给 AI 搭建完整的施工脚手架——不只是调 LLM API，而是构建提示词管理、工具编排、错误处理、监控的完整工程框架。"
  key_points:
    - "LangChain LCEL的局限:"
    - "循环 - 无法实现「执行→检查→重试」循环"
    - "条件分支 - 只能线性管道"
first_principle:
  problem: "为什么需要 LangGraph解决了LangChain的什么问题?什么时候应该用LangGraph？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "LangGraph的Checkpointer如何工作?"
  - "如何实现Human-in-the-Loop?"
---

# LangGraph解决了LangChain的什么问题?什么时候应该用LangGraph

- **LangChain LCEL的局限:**

LCEL是DAG(有向无环图),不支持:
1. **循环** - 无法实现「执行→检查→重试」循环
2. **条件分支** - 只能线性管道
3. **状态管理** - 无全局状态
4. **中断/恢复** - 无法暂停

- **LangGraph解决方案:StateGraph**

```python
# 定义全局状态
class State(TypedDict):
**messages：** list
**current_node：** str
**retry_count：** int

# 构建图
graph = StateGraph(State)
graph.add_node("agent", agent_node)
graph.add_node("tools", tool_node)
graph.add_edge(START, "agent")
graph.add_conditional_edges("agent", should_continue,
{"continue": "tools", "end": END})
graph.add_edge("tools", "agent")  # 循环!

app = graph.compile(checkpointer=MemorySaver())
```

- **什么时候用LangGraph:**
1. 需要循环(Agent重试/反思/多轮)
2. 需要条件分支(路由/审批)
3. 需要全局状态管理
4. 需要中断/恢复(Human-in-the-Loop)
5. 多Agent协作编排

- **什么时候不用:**
- 简单的prompt→model→parser → 用LCEL
- 无状态的API调用 → 用普通函数
