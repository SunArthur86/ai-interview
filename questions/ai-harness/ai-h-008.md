---
id: ai-h-008
difficulty: L3
category: ai-harness
subcategory: LLM框架
tags:
- IO
- IOC
- Elasticsearch
feynman:
  essence: 在生成时刻强行屏蔽非法Token。
  analogy: 像给键盘加个罩子，按下错键根本没反应，保证打出来的全是合法字符。
  first_principle: 如何从数学概率上保证LLM输出的结构严格符合要求？
  key_points:
  - 基于有限状态机（FSM）限制Token生成
  - 修改Logits屏蔽非法路径，保证100%合法
  - 无需重试，支持正则和语法约束
  - 适用于开源模型和非Function Calling场景
follow_up:
- 约束解码对推理速度有多大影响?
- 如何定义自定义Grammar?
---

# Outlines库如何实现100%结构化输出?约束解码的原理是什么

- **Outlines核心:约束解码**

**传统方式：** LLM生成文本 → 解析 → 可能失败
**约束解码：** **在推理时强制LLM只能生成符合格式的token**

- **原理:**
1. 定义目标格式(JSON Schema / Regex / Pydantic)
2. 转换为有限状态机(FSM)
3. 每生成一个token,检查FSM允许的下一个token集合
4. 将logits中不允许的token概率设为-∞
5. softmax后只选合法token

- **效果:100%格式正确,0重试**

```python
from outlines import models, generate

# 定义schema
class User(BaseModel):
    name: str
    age: int

# 加载模型 + 约束
model = models.transformers("mistral-7b")
generator = generate.json(model, User)

# 输出保证是合法User
user = generator("提取用户信息:张三25岁")
# 100%合法,无需重试
```

- **vs Function Calling:**
| | Function Calling | Outlines |
|--|-----------------|----------|
| 依赖 | API支持 | **开源模型即可** |
| 准确率 | ~99% | **100%** |
| 灵活性 | API限制 | **Regex/JSON/Grammar** |
| 模型限制 | 仅特定模型 | **任何模型** |
| 性能 | 无额外开销 | **轻微推理开销** |
