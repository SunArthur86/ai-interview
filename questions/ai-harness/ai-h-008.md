---
id: "ai-h-008"
difficulty: "L3"
category: "ai-harness"
subcategory: "LLM框架"
tags:
  - "IO"
  - "IOC"
  - "Elasticsearch"
feynman:
  essence: "**Outlines核心:约束解码(Constrained Decoding)**"
  analogy: "Harness Engineering 就像给 AI 搭建完整的施工脚手架——不只是调 LLM API，而是构建提示词管理、工具编排、错误处理、监控的完整工程框架。"
  key_points:
    - "Outlines核心:约束解码(Constrained Decoding)"
    - "定义目标格式(JSON Schema / Regex / Pydantic)"
    - "转换为有限状态机(FSM)"
first_principle:
  problem: "剥离所有术语：Outlines库如何实现100%结构化输出?约束解码的原理 底层在做什么？为什么这样做是最优的？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "约束解码对推理速度有多大影响?"
  - "如何定义自定义Grammar?"
---

# Outlines库如何实现100%结构化输出?约束解码的原理是什么

- **Outlines核心:约束解码(Constrained Decoding)**

**传统方式：** LLM生成文本 → 解析 → 可能失败
**约束解码：** **在推理时强制LLM只能生成符合格式的token**

- **原理:**
1. 定义目标格式(JSON Schema / Regex / Pydantic)
2. 转换为有限状态机(FSM)
3. 每生成一个token,检查FSM允许的下一个token集合
4. 将logits中不允许的token概率设为-∞
5. softmax后只选合法token

- **效果:100%格式正确,0重试**

```python
from outlines import models, generate

# 定义schema
class User(BaseModel):
**name：** str
**age：** int

# 加载模型 + 约束
model = models.transformers("mistral-7b")
generator = generate.json(model, User)

# 输出保证是合法User
user = generator("提取用户信息:张三25岁")
# 100%合法,无需重试
```

- **vs Function Calling:**
| | Function Calling | Outlines |
|--|-----------------|----------|
| 依赖 | API支持 | **开源模型即可** |
| 准确率 | ~99% | **100%** |
| 灵活性 | API限制 | **Regex/JSON/Grammar** |
| 模型限制 | 仅特定模型 | **任何模型** |
| 性能 | 无额外开销 | **轻微推理开销** |
