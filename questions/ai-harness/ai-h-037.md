---
id: "ai-h-037"
difficulty: "L2"
category: "ai-harness"
subcategory: "工程实践"
tags:
  - "IO"
  - "IOC"
feynman:
  essence: "选 RAG 还是微调：知识频繁变化、需要可溯源用 RAG（改知识库即可）；改变模型风格/格式/能力、知识相对稳定用微调。二者结合最优：微调教模型「怎么答」（格式、风格、领域推理能力），RAG 提供「答什么」（最新事实知识），先微调打底再 RAG 补实时信息。"
  analogy: "Harness Engineering 就像给 AI 搭建完整的施工脚手架——不只是调 LLM API，而是构建提示词管理、工具编排、错误处理、监控的完整工程框架。"
  key_points:
    - "RAG vs 微调决策矩阵:"
    - "微调教格式/风格 + RAG提供知识"
    - "微调让模型学会输出特定格式"
first_principle:
  problem: "为什么需要 什么时候用RAG,什么时候微调?两者如何结合？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "Harness Engineering 的核心是工程化——把 LLM 的潜力通过系统设计转化为可靠的生产力"
    - "评测驱动开发——没有 Golden Set 和持续评测，AI 系统就是黑盒"
    - "LLM 应用的可靠性 = 提示工程 + 错误处理 + 降级策略 + 可观测性"
  rebuild: "从工程化出发：① 为什么 LLM 应用需要 Harness？② 可观测性的核心指标？③ 如何做评测和回归？④ 理想的 AI 工程平台是什么样？"
follow_up:
  - "RAG什么场景下效果不如微调?"
  - "如何评估是否需要微调?"
---

# 什么时候用RAG,什么时候微调?两者如何结合

- **RAG vs 微调决策矩阵:**

| 维度 | RAG | 微调 |
|------|-----|------|
| 知识更新 | **实时** | 需重新训练 |
| 成本 | **低** | 高 |
| 幻觉控制 | **好(有据可查)** | 中 |
| 风格/格式 | 弱 | **好** |
| 推理速度 | 慢(检索开销) | **快** |
| 知识量 | 受检索限制 | **可内化大量** |

- **选择建议:**
```
需求是「知道新知识」?
├─ 是 → RAG
└─ 需求是「改变行为/风格」?
├─ 是 → 微调
└─ 两者都需要 → RAG + 微调
```

- **两者结合的最佳实践:**

1. **微调教格式/风格** + **RAG提供知识**
- 微调让模型学会输出特定格式
- RAG注入最新的领域知识

2. **微调教工具使用** + **RAG提供上下文**
- 微调提升Function Calling准确率
- RAG提供具体任务信息

3. **微调 embedding模型** + **RAG检索**
- 领域数据微调embedding
- 提升检索准确率

- **成本对比:** RAG成本低10-100倍,优先尝试RAG
