---
id: ai-h-067
difficulty: L2
category: ai-harness
subcategory: 推理与部署
tags:
- IO
- 序列化
- IOC
- Elasticsearch
feynman:
  essence: 针对长时任务特点，采用Drain和Checkpoint机制实现平滑升级。
  analogy: 高速公路修路：旧路上的车跑完才封路，或者引导车到新路面继续跑。
  first_principle: 如何在更新服务时，保证持续时间不确定的长任务不被中断且状态不丢失？
  key_points:
  - 短任务适合金丝雀，长任务需特殊处理避免中断
  - Drain模式让旧版本完成任务后才下线
  - Checkpoint机制支持将任务状态迁移到新版本
  - 关注任务完成率和Token消耗等核心业务指标
follow_up:
- Drain 模式的超时怎么设?
- Checkpoint 如何序列化 Agent 状态?
---

# Agent 服务的蓝绿部署和金丝雀部署如何选择?Agent 长任务怎么做部署不中断

- **部署策略对比**

- **蓝绿部署**
- 两套环境(蓝/绿),切换流量
- 优势:秒级回滚
- 劣势:双倍资源
- 适合:短期请求(<30s)

- **金丝雀部署**
- 新版本只放 5% 流量 → 观察 → 逐步扩大
- 优势:低风险,渐进
- 劣势:需要精细的监控指标
- 适合:迭代频繁

- **Agent 的特殊挑战:长任务**

Agent 任务可能运行几分钟到几小时(如多步研究、代码生成)

- **长任务部署不中断方案**

1. **Drain 模式**
- 旧版本标记为 'draining'
- 不接受新请求,但继续处理进行中的
- 新请求路由到新版本
- 等所有旧请求完成 → 关闭旧版本

2. **Checkpoint 迁移**
- 每步保存 Agent 状态(checkpoint)
- 旧版本 drain 时,进行中的任务保存 checkpoint
- 新版本从 checkpoint 恢复继续
- 需要 Agent 架构支持状态序列化

3. **版本亲和**
- 新请求绑定到新版本
- 旧请求继续用旧版本直到完成
- 通过 sticky session 实现

- **金丝雀关注指标**
- 任务完成率(不能下降)
- 平均步数(不能增加太多)
- Token 消耗(不能暴增)
- 幻觉率(人工抽检)
- 用户满意度(点赞率)

- **推荐**
- 短任务 Agent:金丝雀(5%→20%→100%)
- 长任务 Agent:Drain + Checkpoint 迁移
- 紧急回滚:蓝绿

- **Agent 版本管理**
- Prompt 版本化:每次修改记录 diff
- 模型版本绑定:prompt_v3 + GPT-4-turbo-2024-04
- 工具版本绑定:tool_v2 + 新版本 schema
- 组合签名:`agent:prompt_v3+model_gpt4+tools_v2`
