---
id: "age-029"
difficulty: "L3"
category: "ai-harness"
subcategory: "可观测性"
tags:
  - "IO"
  - "IOC"
  - "Elasticsearch"
feynman:
  essence: "Agent Trace 结构一般是树形：根 Span 是 agent.run，子 Span 包括每次 LLM 调用、检索、工具执行、重试、降级；每个 Span 记录 model、token、tool.name、error.type 等属性，从而串起完整调用链便于排查。"
  analogy: "推理就像模型在考试答题——训练是学习，推理是应用知识回答问题。"
  key_points:
    - "Agent Trace 结构:"
    - "服务名(service.name)"
    - "操作名(operation.name)"
first_principle:
  problem: "为什么需要 OpenTelemetry在Agent系统中一般打哪些Span?如何设计Trace结构？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
follow_up:
  - "如何降低Tracing的性能开销?"
  - "Span之间如何传递上下文?"
---

# OpenTelemetry在Agent系统中一般打哪些Span?如何设计Trace结构

- **Agent Trace 结构:**
```
Trace (一次完整Agent任务)
├─ Span: agent.run (根Span)
│   ├─ Span: llm.call (LLM调用)
│   │   ├─ Span: llm.tokenize
│   │   ├─ Span: llm.inference
│   │   └─ Span: llm.detokenize
│   ├─ Span: tool.call (工具调用)
│   │   ├─ Span: tool.search (搜索)
│   │   └─ Span: tool.execute (执行)
│   ├─ Span: rag.retrieve (RAG检索)
│   │   ├─ Span: embedding
│   │   ├─ Span: vector_search
│   │   └─ Span: rerank
│   └─ Span: memory.update (记忆更新)
```

- **每个Span记录:**
- 服务名(service.name)
- 操作名(operation.name)
- 开始/结束时间(duration)
- 属性(attributes): model, tokens, cost
- 事件(events): 错误、重试
- 状态(status): OK/ERROR

- **关键Span属性:**
```python
span.set_attribute("llm.model", "gpt-4")
span.set_attribute("llm.input_tokens", 1500)
span.set_attribute("llm.output_tokens", 300)
span.set_attribute("llm.cost_usd", 0.012)
span.set_attribute("tool.name", "search")
span.set_attribute("tool.success", True)
```
