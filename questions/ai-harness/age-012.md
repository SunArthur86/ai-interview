---
id: age-012
difficulty: L1
category: ai-harness
subcategory: 工程化实践
feynman:
  essence: RAG 提供参考但不能强制模型遵守，质量取决于检索和约束。
  analogy: 开卷考试给了课本，但学生可能看错页或不信课本。
  first_principle: 如何有效抑制模型在利用外部知识时产生的错误生成？
  key_points:
  - 检索质量差会引入噪音
  - 模型可能忽略检索上下文
  - 需引用约束和拒答机制
---

# RAG 一定能降幻觉吗

不一定。RAG 的核心在于引入外部知识库来约束模型生成，但在以下场景下仍可能产生幻觉：

1.  **检索质量差**：
    *   **召回失败**：文档切分不合理或 Embedding 召回能力不足，导致相关信息未被检索到。
    *   **噪音干扰**：检索到了错误或相关性低的内容，反而误导模型（「错误的上下文比没有上下文更糟糕」）。
2.  **重排序失败**：
    *   跨编码器未能将正确答案排在 Top-K 内，导致模型被非相关文档混淆。
3.  **模型忽略上下文**：
    *   模型的指令遵循能力较弱，或者预训练知识权重过大，导致其「自信」地忽略提供的检索内容而直接回答。
4.  **合成幻觉**：
    *   模型将多个检索片段中的信息错误拼接，生成了不存在的事实。

** mitigation 策略**：
*   **检索评估**：使用 Answer Relevance、Context Recall 等指标监控检索链路。
*   **引用约束**：强制模型在回答中标注引用来源，并在后处理中校验引用的真实性。
*   **拒答策略**：设置置信度阈值，当检索内容相关性低于阈值时，引导模型拒答而非编造。

```text
      用户问题
         │
    ┌────▼─────┐
    │  检索增强 │
    └────┬─────┘
         │
    ┌────▼─────────────┐
    │  上下文 (Context) │
    └────┬─────────────┘
         │
    ┌────▼─────┐       
    │  模型生成 │───────┼──▶ 幻觉来源：
    └────┬─────┘       │    1. 上下文错误 (检索错)
         │             │    2. 上下文不足 (召回漏)
         ▼             │    3. 忽略上下文 (模型固执)
      最终答案          │    4. 拼接错误 (合成假)
```

## 常见考点
1.  **如何解决「检索到错误信息」的问题？**（追问：Reranker 的重要性，或者引入 Web 搜索验证）
2.  **如何量化检测幻觉？**（追问：FactScore、Faithfulness 指标原理）
3.  **如果模型必须回答但检索不到内容怎么办？**（追问：内置知识库与外挂知识库的优先级策略）
