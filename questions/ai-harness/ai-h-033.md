---
id: ai-h-033
difficulty: L2
category: ai-harness
subcategory: RAG工程化
tags:
- IO
- 索引
- Elasticsearch
feynman:
  essence: 通过查询改写或上下文注入，将指代不明确的代词还原为具体实体。
  analogy: 就像听故事时，有人问“他后来怎么了”，你心里明白“他”指代的是刚才讲的主角。
  first_principle: 如何解决自然语言中代词和省略造成的语义缺失问题？
  key_points:
  - 使用LLM将当前的模糊问题改写为包含上下文的独立问题
  - 将历史对话摘要作为背景信息注入系统Prompt
  - 利用滑动窗口保留最近几轮完整对话
  - 检索时结合改写后的Query和历史摘要
follow_up:
- CondenseQuestionMode的原理?
- 多轮RAG如何评估?
---

# RAG系统如何处理多轮对话?上下文中的历史如何与检索结合

- **多轮RAG的核心挑战:**

用户第二轮提问可能依赖第一轮的上下文.

例:
- 第一轮:「什么是Transformer?」
- 第二轮:「它的注意力机制怎么工作?」←「它」指什么?

- **解决方案:**

1. **查询改写(Query Rewriting)**
- 用LLM将「它的注意力机制怎么工作」改写为「Transformer的注意力机制怎么工作」
- 用改写后的查询做检索

2. **对话摘要作为上下文**
- 将之前的对话摘要注入系统prompt
- 检索结果 + 对话摘要一起提供给LLM

3. **LlamaIndex ChatEngine**
- 内置多轮对话RAG支持
- 自动管理上下文窗口
- 支持CondenseQuestionMode(改写查询)

4. **滑动窗口 + 检索**
- 最近3轮对话完整保留
- 更早的历史用检索获取相关片段

- **最佳实践:**
```
系统prompt + 最近2轮对话 + 检索到的文档
→ LLM生成回复
```
