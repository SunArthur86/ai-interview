---
id: agq-066
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- 注解
- IO
- 异常
- IOC
- 循环依赖
- Elasticsearch
feynman:
  essence: 通过自动化检查与人工Review双重机制，强制保证代码标准。
  analogy: 像作文检查，先用软件查错别字，再请老师改逻辑。
  first_principle: 如何在多人协作和快速迭代中维持代码的高可维护性？
  key_points:
  - 强制类型注解和统一代码风格规范
  - 严格的PR审查制度(1+1机制)
  - 静态分析工具检测安全与复杂度
  - 架构守护防止循环依赖和破坏性变更
---

# 代码质量是怎么保证的

**Situation：** AI Agent 项目涉及多个技术领域(LLM、检索、后端、DevOps)，代码质量参差不齐。
**Task：** 建立代码质量保障体系。
**Action：** 
1. **代码规范：**
*   **Python：** 遵循 PEP 8 + 内部补充规范。
*   **类型注解：** 所有函数参数和返回值必须有类型注解。
*   使用 ruff(linter + formatter)统一代码风格，替代 flake8 和 black 以提升速度。
2. **代码审查：**
所有 PR 必须至少 1 人 review。
核心模块(编排器、安全模块)的 PR 需要 2 人 review。
Review 重点：逻辑正确性、异常处理、性能影响、安全性。
3. **静态分析：**
mypy 类型检查(strict 模式)，禁止使用 `Any` 类型，确保类型安全。
bandit 安全扫描，检测常见漏洞（如 SQL 注入、硬编码密码）。
圈复杂度限制(单函数 ≤ 10)，复杂逻辑强制拆分。
4. **架构守护：**
模块依赖关系检查，防止循环依赖。
接口版本管理，防止破坏性变更。

**架构流程图：**
```text
┌─────────────┐    ┌──────────────┐    ┌──────────────┐
│  Local Dev  │───>│  CI/CD Pipe  │───>│  Merge Main  │
└─────────────┘    └──────┬───────┘    └──────┬───────┘
   Pre-commit             │                  │
(Ruff + Mypy)        ┌────▼─────┐        ┌────▼─────┐
                     │  SonarQ │        │  deploy  │
                     │ Scan/CC │        └──────────┘
                     └──────────┘
```

**Result：** 
代码审查覆盖率 100%。
类型检查覆盖率 90%。
线上 bug 密度降低 50%。

## 常见考点
1. **如何强制执行代码规范？** 
   *追问：是否使用 Pre-commit hook？CI 流程中如果检查失败怎么处理？*
2. **Mypy 的严格模式带来的开发成本如何平衡？**
   *追问：对于第三方库没有类型注解的情况如何处理（使用 type: ignore 还是 stub 文件）？*
3. **Code Review 的效率如何保证？**
   *追问：如何应对 Review 意见不一致的情况？是否有 Review SLA（如 24h 内响应）？*
