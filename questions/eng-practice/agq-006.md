---
id: agq-006
difficulty: L4
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- GC
- IOC
- 索引
- Elasticsearch
feynman:
  essence: 基于性能、扩展性与合规性选型的分布式向量库
  analogy: 像为企业图书馆选址，Milvus适合大规模、可扩展且数据不出界的专业需求
  first_principle: 如何在海量非结构化数据检索中平衡性能、成本与数据安全？
  key_points:
  - 计算存储分离支持水平扩展
  - 支持标量过滤与向量混合查询
  - 云原生架构保障高可用性
  - 开源生态规避数据合规风险
---

# 为什么选择 Milvus 而不是其他向量数据库

**Situation：** 项目需要一个向量数据库来存储和检索文档 embedding,候选方案包括 Milvus、Pinecone、Weaviate、Qdrant、Chroma、FAISS 等.

**Task：** 综合评估各方案的性能、功能、运维成本,选择最适合企业级场景的向量数据库.

**Action：** 
1. 排除不适合的方案:
**FAISS：** 单机库,不支持分布式,无法满足高可用和数据持久化需求.
**Chroma：** 定位轻量级,不适合大规模生产环境.
**Pinecone：** 全托管方案,数据出境合规问题(数据必须存在海外).

2. 深度对比 Milvus vs Weaviate vs Qdrant:
**性能：** Milvus 在 1000 万级向量上 QPS 达到 2000+,Qdrant 约 1500,Weaviate 约 1200.
**扩展性：** Milvus 原生支持分布式部署(计算存储分离),可水平扩展.Qdrant 的分布式能力相对较弱.
**索引类型：** Milvus 支持 IVF_FLAT、IVF_SQ8、HNSW、DiskANN 等多种索引,可根据数据规模选择最优方案.
**混合搜索：** Milvus 2.x 支持标量过滤 + 向量搜索的混合查询,满足"按部门+语义"的业务需求.
**生态：** Milvus 有 LangChain、LlamaIndex 等主流框架的官方集成.
**社区：** Milvus 是 LF AI & Data 基金会毕业项目,社区活跃,长期维护有保障.

3. POC 验证(基于 200 万文档的真实数据):
建立测试基准,对比查询延迟、召回率、资源占用.
**Milvus：** P99 延迟 45ms,召回率 98.2%,内存占用 16GB.
**Qdrant：** P99 延迟 62ms,召回率 97.8%,内存占用 14GB.

**Result：** 
最终选择 Milvus,部署了 3 节点集群.
线上稳定运行 8 个月,P99 延迟稳定在 50ms 以内.
数据量从 200 万增长到 800 万文档,通过水平扩展平滑支撑.

```text
┌─────────────────────────────────────────────────────────────┐
│                      Milvus 集群架构                         │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │   Client     │    │   Client     │    │   Client     │  │
│  │ (SDK/API)    │    │ (LangChain)  │    │ (LlamaIndex) │  │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘  │
│         │                  │                  │          │
│         └──────────────────┼──────────────────┘          │
│                            ▼                             │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              Proxy (Load Balancer)                   │ │
│  └─────────────────────┬───────────────────────────────┘ │
│                        │                                 │
│         ┌──────────────┼──────────────┐                 │
│         │              │              │                 │
│  ┌──────▼──────┐ ┌─────▼─────┐ ┌─────▼─────┐            │
│  │ Query Node  │ │Query Node │ │Query Node │  (计算)    │
│  │ (Search)    │ │ (Search)  │ │ (Search)  │            │
│  └──────┬──────┘ └─────┬─────┘ └─────┬─────┘            │
│         │              │              │                 │
│  ┌──────▼──────────────▼──────────────▼──────┐           │
│  │         Data Node (Worker)                │           │
│  │  - 持久化存储 (MinIO/S3)                   │  (存储)   │
│  │  - 索引管理                               │           │
│  └───────────────────────────────────────────┘           │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │          Index (内存/Mmap)                          │  │
│  │  - IVF_FLAT / HNSW / DiskANN                        │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**原理细节补充：**
*   **计算存储分离**：Milvus 的核心优势在于将数据节点（Data Node，负责落盘和构建索引）与查询节点（Query Node，负责加载索引和执行搜索）分离。这使得扩容查询能力（增加 QPS）不需要复制全量数据，极大降低了运维成本。
*   **索引选型策略**：在 200 万数据量时，选择了 **HNSW** 索引（`M=16`, `efConstruction=256`），因为它在召回率和查询速度之间取得了最佳平衡。如果数据量达到亿级，会切换到 **DiskANN** 以降低内存压力。
*   **混合查询实现**：利用 Milvus 的 Bitset 机制，先通过标量字段（如 `department='HR'`）过滤出 Row IDs，生成 Bitset，再在向量索引搜索时跳过不在 Bitset 中的向量，从而实现高性能过滤。

**## 常见考点**
1.  **Milvus 的数据写入流程**：数据写入是先写 WAL（预写日志）保证不丢失，然后写入消息队列，最后异步落盘和构建索引，这保证了高并发写入下的性能。
2.  **如何保证数据一致性**：Milvus 默认支持最终一致性，可以通过配置强一致性来保证读到最新写入的数据，但会牺牲部分性能。
3.  **Vector 与 传统数据库的区别**：Vector DB 针对高维向量计算优化（如 SIMD 指令集），而传统数据库（如 MySQL with pgvector）在处理大规模向量时缺乏专门的索引优化（如 HNSW 的图结构遍历优化），性能差距显著。
4.  **Embedding 维度选择**：如何选择向量的维度？（答：根据模型定，如 OpenAI `text-embedding-3` 支持 256-3072 维。维度越高通常精度越高但计算慢，可根据业务需求通过 SVD 降维或直接选择小模型）。

