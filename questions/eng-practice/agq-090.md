---
id: "agq-090"
difficulty: "L2"
category: "eng-practice"
subcategory: "企业面试问答"
tags:
  - "IOC"
images:
  - "svg_beam_search.svg"
feynman:
  essence: "解码策略从贪心→Beam Search→采样；Temperature 调节概率分布的「尖锐度」（越低越确定），Top-p（核采样）只从累积概率达 p 的候选词里采样，兼顾多样性与合理性。"
  analogy: "AI Agent 就像有自主行动能力的实习生——能理解任务、拆解步骤、使用工具、根据反馈调整，核心是「感知→规划→行动→反思」循环。"
  key_points:
    - "贪心解码(Greedy Decoding):"
    - "采样(Sampling):"
    - "Temperature(温度参数):"
first_principle:
  problem: "为什么需要 LLM 的解码策略有哪些?Temperature 和 Top-p 怎么影响输出？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# LLM 的解码策略有哪些?Temperature 和 Top-p 怎么影响输出

**回答：** 
1. 贪心解码(Greedy Decoding):
每一步选择概率最高的 token.
确定性输出,但容易重复和死板.
2. 采样(Sampling):
按概率分布随机采样下一个 token.
引入随机性,输出更多样化.
3. Temperature(温度参数):
T = 0:等同于贪心解码,完全确定性.
T = 1:标准分布,正常采样.
T > 1:分布更平坦,低概率 token 被选择的概率增大 → 更随机.
P(token_i) = exp(logit_i / T) / Σ exp(logit_j / T)

T < 1:分布更尖锐,高概率 token 更容易被选择 → 更确定.
**建议：** 事实性任务 T=0,创意任务 T=0.7-1.0.
4. Top-p(Nucleus Sampling):
只从概率总和达到 p 的最小 token 集合中采样.
例如 top_p=0.9:只考虑概率累计到 90% 的 tokens.
自适应地控制候选集大小(对比 top-k 固定大小).
5. Top-k:
只从概率最高的 k 个 token 中采样.
简单有效,但 k 的最优值因场景而异.
6. 组合使用:
**实际应用中通常组合使用：** temperature=0.7, top_p=0.9 .
先用 temperature 调整分布,再用 top-p 截断.
