---
id: agq-013
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- Elasticsearch
feynman:
  essence: 通过分块与分层策略，将大文档转化为模型可处理的单元。
  analogy: 吃大披萨，先切块，慢慢吃，或者嚼碎了一起咽。
  first_principle: 如何将海量信息压缩并在有限容量下不丢失关键含义？
  key_points:
  - 语义分块：按段落而非字符切分，保留语义完整
  - Map-Reduce：分块总结后再汇总，适合全局概览
  - Refine迭代：逐步处理，适合细节查询
  - 相关性检索：问答时只提取最相关的切片
---

# 如何处理长文档输入?上下文窗口不够怎么办

**Situation：** 企业文档动辄数万字甚至数十万字,远超模型的 context window 限制(如 GPT-4 的 8K/32K/128K).
**Task：** 设计一套长文档处理方案,在不丢失关键信息的前提下,让系统能处理任意长度的文档.
**Action：** 
1. **分块策略**:
   基于语义的分块(不是简单按固定长度切分):
   - 先按章节/段落自然分割.
   - 每个 chunk 大小目标 512 tokens,允许 ±20% 浮动.
   - 相邻 chunk 有 10% 的重叠,避免语义断裂.
   - **递归分块：** 先按 `\n\n` 分,不够再按 `\n` 分,再按句号分.
   - **索引优化**：为每个 chunk 生成摘要并一起 Embedding，提高检索的语义匹配度（Parent Document Retriever 思想）.
2. **Map-Reduce 策略(超长文档总结)**:
   - **Map 阶段**: 对每个 chunk 独立做摘要.
   - **Reduce 阶段**: 对所有 chunk 摘要再做综合总结.
   - 适用于"总结这份 100 页的报告"类需求.
   - **边界控制**：若 Map 阶段 chunk 数量 N 过大(如 N>20)，需采用层级 Reduce（树状聚合）以防最终 Prompt 超限.
3. **Refine 策略(需要细节的场景)**:
   逐 chunk 处理,每次将上一步结果和当前 chunk 一起输入.
   适用于"找到文档中所有提到的风险点"类需求.
   - **缺点**：上下文长度线性增长，Token 消耗大，且存在"迷失中间"现象，中间部分的信息容易被遗忘.
4. **智能截断(实时问答场景)**:
   RAG 检索后,只取最相关的 top-5 chunk 放入 context.
   通过 reranker (如 BGE-Reranker 或 Cohere Rerank) 精排,确保最相关的内容在前面.
   - **混合检索**：结合关键词检索（BM25）和向量检索，召回率提升约 15%.
**Result：** 
- 支持处理最长 50 万字的文档.
- Map-Reduce 总结准确率达到 88%(与人工总结对比).
- 实时问答场景的检索-生成延迟控制在 3s 以内.

```text
Long Document Input
       │
       ▼
┌─────────────────┐
│  Semantic Chunk │
│  Strategy       │ (Recursive splitting based on headers/paragraphs)
│  (Indexing)     │
└────────┬────────┘
         │
         ▼
   [Chunk 1][Chunk 2][Chunk 3] ... [Chunk N]
         │                    │
         │ Stored            │ Embedding
         ▼                    ▼
   Vector Store (DB)
         │
         ├───────────────────┼────────────────────┐
         ▼                   ▼                    ▼
┌──────────────┐   ┌──────────────────┐   ┌─────────────────┐
│  Map-Reduce  │   │  Refine (Loop)   │   │  RAG (Query)    │
│ (Summarize)  │   │ (Detail Extract) │   │  (QA Scenario)  │
└──────┬───────┘   └────────┬─────────┘   └────────┬────────┘
       │                   │                     │
       │    ┌──────────────┼──────────────┐      │
       ▼    ▼              ▼              ▼      ▼
   Summary Map      Prev Context     Vector Search
       │            + Current Chunk    (Top-k Chunks)
       ▼                   │             │
   Reduce/Refine    ─────────┘             │
       │               │                   │
       ▼               ▼                   ▼
   Final Summary    Detailed Answer    LLM Context
```

## 常见考点
1. **分块参数的选择**：为什么是 512 tokens？如何根据不同模型的 Context Window 动态调整分块大小？（答案：权衡检索召回率和噪声，一般设为 Context Window 的 5%-10%）
2. **全局 coherence（连贯性）问题**：Map-Reduce 可能导致各子块摘要缺乏全局关联，如何解决？（答案：Reduce 阶段 Prompt 中增加“注意各部分之间的逻辑关系”指令，或增加一轮全局综述）
3. **索引结构**：Parent Document Retriever 中，是索引小块还是大块？（答案：存小块用于检索，返回时关联并返回父块/大块用于生成，保证上下文完整性）

