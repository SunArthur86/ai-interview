---
id: agq-051
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- Elasticsearch
feynman:
  essence: 通过分级超时控制、自动重试和模型降级，确保服务始终有响应。
  analogy: 打不通主电话就自动打备用电话，还打不通就发短信兜底。
  first_principle: 如何在依赖不稳定的外部模型服务时，保证系统的高可用性？
  key_points:
  - 连接/首字/总超时分级设置
  - 失败自动重试与模型降级
  - 非流式异步推送结果
  - 兜底话术保底线
---

# 模型超时怎么处理

**Situation：** LLM API 调用有时会出现超长延迟(> 30s)或直接超时,影响用户体验.

**Task：** 设计模型超时的处理策略,保证用户体验.

**Action：** 
1. **超时策略分级:**
   - **连接超时：** 5s(TCP 连接建立).
   - **首 token 超时:** 10s(等待第一个 token 返回).
   - **总超时：** 60s(整个请求的最长时间).

2. **超时处理流程:**
   - 首次调用超时 → 自动重试(切换备用端点,最多1次)
   - → 重试超时 → 降级到备用模型
   - → 备用模型也超时 → 返回兜底话术

3. **降级矩阵:**
| 主模型 | 备用模型 | 兜底 |
| :--- | :--- | :--- |
| GPT-4o | GPT-4o-mini | 缓存/话术 |
| Claude 3.5 | GPT-4o-mini | 缓存/话术 |
| 本地模型 | GPT-4o-mini | 缓存/话术 |

4. **用户侧处理:**
   - **流式模式下：** 超时前如果已有部分输出,告知用户"回答可能不完整".
   - **非流式模式：** 返回"处理时间较长,正在努力处理中...",并提供重试按钮.
   - 记录超时请求,后台异步完成后主动推送结果.

**Result：** 
- 用户可见的超时错误从 5% 降低到 0.5%.
- 降级后的回答质量虽然有所下降,但 85% 的用户仍然接受.
- 模型不可用时的系统可用性从 0% 提升到 95%(降级兜底).
