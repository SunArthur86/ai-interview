---
id: "agq-017"
difficulty: "L2"
category: "eng-practice"
subcategory: "企业面试问答"
tags:
  - "IO"
  - "IOC"
  - "Redis"
  - "Elasticsearch"
feynman:
  essence: "**Situation：** 系统需要同时服务多个企业客户,每个客户有自己的知识库、配置和数据隔离要求."
  analogy: "多租户就像写字楼——多个公司（租户）共享一栋楼（系统），各自有独立办公室（数据隔离），共用电梯和水电（共享资源）。"
  key_points:
    - "**Situation：** 系统需要同时服务多个企业客户,每个客户有自己的知识库、配置和数据隔离要求."
    - "**Task：** 设计多租户架构,确保数据隔离、配置独立、性能互不影响."
    - "**知识库隔离：** 每个租户在 Milvus 中使用独立的 Collection(分区方案在数据量大时性能不"
first_principle:
  problem: "为什么需要 你的系统支持多租户吗?怎么实现的？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 你的系统支持多租户吗?怎么实现的

**Situation：** 系统需要同时服务多个企业客户,每个客户有自己的知识库、配置和数据隔离要求.
**Task：** 设计多租户架构,确保数据隔离、配置独立、性能互不影响.
**Action：** 
1. 数据隔离策略:
**知识库隔离：** 每个租户在 Milvus 中使用独立的 Collection(分区方案在数据量大时性能不
好).
**对话数据隔离：** 每个租户的对话记录存储在独立的数据库 schema 中.
retry_delay = base_delay * (2 ^ attempt) + random_jitter
base_delay = 1s, max_delay = 30s, max_attempts = 3

**缓存隔离：** Redis 中使用 tenant_id:  作为 key 前缀.
2. 配置独立:
**每个租户可以自定义：** System Prompt、工具集合、模型选择、检索策略.
配置存储在数据库中,支持热更新(无需重启服务).
3. 资源限制:
每个租户有独立的 QPS 限制和 token 配额.
使用 Redis 令牌桶算法实现限流.
超出配额时返回 429 Too Many Requests.
**Result：** 
支持了 20+ 企业客户同时使用.
零数据泄露事故(严格的 tenant_id 过滤).
单个客户的异常不会影响其他客户(资源隔离).
