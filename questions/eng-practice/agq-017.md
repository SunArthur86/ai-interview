---
id: agq-017
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- IOC
- Redis
- Elasticsearch
feynman:
  essence: 在同一系统中实现多客户的数据、配置与资源隔离。
  analogy: 像一家大公司，不同部门在同一栋楼但独立办公，互不干扰。
  first_principle: 如何在共享基础设施的同时，保证不同客户的私密性与独立性？
  key_points:
  - 数据隔离：逻辑隔离(如租户ID)或物理隔离(独立库)
  - 配置独立：支持不同租户自定义业务参数
  - 资源配额：限制单租户QPS和Token用量
  - 互不影响：防止单租户异常拖垮整个系统
---

# 你的系统支持多租户吗?怎么实现的

**Situation：** 系统需要同时服务多个企业客户,每个客户有自己的知识库、配置和数据隔离要求.
**Task：** 设计多租户架构,确保数据隔离、配置独立、性能互不影响.
**Action：** 
1. **数据隔离策略**:
   - **知识库隔离：** 每个租户在 Milvus 中使用独立的 Collection(分区方案在数据量大时性能不好).
   - **对话数据隔离：** 每个租户的对话记录存储在独立的数据库 schema 中.
   - **缓存隔离：** Redis 中使用 `tenant_id:` 作为 key 前缀.
2. **配置独立**:
   - **每个租户可以自定义：** System Prompt、工具集合、模型选择、检索策略.
   - 配置存储在数据库中,支持热更新(无需重启服务).
3. **资源限制**:
   - 每个租户有独立的 QPS 限制和 token 配额.
   - 使用 Redis 令牌桶算法实现限流.
   - 超出配额时返回 429 Too Many Requests.
**Result：** 
- 支持了 20+ 企业客户同时使用.
- 零数据泄露事故(严格的 tenant_id 过滤).
- 单个客户的异常不会影响其他客户(资源隔离).
