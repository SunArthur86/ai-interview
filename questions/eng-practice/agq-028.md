---
id: "agq-028"
difficulty: "L2"
category: "eng-practice"
subcategory: "企业面试问答"
tags:
  - "IO"
  - "IOC"
  - "索引"
  - "Elasticsearch"
feynman:
  essence: "**Situation：** 嵌入模型的选择直接影响向量检索质量.需要在效果、速度、成本之间找到平衡. **Task：** 选择最适合中文企业场景的嵌入模型."
  analogy: "Embedding 就像把词语放到语义地图上——意思相近的词距离近，无关的词距离远，让机器能计算「相似度」。"
  key_points:
    - "**Situation：** 嵌入模型的选择直接影响向量检索质量.需要在效果、速度、成本之间找到平衡."
    - "**Task：** 选择最适合中文企业场景的嵌入模型."
    - "WRRF_score(d) = Σ(i=1 to n) w_i / (k + rank_i(d))"
first_principle:
  problem: "为什么需要 嵌入模型用的什么?维度多少?怎么选的？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 嵌入模型用的什么?维度多少?怎么选的

**Situation：** 嵌入模型的选择直接影响向量检索质量.需要在效果、速度、成本之间找到平衡.
**Task：** 选择最适合中文企业场景的嵌入模型.
**Action：**
WRRF_score(d) = Σ(i=1 to n) w_i / (k + rank_i(d))

1. 候选模型对比:
模型 维度 中文能力 MTEB 排名 速度 BGE-M3 1024 优秀 前 5 中
text-embedding-3-large
3072 良好 前 3 快(API) BGE-large-zh 1024 优秀 前 10 中 m3e-large 1024 优秀 前 15 中 Cohere embed-v3 1024 良好 前 5 快(API)
2. 评估维度:
**中文效果：** 使用内部评估集(300 条中文 query-doc pair),计算 Recall@10.
**多语言支持：** 部分文档是英文,需要跨语言检索能力.
**部署成本：** 本地部署 vs API 调用的成本对比.
**维度和存储：** 维度越高存储越大,检索越慢.
3. 最终选择 -- BGE-M3:
中文检索 Recall@10 最高(94.3%).
原生支持中英日韩多语言和跨语言检索.
支持稠密向量(dense)、稀疏向量(sparse)、多向量(ColBERT)三种表示.
1024 维在效果和效率间取得平衡. 可以本地部署,无数据出境风险.
4. 部署方案:
使用 ONNX Runtime 加速推理.
GPU 部署(T4):单条 embedding 延迟 15ms,batch 1000 条约 3s.
预计算 + 增量更新:新文档入库时实时计算 embedding.
**Result：**
中文检索 Recall@10 达到 94.3%,比 text-embedding-3-large 高 2.1%.

嵌入维度 1024,单条向量占 4KB,800 万文档的索引大小约 32GB.
本地部署避免了 API 调用成本和延迟.
