---
id: agq-022
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- 异常
- CAS
- IOC
- Redis
- Elasticsearch
feynman:
  essence: 短期记忆存当前对话上下文，长期记忆存用户画像和历史，双向流动。
  analogy: 大脑的工作记忆记当下的想法，海马体存长期的知识和习惯。
  first_principle: 如何让 AI 既能处理当下的复杂对话，又能记住用户的历史信息？
  key_points:
  - 短期：Redis缓存+滑动窗口，会话后压缩清理
  - 长期：向量库+关系型数据库，存储画像和摘要
  - 会话结束由LLM提取关键信息沉淀至长期记忆
  - 新会话触发相关历史信息召回
---

# 记忆系统的短期和长期记忆是怎么配合的

**Situation：** Agent 需要在单次对话中维护上下文(短期记忆),同时在跨会话中记住用户偏好和历史交互(长期记忆).

**Task：** 设计分层记忆系统,实现短期和长期记忆的协同工作.

**Action：** 
1. 短期记忆:
   - **存储位置：** 内存(进程内 + Redis 备份).
   - **内容：** 当前会话的对话历史、中间推理结果、工具调用结果.
   - **管理策略：**
     - **滑动窗口：** 保留最近 10 轮完整对话.
     - 超出窗口的对话自动摘要压缩.
     - 会话结束后 30 分钟过期清理.

2. 长期记忆:
   - **存储位置：** PostgreSQL(结构化信息)+ Milvus(语义检索).
   - **内容：** 
     - **用户画像：** 偏好、常用术语、业务领域.
     - **历史摘要：** 过往会话的关键信息摘要.
     - **FAQ 记忆: 用户高频问的问题和答案对.
   - **更新策略：** 
     - 会话结束时,提取关键信息更新用户画像.
     - 每日批量处理,对历史对话做摘要归档.

3. 短期 → 长期记忆的转化:
   - 会话结束时,LLM 自动提取本次对话的关键信息:
     - 用户提到的新偏好.
     - 解决的问题和方案.
     - 未解决的问题.
   - 提取结果写入长期记忆.

4. 长期 → 短期记忆的召回:
   - 新会话开始时,根据用户 ID 加载用户画像.
   - 根据当前话题,从长期记忆中检索相关历史信息.
   - 将召回的信息注入到当前会话的 context 中.

**架构图：**
```text
┌──────────────────────────────────────────────────────┐
│                  User Interaction                    │
└───────────────────────────┬──────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────┐
│                   Agent Engine                       │
├──────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────┐  │
│  │           Short-Term Memory (Cache)           │  │
│  │  ┌─────────┐  ┌──────────┐  ┌───────────────┐ │  │
│  │  │ History │  │ Chain of │  │  Tool Results│ │  │
│  │  │ (10 msgs)│  │ Thought  │  │               │ │  │
│  │  └────┬────┘  └──────────┘  └───────┬───────┘ │  │
│  └───────┼───────────────────────────────┼─────────┘  │
│          │ (Session End)                  │ (Query)   │
│          │                               │           │
│  ┌───────▼───────────────────────────────▼───────┐  │
│  │         Memory Controller / Orchestrator      │  │
│  └───────┬───────────────────────┬───────────────┘  │
└──────────┼───────────────────────┼──────────────────┘
           │ Write                 │ Read (RAG)
┌──────────▼───────────┐   ┌───────▼─────────────────┐
│  Long-Term Memory    │   │  Vector DB (Milvus)     │
├──────────────────────┤   ├─────────────────────────┤
│  ┌────────────────┐  │   │ Session Summaries      │
│  │ User Profile   │  │   │ Knowledge Snippets     │
│  │ (PostgreSQL)   │  │   │                        │
│  └────────────────┘  │   └─────────────────────────┘
└──────────────────────┘
```

**Result：** 
- 跨会话上下文一致性从 30% 提升到 78%.
- 用户偏好记忆准确率 85%(基于 200 个测试 case).
- 长期记忆占用控制在每用户 50MB 以内.

## 常见考点
1. **Redis 与内存的一致性**：如何保证进程内内存和 Redis 备份数据的一致性？是否考虑 Redis 挂了降级到只读内存？
2. **召回策略**：长期记忆召回时，如果检索到的历史信息与当前意图冲突（例如用户改变了主意），如何处理？（答案：优先级设置，短期记忆覆盖长期记忆）。
3. **数据隐私**：用户画像和敏感信息存入 PG 前，是否做了脱敏或加密处理？
4. **更新时效性**：为什么选择“会话结束”更新而不是“实时更新”？（答案：避免高频写入 DB，且对话中途信息可能不稳定，结束后总结更准确）。
