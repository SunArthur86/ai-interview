---
id: agq-056
difficulty: L4
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- IOC
- Redis
- Elasticsearch
feynman:
  essence: 通过分层限流和分级降级，牺牲非核心功能来保障系统在洪峰下的存活。
  analogy: 商场人太多时，暂停非必要服务，只开快速通道，保证不发生踩踏。
  first_principle: 如何在流量超过系统承载极限时，优先保障核心业务不受影响？
  key_points:
  - API/租户/用户三级限流
  - 令牌桶算法平滑流量
  - L0-L3分级自动降级
  - CPU/错误率触发阈值
---

# 高并发下的限流降级策略是什么

**Situation：** 突发流量(如营销活动、批量查询)可能导致系统过载,需要保护核心服务不被打垮.

**Task：** 设计分级限流和优雅降级策略.

**Action：** 
1. **限流层次:**
   - **API 网关层:** 全局限流(总 QPS 上限 500).
   - **租户级限流：** 每个租户有独立的 QPS 配额(如 50 QPS).
   - **用户级限流：** 每个用户每分钟最多 20 次请求.

2. **限流算法:**
   - 使用 Redis + 令牌桶算法(Token Bucket).
   - 支持突发流量(桶容量 = 2倍的 QPS 配额).

3. **降级策略(分四个等级):**
| 等级 | 策略 |
| :--- | :--- |
| L0(正常) | 所有功能正常运行 |
| L1(轻微) | 关闭非核心功能(如推荐、个性化) |
| L2(中度) | 模型降级(复杂查询也走快速模型) |
| L3(严重) | 只返回缓存和FAQ回答,不调用LLM |

4. **自动降级触发条件:**
   - QPS > 阈值的 80% → L1.
   - LLM API 错误率 > 10% → L2.
   - 系统 CPU > 90% 或内存 > 85% → L3.

**Result：** 
- 在 5 倍突发流量下,核心功能仍然可用.
- 降级响应延迟 < 200ms(缓存/FAQ).
- 零系统崩溃事故.
