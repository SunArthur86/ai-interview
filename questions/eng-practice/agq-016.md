---
id: agq-016
difficulty: L3
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- IOC
- Redis
- Elasticsearch
feynman:
  essence: 通过分类处理和状态持久化，实现系统的鲁棒性。
  analogy: 游戏断线重连，不用从头开始，网络卡顿时自动重试。
  first_principle: 分布式系统在部分组件失败时，如何整体继续提供服务？
  key_points:
  - 错误分类：区分可重试、可降级和不可恢复错误
  - 指数退避：避免重试风暴，缓解服务端压力
  - 自愈决策：利用LLM分析错误并尝试修正
  - 状态保存：持久化中间状态，支持断点续传
---

# 如何设计 Agent 的错误恢复和重试机制

**Situation：** 生产环境中 Agent 执行过程会遇到各种错误:LLM API 超时、工具调用失败、检索无结果等.简单的重试可能导致资源浪费或无限循环.
**Task：** 设计一套智能的错误恢复机制,让 Agent 能优雅地处理各种异常情况.
**Action：** 
1. **分层错误处理**:
   - **可恢复错误**: 网络超时、API 限流 → 指数退避重试(最多 3 次).
   - **可降级错误**: 主模型不可用 → 切换到备用模型.
   - **不可恢复错误**: 权限不足、参数错误 → 向用户报告错误原因.
2. **指数退避重试**:
   ```python
   retry_delay = base_delay * (2 ^ attempt) + random_jitter
   base_delay = 1s, max_delay = 30s, max_attempts = 3
   ```
   - **Jitter (抖动)**：增加随机性，避免多个客户端同时重试导致「惊群效应」，加重服务端负担。
3. **Agent 级别的自愈**:
   工具调用失败时,Agent 会收到错误信息,由 LLM 决定下一步:
   - 换一种方式调用工具(修改参数).
   - 尝试使用替代工具.
   - 告知用户无法完成并解释原因.
4. **断点续传**:
   复杂任务的中间状态持久化到 Redis.
   系统崩溃后可以从最近的检查点恢复执行.
   - **State Management**：不仅保存最终状态，还保存当前的 Thought、Action 序列，便于 ReAct 模式的回溯。
5. **兜底策略**:
   - 设置最大步数限制，防止陷入死循环。
   - 引入 "Human in the Loop"，当 Agent 连续 3 次尝试失败后，自动请求人工辅助。
**Result：** 
- 因 API 超时导致的用户可见错误从 5% 降低到 0.3%.
- 模型降级机制确保了 99.5% 的可用性.
- 断点续传避免了复杂任务的重复执行,节省了约 15% 的 token 成本.

```text
Start Task
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│               Action Execution (LLM / Tool)                  │
└──────────────────────────────┬──────────────────────────────┘
                               │
                     ┌─────────┴─────────┐
                     │    Error?         │
                     └─────────┬─────────┘
                  Yes │           │ No
          ┌─────────────┘           │
          ▼                         │
┌─────────────────────┐            │
│ Error Classification│            │
└──────────┬──────────┘            ▼
           │              ┌──────────────────┐
    ┌──────┴──────┬────────┤   Success        │
    ▼             ▼        └──────────────────┘
┌─────────┐ ┌──────────┐
│Transient│ │ Degrade  │
│ (Network)│ │ (LLM Down)│
└────┬────┘ └─────┬────┘
     │           │
     ▼           ▼
┌─────────┐ ┌──────────┐
│Retry w/ │ │Fallback  │
│Backoff  │ │Model     │
└────┬────┘ └─────┬────┘
     │           │
     │    ┌──────┴──────────┐
     │    ▼               ▼
     │ ┌─────────┐   ┌──────────┐
     └─┤Recover? │   │Fatal     │
       └────┬────┘   │(Auth/Arg)│
            │       └────┬─────┘
            ▼            │
       ┌──────────┐      ▼
       │Self-Heal │  ┌──────────┐
       │(LLM Plan)│  │Human in  │
       └────┬─────┘  │the Loop  │
            │       └──────────┘
            ▼
       Resume / Fail
```

## 常见考点
1. **幂等性设计**：在重试机制中，如何保证工具调用的幂等性，防止重复操作（如多次发送邮件）？（答案：工具层实现幂等校验，或在 Context 中传递 `request_id` 进行去重）
2. **状态快照的存储成本**：如果 Agent 步骤非常多，保存所有 Thought 会导致 Redis 内存爆炸，如何优化？（答案：仅保存最近 N 步的详细 Context，更早的步骤仅保存 Action 结果摘要）
3. **降级策略的触发**：从 GPT-4 切换到 GPT-3.5 时，Prompt 往往需要调整，系统如何处理这种 Prompt 差异？（答案：维护 Prompt 版本映射表，或者在通用 Prompt 设计时避免依赖特定模型的高级指令）

