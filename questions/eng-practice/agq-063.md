---
id: agq-063
difficulty: L3
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- Elasticsearch
- Docker
- Kubernetes
feynman:
  essence: 自动化流水线交付，配合灰度发布实现快速安全迭代。
  analogy: 像工厂流水线生产，先小批量试销(灰度)没问题再上架。
  first_principle: 如何自动化且安全地将代码变更交付给用户？
  key_points:
  - CI阶段自动化完成测试、构建和镜像推送
  - CD阶段采用灰度发布策略降低风险
  - 配置一键回滚机制应对发布异常
  - 使用Helm管理K8s配置实现环境隔离
---

# 怎么部署的?CI/CD 是什么流程

**Situation：** 系统需要频繁迭代(每周 1-2 次发版)，需要可靠的 CI/CD 流程保障发布质量。
**Task：** 建立自动化的 CI/CD 流水线，确保快速、安全的发布。
**Action：** 
1.  **CI 流水线(代码合入前)：**
    全部通过后才允许合入主分支。CI 时间目标：< 15 分钟。
    流程：代码提交 → 代码扫描(lint + type check) → 单元测试(覆盖率 > 80%) → 集成测试 → AI 评估测试 → 安全扫描(Trivy) → 构建 Docker 镜像 → 推送镜像仓库。
2.  **CD 流水线(发布流程)：**
    主分支合入 → 自动部署到 Staging → 冒烟测试 → 人工验证 → 灰度发布(10% → 50% → 100%) → 全量部署。
    灰度发布使用 Kubernetes 的金丝雀部署策略。每个阶段观察 30 分钟，无异常后进入下一阶段。
3.  **回滚机制：**
    保留最近 5 个版本的 Docker 镜像。
    **一键回滚：** kubectl rollout undo deployment/agent-api。
    灰度期间发现问题自动回滚（基于 Prometheus 的 Error Rate 指标触发）。
4.  **环境管理：**
    Dev → Staging → Production 三个环境。
    使用 Helm Chart 管理 Kubernetes 部署配置，实现 GitOps。
    环境配置通过 ConfigMap 和 Secret 管理，敏感信息使用 KMS 加密。
5.  **CI/CD 流水线架构图：**
```text
[开发人员] -> Git Commit (Main Branch)
      │
      ▼
┌─────────────────────────────────────┐
│  CI Pipeline (Jenkins/GitLab CI)    │
│  1. Lint & Security Scan             │
│  2. Unit Test & Integration Test     │
│  3. Build Docker Image               │
│  4. Push to Harbor (Registry)        │
└─────────────────┬───────────────────┘
                  │
                  ▼
         Update Helm Chart Repo
                  │
                  ▼
┌─────────────────────────────────────┐
│        CD Pipeline (ArgoCD)          │
│                                     │
│  [Staging] -> [Smoke Test] -> [OK]  │
│       │                            │
│       ▼                            │
│  [Prod: Canary 10%] -> [Observe]   │
│       │                            │
│       ▼ (Metrics Normal)           │
│  [Prod: Full Rollout 100%]          │
└─────────────────────────────────────┘
```
**Result：** 
发布频率从月到周。
发布成功率 98%(2% 的发布在灰度阶段被回滚)。
回滚时间 < 3 分钟。
零因发布导致的线上事故。

## 常见考点
1. **AI 评估测试**：在 CI 阶段，你们是如何自动化测试 AI 模型回答质量的？有没有使用“黄金数据集”做回归测试？
2. **灰度策略**：K8s 的金丝雀发布和蓝绿发布在 Agent 系统中你们是如何选择的？灰度期间如何保证用户会话的一致性？
3. **配置管理**：Prompt 模板和 System Prompt 是如何通过 ConfigMap 管理的？更新配置是否需要重启 Pod？
4. **构建优化**：为了加快 CI 速度，你们在 Docker 镜像构建层面做了哪些优化（如多阶段构建、缓存层）？
