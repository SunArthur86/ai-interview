---
id: agq-059
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- Elasticsearch
feynman:
  essence: 通过输入脱敏、临时占位还原和最小权限原则，阻断敏感信息的泄露路径。
  analogy: 把文件里的秘密数字换成代号，处理后换回来，处理过程不留底。
  first_principle: 如何在利用外部大模型处理数据的同时，确保敏感数据不被窃取或记忆？
  key_points:
  - 正则/NER识别并替换
  - 映射表内存暂存
  - LLM不可信与加密传输
  - 日志脱敏存储
---

# 敏感信息泄露怎么防止

**Situation：** Agent 在处理用户请求时,可能接触到敏感信息(个人隐私、商业机密),需要防止信息通过 LLM 泄露.

**Task：** 建立敏感信息的全生命周期防护机制.

**Action：** 
1. **输入脱敏:**
   - 使用正则 + NER 模型识别敏感信息:手机号、身份证号、银行卡号、邮箱、地址等.
   - **替换为占位符：** [手机号_1] 、[身份证号_1] .
   - 脱敏映射表存储在临时内存中(会话结束后清除).

2. **输出还原:**
   - LLM 回答中的占位符在返回前还原为原始值.
   - 只还原用户有权查看的信息.

3. **LLM 不可信原则:**
   - 假设 LLM 可能记忆训练数据中的信息 → 敏感数据绝不发送给 LLM.
   - 系统间通信使用加密传输(TLS 1.3).

4. **日志安全:**
   - 对话日志中的敏感信息自动脱敏后再存储.
   - 日志访问权限严格控制(最小权限原则).

**Result：** 
- 敏感信息防护覆盖率 99.5%.
- 零信息泄露事故.
- 通过了等保三级评审.
