---
id: agq-086
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IOC
- Elasticsearch
images:
- svg_embedding_training.svg
feynman:
  essence: 拉近相似文本，推远不相似文本，学习向量表示。
  analogy: 把意思相近的书放同一个书架，意思远的分开。
  first_principle: 如何将文本的语义转化为可计算的向量距离？
  key_points:
  - 利用InfoNCE等对比损失函数
  - 正样本拉近，负样本推远
  - Hard Negatives提升区分度
  - 结合数据增强提升泛化性
---

# Embedding 模型是怎么训练的?对比学习怎么做的

**1. 训练目标：**
让语义相似的文本在向量空间中距离更近，语义不同的文本距离更远。

**2. 对比学习：
- **正样本对：** 语义相近 (如问题-答案、标题-正文)。
- **负样本对：** 语义不相关。
- **InfoNCE 损失函数：**
  $$ L = -\log \frac{\exp(\text{sim}(q, k^+)/\tau)}{\sum_i \exp(\text{sim}(q, k_i)/\tau)} $$
  - $	ext{sim}$：余弦相似度。
  - $\tau$：温度参数 (控制分布的锐度)。
  - $k^+$：正样本，$k_i$：所有样本 (包含 in-batch 负样本)。

**3. 负样本策略：**
- **In-batch Negatives：** 同一 batch 内其他样本的正样本作为当前样本的负样本。
- **Hard Negatives：** 挖掘语义相近但答案不同的样本 (更有挑战性的负样本)。Hard negative mining 对效果提升非常关键。

**4. 训练数据来源：**
- 搜索日志中的 query-doc 点击对。
- NLI 数据集 (蕴含关系作为正样本)。
- 人工标注的相似句对。
- **数据增强：** 回译、同义替换等。

**5. BGE 系列的训练策略：**
- **预训练：** RetroMAE (基于 masked auto-encoding)。
- **微调：** 大规模对比学习 + hard negative mining。
- **指令微调：** 让 embedding 模型能理解 query 前的指令 (如 "为这个查询检索相关段落:")。
