---
id: agq-021
difficulty: L3
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- Elasticsearch
- 熔断
feynman:
  essence: 通过状态机控制请求通路，故障时快速失败，恢复时试探性放开。
  analogy: 像电路保险丝，电流过大自动断开，冷却后尝试合上。
  first_principle: 如何在依赖服务不稳定时防止系统崩溃并实现快速恢复？
  key_points:
  - 闭合(记录)、断开(拦截)、半开(探测)三态流转
  - 根据失败率或超时触发状态变更
  - 配合降级策略(缓存/兜底)保证可用性
  - 防止级联故障和资源耗尽
---

# 熔断器是怎么实现的?三个状态是什么

**Situation：** LLM API 和外部服务经常出现间歇性故障,如果持续重试会浪费资源并增加延迟.需要一个熔断机制来快速失败和自动恢复.
**Task：** 实现 Circuit Breaker 模式,保护系统在下游服务异常时的稳定性.
**Action：** 
1. 三个核心状态:
   - **CLOSED(闭合/正常):** 请求正常通过,记录失败率。此时维护一个滑动窗口计数器。
   - **OPEN(断开/熔断):** 所有请求直接失败(返回降级响应),不再调用下游，保护系统不阻塞。
   - **HALF_OPEN(半开/探测):** 熔断一段时间后,允许少量探测请求通过,检测下游是否恢复。如果成功则认为服务已恢复，失败则重新熔断。

2. 状态转换规则:
   - **CLOSED → OPEN:** 最近 60s 内失败率超过 50%(至少 10 次请求)。防止偶发抖动导致误判。
   - **OPEN → HALF_OPEN:** 熔断 30s 后自动进入半开状态（冷却时间 Cooldown）。
   - **HALF_OPEN → CLOSED:** 连续 3 次探测成功 → 恢复正常。
   - **HALF_OPEN → OPEN:** 任何一次探测失败 → 重新熔断（无需再等 30s，立即快速失败）。

3. 实现细节:
```python
class CircuitBreaker:
    def __init__(self, failure_threshold=0.5, recovery_timeout=30, min_requests=10, probe_success_threshold=3):
        self.state = State.CLOSED
        self.failure_count = 0
        self.success_count = 0
        self.last_failure_time = None
        self.probe_success_count = 0
        self._window = deque(maxlen=100)  # 滑动窗口，记录最近请求结果

    async def call(self, func, *args, **kwargs):
        if self.state == State.OPEN:
            # 检查是否到了冷却时间，进入半开探测
            if time.time() - self.last_failure_time > self.recovery_timeout:
                self.state = State.HALF_OPEN
                self.probe_success_count = 0 # 重置探测计数
            else:
                raise CircuitOpenError("Circuit is open")
        try:
            result = await func(*args, **kwargs)
            self._on_success()
            return result
        except Exception as e:
            self._on_failure()
            raise
```

4. 降级策略:
   - **熔断时返回缓存的结果**：如果有旧缓存，返回并标记为“可能过期”。
   - **切换到备用服务**：如主模型熔断后切换到备用模型（降级服务）。
   - **返回兜底话术**：("系统繁忙,请稍后重试")。

**熔断器状态流转图：**
```text
      Failure Rate > 50%
      + Min Requests > 10
            │
            ▼
┌───────────────────────┐
│      CLOSED           │─────────────────┐
│ (Requests Pass)       │                 │
└───────────┬───────────┘                 │
            │                             │
            │  Success                     │ Fail (Any)
            ▼                             │
┌───────────────────────┐   Recovery      │
│    HALF_OPEN          │◀──────── Timeout│
│ (Probe Requests)      │                 │
└───────────┬───────────┘                 │
            │                             │
            │ N Success < Threshold        │
            │                             │
            │      N Success >= Threshold  │
            ▼                             │
      (Reset to CLOSED)                   ▼
                              ┌───────────────────────┐
                              │        OPEN           │
                              │ (Fast Fail / Degrade) │
                              └───────────────────────┘
```
**Result：** 
- 下游服务故障时,系统响应时间从 30s(超时等待)降低到 100ms(快速失败).
- 自动恢复机制确保下游恢复后 30s 内自动恢复正常.
- 级联故障发生率降低 90%.

## 常见考点
1. **滑动窗口实现**：如何实现一个精确的滑动窗口计数器？（固定窗口计数桶、环形数组）。
2. **熔断与重试的区别**：重试是为了应对瞬态故障（如网络抖动），熔断是为了应对持续性故障（如服务宕机），两者如何结合使用？（先重试 N 次，失败后计入熔断计数器）。
3. **分布式熔断**：微服务架构中，每个实例都维护本地熔断器状态，是否一致？如果不一致有什么影响？（通常允许不一致，为了保护自身实例；若需全局一致则需引入中心化存储，但这会增加延迟）。
