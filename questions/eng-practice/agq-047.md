---
id: agq-047
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- IOC
- Elasticsearch
feynman:
  essence: 批量并行计算，仅更新变动部分。
  analogy: 像洗碗机一次洗一筐（批量），只洗脏盘子（增量）。
  first_principle: 如何最大化向量计算的生产效率？
  key_points:
  - 大批量计算充分利用GPU并行能力
  - 多GPU并行处理提升吞吐量
  - 仅对变更文档做增量计算
  - 必要时降低维度平衡精度和速度
---

# Embedding 计算是怎么优化的

**Situation：** 知识库初始化需要对大量文档计算 embedding(800 万个 chunk),耗时过长.日常增量更新也需要高效的 embedding 计算.
**Task：** 优化 embedding 计算的吞吐量和效率.
**Action：** 
1. 批量计算优化:
batch_size=256,GPU 利用率最大化.
使用 DataLoader 预加载数据,避免 GPU 等待 CPU 数据处理.
2. 增量更新策略:
**文档变更检测：** 对比 MD5 hash,只对变更的文档重新计算 embedding.
新增文档实时计算并入库,不需要全量重建.
3. 多 GPU 并行:
使用 DataParallel 在多张 GPU 上并行计算.
4 张 T4 GPU 并行,吞吐量提升 3.6 倍.
4. 维度压缩(可选):
对于存储和检索速度敏感的场景,使用 PCA 将 1024 维降到 512 维.
召回率仅下降 1.2%,存储减少 50%.
**Result：** 
800 万 chunk 的全量 embedding 计算:从 48 小时降到 12 小时.
**日常增量更新：** 1000 条新文档的 embedding 计算 < 30s.
GPU 利用率稳定在 80%+.
