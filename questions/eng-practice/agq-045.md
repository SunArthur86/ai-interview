---
id: agq-045
difficulty: L3
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- IOC
- Redis
- Elasticsearch
- Kubernetes
feynman:
  essence: 服务启动时预加载资源并渐进式接入流量。
  analogy: 像让运动员先热身（预热）再上场比赛，避免受伤。
  first_principle: 如何避免服务启动初期因资源未就绪导致的失败？
  key_points:
  - 启动时预加载模型和建立连接池
  - 从其他实例同步热点数据预热缓存
  - 健康检查通过前不对外提供服务
  - 流量逐步接入避免瞬时高压冲击
---

# 冷启动问题是怎么解决的

**Situation：** 系统重启或新实例扩容时存在冷启动问题:模型加载慢、缓存为空、连接池未预热.
**Task：** 减少冷启动对用户的影响.
**Action：** 
1. 模型预加载:
使用 Kubernetes 的 readinessProbe,模型加载完成后才接收流量.
模型文件使用共享存储(NFS/OSS),避免每次从网络下载.
**预加载时间：** 约 30s(embedding 模型 + reranker 模型).
2. 缓存预热:
服务启动时自动加载 FAQ 缓存和热门问题缓存.
从其他健康实例同步缓存数据(peer 预热).
预热完成前使用降级策略(跳过缓存直接查询).
3. 连接池预建:
服务启动时立即创建最小数量的数据库连接.
Redis 连接预建并做 PING 检查.
4. 流量预热:
新实例启动后,负载均衡器先分配少量流量(权重渐增).
避免大量冷请求同时打到新实例.
**Result：** 
冷启动时间从 120s 降低到 45s.
冷启动期间的请求成功率从 70% 提升到 98%.
缓存预热后命中率快速回升到正常水平.
