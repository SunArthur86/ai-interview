---
id: "agq-045"
difficulty: "L3"
category: "eng-practice"
subcategory: "企业面试问答"
tags:
  - "IO"
  - "IOC"
  - "Redis"
  - "Elasticsearch"
  - "Kubernetes"
feynman:
  essence: "**Situation：** 系统重启或新实例扩容时存在冷启动问题:模型加载慢、缓存为空、连接池未预热. **Task：** 减少冷启动对用户的影响. **Ac"
  analogy: "冷启动就像新开餐厅第一天——没有历史数据、没有口碑（模型没见过），只能先用规则兜底，再逐步积累数据优化。"
  key_points:
    - "**Situation：** 系统重启或新实例扩容时存在冷启动问题:模型加载慢、缓存为空、连接池未预热."
    - "**Task：** 减少冷启动对用户的影响."
    - "使用 Kubernetes 的 readinessProbe,模型加载完成后才接收流量."
first_principle:
  problem: "为什么需要 冷启动问题是怎么解决的？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 冷启动问题是怎么解决的

**Situation：** 系统重启或新实例扩容时存在冷启动问题:模型加载慢、缓存为空、连接池未预热.
**Task：** 减少冷启动对用户的影响.
**Action：** 
1. 模型预加载:

使用 Kubernetes 的 readinessProbe,模型加载完成后才接收流量.
模型文件使用共享存储(NFS/OSS),避免每次从网络下载.
**预加载时间：** 约 30s(embedding 模型 + reranker 模型).
2. 缓存预热:
服务启动时自动加载 FAQ 缓存和热门问题缓存.
从其他健康实例同步缓存数据(peer 预热).
预热完成前使用降级策略(跳过缓存直接查询).
3. 连接池预建:
服务启动时立即创建最小数量的数据库连接.
Redis 连接预建并做 PING 检查.
4. 流量预热:
新实例启动后,负载均衡器先分配少量流量(权重渐增).
避免大量冷请求同时打到新实例.
**Result：** 
冷启动时间从 120s 降低到 45s.
冷启动期间的请求成功率从 70% 提升到 98%.
缓存预热后命中率快速回升到正常水平.
