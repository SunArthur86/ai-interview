---
id: agq-004
difficulty: L4
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- Elasticsearch
feynman:
  essence: 通过专业分工解决单Agent上下文过载与能力退化
  analogy: 像公司从全能超人（单Agent）改为各部门协作（多Agent），主管分派任务
  first_principle: 如何解决单一模型上下文窗口有限与任务复杂度无限之间的矛盾？
  key_points:
  - 单Agent受限于Prompt长度与工具数量
  - Supervisor模式实现任务分发与统筹
  - 专业化Agent提升单一任务准确率
  - 结构化通信确保信息无损传递
---

# 多 Agent 协作和单 Agent 有什么区别?你怎么选择

**Situation：** 随着业务复杂度增加,单个 Agent 的 Prompt 越来越长,工具数量越来越多,开始出现能力退化和维护困难的问题.

**Task：** 评估是否需要从单 Agent 架构迁移到多 Agent 协作架构,以及如何设计协作机制.

**Action：** 
1. 单 Agent 的瓶颈分析:
单 Agent 的系统 Prompt 超过 4000 tokens 后,指令遵循能力明显下降.
工具数量超过 15 个时,LLM 选择工具的准确率从 95% 下降到 78%.
所有逻辑耦合在一个 Prompt 中,修改一个功能可能影响其他功能.

2. 多 Agent 架构设计:
Supervisor Agent(主管 Agent): 负责意图识别和任务路由,不直接执行任务.
RAG Agent(检索 Agent): 专注于知识检索和答案生成.
Tool Agent(工具 Agent): 专注于外部 API 调用和数据处理.
Summary Agent(摘要 Agent): 专注于长文本摘要和对话总结.

3. 协作机制:
**通信方式：** Agent 之间通过结构化消息传递(JSON Schema 定义),避免自然语言传递导致的信息损失.
**执行策略：** Supervisor 判断任务类型后,将任务分派给专业 Agent;支持顺序执行和并行执行.
**结果汇总：** 由 Supervisor Agent 整合各子 Agent 结果,生成最终回答.

**架构流程图：**
```text
┌─────────────────────────────────────────────────────────┐
│                  用户请求                                │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Supervisor Agent (Router)                   │
│  (解析意图，拆解任务，分发指令)                            │
└─────┬───────────────────────┬───────────────────────┬────┘
      │                       │                       │
      ▼                       ▼                       ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  RAG Agent   │      │ Tool Agent   │      │Code Agent    │
│ (知识问答)    │      │ (API调用)     │      │ (代码执行)    │
└──────┬───────┘      └──────┬───────┘      └──────┬───────┘
       │                    │                    │
       └────────┬───────────┴────────┬───────────┘
                │ (JSON 结果回传)     │
                ▼                    │
       ┌────────────────────┐        │
       │  Context Aggregator│◄───────┘
       │  (汇总上下文)       │
       └─────────┬──────────┘
                 │
                 ▼
         ┌──────────────────┐
         │ Supervisor 整合  │
         │   生成最终回复     │
         └──────────────────┘
```

4. 选择标准:
工具数 ≤ 8 且业务逻辑简单 → 单 Agent.
工具数 > 8 或需要专业分工 → 多 Agent.
有跨域协作需求(如同时查知识库和调外部 API)→ 多 Agent.

**Result：** 
迁移到多 Agent 后,工具选择准确率回升到 96%.
每个 Agent 的 Prompt 控制在 2000 tokens 以内,维护性大幅提升.
新增业务场景只需新增专业 Agent,不影响现有功能.
系统整体任务完成率从 85% 提升到 93%.

## 常见考点
1. **通信开销**：多 Agent 模式下，频繁的 LLM 调用会显著增加成本和延迟，如何优化？（可回答共享部分 Context、减少不必要的交互轮次、使用小模型作为子 Agent）。
2. **循环依赖**：如果 Agent A 依赖 Agent B，Agent B 反过来又依赖 Agent A，如何处理？（可回答在定义 Schema 或路由规则时限制有向图的层级，禁止循环引用）。
3. **Sub-agent 独立性**：子 Agent 是否需要不同的 System Prompt？（是的，子 Agent 需要专门的 Prompt 来聚焦其特定领域的角色和能力限制）。
