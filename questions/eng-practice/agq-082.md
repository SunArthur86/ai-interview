---
id: agq-082
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- IOC
feynman:
  essence: 冻结原权重，旁路低秩矩阵更新，大幅减少参数。
  analogy: 大书不想重写，只在旁边贴几张修正贴纸。
  first_principle: 如何在保持模型能力不变的前提下大幅降低微调成本？
  key_points:
  - 冻结主干网络权重
  - 注入低秩分解矩阵BA
  - 推理时可合并零延迟
  - 结合QLoRA可进一步省显存
---

# LoRA 的原理是什么?为什么低秩有效

**LoRA 核心思想：**
在微调时不修改原始模型的权重 $W$，而是添加一个低秩分解的增量 $\Delta W$。
\[ W' = W + \Delta W = W + B \cdot A \]
其中 $W \in \mathbb{R}^{d \times d}$， $B \in \mathbb{R}^{d \times r}$， $A \in \mathbb{R}^{r \times d}$， $r \ll d$。

**参数量对比：** 例如 $d=4096, r=8$ 时，参数量从 $4096^2 = 16.7\text{M}$ 降到 $2 \times 4096 \times 8 = 65\text{K}$ (减少 99.6%)。

**为什么低秩有效？**
- **内在维度假说:** 预训练模型在微调时，参数变化实际上存在于一个低维子空间中。
- 实验表明，大模型微调的权重变化矩阵的有效秩远小于其原始维度。

**LoRA 实现细节：**
- $A$ 矩阵用高斯初始化，$B$ 矩阵初始化为零 (保证训练开始时 $\Delta W=0$)。
- 只在 Attention 的 $Q$、$V$ 投影矩阵上应用 LoRA (实践中效果最好)。
- 推理时可以将 $\Delta W$ 合并到 $W$ 中，不增加推理开销。

**与其他方法对比：**
- **全量微调：** 效果最好，但需要大量显存和计算资源。
- **Prompt Tuning：** 参数量更少，但效果不如 LoRA。
- **Adapter：** 增加推理延迟，LoRA 合并后零额外延迟。

**QLoRA：** 结合量化 (4-bit) 和 LoRA，进一步降低显存需求，使得在消费级 GPU 上微调大模型成为可能。
