---
id: agq-091
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
feynman:
  essence: 赋予大模型感知环境并调用外部API以获取实时信息或执行操作的能力。
  analogy: 给大脑配双手，让它不仅能思考还能动手做事。
  first_principle: 如何让纯语言模型连接物理世界或数字服务？
  key_points:
  - 核心是意图识别与参数生成
  - Prompt Engineering与微调结合
  - 需处理多步调用与错误恢复
---

# 什么是 Agent 的工具学习(Tool Learning)

**回答：** 
1. **定义**: 
让 LLM 学会识别何时需要调用外部工具、选择哪个工具、生成正确的参数。
**本质**: 将自然语言意图映射为结构化的 API 调用。

2. **实现方式:**
- **Prompt 方式 (主流/原生):**
  - 在 Prompt 中描述可用工具的名称、功能、参数格式 (JSON Schema)。
  - LLM 通过 in-context learning 学会调用工具。
  - **优点：** 灵活、无需训练、适配任何 LLM。
  - **缺点:** 受 Prompt 长度限制，工具多时上下文拥挤；参数格式生成可能出错。

- **Fine-tuning 方式:**
  - 使用工具调用的标注数据微调模型。
  - 代表工作: Gorilla、ToolLLM 等。
  - **优点：** 更准确，能处理更复杂的 API 文档。
  - **缺点:** 需要训练数据，更新工具需重新训练或持续预训练。

- **Function Calling (API 级别):**
  - OpenAI、Anthropic 等厂商在 API 层面强制约束输出格式。
  - 模型经过专门训练以支持 function calling（特殊 token 强制输出 JSON）。
  - **优点：** 最可靠，解析容错率高。
  - **缺点:** 依赖特定厂商，不仅限于 Prompt，涉及模型底层微调。

3. **挑战:**
- **工具选择准确性:** 工具数量多时容易选错，特别是功能相似的工具。
- **参数生成:** 需要正确理解参数的语义、类型（int, str, enum）和格式（日期格式等）。
- **多工具协同:** 需要理解工具间的依赖关系（如先 A 后 B，B 的参数依赖 A 的结果）。
- **错误恢复:** 工具调用失败（如 API 500、参数校验失败）后的重试和替代策略。

4. **最佳实践:**
- **描述优化**: 工具描述要精确且简洁，包含参数的业务含义，而不仅仅是技术定义。
- **Few-shot**: 提供调用示例，特别是复杂参数组合的示例。
- **数量控制**: 限制单次 Context 中可选工具数量（建议 ≤ 8 个），使用路由层先筛选工具集。
- **容错机制**: 实现 Tool 的输入校验，如果 LLM 生成格式错误，返回 Error Message 给 LLM 进行 Self-Correction。

```text
       Agent Tool Learning Loop

      ┌──────────────────────────────────────────┐
      │               User Query                 │
      └──────────────────┬───────────────────────┘
                         │
      ┌──────────────────▼───────────────────────┐
      │  LLM (System Prompt + Tool Definitions) │
      └──────────────────┬───────────────────────┘
                         │
          ┌──────────────┼──────────────┐
          │              │              │
      Need Info?     Use Tool?    Direct Answer
          │              │              │
          ▼              ▼              │
    Ask User     ┌─────────────────┐      │
                 │  Generate JSON  │      │
                 │  (Func/Args)    │      │
                 └────────┬────────┘      │
                          │               │
                 ┌────────▼────────┐      │
                 │ Execute Tool    │      │
                 │ (External API)  │      │
                 └────────┬────────┘      │
                          │               │
                 ┌────────▼────────┐      │
                 │ Tool Observation│      │
                 └────────┬────────┘      │
                          └───────┬───────┘
                                  ▼
                         ┌─────────────────┐
                         │    Final Reply  │
                         └─────────────────┘
```

**## 常见考点**
1. **Function Calling vs ReAct**：Function Calling 通常指单次结构化输出（AI -> Tool），ReAct 指包含 Thought/Action/Observation 的循环过程。两者如何结合？（Function Calling 是实现 ReAct 中 Action 步骤的一种高效手段）。
2. **工具库过大怎么办**？（引入 Router Agent，先由一个小模型检索出 Top-K 相关工具，再由主模型决策）。
3. **并发调用**：如果 Agent 一次性生成了多个无依赖的 Tool Call，工程上如何优化？（并发执行 API 请求，减少等待时间）。

