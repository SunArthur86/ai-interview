---
id: agq-079
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- Elasticsearch
feynman:
  essence: 通过数据化拆解与针对性修复解决复杂系统问题。
  analogy: 像医生治病，先验血确诊（数据分析），再对症下药（针对性修复），最后复查（效果验证）。
  first_principle: 如何在不确定的黑盒系统中找到确定性的优化路径？
  key_points:
  - 数据驱动分析问题根因，不凭直觉
  - 针对检索、幻觉、理解等不同病灶分类治疗
  - 建立可复用的优化方法论与闭环习惯
---

# 项目中遇到的最大挑战是什么?怎么解决的

**Situation：** 面试官考察面对困难的应对能力和解决问题的思维方式.
**Task：** 分享一个真实且有技术深度的挑战.
**Action：** 
1. 挑战描述: 系统上线初期,用户满意度只有 68%,远低于 80% 的目标.主要问题是检索不准确和 LLM 幻觉.
2. 问题拆解:
抽样分析 200 条不满意反馈,分类如下:
检索问题(45%):关键信息未被检索到.
幻觉问题(30%):LLM 编造了不存在的信息.
理解偏差(15%):AI 误解了用户意图.
其他(10%):格式问题、延迟等.
3. 逐个击破:
检索问题 → 引入多路检索 + reranker,检索准确率提升 17%.
幻觉问题 → 多层幻觉防控(引用机制 + NLI 检查 + temperature=0),幻觉率从 18% 降到 3%.
理解偏差 → 优化意图识别(两阶段分类 + 置信度阈值),准确率提升到 94%.
4. 方法论: 不是凭感觉优化,而是"数据驱动 + 逐个击破".

### 幻觉防控架构图
```text
用户提问
   │
   ▼
┌──────────────┐
│  LLM 生成    │◀───── Prompt: "仅基于上下文回答，不确定说不知道"
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────┐
│  1. 事实性校验  │
│     (检查生成内容是否包含未提及实体) │
└──────┬─────────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  2. 引用归因检查  │
│     (检测生成的每一句话是否有出处) │
└──────┬─────────────────────────────┘
       │ (Pass) / (Fail -> 拒绝回答)
       ▼
   返回结果
```

**Result：** 
用户满意度从 68% 提升到 88%,超出目标.
形成了一套可复用的 RAG 系统优化方法论.
团队建立了"问题分析 → 根因定位 → 量化改进"的工作习惯.

## 常见考点
1. **Reranker 的选择**：使用了哪种 Reranker 模型（如 BGE-Reranker, Cross-Encoder）？它引入了多少延迟？如何平衡准确率和速度？
2. **NLI 检查原理**：在幻觉防控中提到的 NLI（自然语言推理）检查是如何实现的？是单独调用一个模型还是利用 LLM 自身的能力？
3. **Bad Case 分析**：在 45% 的检索问题中，哪一类问题最难解决？（提示：多跳推理问题、模糊语义匹配）
4. **评估标准**：如何定义“幻觉率”？是人工抽检还是自动化的？如果是自动化的，标准是什么？
