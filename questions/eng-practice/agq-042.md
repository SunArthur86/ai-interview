---
id: "agq-042"
difficulty: "L3"
category: "eng-practice"
subcategory: "企业面试问答"
tags:
  - "String"
  - "IO"
  - "IOC"
  - "Redis"
  - "索引"
  - "Elasticsearch"
feynman:
  essence: "async def process_query(query): intent_task = asyncio.create_task(identify_inten"
  analogy: "数据库查询优化就像图书馆找书——先把意图分类（找什么书），再并行检索（同时查多个书架），最后汇总结果，asyncio 让这些步骤同时跑而不是排队等。"
  key_points:
    - "向量数据库(Milvus)优化:"
    - "关系型数据库(PostgreSQL)优化:"
first_principle:
  problem: "从第一性原理看：数据库查询是怎么优化的 的根本优势/劣势来源于什么？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 数据库查询是怎么优化的

async def process_query(query):
intent_task = asyncio.create_task(identify_intent(query))
retrieval_task = asyncio.create_task(retrieve_documents(query))
intent, docs = await asyncio.gather(intent_task, retrieval_task)
return await generate_answer(intent, docs, query)
python

**Situation：** 随着数据量增长,部分数据库查询变慢,影响 Agent 工具调用的响应速度.
**Task：** 优化数据库查询性能,确保工具调用延迟在可接受范围内.
**Action：** 
1. 向量数据库(Milvus)优化:
**索引选择：** 数据量 < 100 万用 HNSW(延迟低),> 100 万用 IVF_SQ8(内存占用小).
HNSW 参数调优:M=16, efConstruction=200, ef=128 .
Collection 按租户分,避免跨租户查询.
2. 关系型数据库(PostgreSQL)优化:
为高频查询添加组合索引.
使用 EXPLAIN ANALYZE 分析慢查询.
对话历史表按月分区(partition).
**连接池配置：** min=5, max=20, max_idle=300s.
3. Redis 优化:
**数据结构选择：** 缓存用 String,会话状态用 Hash,排行用 Sorted Set.
Key 设计规范:{service}:{tenant}:{type}:{id} .
热 key 探测和打散.
4. 查询模式优化:
避免 N+1 查询(批量查询替代循环查询).
使用预编译语句(PreparedStatement),减少 SQL 解析开销.
只查询需要的字段,避免 SELECT *.
**Result：** 
向量检索 P99 延迟稳定在 50ms 以内.
SQL 慢查询数量从日均 200 降到 5.
Redis 缓存命中率 95%,读延迟 < 1ms.
