---
id: "agq-012"
difficulty: "L2"
category: "eng-practice"
subcategory: "企业面试问答"
tags:
  - "IO"
  - "Elasticsearch"
feynman:
  essence: "面对客户多样的模型偏好（OpenAI/国产/本地部署），需要抽象出统一模型网关，通过配置中心维护模型能力与配额，按租户路由并支持 failover，从而解耦业务与具体模型供应商。"
  analogy: "AI Agent 就像有自主行动能力的实习生——能理解任务、拆解步骤、使用工具、根据反馈调整，核心是「感知→规划→行动→反思」循环。"
  key_points:
    - "**Situation：** 企业客户有不同的模型偏好:有的要用 OpenAI,有的要用国产模型(如通义千问、"
    - "文心一言),有的有本地部署需求."
    - "**Task：** 设计统一的模型抽象层,让系统可以无缝切换不同的 LLM 提供商."
first_principle:
  problem: "为什么需要 你的系统支持哪些模型?怎么做模型适配的？如果不存在它会怎样？它解决了什么根本问题？"
  axioms:
    - "模型本质是数学函数的参数优化——所有能力都来自数据和参数"
    - "质量 > 数量：数据质量决定模型上限，算法决定达到上限的效率"
  rebuild: "从 AI 系统出发：① 核心挑战是什么？② 现有方案如何解决？③ 有哪些 trade-off？④ 如果重新设计你会怎么做？"
---

# 你的系统支持哪些模型?怎么做模型适配的

**Situation：** 企业客户有不同的模型偏好:有的要用 OpenAI,有的要用国产模型(如通义千问、
文心一言),有的有本地部署需求.
**Task：** 设计统一的模型抽象层,让系统可以无缝切换不同的 LLM 提供商.
**Action：** 
1. 统一接口抽象:
2. 适配器模式实现:
OpenAIProvider :适配 OpenAI / Azure OpenAI API.
AnthropicProvider :适配 Claude 系列.
QwenProvider :适配通义千问(兼容 OpenAI 格式).
class LLMProvider(ABC):
async def chat(self, messages, **kwargs) -> LLMResponse: ...
async def stream_chat(self, messages, **kwargs) ->
AsyncIterator[str]: ...
def count_tokens(self, text) -> int: ...
def get_model_info(self) -> ModelInfo: ...
python

LocalProvider :适配 vLLM / Ollama 部署的本地模型.
3. 差异化处理:
Function Calling 格式:OpenAI 用 tools  字段,Claude 用 tool_use ,统一抽象后业
务无感知.
Token 计算:不同模型的 tokenizer 不同,每个 Provider 实现自己的 count_tokens .
**流式响应：** 不同 API 的 SSE 格式略有差异,在 Provider 层统一处理.
**Result：** 
支持 6 种模型提供商,切换只需改配置.
**新模型适配：** 平均 1 天完成(实现 Provider + 测试).
多个客户使用不同模型,同一套业务代码.
