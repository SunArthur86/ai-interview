---
id: agq-012
difficulty: L2
category: eng-practice
subcategory: 企业面试问答
tags:
- IO
- Elasticsearch
feynman:
  essence: 通过抽象层统一不同模型的接口差异，实现灵活切换。
  analogy: 万能充电头，不管是苹果还是安卓，插上就能充。
  first_principle: 如何屏蔽底层实现的差异，提供一致的上层服务？
  key_points:
  - 统一接口：定义标准化的调用方法
  - 适配器模式：封装各厂商API差异
  - 兼容处理：统一流式输出、Token计算格式
  - 配置化切换：业务代码无需修改
---

# 你的系统支持哪些模型?怎么做模型适配的

**Situation：** 企业客户有不同的模型偏好:有的要用 OpenAI,有的要用国产模型(如通义千问、文心一言),有的有本地部署需求.
**Task：** 设计统一的模型抽象层,让系统可以无缝切换不同的 LLM 提供商.
**Action：** 
1. **统一接口抽象**:
   ```python
   class LLMProvider(ABC):
       async def chat(self, messages, **kwargs) -> LLMResponse: ...
       async def stream_chat(self, messages, **kwargs) -> AsyncIterator[str]: ...
       def count_tokens(self, text) -> int: ...
       def get_model_info(self) -> ModelInfo: ...
   ```
2. **适配器模式实现**:
   - **OpenAIProvider**: 适配 OpenAI / Azure OpenAI API.
   - **AnthropicProvider**: 适配 Claude 系列.
   - **QwenProvider**: 适配通义千问(兼容 OpenAI 格式).
   - **LocalProvider**: 适配 vLLM / Ollama 部署的本地模型.
3. **差异化处理**:
   - **Function Calling 格式**: OpenAI 用 `tools` 字段, Claude 用 `tool_use`,统一抽象后业务无感知.
   - **Token 计算**: 不同模型的 tokenizer 不同,每个 Provider 实现自己的 `count_tokens`.
   - **流式响应：** 不同 API 的 SSE 格式略有差异,在 Provider 层统一处理.
4. **配置热更新与路由**:
   - 使用配置中心管理模型参数,支持运行时动态切换 Provider 和超时设置.
   - 实现权重路由,支持 A/B 测试或灰度发布(如 90% 流量走 GPT-4, 10% 走 Claude 3).
   - 针对模型限流,在 Provider 层实现了令牌桶算法进行请求整形.
**Result：** 
- 支持 6 种模型提供商,切换只需改配置.
- **新模型适配：** 平均 1 天完成(实现 Provider + 测试).
- 多个客户使用不同模型,同一套业务代码.

```text
┌─────────────────────────────────────────────────────────────┐
│                      Business Logic Layer                    │
│  (Agent Workflow, RAG Pipeline, Chat Service)               │
└────────────────────────┬────────────────────────────────────┘
                         │ Calls Standard Interface
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    LLM Abstraction Layer                     │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  LLMProvider (Abstract Base Class)                       │ │
│  │  + chat(messages, **kwargs): LLMResponse                │ │
│  │  + stream_chat(messages): AsyncIterator[str]            │ │
│  │  + count_tokens(text): int                              │ │
│  └─────────────┬───────────────────────────────────────────┘ │
└────────────────┼─────────────────────────────────────────────┘
                 │ Implements
    ┌────────────┼────────────┬──────────────┐
    ▼            ▼            ▼              ▼
┌─────────┐  ┌─────────┐  ┌─────────┐   ┌─────────┐
│OpenAI   │  │Anthropic│  │ Qwen    │   │ vLLM    │
│Provider │  │Provider │  │Provider │   │Provider │
└────┬────┘  └────┬────┘  └────┬────┘   └────┬────┘
     │            │            │              │
     ▼            ▼            ▼              ▼
┌─────────┐  ┌─────────┐  ┌─────────┐   ┌─────────┐
│OpenAI   │  │Claude   │  │Tongyi   │   │Local    │
│API      │  │API      │  │API      │   │Service  │
└─────────┘  └─────────┘  └─────────┘   └─────────┘
```

## 常见考点
1. **非通用功能的处理**：对于某些独有功能（如 OpenAI 的 DALL-E 图像生成），如何在通用接口中扩展而不破坏抽象？（答案：使用 `**kwargs` 或独立的 `Client` 接口，不强求统一）
2. **异步流式处理**：不同厂商的 SSE（Server-Sent Events）数据格式不同（如 `data: [DONE]`），如何在 Adapter 层做统一的解析和异常捕获？
3. **Token 计算的准确性**：本地计算 Token（如用 tiktoken）与云端实际计费不一致怎么办？（答案：允许 5-10% 的误差，或以云端返回的 `usage` 字段为准进行二次校准）

